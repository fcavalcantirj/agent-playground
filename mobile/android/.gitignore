gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Keystore secrets ARE committed (mobile/keys/android/*) since this repo
# is private. key.properties points at the in-repo keystore via a relative
# path. If you fork this into a public repo, re-add key.properties + *.jks
# to .gitignore and document the substitute path.
