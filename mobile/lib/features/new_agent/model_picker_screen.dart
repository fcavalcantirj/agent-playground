// Phase 25 Wave 1 stub — replaced by Wave 3 plan 25-05 (New Agent wizard).

import 'package:flutter/material.dart';

class ModelPickerScreen extends StatelessWidget {
  const ModelPickerScreen({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('Pick a model')),
        body: const Center(child: Text('Model picker — coming Wave 3')),
      );
}
