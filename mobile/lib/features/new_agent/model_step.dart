// Phase 25 Wave 1 stub — replaced by Wave 3 plan 25-05 (New Agent wizard).

import 'package:flutter/material.dart';

class ModelStep extends StatelessWidget {
  const ModelStep({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('New agent — Model + Key')),
        body: const Center(child: Text('Step 2: Model + BYOK — coming Wave 3')),
      );
}
