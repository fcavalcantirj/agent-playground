// Phase 25 Wave 1 stub — replaced by Wave 3 plan 25-05 (New Agent wizard).

import 'package:flutter/material.dart';

class DeployStep extends StatelessWidget {
  const DeployStep({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('New agent — Name & Deploy')),
        body: const Center(
          child: Text('Step 3: Name + Telegram + Deploy — coming Wave 3'),
        ),
      );
}
