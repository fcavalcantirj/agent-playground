// Phase 25 Wave 1 stub — replaced by Wave 3 plan 25-05 (New Agent wizard).

import 'package:flutter/material.dart';

class CloneStep extends StatelessWidget {
  const CloneStep({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(title: const Text('New agent — Clone')),
        body: const Center(child: Text('Step 1: Clone — coming Wave 3')),
      );
}
