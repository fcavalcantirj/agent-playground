---
phase: 29
plan_number: 09
plan: 09
type: execute
wave: 5
depends_on: [02, 04, 05, 06, 07, 08]
files_modified:
  - tools/migrate_phase29_nanobot_cutover.py
  - api_server/tests/tools/test_migrate_phase29_nanobot_cutover.py
  - api_server/tests/e2e/test_phase29_acceptance.py
  - .planning/phases/29-llm-egress-proxy/29-VERIFICATION.md
autonomous: false
acceptance_gates: [1, 2, 3, 4, 5, 6, 7]
addresses:
  - "D-19"
  - "AMD-01 (cutover script targets recipe_name='nanobot')"
  - "All 7 acceptance gates from CONTEXT.md"
requirements:
  - "GATE-01: nano-kaiku end-to-end smoke records usage_logs row with non-zero tokens"
  - "GATE-02: OpenRouter post-hoc backfill within ±$0.001"
  - "GATE-03: Mobile Usage screen shows non-zero $ within 5s of send"
  - "GATE-04: Pre-existing usage_logs.status='unknown' rows deleted (Plan 02 already applied; this plan verifies post-cutover state)"
  - "GATE-05: Failure-injection — kill api_server during chat → user sees bot_timeout"
  - "GATE-06: BYOK key never appears in any log line"
  - "GATE-07: Other 4 recipes (zeroclaw, nullclaw, hermes, openclaw) continue via legacy non-proxy path"
must_haves:
  truths:
    - "tools/migrate_phase29_nanobot_cutover.py exists and supports --dry-run + --limit + DATABASE_URL env"
    - "Cutover script SELECTs agent_containers WHERE recipe_name='nanobot' AND container_status IN ('running','starting') (per AMD-01)"
    - "For each row: stops the Docker container (idempotent — 404 is OK); UPDATEs row to container_status='stopped', stopped_at=NOW()"
    - "Cutover script is idempotent — running twice on the same state is safe; the second run is a no-op (0 rows)"
    - "All 7 acceptance gates have an explicit test (e2e or manual checkpoint) in this plan"
    - "Phase 29 exit doc .planning/phases/29-llm-egress-proxy/29-VERIFICATION.md exists with PHASE-29-EXIT-GATE-PASSED marker after all 7 gates green"
  artifacts:
    - path: "tools/migrate_phase29_nanobot_cutover.py"
      provides: "Live container migration at cutover (D-19 + AMD-01)"
      contains: "recipe_name = 'nanobot'"
    - path: "api_server/tests/e2e/test_phase29_acceptance.py"
      provides: "Automated acceptance gate verification (gates 1, 2, 4, 7)"
    - path: ".planning/phases/29-llm-egress-proxy/29-VERIFICATION.md"
      provides: "Phase exit gate doc with the 7-gate verdict"
      contains: "PHASE-29-EXIT-GATE-PASSED"
  key_links:
    - from: "tools/migrate_phase29_nanobot_cutover.py"
      to: "agent_containers WHERE recipe_name='nanobot'"
      via: "asyncpg SELECT + per-row docker stop + UPDATE"
    - from: ".planning/phases/29-llm-egress-proxy/29-VERIFICATION.md"
      to: "all 7 acceptance gate verdicts"
      via: "explicit per-gate PASS/FAIL line"
---

<objective>
Final cutover + acceptance verification. Lands the cutover script that wipes existing nanobot containers (so they re-deploy with the new `via_proxy: true` env shape per D-19 + AMD-01), runs the 7 acceptance gates against the fully-wired stack via `make e2e-inapp-docker`, and produces the phase-exit doc with `PHASE-29-EXIT-GATE-PASSED` marker.

Purpose: phase exit. Plans 01-08 deliver the components; this plan ties them together end-to-end against real infra (Postgres + Docker bridge + real OpenRouter LLM) and produces the verification artifact that closes the phase.

Output: 1 cutover script + 1 cutover unit test + 1 e2e test module + 1 phase-exit doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-llm-egress-proxy/29-CONTEXT.md
@.planning/phases/29-llm-egress-proxy/29-RESEARCH.md
@.planning/phases/29-llm-egress-proxy/29-PATTERNS.md
@.planning/phases/29-llm-egress-proxy/29-VALIDATION.md
@tools/migrate_phase28_stuck_rows.py
@api_server/src/api_server/services/runner_bridge.py
@CLAUDE.md
@Makefile

<interfaces>
From tools/migrate_phase28_stuck_rows.py:
```
argparse + DSN normalization + asyncpg connect + --dry-run branch + --limit safety cap + transactional UPDATE
```

From CLAUDE.md macOS gotcha:
```
Acceptance MUST run via: make e2e-inapp-docker (NOT native uvicorn — bridge-IP gotcha)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cutover script (RED + GREEN)</name>
  <files>
    tools/migrate_phase29_nanobot_cutover.py,
    api_server/tests/tools/test_migrate_phase29_nanobot_cutover.py
  </files>
  <read_first>
    tools/migrate_phase28_stuck_rows.py (full file — argparse + DSN + asyncpg + --dry-run + --limit pattern),
    api_server/src/api_server/services/runner_bridge.py (find the docker stop pattern — execute_persistent_stop or equivalent),
    .planning/phases/29-llm-egress-proxy/29-CONTEXT.md (D-19 — exact SWEEP_SQL + UPDATE),
    .planning/phases/29-llm-egress-proxy/29-PATTERNS.md (lines 666-682 — cutover skeleton + AMD-01 nanobot reference)
  </read_first>
  <behavior>
    - Test 1 (dry-run prints + does not write): seed 3 nanobot containers (status='running'); run with --dry-run; assert prints the 3 rows; the DB rows are unchanged; NO docker stop calls happened.
    - Test 2 (live mode stops containers + UPDATEs rows): seed 3 nanobot containers; mock the docker SDK stop to track calls; run live; assert (a) 3 stop calls happened; (b) all 3 rows transitioned to status='stopped' AND stopped_at IS NOT NULL.
    - Test 3 (limit cap): seed 10 nanobot containers; run with --limit=2; only 2 are stopped + UPDATEd.
    - Test 4 (404-tolerant docker stop): mock docker SDK to raise NotFound for one row; assert the script logs the not-found, continues, and UPDATEs ALL 3 rows (orphan reconciliation).
    - Test 5 (filters by recipe_name='nanobot'): seed 3 nanobot rows + 2 hermes rows + 1 openclaw row; run live; assert ONLY the 3 nanobot rows were touched.
    - Test 6 (filters by container_status IN running/starting): seed 1 nanobot row in 'stopped'; run live; that row is NOT touched (status preservation).
    - Test 7 (DATABASE_URL env reads from env): set DATABASE_URL=...; script connects without an explicit --dsn argument.
    - Test 8 (CLI returns non-zero on missing DATABASE_URL): without env or --dsn; script prints helpful error to stderr + exits non-zero.
  </behavior>
  <action>
    Create tools/migrate_phase29_nanobot_cutover.py mirroring tools/migrate_phase28_stuck_rows.py structure verbatim. Replace SWEEP_SQL with the nanobot filter (recipe_name='nanobot' AND container_status IN ('running','starting')).

    Per-row processing: (1) print dry-run preview when --dry-run is set; (2) docker SDK stop with timeout=10 wrapped in try/except for docker.errors.NotFound (404 is OK, log and continue); (3) UPDATE row to container_status='stopped', stopped_at=NOW().

    CLI flags:
    - --dsn (defaults to env DATABASE_URL)
    - --dry-run (default false)
    - --limit N (default unlimited; safety cap)

    On exit: print summary "Stopped N containers (M not-found); set N+M rows to status='stopped'".

    Create api_server/tests/tools/test_migrate_phase29_nanobot_cutover.py (pytestmark = pytest.mark.api_integration) with the 8 tests above. Use testcontainers PG 17; mock the docker SDK client via monkeypatch. Run the script's main function in-process for speed; the existing test_migrate_phase28_stuck_rows.py is the precedent (if absent, create a fresh pattern that imports the script's main as a module).

    AMD-01 ENFORCEMENT: the string "nano-kaiku" MUST NOT appear anywhere in the cutover script. The acceptance grep gate below catches accidental references.
  </action>
  <verify>
    <automated>cd api_server && uv run pytest tests/tools/test_migrate_phase29_nanobot_cutover.py -v -m api_integration</automated>
  </verify>
  <acceptance_criteria>
    File tools/migrate_phase29_nanobot_cutover.py exists,
    File contains the literal SQL "WHERE recipe_name = 'nanobot'" (NOT nano-kaiku),
    File contains the literal SQL "container_status IN ('running', 'starting')",
    File contains the literal SQL "UPDATE agent_containers SET container_status='stopped'",
    File contains argparse flags --dry-run AND --limit,
    Cutover script does NOT contain the string "nano-kaiku" — grep -c "nano-kaiku" tools/migrate_phase29_nanobot_cutover.py outputs 0,
    File api_server/tests/tools/test_migrate_phase29_nanobot_cutover.py exists with 8 test functions,
    All 8 tests PASS
  </acceptance_criteria>
  <done>
    Cutover script GREEN; 8/8 tests pass; AMD-01 enforced; --dry-run + --limit safety caps + 404-tolerant.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the cutover live + restart api_server</name>
  <files>
    (no source files modified — live infra mutation)
  </files>
  <read_first>
    tools/migrate_phase29_nanobot_cutover.py (Task 1),
    .planning/phases/29-llm-egress-proxy/29-CONTEXT.md (D-19 — wipe nanobot containers; user redeploys post-cutover)
  </read_first>
  <action>
    Step A — DRY RUN first:
    docker exec -i deploy-api_server-1 python /app/tools/migrate_phase29_nanobot_cutover.py --dry-run

    Inspect the printed list. If 0 rows, the live deploy has no live nanobot containers and step B is a no-op (acceptable).

    Step B — LIVE cutover:
    docker exec -i deploy-api_server-1 python /app/tools/migrate_phase29_nanobot_cutover.py

    Capture stdout into the SUMMARY for audit trail.

    Step C — Verify the wipe:
    docker exec deploy-postgres-1 psql -U postgres -d agent_playground_api -c "SELECT COUNT(*) FROM agent_containers WHERE recipe_name='nanobot' AND container_status IN ('running','starting')"

    Expected: 0.

    Step D — Restart api_server so the new lifespan order runs (proxy_byok_cache.rehydrate_from_db, proxy_ip_map.refresh, router mount, etc.):
    docker compose -f deploy/docker-compose.local.yml restart api_server
    docker logs deploy-api_server-1 2>&1 | tail -50 | grep -E "proxy_byok_cache|proxy_ip_map|llm_proxy"

    Expected log lines (one each):
    - proxy_byok_cache.rehydrated count=0 (since cutover wiped them)
    - proxy_ip_map.refresh OR equivalent (count=N)
    - router mount for /v1/llm/forward (the FastAPI route registration log)

    Step E — Idempotency check: run the cutover ONCE MORE in --dry-run mode:
    docker exec -i deploy-api_server-1 python /app/tools/migrate_phase29_nanobot_cutover.py --dry-run

    Expected: 0 rows printed (idempotent).
  </action>
  <verify>
    <automated>docker exec deploy-postgres-1 psql -U postgres -d agent_playground_api -c "SELECT COUNT(*) FROM agent_containers WHERE recipe_name='nanobot' AND container_status IN ('running','starting')" 2>&amp;1 | grep -qE "^\s+0\s*$" &amp;&amp; docker logs deploy-api_server-1 2>&amp;1 | grep -qE "proxy_byok_cache|proxy_ip_map" &amp;&amp; echo "CUTOVER-OK"</automated>
  </verify>
  <acceptance_criteria>
    SELECT COUNT(*) on running/starting nanobot containers returns 0,
    api_server logs show proxy_byok_cache.rehydrated AND proxy_ip_map.refresh log lines,
    Re-run of cutover --dry-run prints 0 rows (idempotent),
    The verify command echoes "CUTOVER-OK"
  </acceptance_criteria>
  <done>
    Live cutover applied; api_server restarted; lifespan logs confirm Phase 29 components booted; idempotency proven.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Acceptance gates 1, 2, 4, 7 — automated e2e (RED + GREEN)</name>
  <files>
    api_server/tests/e2e/test_phase29_acceptance.py
  </files>
  <read_first>
    .planning/phases/29-llm-egress-proxy/29-VALIDATION.md (lines 38-65 — per-task verification map),
    .planning/phases/29-llm-egress-proxy/29-CONTEXT.md (acceptance gates 1-7 + AMD-01),
    api_server/tests/e2e/ (existing e2e test patterns — find files like test_inapp_e2e.py or similar; mirror their fixture style),
    Makefile (the e2e-inapp-docker target),
    .planning/phases/22c.3-inapp-chat-channel/22c.3-15-PLAN.md (5x5 e2e matrix pattern from Phase 22c.3 — gate 7 mirrors)
  </read_first>
  <behavior>
    - **Gate 1 (nanobot e2e records non-zero usage)**: deploy a nanobot agent (real BYOK key from .env), send "hi" in chat, wait for the bot's reply, query usage_logs for the most recent row. Assert: input_tokens > 0 AND output_tokens > 0 AND cost_usd > 0 AND upstream_request_id IS NOT NULL AND status='success'.
    - **Gate 2 (post-hoc backfill within ±$0.001)**: same setup; after the chat completes, sleep 5s (PROBE-VAL-03 p95 ≤ 5s); re-query usage_logs for the same row; assert the row's cost_usd is within $0.001 of an independent direct fetch from `GET /api/v1/generation?id=<upstream_request_id>` (the test makes the same call to OpenRouter and computes `abs(row.cost_usd - data.total_cost) < 0.001`).
    - **Gate 4 (no unknown rows post-cutover)**: query SELECT COUNT(*) FROM usage_logs WHERE status='unknown'. Assert: 0.
    - **Gate 7 (other 4 recipes work via legacy path)**: deploy each of hermes, openclaw, zeroclaw, nullclaw (skip picoclaw — deferred); send "hi"; assert each returns a reply (non-empty content). Each MUST go via the legacy non-proxy path: assert `agent_containers.upstream_provider IS NULL` for those rows OR `via_proxy=false` is reflected in the runner-side env (the env-injection still has the real key, no BASE_URL override). Quick proof: query `agent_containers WHERE recipe_name IN ('hermes','openclaw','zeroclaw','nullclaw')` and assert `provider_key_enc IS NULL` (Phase 29 doesn't write provider_key_enc for non-via_proxy recipes).
  </behavior>
  <action>
    Create api_server/tests/e2e/test_phase29_acceptance.py (pytestmark includes pytest.mark.e2e + pytest.mark.api_integration). Use the dockerized harness pattern from `make e2e-inapp-docker` (the test runs INSIDE a container that joins the bridge network). Mirror existing e2e test fixtures from api_server/tests/e2e/.

    Test functions (one per gate):
    - test_gate_01_nanobot_e2e_records_nonzero_usage
    - test_gate_02_openrouter_backfill_within_tolerance
    - test_gate_04_no_unknown_status_rows
    - test_gate_07_legacy_recipes_still_work

    For Gate 1, the test does the full chain: POST /v1/agents/{id}/deploy with BYOK Bearer; await ready; POST /v1/agents/{id}/messages with content="hi"; await reply (poll inapp_messages); SELECT usage_logs.

    For Gate 2, the test repeats the gate-1 setup, captures the upstream_request_id, sleeps 5s, then makes its OWN call to OpenRouter's `/api/v1/generation?id=<id>` (using the same BYOK key from the test fixture) to get the canonical cost, and asserts the proxy's recorded cost_usd is within $0.001 (`Decimal('0.001')`).

    For Gate 4, a single SELECT.

    For Gate 7, deploys 4 agents in parallel (asyncio.gather) and asserts all 4 reply non-empty + agent_containers rows reflect the legacy path.

    Each test runs against the live (post-Task-2) api_server. Run via `make e2e-inapp-docker E2E_FILTER=phase29_acceptance` per 29-VALIDATION.md.
  </action>
  <verify>
    <automated>make e2e-inapp-docker E2E_FILTER=phase29_acceptance</automated>
  </verify>
  <acceptance_criteria>
    File api_server/tests/e2e/test_phase29_acceptance.py exists with 4 test functions named test_gate_01, test_gate_02, test_gate_04, test_gate_07,
    All 4 tests PASS via make e2e-inapp-docker,
    Test 01 asserts usage_logs row has input_tokens > 0 AND output_tokens > 0 AND cost_usd > 0 AND status='success',
    Test 02 asserts abs(row.cost_usd - data.total_cost) < Decimal('0.001'),
    Test 04 asserts COUNT(*) WHERE status='unknown' is 0,
    Test 07 asserts all 4 of hermes/openclaw/zeroclaw/nullclaw reply non-empty AND their agent_containers rows have provider_key_enc IS NULL (legacy path)
  </acceptance_criteria>
  <done>
    4 automated acceptance gates GREEN via dockerized harness; gates 1, 2, 4, 7 closed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual acceptance gates 3, 5, 6 — human verification</name>
  <files>
    (no source files modified — human-driven verification of gates 3, 5, 6 against the live stack)
  </files>
  <action>
    Pause execution. The orchestrator surfaces this checkpoint to the human reviewer with the &lt;what-built&gt; and &lt;how-to-verify&gt; blocks below. The executor takes NO automated action — it waits for the resume-signal. On "approved" Task 5 (29-VERIFICATION.md write) proceeds; on any failure description the phase exit gate is BLOCKED until the failing component is repaired.
  </action>
  <verify>
    <automated>echo "manual checkpoint — verification recorded by human reviewer in 29-VERIFICATION.md (Task 5)"</automated>
  </verify>
  <done>
    Human reviewer has typed "approved" after confirming gates 3 (mobile ticker non-zero within 5s), 5 (api_server kill yields bot_timeout + status='failed' or no row), and 6 (BYOK key never grep-matches in api_server logs). Timestamps + reviewer name captured for the verification doc.
  </done>
  <what-built>
    The full Phase 29 stack: proxy router live; nanobot recipe routes through the proxy; record_usage writes the row; backfill activity refines cost_usd within ~3-5s; mobile usage ticker reads from /v1/usage. Gates 1, 2, 4, 7 are automated and PASSED in Task 3. Gates 3, 5, 6 require human verification per 29-VALIDATION.md §"Manual-Only Verifications".
  </what-built>
  <how-to-verify>
    **Gate 3 — Mobile ticker shows non-zero $ within 5s of send:**
    1. Source .env: `set -a; source .env; set +a`
    2. Boot the mobile app: `cd mobile && make ios DEVICE=<id> BASE_URL=http://localhost:8000` (or substitute the host's bridge IP if testing against a live deploy box)
    3. In the app: Sign in via Google or GitHub OAuth.
    4. Deploy a nanobot agent (paste a valid OpenRouter BYOK key).
    5. Open the chat, send the message "hi".
    6. Within 5 seconds of receiving the bot reply: verify the AppBar Usage chip shows a non-zero `$X.XXXXX` value.
    7. Tap the chip → verify the drawer shows non-zero token counts (input + output, possibly cache_read).
    8. Wait another ~5s — the cost_usd value should refine slightly (Plan 07 backfill activity).

    **Gate 5 — Failure injection — kill api_server during chat → user sees bot_timeout:**
    1. With nanobot deployed and chat open, start sending a long-running message ("write a 200-word essay about turtles"). The streaming response should be visible.
    2. Mid-stream: `docker kill deploy-api_server-1` from another terminal.
    3. The bot's outbound LLM call fails (its OPENAI_BASE_URL points at the dead api_server).
    4. Temporal forward_to_agent retries [1s, 2s, 4s] — after ~10s of retries, the workflow surfaces `bot_timeout`.
    5. The mobile UI displays a `bot_timeout` error chip on the message bubble.
    6. Restart: `docker compose -f deploy/docker-compose.local.yml start api_server`.
    7. Confirm: `docker exec deploy-postgres-1 psql -U postgres -d agent_playground_api -c "SELECT id, status, status_code, cost_usd, input_tokens, output_tokens FROM usage_logs ORDER BY ts DESC LIMIT 5"` — the killed-mid-stream message either has NO usage_logs row OR a single row with status='failed' AND cost_usd=0 AND tokens=0 (per D-15). NEVER a partial row that misrepresents cost.

    **Gate 6 — BYOK key never appears in any log line:**
    1. After completing the gates 3 + 5 manual flows above (real chat traffic with a real BYOK key):
    2. `docker logs deploy-api_server-1 2>&1 | grep -E "sk-or-|sk-ant-|sk-proj-|sk-" || echo "PASS"`
    3. Expected: prints "PASS" (no matches found).
    4. Also: `docker logs deploy-api_server-1 2>&1 | head -200` — visually scan the last few hundred lines; confirm no key-shaped strings.

    If ALL THREE manual gates pass, type "approved" — Task 5 then writes the phase-exit doc.
    If ANY gate fails, document the failure mode + escalate. The phase does NOT close until all 7 gates are green.
  </how-to-verify>
  <resume-signal>Type "approved" once gates 3, 5, 6 all manually pass. If any failure, describe the symptom + the offending log lines.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write 29-VERIFICATION.md phase-exit doc</name>
  <files>
    .planning/phases/29-llm-egress-proxy/29-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/29-llm-egress-proxy/29-CONTEXT.md (the 7 acceptance gates),
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-01.md (and the other 14 — 15 total),
    api_server/tests/e2e/test_phase29_acceptance.py (the gate-1/2/4/7 automated outcomes),
    .planning/phases/22c.3-inapp-chat-channel/22c.3-15-PLAN.md (phase-exit-gate-passed marker convention from Phase 22c.3 — mirror the format)
  </read_first>
  <action>
    Create .planning/phases/29-llm-egress-proxy/29-VERIFICATION.md. Format:

    ```markdown
    # Phase 29 — LLM egress proxy + provider-agnostic cost capture: VERIFICATION

    **Verified:** YYYY-MM-DD
    **Verifier:** GSD-execute-phase + manual checkpoint
    **Status:** PHASE-29-EXIT-GATE-PASSED

    ## Wave 0 spike artifacts (15/15 PASS)

    | Probe | Verdict | Highlight |
    |---|---|---|
    | PROBE-VAL-01 | PASS | OpenRouter usage chunk arrives BEFORE/AFTER [DONE] (record exact ordering) |
    | PROBE-VAL-02 | PASS | X-Generation-Id present on success + streaming variants |
    | PROBE-VAL-03 | PASS | post-hoc latency p95 = X.Xs (≤5s budget) |
    | PROBE-VAL-04 | PASS | auth-swap shapes verified across 3 providers |
    | PROBE-VAL-05 | PASS | nanobot honors OPENAI_BASE_URL; other 4 flagged for Phase 30 |
    | PROBE-VAL-06 | PASS | gap reproduced + AMD-03 single-charges |
    | PROBE-VAL-07 | PASS | age-cipher round-trip + cross-user isolation |
    | PROBE-VAL-08 | PASS | StreamingResponse tee per-chunk overhead < 50ms |
    | PROBE-VAL-09 | PASS | idempotency_keys shape + migration-013 delta |
    | PROBE-VAL-10 | PASS | 60s polling chosen (Docker events not needed) |
    | PROBE-VAL-11 | PASS | OpenRouter /key endpoint reliable (10/10 status correct) |
    | PROBE-VAL-12 | PASS | openai-SDK + langchain-openai accept ap-proxy-* placeholder |
    | PROBE-VAL-13 | PASS | cumulative-output empirically confirmed (output_tokens monotonic) |
    | PROBE-VAL-14 | PASS | streaming 4xx parser strategy supportable |
    | PROBE-VAL-15 | PASS | bridge_ip uniqueness — running-only scope enforced |

    ## Acceptance gates (7/7 PASS)

    | Gate | Behavior | Status | Evidence |
    |---|---|---|---|
    | 1 | nanobot e2e records non-zero usage | PASS | test_gate_01_nanobot_e2e_records_nonzero_usage (automated) — see test output |
    | 2 | OpenRouter post-hoc backfill within ±$0.001 | PASS | test_gate_02_openrouter_backfill_within_tolerance (automated) |
    | 3 | Mobile Usage screen shows non-zero $ within 5s | PASS | manual verification YYYY-MM-DD HH:MM by <verifier> |
    | 4 | Pre-existing usage_logs.status='unknown' rows deleted | PASS | migration 013 + test_gate_04_no_unknown_status_rows |
    | 5 | Failure-injection — kill api_server → bot_timeout | PASS | manual verification YYYY-MM-DD HH:MM by <verifier> |
    | 6 | BYOK key never appears in any log line | PASS | manual grep verification YYYY-MM-DD HH:MM by <verifier> |
    | 7 | Other 4 recipes work via legacy non-proxy path | PASS | test_gate_07_legacy_recipes_still_work (automated) |

    ## D-XX + AMD-XX coverage

    Every D-XX and AMD-XX is owned by exactly one plan; verified via cross-reference:

    | Decision | Owning Plan | Implementation evidence |
    |---|---|---|
    | D-01 (topology) | 04 | routes/llm_proxy.py mounted on /v1 |
    | D-02 (BYOK custody) | 05 | services/proxy_byok_cache.py + agent_containers.provider_key_enc column |
    | D-02b (validate on deploy) | 05 | services/byok_validator.py + agent_lifecycle integration |
    | D-03 (streaming usage capture) | 04 + 07 | StreamUsageParser + backfill activity |
    | D-04 (cadence — 1 recipe) | 08 | nanobot.yaml flipped (AMD-01 supersedes "nano-kaiku") |
    | D-05 (fail-closed) | 04 | upstream errors propagate; record_usage status='failed' |
    | D-06 (wipe unknown) | 02 | migration 013 DELETE |
    | D-07 (IP-map auth) | 04 | ProxyIPMap + Authorization Bearer match |
    | D-08 (OR user param) | 04 | f"ap_{user_id}_{agent_instance_id}" |
    | D-09 (provider routing) | 03 | proxy_dispatcher.PROVIDERS dict |
    | D-10 (recording path) | 06 + 07 | direct record_usage + Temporal backfill |
    | D-11 (schema additions) | 02 | migration 013 columns |
    | D-12 (Anthropic parser) | 06 | _parse_anthropic_native + StreamUsageParser anthropic branch |
    | D-13 (no rate-limit) | (no work needed — defer to Phase B) |
    | D-14 (body mutation) | 04 | inject user + stream_options + OpenTelemetry |
    | D-15 (failed status) | 02 + 04 + 06 | check constraint + status='failed' write path |
    | D-16 (idempotency) | 04 | services/idempotency.py extended |
    | D-17 (provider derivation) | 03 + 05 | derive_provider + start_agent integration |
    | D-18 (via_proxy flag) | 08 | runner env injection + recipe flip |
    | D-19 (cutover wipe) | 09 | tools/migrate_phase29_nanobot_cutover.py |
    | AMD-01 (nanobot not nano-kaiku) | 08 + 09 | recipe + cutover script |
    | AMD-02 (Postgres not Redis) | 04 | services/idempotency.py reuse |
    | AMD-03 (reserved-row in_flight) | 04 | insert_reserved_row + poll_for_completion |
    | AMD-04 (bridge_ip column) | 02 | migration 013 column + partial index |
    | AMD-05 (separate proxy_upstream_client) | 04 | main.py lifespan |
    | AMD-06 (anthropic env dispatch) | 08 | runner env injection dispatches on api_key |
    | AMD-07 (last-wins on output_tokens) | 04 + 06 | StreamUsageParser AMD-07 branch |

    ## Test matrix (final state)

    | Plan | New tests | Status |
    |---|---|---|
    | 01 | 15 spike artifacts | PASS |
    | 02 | 7 migration tests | PASS |
    | 03 | 10 dispatcher unit tests | PASS |
    | 04 | 35 proxy + ip-map + parser + idempotency tests | PASS |
    | 05 | 24 BYOK cache + validator + integration tests | PASS |
    | 06 | 7 usage_recorder extension tests | PASS |
    | 07 | 8 backfill activity tests | PASS |
    | 08 | 13 runner + recipe-flip tests | PASS |
    | 09 | 8 cutover + 4 e2e gate tests + 3 manual gates | PASS |
    | **Total** | **134 tests + 15 spikes + 3 manual gates** | **ALL GREEN** |

    ## PHASE-29-EXIT-GATE-PASSED

    Phase 29 is COMPLETE. The proxy is live; nanobot routes through it; usage_logs records non-zero rows with non-zero cost_usd; mobile ticker reflects them within 5s; BYOK keys never leak. The other 4 recipes (hermes, openclaw, zeroclaw, nullclaw) continue via the legacy non-proxy path and are queued for Phase 30.

    **Next:** Phase 30 (migrate remaining recipes — one PR each).
    ```

    Replace YYYY-MM-DD and HH:MM placeholders with actual timestamps captured from Tasks 3 + 4. Replace test counts with actuals if they diverge from the planning estimate.
  </action>
  <verify>
    <automated>grep -q "PHASE-29-EXIT-GATE-PASSED" .planning/phases/29-llm-egress-proxy/29-VERIFICATION.md &amp;&amp; grep -cE "^\| (PROBE-VAL-[0-9]+|Gate [0-9]+) " .planning/phases/29-llm-egress-proxy/29-VERIFICATION.md | awk '{ if ($1 < 22) exit 1 }' &amp;&amp; echo "VERIFICATION-COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    File .planning/phases/29-llm-egress-proxy/29-VERIFICATION.md exists,
    File contains the literal string PHASE-29-EXIT-GATE-PASSED,
    File enumerates all 15 PROBE-VAL items with PASS verdicts (grep for PROBE-VAL-XX returns 15 lines),
    File enumerates all 7 acceptance gates with PASS verdicts (grep for "Gate [1-7] " returns 7 lines),
    File enumerates all 19 D-XX + 7 AMD-XX with owning plan numbers (D-XX coverage table),
    The verify command echoes "VERIFICATION-COMPLETE"
  </acceptance_criteria>
  <done>
    29-VERIFICATION.md committed with all 7 gates green; PHASE-29-EXIT-GATE-PASSED marker present; D-XX + AMD-XX coverage table complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cutover script → Docker daemon | Existing trust (operator running on dev box) |
| Cutover script → Postgres | UPDATE on agent_containers — narrow scope |
| Manual gate 5 SIGKILL → api_server | Operator action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-15 | Denial of Service | Cutover script stops live nanobot containers | accept | D-19 explicit decision: dev-OK; user re-deploys post-cutover. Mobile app's "deploy" UX is unchanged so re-deploy is one tap. |
| T-29-16 | Tampering | Failure-injection (gate 5) — partial usage_logs row | mitigate | D-15 enforces: incomplete streams write status='failed' with tokens=0, cost_usd=0. Plan 04 StreamingResponse generator finally block ensures this even on aclose-during-stream. Gate 5 manual test verifies. |
</threat_model>

<verification>
- 8 cutover unit tests + 4 automated e2e gates + 3 manual gates = 15 acceptance signals.
- All 7 phase acceptance gates green.
- Live api_server running with proxy router mounted; lifespan logs confirm.
- 29-VERIFICATION.md committed with PHASE-29-EXIT-GATE-PASSED marker.
- Phase 30 (recipe migration cadence) is now unblocked.
</verification>

<success_criteria>
- Cutover script committed + tested + executed live (gates 1-7 reachable from clean state).
- 4 automated e2e gate tests PASS.
- 3 manual gate verifications confirmed by human reviewer.
- 29-VERIFICATION.md exists with PHASE-29-EXIT-GATE-PASSED.
- All 19 D-XX + 7 AMD-XX accounted for in the coverage table.
</success_criteria>

<output>
After completion, create .planning/phases/29-llm-egress-proxy/29-09-SUMMARY.md as the final phase summary linking to 29-VERIFICATION.md and listing the cutover commit + the e2e test commit + the manual-gate timestamps.
</output>
