---
phase: 29
plan_number: 01
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - api_server/tests/spikes/test_phase29_probe_val_01_openrouter_usage.py
  - api_server/tests/spikes/test_phase29_probe_val_02_x_generation_id.py
  - api_server/tests/spikes/test_phase29_probe_val_03_post_hoc_latency.py
  - api_server/tests/spikes/test_phase29_probe_val_04_auth_swap.py
  - api_server/tests/spikes/test_phase29_probe_val_05_recipe_base_url.py
  - api_server/tests/spikes/test_phase29_probe_val_06_idempotency_in_flight.py
  - api_server/tests/spikes/test_phase29_probe_val_07_age_cipher_provider_key.py
  - api_server/tests/spikes/test_phase29_probe_val_08_streaming_tee.py
  - api_server/tests/spikes/test_phase29_probe_val_09_idempotency_keys_shape.py
  - api_server/tests/spikes/test_phase29_probe_val_10_docker_bridge_refresh.py
  - api_server/tests/spikes/test_phase29_probe_val_11_openrouter_key_endpoint.py
  - api_server/tests/spikes/test_phase29_probe_val_12_ap_proxy_placeholder_bearer.py
  - api_server/tests/spikes/test_phase29_probe_val_13_anthropic_sse.py
  - api_server/tests/spikes/test_phase29_probe_val_14_streaming_4xx.py
  - api_server/tests/spikes/test_phase29_probe_val_15_bridge_ip_uniqueness.py
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-01.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-02.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-03.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-04.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-05.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-06.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-07.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-08.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-09.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-10.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-11.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-12.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-13.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-14.md
  - .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-15.md
autonomous: false
acceptance_gates: []
addresses:
  - "PROBE-VAL-01"
  - "PROBE-VAL-02"
  - "PROBE-VAL-03"
  - "PROBE-VAL-04"
  - "PROBE-VAL-05"
  - "PROBE-VAL-06"
  - "PROBE-VAL-07"
  - "PROBE-VAL-08"
  - "PROBE-VAL-09"
  - "PROBE-VAL-10"
  - "PROBE-VAL-11"
  - "PROBE-VAL-12"
  - "PROBE-VAL-13"
  - "PROBE-VAL-14"
  - "PROBE-VAL-15"
requirements:
  - "GATE-PRE-01: spike-gate"
must_haves:
  truths:
    - "All 15 PROBE-VAL spike artifacts exist under .planning/phases/29-llm-egress-proxy/spikes/"
    - "Each artifact includes empirical numbers (counts, latencies, status codes, raw transcripts) from real upstream HTTP traffic"
    - "Each artifact ends with a single line `VERDICT: PASS` or `VERDICT: FAIL`"
    - "Any FAIL verdict blocks Wave 1 and surfaces as an AMD-08+ amendment cycle to CONTEXT.md"
  artifacts:
    - path: ".planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-01.md"
      contains: "VERDICT:"
    - path: ".planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-13.md"
      contains: "VERDICT:"
    - path: "api_server/tests/spikes/test_phase29_probe_val_13_anthropic_sse.py"
      contains: "output_tokens"
  key_links:
    - from: "29-VALIDATION.md Wave 0 Requirements"
      to: ".planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-NN.md"
      via: "1:1 mapping; planner verifies all 15 markdown files exist"
---

<objective>
Wave 0 spike gate: empirically validate the 15 PROBE-VAL items in 29-RESEARCH.md before Wave 1 plans seal. Each spike produces a markdown artifact under `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-NN.md` with raw transcripts, empirical numbers (latencies, status codes, byte shapes), and a single `VERDICT: PASS`/`VERDICT: FAIL` line.

Purpose: golden rule #5 ("Test everything; spike gray areas BEFORE planning") — every non-trivial mechanism (streaming tee, cumulative-usage parsing, bridge-IP refresh, age-cipher reuse, openai-SDK Bearer placeholder acceptance) must hit real infra BEFORE downstream plans depend on its assumed shape. A FAIL verdict on any probe blocks Wave 1 and triggers an AMD-08+ amendment cycle.

Output: 15 pytest spike modules under `api_server/tests/spikes/test_phase29_probe_val_NN_*.py` (each marked `pytest.mark.spike`) + 15 markdown artifacts under `.planning/phases/29-llm-egress-proxy/spikes/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-llm-egress-proxy/29-CONTEXT.md
@.planning/phases/29-llm-egress-proxy/29-RESEARCH.md
@.planning/phases/29-llm-egress-proxy/29-VALIDATION.md
@.planning/phases/29-llm-egress-proxy/29-PATTERNS.md
@api_server/tests/spikes/test_phase28_spike_c_worker_bridge_ip.py
@api_server/tests/spikes/test_respx_authlib.py
@api_server/src/api_server/crypto/age_cipher.py
@api_server/src/api_server/services/idempotency.py
@api_server/src/api_server/services/inapp_recipe_index.py

<interfaces>
<!-- existing primitives the spikes probe -->

From api_server/src/api_server/crypto/age_cipher.py:
```python
def encrypt_channel_config(user_id: UUID, blob: dict) -> bytes
def decrypt_channel_config(user_id: UUID, ciphertext: bytes) -> dict
# master key from AP_CHANNEL_MASTER_KEY env var; per-user KEK via HKDF
```

From api_server/src/api_server/services/idempotency.py:
```python
async def check_or_reserve(conn, *, user_id: UUID, key: str, request_body_hash: str) -> tuple[Literal["hit","miss","mismatch"], dict | None]
async def write_idempotency(conn, *, user_id: UUID, key: str, request_body_hash: str, verdict_json: dict) -> None
# Postgres-backed (NOT Redis); idempotency_keys table; pg_advisory_xact_lock; 24h TTL
```

From api_server/src/api_server/services/inapp_recipe_index.py:
```python
def get_container_ip(container_id: str, network_name: str) -> str | None
# Docker SDK call: client.containers.get(container_id).attrs['NetworkSettings']['Networks'][network_name]['IPAddress']
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROBE-VAL-01 + 02 + 04 + 11 + 14 — OpenRouter / OpenAI / Anthropic upstream HTTP probes</name>
  <files>
    api_server/tests/spikes/test_phase29_probe_val_01_openrouter_usage.py,
    api_server/tests/spikes/test_phase29_probe_val_02_x_generation_id.py,
    api_server/tests/spikes/test_phase29_probe_val_04_auth_swap.py,
    api_server/tests/spikes/test_phase29_probe_val_11_openrouter_key_endpoint.py,
    api_server/tests/spikes/test_phase29_probe_val_14_streaming_4xx.py,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-01.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-02.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-04.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-11.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-14.md
  </files>
  <read_first>
    .planning/phases/29-llm-egress-proxy/29-RESEARCH.md (lines 209-285 — Provider Dispatch Table + Streaming Capture Strategy + Stream interrupted / 4xx mid-stream),
    .planning/phases/29-llm-egress-proxy/29-VALIDATION.md (lines 71-86 — Wave 0 Requirements),
    api_server/tests/spikes/test_phase28_spike_c_worker_bridge_ip.py (pytest.mark.spike pattern + subprocess.run scaffold),
    api_server/tests/spikes/test_respx_authlib.py (httpx + respx vs real-upstream pattern)
  </read_first>
  <action>
    Create 5 pytest spike modules, each marked `pytestmark = [pytest.mark.spike, pytest.mark.api_integration]`. Each spike makes REAL HTTP calls to upstream LLM providers (OpenRouter / OpenAI / Anthropic) — NO mocks, NO respx. Read the BYOK key from the env var that the local `.env` sets (probe SHALL fail loud with a clear message if missing). Each spike must also write a markdown artifact under `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-NN.md` containing raw curl-equivalent transcripts (request line, headers, full response bytes for short responses, first 4KB + last 1KB for streams), empirical numbers, and end with `VERDICT: PASS` or `VERDICT: FAIL`.

    Per-probe content:

    **PROBE-VAL-01** (`test_phase29_probe_val_01_openrouter_usage.py`): Hit `POST https://openrouter.ai/api/v1/chat/completions` with `{"model":"anthropic/claude-haiku-4-5","messages":[{"role":"user","content":"hi"}],"stream":true,"stream_options":{"include_usage":true},"max_tokens":50}`. Capture EVERY `data: ` line. Assert: (a) at least one chunk has `usage.prompt_tokens > 0` AND `usage.completion_tokens > 0`; (b) note position of `[DONE]` relative to the usage chunk (before/after — record literally in artifact). Repeat with `stream=false` and assert response body has `usage` block. Artifact records the FULL ordering of `[DONE]` vs usage chunk. VERDICT PASS iff both stream=true and stream=false carry usage.

    **PROBE-VAL-02** (`test_phase29_probe_val_02_x_generation_id.py`): Same chat call (stream=true + stream=false + intentional 401 by sending key="invalid"). For each variant dump the FULL response headers and assert `X-Generation-Id` (or `X-Request-Id` fallback per OR docs) is present. Artifact records the exact header name + value shape (regex-matchable) + on which variants it appears. VERDICT PASS iff X-Generation-Id appears on success AND streaming variants (4xx may legitimately omit).

    **PROBE-VAL-04** (`test_phase29_probe_val_04_auth_swap.py`): Three sub-tests. (a) `GET https://openrouter.ai/api/v1/key` with valid key → 200; with `key="invalid"` → 401. (b) `POST https://api.anthropic.com/v1/messages` with `{"model":"claude-haiku-4-5","max_tokens":1,"messages":[{"role":"user","content":"hi"}]}` + headers `x-api-key: <valid>` + `anthropic-version: 2023-06-01` → 200; OMIT `anthropic-version` → expect 400 (record body); replace with `Authorization: Bearer <valid>` → expect 401. (c) `GET https://api.openai.com/v1/models` with `Authorization: Bearer <valid>` → 200; with invalid → 401. Artifact records all 6 status codes + response bodies. VERDICT PASS iff all auth-swap shapes match the dispatch table in 29-RESEARCH.md §Provider Dispatch Table.

    **PROBE-VAL-11** (`test_phase29_probe_val_11_openrouter_key_endpoint.py`): Reliability sweep — call `GET https://openrouter.ai/api/v1/key` with valid key 5 times in succession, with invalid key 5 times. Assert: 5/5 valid return 200 with `data.label` AND `data.usage` fields populated; 5/5 invalid return 401. Record the `data.usage` and `data.limit` field shapes verbatim in the artifact. VERDICT PASS iff 10/10 calls return the expected status.

    **PROBE-VAL-14** (`test_phase29_probe_val_14_streaming_4xx.py`): Force OpenRouter into mid-stream error by sending a chat with `max_tokens=999999999` (or some intentionally invalid combo that triggers an upstream-model error AFTER the stream opens). Capture the raw stream bytes. Document: did the response open with HTTP 200 then emit `event: error\ndata: {...}` mid-stream, OR did it open with HTTP 4xx? Record the raw bytes in the artifact. Repeat for OpenAI and Anthropic. VERDICT PASS iff the parser strategy in 29-RESEARCH.md §Stream interrupted / 4xx mid-stream is empirically supportable (i.e. errors arrive in a recognizable shape).

    Each spike module imports `httpx`, `pytest`, and `os`. The artifact-writing helper takes the form:
    ```python
    def _write_artifact(path: Path, body: str, verdict: str) -> None:
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(f"# {path.stem}\n\n{body}\n\nVERDICT: {verdict}\n")
    ```
  </action>
  <verify>
    <automated>cd api_server && uv run pytest tests/spikes/test_phase29_probe_val_01_openrouter_usage.py tests/spikes/test_phase29_probe_val_02_x_generation_id.py tests/spikes/test_phase29_probe_val_04_auth_swap.py tests/spikes/test_phase29_probe_val_11_openrouter_key_endpoint.py tests/spikes/test_phase29_probe_val_14_streaming_4xx.py -v -m spike</automated>
  </verify>
  <acceptance_criteria>
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-01.md` exists and contains the literal string `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-02.md` exists and contains `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-04.md` exists and contains `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-11.md` exists and contains `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-14.md` exists and contains `VERDICT: PASS`,
    Command `grep -l "VERDICT: PASS" .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-{01,02,04,11,14}.md | wc -l` outputs `5`,
    All 5 pytest modules carry `pytestmark = [pytest.mark.spike, pytest.mark.api_integration]` (grep-checkable: `grep -l "pytest.mark.spike" api_server/tests/spikes/test_phase29_probe_val_{01,02,04,11,14}_*.py | wc -l` outputs `5`)
  </acceptance_criteria>
  <done>
    5/5 PASS verdicts recorded; one artifact per probe; pytest passes with `-m spike` filter; raw transcripts embedded for downstream plan reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: PROBE-VAL-03 + 13 — OpenRouter post-hoc latency + Anthropic SSE cumulative-output</name>
  <files>
    api_server/tests/spikes/test_phase29_probe_val_03_post_hoc_latency.py,
    api_server/tests/spikes/test_phase29_probe_val_13_anthropic_sse.py,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-03.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-13.md
  </files>
  <read_first>
    .planning/phases/29-llm-egress-proxy/29-CONTEXT.md (AMD-07 — last-wins on output_tokens),
    .planning/phases/29-llm-egress-proxy/29-RESEARCH.md (lines 247-285 — Anthropic SSE event sequence + Pitfall 1),
    .planning/phases/29-llm-egress-proxy/29-PATTERNS.md (lines 363-466 — StreamUsageParser specification with AMD-07 cumulative rule)
  </read_first>
  <action>
    **PROBE-VAL-03** (`test_phase29_probe_val_03_post_hoc_latency.py`): Loop 5 chat completions against OpenRouter (small model + small max_tokens to keep cost low). For each iteration: (1) POST stream=true; (2) capture `X-Generation-Id`; (3) record `t0=time.monotonic()` AT stream-close; (4) poll `GET https://openrouter.ai/api/v1/generation?id=<X-Generation-Id>` every 500ms until status=200 (max 30s timeout); (5) record `latency_s = first_200_time - t0`. Compute p50/p95/p99 of the 5 latencies. Also: confirm the SAME per-deploy BYOK key (the one that minted the generation) returns 200 — verify Assumption A1 from 29-RESEARCH.md. Record `data.total_cost` field shape (Decimal-castable string). Artifact records all 5 (latency, total_cost) tuples + the percentiles + Assumption A1 verdict. VERDICT PASS iff (a) p95 ≤ 5.0s, (b) `data.total_cost` exists as a number, (c) the original BYOK key works for the lookup.

    **PROBE-VAL-13** (`test_phase29_probe_val_13_anthropic_sse.py`): Real Anthropic streaming call to `POST https://api.anthropic.com/v1/messages` with `{"model":"claude-haiku-4-5","max_tokens":50,"stream":true,"messages":[{"role":"user","content":"Count to ten in Spanish."}]}`. Capture EVERY raw `data:` line. Assert: (a) `message_start.message.usage.input_tokens > 0`; (b) `message_start.message.usage.cache_creation_input_tokens` and `cache_read_input_tokens` are present (may be 0); (c) at least 2 `message_delta` events; (d) **the sequence of `message_delta.usage.output_tokens` values is monotonically non-decreasing** (proves cumulative); (e) the LAST `message_delta.usage.output_tokens` value matches the API's reported total (the spike compares the last value vs `final_usage_total = sum_of_all_content_tokens` heuristically; if monotonic non-decreasing AND len(deltas) ≥ 2 AND values not all equal, the cumulative behavior is confirmed). Record raw `output_tokens` values (e.g. `[5, 12, 17, 23, 23]`) in the artifact. VERDICT PASS iff cumulative-not-delta is empirically confirmed AND the parser spec in 29-PATTERNS.md (lines 410-465) handles this shape (last-wins, NOT sum).
  </action>
  <verify>
    <automated>cd api_server && uv run pytest tests/spikes/test_phase29_probe_val_03_post_hoc_latency.py tests/spikes/test_phase29_probe_val_13_anthropic_sse.py -v -m spike</automated>
  </verify>
  <acceptance_criteria>
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-03.md` exists and contains `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-03.md` contains the literal string `p95` AND a line matching the regex `latency.*: [0-9]+\.[0-9]+`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-13.md` exists and contains `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-13.md` contains the literal string `output_tokens` AND a Python-parseable list of integers (e.g. `[5, 12, 17]`),
    Spike module `test_phase29_probe_val_13_anthropic_sse.py` contains an assertion proving monotonic non-decreasing `output_tokens` (grep-checkable: `grep -E "monotonic|non.decreas|<=" api_server/tests/spikes/test_phase29_probe_val_13_anthropic_sse.py | wc -l` ≥ 1)
  </acceptance_criteria>
  <done>
    PROBE-VAL-03 + PROBE-VAL-13 PASS; latency percentiles + cumulative-output empirics committed; AMD-07 parser rule empirically validated.
  </done>
</task>

<task type="auto">
  <name>Task 3: PROBE-VAL-05 + 12 — Recipe BASE_URL honoring + openai-SDK Bearer placeholder acceptance</name>
  <files>
    api_server/tests/spikes/test_phase29_probe_val_05_recipe_base_url.py,
    api_server/tests/spikes/test_phase29_probe_val_12_ap_proxy_placeholder_bearer.py,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-05.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-12.md
  </files>
  <read_first>
    .planning/phases/29-llm-egress-proxy/29-CONTEXT.md (D-18 + AMD-06 — runtime.via_proxy env shape),
    .planning/phases/29-llm-egress-proxy/29-RESEARCH.md (lines 525-549 — bot-side env injection),
    recipes/nanobot.yaml (channels.inapp + runtime.process_env shape),
    recipes/hermes.yaml,
    recipes/openclaw.yaml,
    recipes/zeroclaw.yaml,
    recipes/nullclaw.yaml
  </read_first>
  <action>
    **PROBE-VAL-05** (`test_phase29_probe_val_05_recipe_base_url.py`): For each of 5 recipes (`hermes`, `nanobot`, `openclaw`, `zeroclaw`, `nullclaw`), load the YAML and inspect `runtime.process_env.api_key`. For each, run a `subprocess.run(["docker","run","--rm","-e",f"OPENAI_BASE_URL=http://example.invalid/v1","-e","OPENAI_API_KEY=test","-e",f"ANTHROPIC_BASE_URL=http://example.invalid","-e","ANTHROPIC_API_KEY=test", image, *args])` for ~5s and `--rm` semantics permitting; tail stderr/stdout for the bot's outbound resolution attempts (DNS lookup of `example.invalid`, or `httpx.ConnectError` referencing `example.invalid`). Record: did the bot emit traffic to example.invalid (PASS — honors BASE_URL) or did it try to reach api.openrouter.ai/api.anthropic.com (FAIL — hardcoded). Use `docker logs` after a short timeout to capture output even if container blocks. Recipes that don't honor BASE_URL must be flagged in the artifact for Phase 30 cleanup. VERDICT PASS iff `nanobot` (the only Phase 29 cutover recipe per AMD-01) honors BASE_URL; other 4 recipes' verdicts are recorded as `FLAGGED-FOR-PHASE-30` (informational, NOT blocking Phase 29).

    **PROBE-VAL-12** (`test_phase29_probe_val_12_ap_proxy_placeholder_bearer.py`): Probe both `openai>=1.0` Python SDK and (if installed) `langchain-openai` to verify they accept `OPENAI_API_KEY=ap-proxy-<random32hex>` without local validation rejection. Construct: `from openai import OpenAI; client = OpenAI(api_key="ap-proxy-deadbeef" * 8, base_url="http://localhost:39999/v1"); resp = client.chat.completions.create(model="x", messages=[{"role":"user","content":"hi"}])` and assert the failure mode is a CONNECTION error (couldn't reach localhost:39999) NOT a key-validation error. If `langchain_openai` is importable, repeat with `from langchain_openai import ChatOpenAI; llm = ChatOpenAI(api_key="ap-proxy-...", base_url="http://localhost:39999/v1"); llm.invoke("hi")`. Use a `socket.socket().bind(("127.0.0.1", 39999)); listen()` to accept the connection and immediately close (so the SDK's first byte is a TCP RST, distinguishing key-validation-rejection from connection-shape problems). VERDICT PASS iff both libraries connect to the placeholder URL (don't reject the key shape locally).
  </action>
  <verify>
    <automated>cd api_server && uv run pytest tests/spikes/test_phase29_probe_val_05_recipe_base_url.py tests/spikes/test_phase29_probe_val_12_ap_proxy_placeholder_bearer.py -v -m spike</automated>
  </verify>
  <acceptance_criteria>
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-05.md` exists and contains `VERDICT: PASS` AND the literal string `nanobot:` followed by either `HONORS-BASE_URL` or `FAIL` (grep-checkable),
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-05.md` contains explicit verdicts for ALL 5 recipes (one line per recipe matching `^- (hermes|nanobot|openclaw|zeroclaw|nullclaw):`),
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-12.md` exists and contains `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-12.md` contains the literal string `ap-proxy-` AND records the connection-vs-validation outcome verbatim
  </acceptance_criteria>
  <done>
    nanobot's BASE_URL-honoring confirmed (Phase 29 cutover unblocked); other 4 recipes flagged for Phase 30; openai-SDK + langchain-openai accept placeholder Bearer.
  </done>
</task>

<task type="auto">
  <name>Task 4: PROBE-VAL-06 + 09 — Idempotency in-flight gap + idempotency_keys table shape</name>
  <files>
    api_server/tests/spikes/test_phase29_probe_val_06_idempotency_in_flight.py,
    api_server/tests/spikes/test_phase29_probe_val_09_idempotency_keys_shape.py,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-06.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-09.md
  </files>
  <read_first>
    api_server/src/api_server/services/idempotency.py (existing primitive — check_or_reserve + write_idempotency + idempotency_keys schema),
    api_server/tests/test_idempotency.py (existing race tests),
    .planning/phases/29-llm-egress-proxy/29-CONTEXT.md (AMD-02 + AMD-03 — Postgres-backed + reserved-row pattern),
    .planning/phases/29-llm-egress-proxy/29-RESEARCH.md (lines 348-373 — Idempotency Strategy + O-03 in-flight gap)
  </read_first>
  <action>
    **PROBE-VAL-06** (`test_phase29_probe_val_06_idempotency_in_flight.py`): Empirically reproduce the O-03 in-flight gap. Spin up testcontainers Postgres 17, run alembic upgrade head (latest = 012 today; 013 lands in Plan 02 so this spike runs against pre-013 schema). Two coroutines call `check_or_reserve(conn, user_id=U, key=K, request_body_hash=H)` 50ms apart; both `await asyncio.sleep(3.0)` (simulating upstream); both write a `usage_logs`-shaped verdict. Assert: BOTH reach `("miss", None)` (proving the existing primitive's gap; required to justify AMD-03's reserved-row extension). THEN reproduce AMD-03 strategy: insert a placeholder row `INSERT INTO idempotency_keys(user_id, key, request_body_hash, verdict_json, status, expires_at) VALUES (..., NULL, 'in_flight', NOW()+INTERVAL '1 hour') ON CONFLICT DO NOTHING RETURNING xmax`; the second caller sees `xmax=0` (existing row) → polls `SELECT verdict_json, status FROM idempotency_keys WHERE user_id=$1 AND key=$2` every 100ms (max 5s) until status='success'; replays the verdict. Assert: 2/2 callers terminate with the SAME verdict; only 1 upstream "call" happened (counter increment). Note: the AMD-03 path requires migration 013 to add `status` column + `'in_flight'` literal — the spike implements both branches and records that the existing primitive does NOT support in_flight today (motivating the column add). VERDICT PASS iff (a) the gap is reproduced (existing primitive double-charges) AND (b) the AMD-03 strategy single-charges.

    **PROBE-VAL-09** (`test_phase29_probe_val_09_idempotency_keys_shape.py`): Inspect the existing `idempotency_keys` table shape. Run `\d idempotency_keys` (via asyncpg `pg_attribute` query) and record verbatim: column names, types, nullability, indexes. Confirm the columns the proxy needs (user_id, key, request_body_hash, verdict_json, expires_at) exist; identify what AMD-03 requires migration 013 to ADD (`status` text column with CHECK constraint allowing `'in_flight'|'success'|'failed'`). Verify the existing `expires_at` column supports the 24h TTL Phase 29 inherits. VERDICT PASS iff the shape is documented + the migration-013 delta is enumerated.
  </action>
  <verify>
    <automated>cd api_server && uv run pytest tests/spikes/test_phase29_probe_val_06_idempotency_in_flight.py tests/spikes/test_phase29_probe_val_09_idempotency_keys_shape.py -v -m spike</automated>
  </verify>
  <acceptance_criteria>
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-06.md` exists and contains `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-06.md` contains the literal string `gap reproduced` AND `AMD-03 single-charges`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-09.md` exists and contains `VERDICT: PASS`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-09.md` enumerates the migration-013 delta (must contain `status` column AND `in_flight` literal),
    Spike module `test_phase29_probe_val_06_idempotency_in_flight.py` contains an assertion comparing upstream-call-count between the gap and the AMD-03 path (grep-checkable: `grep -E "call_count|upstream_count|attempts" api_server/tests/spikes/test_phase29_probe_val_06_idempotency_in_flight.py | wc -l` ≥ 2)
  </acceptance_criteria>
  <done>
    O-03 gap empirically confirmed; AMD-03 reserved-row strategy empirically validates; migration-013 column delta enumerated.
  </done>
</task>

<task type="auto">
  <name>Task 5: PROBE-VAL-07 + 08 + 10 + 15 — age-cipher reuse + StreamingResponse tee + Docker bridge IP refresh + uniqueness scope</name>
  <files>
    api_server/tests/spikes/test_phase29_probe_val_07_age_cipher_provider_key.py,
    api_server/tests/spikes/test_phase29_probe_val_08_streaming_tee.py,
    api_server/tests/spikes/test_phase29_probe_val_10_docker_bridge_refresh.py,
    api_server/tests/spikes/test_phase29_probe_val_15_bridge_ip_uniqueness.py,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-07.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-08.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-10.md,
    .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-15.md
  </files>
  <read_first>
    api_server/src/api_server/crypto/age_cipher.py (encrypt_channel_config + decrypt_channel_config + AP_CHANNEL_MASTER_KEY shape),
    api_server/src/api_server/services/inapp_recipe_index.py (lines 346-396 — get_container_ip + Docker SDK shape),
    api_server/tests/spikes/test_phase28_spike_c_worker_bridge_ip.py (Docker SDK + bridge-IP probe pattern),
    .planning/phases/29-llm-egress-proxy/29-RESEARCH.md (lines 376-401 — Restart Resilience + bridge_ip uniqueness)
  </read_first>
  <action>
    **PROBE-VAL-07** (`test_phase29_probe_val_07_age_cipher_provider_key.py`): Round-trip encrypt + decrypt a known plaintext with `encrypt_channel_config(user_id, {"key": "or-test-deadbeef" * 4})` → bytes; `decrypt_channel_config(user_id, ciphertext)` → dict. Assert decrypted dict matches input. Verify the master key resolution: with `AP_CHANNEL_MASTER_KEY` set + with it unset (dev fallback should be the deterministic 32-zero-bytes path). Verify per-user-KEK isolation: `decrypt_channel_config(other_user_id, ciphertext)` MUST raise — different users cannot decrypt each other's keys. Record the hex-encoded ciphertext byte length + entropy in the artifact. VERDICT PASS iff round-trip + cross-user-isolation both hold AND the dev-fallback path works without `AP_CHANNEL_MASTER_KEY`.

    **PROBE-VAL-08** (`test_phase29_probe_val_08_streaming_tee.py`): Pure FastAPI + httpx in-process probe (no Docker). Spin up a tiny aiohttp server on `127.0.0.1:39998` that streams 10 `data: chunk{N}\n\n` lines with 100ms inter-line sleeps. Use `httpx.AsyncClient.stream("GET", "http://127.0.0.1:39998/sse")` + `aiter_raw()` and `yield chunk` from a `StreamingResponse` mounted on a tiny FastAPI app. Use `TestClient.stream(...)` (or httpx against the FastAPI app via ASGI in-process) to consume the proxy's stream and measure: (a) wall-clock latency between bot's first-byte-received and api_server's first-byte-from-upstream (target ≤50ms); (b) NO buffering — chunks arrive at the bot at the same 100ms cadence as upstream emits them; (c) `aclose()` on premature client disconnect closes the upstream stream cleanly (no `httpx.ResourceWarning` in the test output). Record empirical wall-clock numbers. VERDICT PASS iff per-chunk latency overhead < 50ms median AND no resource leak on premature close.

    **PROBE-VAL-10** (`test_phase29_probe_val_10_docker_bridge_refresh.py`): Docker daemon required (skip on macOS-only-no-Docker; allow on Linux + macOS-with-Docker-Desktop). Start a `nginx:alpine` container on the existing Docker bridge net (`deploy_default` if available, else `bridge`). Capture its IP via `client.containers.get(c.id).attrs['NetworkSettings']['Networks'][<net>]['IPAddress']`. Restart it via `c.restart()`. Capture the new IP. Record whether the IP changed. Also: subscribe to `client.events(filters={'event': ['start', 'die']}, decode=True)` for 5s while restarting; record whether the events arrive in real-time. Decide between 60s polling and `events()` based on the empirical evidence — the artifact must record the decision verbatim. VERDICT PASS iff (a) IP behavior is documented (whether it changes or not) AND (b) `events()` viability is empirically established (works or doesn't on this platform).

    **PROBE-VAL-15** (`test_phase29_probe_val_15_bridge_ip_uniqueness.py`): Same testcontainer pattern as PROBE-VAL-10 but focused on uniqueness. Start container A (record IP), STOP it (state='exited'), start container B (record IP). On Linux Docker bridge, IPs are typically reused. Assert: the IP→user lookup MUST scope to `container_status='running'` to avoid mis-identifying user A's freshly-stopped container as user B's new container. The spike inserts simulated `agent_containers` rows with the same IP but different statuses and runs the SQL query `SELECT user_id FROM agent_containers WHERE bridge_ip=$1 AND container_status='running'` to assert exactly ONE row returns. Also: verify the partial unique index `ix_agent_containers_bridge_ip_running` (defined in 29-PATTERNS.md migration block) prevents two `running` rows from claiming the same IP simultaneously. VERDICT PASS iff (a) IP reuse on Docker bridge is empirically documented AND (b) the running-only scope filter is empirically validated against the simulated data.
  </action>
  <verify>
    <automated>cd api_server && uv run pytest tests/spikes/test_phase29_probe_val_07_age_cipher_provider_key.py tests/spikes/test_phase29_probe_val_08_streaming_tee.py tests/spikes/test_phase29_probe_val_10_docker_bridge_refresh.py tests/spikes/test_phase29_probe_val_15_bridge_ip_uniqueness.py -v -m spike</automated>
  </verify>
  <acceptance_criteria>
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-07.md` exists and contains `VERDICT: PASS` AND the literal string `cross-user-isolation`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-08.md` exists and contains `VERDICT: PASS` AND a numeric latency measurement matching the regex `latency.*: [0-9]+(\.[0-9]+)?\s*(ms|s)`,
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-10.md` exists and contains `VERDICT: PASS` AND records a decision between `60s polling` and `events() subscription` (one of those two strings must appear),
    File `.planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-15.md` exists and contains `VERDICT: PASS` AND the literal string `container_status='running'`,
    All 4 spike modules carry `pytestmark = [pytest.mark.spike, pytest.mark.api_integration]` (grep-checkable),
    The full Wave-0 artifact set is complete: `ls .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-*.md | wc -l` outputs `15`
  </acceptance_criteria>
  <done>
    All 15 PROBE-VAL artifacts committed with PASS verdicts; Wave-0 complete; Wave 1 unblocked. The orchestrator pauses HERE for user confirmation that the spike findings agree with research assumptions before Wave 1 plans execute.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Wave 0 spike-gate review checkpoint</name>
  <files>
    (no source files modified — human review of the 15 PROBE-VAL artifacts produced by Tasks 1-5)
  </files>
  <action>
    Pause execution. The orchestrator surfaces this checkpoint to the human reviewer with the contents of the &lt;what-built&gt; and &lt;how-to-verify&gt; blocks below. The executor takes NO automated action; it waits for the resume-signal. On "approved" Wave 1 begins; on a FAIL description the orchestrator triggers an AMD-08+ amendment cycle on 29-CONTEXT.md.
  </action>
  <verify>
    <automated>ls .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-*.md | wc -l | grep -q '^15$' &amp;&amp; ! grep -L 'VERDICT: PASS' .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-*.md | grep -q PROBE</automated>
  </verify>
  <done>
    Human reviewer has typed "approved" after confirming all 15 PROBE-VAL artifacts exist and end with VERDICT: PASS, and that the empirical numbers match the assumptions baked into Plans 02-09. Wave 1 unblocked.
  </done>
  <what-built>
    All 15 PROBE-VAL pytest spike modules + 15 markdown artifacts under `.planning/phases/29-llm-egress-proxy/spikes/`. Each artifact ends with `VERDICT: PASS` (or `VERDICT: FAIL` if a probe surfaced an unexpected finding).
  </what-built>
  <how-to-verify>
    1. Run `ls .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-*.md | wc -l` — must output `15`.
    2. Run `grep -L "VERDICT: PASS" .planning/phases/29-llm-egress-proxy/spikes/PROBE-VAL-*.md` — must output NOTHING (empty list = all PASS).
    3. Open each PROBE-VAL-NN.md and skim the empirical numbers — confirm they match the assumptions in 29-RESEARCH.md (esp. PROBE-VAL-13 cumulative-output, PROBE-VAL-03 ≤5s p95, PROBE-VAL-08 ≤50ms tee overhead).
    4. If ANY probe FAILed: triage the finding, decide whether to add an AMD-08+ amendment to CONTEXT.md (changing locked decisions to match empirical reality), or revise the failing assumption inside the plan that owns it.
    5. If all 15 PASS: type "approved" and Wave 1 begins.
  </how-to-verify>
  <resume-signal>Type "approved" to unblock Wave 1, or describe the FAIL finding to trigger an amendment cycle.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spike module → upstream LLM provider | BYOK key crosses TLS boundary; key MUST come from .env (never hardcoded in test) |
| Spike artifact ← test output | spike must NOT write the BYOK key into the artifact (artifacts are committed to git) |
| Docker daemon ← spike subprocess | docker SDK calls run with the ambient daemon's privileges (already trusted boundary on dev box) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | spike artifact files | mitigate | Each spike's artifact-writer redacts BYOK keys via the same `_redact_creds(text, key)` helper specified in 29-PATTERNS.md (or skips logging the key entirely — only status codes + structural shapes go into the markdown) |
| T-29-06 | Information Disclosure | OpenRouter `user` parameter (echoed in `data.usage` of generation endpoint) | accept | The user_id and agent_instance_id are UUIDs already exposed to the user via the dashboard; no PII risk |
</threat_model>

<verification>
- All 15 PROBE-VAL pytest modules pass with `pytest -m spike` (real upstream calls; will fail on missing `.env` BYOK keys).
- All 15 markdown artifacts exist under `.planning/phases/29-llm-egress-proxy/spikes/` with `VERDICT: PASS` or `VERDICT: FAIL`.
- BYOK keys never appear in any artifact (`grep -RE "sk-or-|sk-ant-|sk-proj-" .planning/phases/29-llm-egress-proxy/spikes/` outputs nothing).
- The Wave-0 checkpoint Task 6 has been resolved with "approved" by the human reviewer.
</verification>

<success_criteria>
- 15/15 PROBE-VAL artifacts exist and end with `VERDICT: PASS`.
- Empirical numbers (latencies, percentiles, status codes, byte shapes) recorded for each probe.
- Cumulative-output rule (AMD-07) empirically validated against real Anthropic stream.
- Reserved-row idempotency strategy (AMD-03) empirically validated against testcontainer PG.
- nanobot recipe confirmed honoring `OPENAI_BASE_URL` env (PROBE-VAL-05).
- openai-SDK + langchain-openai accept `ap-proxy-*` placeholder Bearer (PROBE-VAL-12).
- Wave-0 spike-gate Task 6 approved by human reviewer.
</success_criteria>

<output>
After completion, create `.planning/phases/29-llm-egress-proxy/29-01-SUMMARY.md` listing each PROBE-VAL with its verdict + empirical highlights + a "no surprises" statement (or, if surprises occurred, the AMD-08+ amendment proposed).
</output>
