---
phase: 09-spec-lint-test-harness-foundations
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - Makefile
  - .github/workflows/test-recipes.yml
autonomous: true
requirements:
  - D-10
  - D-20
  - D-21

must_haves:
  truths:
    - "make install-tools succeeds and installs ap-recipe-tools with dev deps"
    - "make test runs pytest and exits 0"
    - "make lint-recipes runs --lint-all and exits 0"
    - "make check runs lint-recipes then test and exits 0"
    - "GitHub Actions workflow runs make check on push and PR"
  artifacts:
    - path: "Makefile"
      provides: "install-tools, test, lint-recipes, check targets"
      contains: "install-tools"
    - path: ".github/workflows/test-recipes.yml"
      provides: "CI pipeline for recipe lint + tests"
      contains: "make check"
  key_links:
    - from: "Makefile (lint-recipes)"
      to: "tools/run_recipe.py --lint-all"
      via: "make target"
      pattern: "run_recipe.py --lint-all"
    - from: "Makefile (test)"
      to: "pytest tools/tests/"
      via: "make target"
      pattern: "pytest"
    - from: ".github/workflows/test-recipes.yml"
      to: "Makefile (check)"
      via: "CI step"
      pattern: "make check"
---

<objective>
Add Makefile targets for the Python tooling (install-tools, test, lint-recipes, check) and create a minimal GitHub Actions workflow that runs `make check` on push and PR.

Purpose: Developers and CI can validate recipes and run tests with a single command. This is the integration layer that ties together the schema, lint, and tests from Plans 01-03.

Output: Updated `Makefile` with 4 new targets. New `.github/workflows/test-recipes.yml` CI workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-spec-lint-test-harness-foundations/09-CONTEXT.md
@Makefile
@tools/pyproject.toml

<interfaces>
<!-- From Plans 01-03: what the Makefile targets invoke -->

From tools/pyproject.toml (Plan 01):
  pip install -e "tools/[dev]"  -- installs ruamel.yaml, jsonschema, pytest

From tools/run_recipe.py (Plan 02):
  python3 tools/run_recipe.py --lint-all  -- validates all recipes/*.yaml

From tools/tests/ (Plan 03):
  pytest tools/tests/ -v  -- runs all test modules
  pytest tools/tests/ -x --tb=short  -- quick check mode
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Makefile targets for Python tooling</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (full current content -- must append to existing targets without breaking them)
    - tools/pyproject.toml (created in Plan 01 -- confirms install command)
  </read_first>
  <action>
Append 4 new targets to the existing `Makefile` (per D-10, D-20). Add them after the existing `lint-frontend:` target, under a new section comment.

Add to the `.PHONY` declaration at the top of the file: `install-tools test lint-recipes check`

Then add the targets:

```makefile
# --- Phase 09: Python recipe tools (D-10, D-20) ---

install-tools:
	pip install -e "tools/[dev]"

test:
	pytest tools/tests/ -v

lint-recipes:
	python3 tools/run_recipe.py --lint-all

check: lint-recipes test
```

The `check` target chains `lint-recipes` then `test` (D-20). If lint fails, test does not run (make's default sequential behavior).

IMPORTANT: Do NOT modify any existing targets. Only append the new section.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && make -n install-tools && make -n test && make -n lint-recipes && make -n check && echo "All 4 targets defined"</automated>
  </verify>
  <acceptance_criteria>
    - Makefile contains target `install-tools:` with command `pip install -e "tools/[dev]"`
    - Makefile contains target `test:` with command containing `pytest tools/tests/`
    - Makefile contains target `lint-recipes:` with command containing `run_recipe.py --lint-all`
    - Makefile contains target `check: lint-recipes test`
    - Makefile .PHONY line includes `install-tools test lint-recipes check`
    - `make -n check` (dry run) shows lint-recipes then test commands
    - All existing Makefile targets (build-ap-base, build-recipes, smoke-test, etc.) are unchanged
  </acceptance_criteria>
  <done>4 Makefile targets (install-tools, test, lint-recipes, check) are defined and correctly chained. Existing targets unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions workflow for recipe CI</name>
  <files>.github/workflows/test-recipes.yml</files>
  <read_first>
    - Makefile (verify the check target exists and what it runs)
  </read_first>
  <action>
Create `.github/workflows/test-recipes.yml` (per D-21). This is a minimal CI workflow that runs `make check` (which runs `lint-recipes` then `test`) on push and PR.

Per D-21: Python tooling only -- no Go tests, no deploy, no Docker.

```yaml
name: Recipe lint + tests

on:
  push:
    branches: [main]
    paths:
      - 'tools/**'
      - 'recipes/**'
      - 'docs/RECIPE-SCHEMA.md'
  pull_request:
    branches: [main]
    paths:
      - 'tools/**'
      - 'recipes/**'
      - 'docs/RECIPE-SCHEMA.md'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: make install-tools

      - name: Run lint + tests
        run: make check
```

Key design decisions:
- `paths` filter: only triggers on changes to `tools/`, `recipes/`, or `docs/RECIPE-SCHEMA.md`. Go code, frontend, and infrastructure changes do not trigger this workflow.
- Python 3.12: CI enforces the stricter Python version per D-19 (local dev allows 3.10).
- `ubuntu-latest`: standard, no special requirements.
- No Docker needed: all tests mock subprocess.run (D-12).
- The `make check` target chains `lint-recipes` (validates all recipes) then `test` (runs pytest suite).

Create the `.github/workflows/` directory if it does not exist.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && test -f .github/workflows/test-recipes.yml && python3 -c "
import yaml
with open('.github/workflows/test-recipes.yml') as f:
    wf = yaml.safe_load(f)
assert 'on' in wf, 'Missing on trigger'
assert 'push' in wf['on'], 'Missing push trigger'
assert 'pull_request' in wf['on'], 'Missing PR trigger'
assert 'check' in wf['jobs'], 'Missing check job'
steps = wf['jobs']['check']['steps']
step_names = [s.get('name', '') for s in steps]
assert any('Install' in n for n in step_names), 'Missing install step'
assert any('lint' in n.lower() or 'check' in n.lower() for n in step_names), 'Missing check step'
print('Workflow structure valid')
"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/test-recipes.yml exists
    - Workflow triggers on push to main and pull_request to main
    - Workflow has path filters for tools/, recipes/, docs/RECIPE-SCHEMA.md
    - Workflow uses actions/setup-python@v5 with python-version "3.12"
    - Workflow runs `make install-tools` followed by `make check`
    - Workflow does NOT include Docker, Go, or deployment steps
    - Workflow job runs on ubuntu-latest
  </acceptance_criteria>
  <done>GitHub Actions workflow at .github/workflows/test-recipes.yml runs make check on push/PR. Filters to tools/, recipes/, and schema changes only. Uses Python 3.12 per D-19 CI enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> repo code | Standard CI trust model; runner executes repo code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Elevation of Privilege | GitHub Actions workflow | accept | Workflow has no write permissions, no secrets, no deploy steps; runs read-only lint and tests |
| T-09-08 | Tampering | Makefile targets | accept | Makefile is version-controlled; make targets run local Python tools with no network access |
</threat_model>

<verification>
1. `make -n check` shows lint-recipes then test commands (dry run)
2. `make check` runs successfully (lint all recipes + run all tests)
3. `.github/workflows/test-recipes.yml` passes YAML syntax check
4. Workflow triggers are scoped to relevant paths only
</verification>

<success_criteria>
- `make install-tools` installs the Python package in editable mode with dev dependencies
- `make test` runs the pytest suite and exits 0
- `make lint-recipes` validates all 5 recipes and exits 0
- `make check` chains lint then test and exits 0
- GitHub Actions workflow is valid YAML with correct triggers, Python 3.12 setup, and `make check` execution
</success_criteria>

<output>
After completion, create `.planning/phases/09-spec-lint-test-harness-foundations/09-04-SUMMARY.md`
</output>
