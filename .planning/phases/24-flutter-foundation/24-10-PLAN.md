---
phase: 24-flutter-foundation
plan: 10
type: execute
wave: 5
depends_on: [24-09]
files_modified:
  - spikes/flutter-api-roundtrip.md
autonomous: false
worktree_safe: false
requirements: [APP-05]
must_haves:
  truths:
    - "spikes/flutter-api-roundtrip.md exists at REPO ROOT (not under mobile/)"
    - "YAML frontmatter has all 7 reproducibility fields (date / git_sha / flutter_sdk_version / recipe / model / base_url / target / verdict)"
    - "verdict is exactly the string PASS (per D-54)"
    - "Body narrates all 9 D-46 steps with observed output"
    - "Raw OPENROUTER_KEY value is NOT recorded (only its presence as env var)"
  artifacts:
    - path: "spikes/flutter-api-roundtrip.md"
      provides: "Phase 24 exit-gate evidence (D-53/D-54 + APP-05)"
      min_lines: 60
      contains: "verdict: PASS"
  key_links:
    - from: "spikes/flutter-api-roundtrip.md"
      to: "Phase 25 plan-checker precondition"
      via: "verdict: PASS string match"
      pattern: "verdict: PASS"
---

<objective>
Capture the spike artifact at `spikes/flutter-api-roundtrip.md` (REPO ROOT,
not under `mobile/` per D-45) with YAML frontmatter recording the
reproducibility metadata (D-54) AFTER a green `make spike` run from
Plan 09.

Purpose: Phase 25's plan-checker enforces the existence of this file
with `verdict: PASS` as a HARD precondition (D-53). Without it the
foundation has no exit-gate evidence.

Output: A committed markdown artifact mirroring the existing
`api_server/tests/spikes/` convention.

**This plan is `autonomous: false`** — manual capture by the developer
after a green run; the executor records the metadata + observed step
outputs. Same `worktree_safe: false` reasoning as Plan 09 (the spike
ran against the live api_server bind-mount; we commit on `main`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/24-flutter-foundation/24-CONTEXT.md
@.planning/phases/24-flutter-foundation/24-RESEARCH.md
@.planning/phases/24-flutter-foundation/24-PATTERNS.md

@.planning/phases/24-flutter-foundation/24-09-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Capture observed output from the green spike run</name>
  <files>(none — evidence-collection step; the dev paste-buffers raw output and DB rows. Task 1 writes the markdown.)</files>
  <action>HUMAN evidence collection. The developer copies the 6 pieces of evidence listed under <how-to-verify> from the green spike run into the resume-signal: (1) make spike stdout, (2) git rev-parse HEAD, (3) fvm flutter --version output, (4) target device label, (5) agent_instance DB row, (6) the 2 inapp_messages rows. The executor MUST NOT write the markdown until all 6 are provided. This task creates no files; it gates Task 1.</action>
  <verify>
    <automated>echo "checkpoint task — no automation; resume after dev pastes all 6 evidence items"</automated>
  </verify>
  <done>Developer pastes all 6 captured pieces of evidence into the resume signal; executor proceeds to Task 1.</done>
  <what-built>The dev gathered: the green output from `make spike` (Plan 09 Task 2), the runtime metadata (date, git sha, Flutter SDK version, target device), and the post-run DB state (agent_instance row + inapp_messages rows).</what-built>
  <how-to-verify>
    Provide the executor with the following evidence captured from the green run:

    1. **`make spike` stdout**: all "Step N PASS" lines (or the final "All tests passed!" if no per-step output was logged).

    2. **`git rev-parse HEAD`** at the moment of the green run.

    3. **`fvm flutter --version`** output (Flutter SDK version + Dart version + framework revision).

    4. **Target device**: was it `ios-simulator` (iPhone NN), `android-emulator` (Pixel NN), or `physical-device` (model + OS)?

    5. **The agent name + agent_instance_id** from the DB:
       `docker exec deploy-postgres-1 psql -U postgres -c "SELECT id, name, recipe_name, model, status FROM agent_instances ORDER BY created_at DESC LIMIT 1;"`

    6. **The 2 inapp_messages rows**:
       `docker exec deploy-postgres-1 psql -U postgres -c "SELECT id, content, status, length(bot_response) AS bot_response_len FROM inapp_messages WHERE agent_id = '<agent_id>' ORDER BY created_at;"`
  </how-to-verify>
  <resume-signal>Paste the 6 captured pieces of evidence; proceed to Task 1 once collected.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Write spikes/flutter-api-roundtrip.md with YAML frontmatter + 9-step narrative</name>
  <files>spikes/flutter-api-roundtrip.md</files>
  <read_first>
    - `.planning/phases/24-flutter-foundation/24-CONTEXT.md` D-45 (line 106 — markdown at spikes/flutter-api-roundtrip.md), D-46 (lines 108-116 — 9 steps), D-47 (line 117 — recipe + model pinned), D-54 (line 124 — frontmatter shape)
    - `.planning/phases/24-flutter-foundation/24-PATTERNS.md` Group 9 lines 749-768 (frontmatter shape spec)
  </read_first>
  <action>
    Create `spikes/` at the repo root if it doesn't exist (PATTERNS.md line 751 confirms it doesn't). Then create `spikes/flutter-api-roundtrip.md` with the captured evidence. Use this template — populate `<DEV-CAPTURED>` placeholders from Task 0:
    ```markdown
    ---
    date: <DEV-CAPTURED — yyyy-mm-dd of the green run>
    git_sha: <DEV-CAPTURED — full SHA from git rev-parse HEAD>
    flutter_sdk_version: 3.41.0
    recipe: nullclaw
    model: anthropic/claude-haiku-4-5
    base_url: http://localhost:8000
    target: <DEV-CAPTURED — ios-simulator | android-emulator | physical-device>
    verdict: PASS
    ---

    # Phase 24 Spike — Flutter ↔ Phase 23 backend 9-step round-trip

    **Status:** PASS — Phase 24 exit gate cleared (D-53).

    ## Why this exists

    Per CLAUDE.md Golden Rule #5 ("Test everything. Probe gray areas
    empirically BEFORE planning.") and Phase 24 D-46/D-53, a 9-step
    round-trip against a REAL local `api_server` + REAL Docker + REAL
    OpenRouter must demonstrate end-to-end that the foundation supports
    the Phase 25 chat UI BEFORE Phase 25's plan seals.

    ## What was exercised

    Each step exercises a load-bearing mechanism the Phase 25 chat UI will
    rely on. The 9 steps map verbatim to D-46:

    | Step | Endpoint / Behavior                                                    | Mechanism Proven                                |
    |------|------------------------------------------------------------------------|-------------------------------------------------|
    | 1    | POST /v1/runs (BYOK)                                                   | Authorization: Bearer header on /runs only (D-40); recipe + model passthrough |
    | 2    | POST /v1/agents/:id/start (channel='inapp')                            | Mobile-channel container spawn (Phase 23 D-28)  |
    | 3    | SSE connect on GET /v1/agents/:id/messages/stream                      | flutter_client_sse 2.0.3 + cookie injection     |
    | 4    | POST /v1/agents/:id/messages (Idempotency-Key)                         | Phase 23 D-09 — Idempotency-Key REQUIRED        |
    | 5    | SSE delivers an inapp_outbound event with non-empty content            | dispatcher → bot → reply path (Phase 22c.3)     |
    | 6    | GET /v1/agents/:id/messages?limit=10 — assistant content byte-equal SSE | Phase 23 D-08 — cross-channel parity            |
    | 7    | POST /v1/agents/:id/messages with SAME Idempotency-Key → SAME message_id | Phase 23 D-09 — replay cached                   |
    | 8    | disconnect mid-stream + reconnect with Last-Event-Id → no duplicates   | RESEARCH Pitfall #2 mitigation (Plan 05)         |
    | 9    | POST /v1/agents/:id/stop                                               | D-48 cleanup                                    |

    ## How to reproduce

    ```bash
    # 1. Start the api_server stack
    cd api_server && make dev   # Postgres + Redis + FastAPI under docker compose

    # 2. Sign in via browser OAuth and grab the cookie
    open http://localhost:3000/login
    # → sign in with Google → DevTools → Application → Cookies → ap_session value

    # 3. Run the spike (15 min hard timeout; typical 2-4 min)
    cd mobile && make spike \
      BASE_URL=http://localhost:8000 \
      SESSION_ID=<paste-from-browser> \
      OPENROUTER_KEY=$(grep OPENROUTER_KEY ../.env | cut -d= -f2)
    ```

    ## Observed output

    Test framework: `flutter_test` + `integration_test` (D-45).
    Target: <DEV-CAPTURED>.
    Wall clock: <DEV-CAPTURED — total runtime, e.g. ~3 min>.

    Test reports: `All tests passed!`

    ### Step-by-step narrative

    - **Step 1** — POST /v1/runs returned `agent_instance_id=<DEV-CAPTURED-uuid>`, `smoke_ok=true`. Recipe `nullclaw`, model `anthropic/claude-haiku-4-5` (D-47). Agent name: `<DEV-CAPTURED — spike-roundtrip-<ts>-<uuid>>`.
    - **Step 2** — POST /v1/agents/<id>/start with `{channel: 'inapp', channel_inputs: {}}` returned 202 within 60s.
    - **Step 3** — SSE connection established with `Cookie: ap_session=<redacted-last8>` header. `MessagesStream.lastEventId` is null at this point (no events yet).
    - **Step 4** — POST /v1/agents/<id>/messages with body `{content: "spike roundtrip"}` and header `Idempotency-Key: <uuid-v4>`. Response 202 + `message_id=<DEV-CAPTURED-msg-uuid>`.
    - **Step 5** — `MessagesStream.events` delivered an `inapp_outbound` event within bot timeout. `data` non-empty (assistant reply). `MessagesStream.lastEventId` updated to the seq id of the event.
    - **Step 6** — GET /v1/agents/<id>/messages?limit=10 returned the user message and the assistant reply. Last assistant row's `content` was byte-equal to the SSE-delivered content (Phase 23 D-08 verified).
    - **Step 7** — POST /v1/agents/<id>/messages with the SAME Idempotency-Key returned the SAME `message_id` (Phase 23 D-09 verified — IdempotencyMiddleware replayed the cached 202).
    - **Step 8** — `MessagesStream.disconnect()` did NOT clear `_lastEventId`. `MessagesStream.connect()` re-injected `Last-Event-Id: <prior-seq>` as a header. After posting "after-resume" with a new Idempotency-Key, the resumed stream received a new `inapp_outbound` event AFTER the cursor, with NO duplicate of the cursor event (RESEARCH Pitfall #2 mitigation proven).
    - **Step 9** — POST /v1/agents/<id>/stop returned 200/202; container cleaned up; `agent_instances.status` is `stopped`. D-48 honored.

    ## DB state post-run (D-48 — kept for inspection)

    ```
    agent_instances:
      id              | <DEV-CAPTURED uuid>
      name            | <DEV-CAPTURED spike-roundtrip-...>
      recipe_name     | nullclaw
      model           | anthropic/claude-haiku-4-5
      status          | stopped

    inapp_messages (2 rows for this agent):
      content="spike roundtrip"  status=done  bot_response_len=<DEV-CAPTURED int>
      content="after-resume"     status=done  bot_response_len=<DEV-CAPTURED int>
    ```

    ## What this UNBLOCKS

    - Phase 25 (mobile screens) plan-checker precondition satisfied (`verdict: PASS`).
    - The Plan 04 ApiClient + Plan 05 MessagesStream are proven against real infra; Phase 25 screens consume the same code paths.
    - The Phase 23 backend's Idempotency-Key + Last-Event-Id contracts are proven from a non-Python client.

    ## Caveats / known follow-ups

    - **OpenRouter key is BYOK in the spike** (D-49 / D-51). Phase 25 wires the
      actual entry UX (settings screen or first-deploy modal). The raw key
      value is NOT recorded in this artifact — only its presence as an env
      variable. The dev's gitignored `.env` holds the live secret.
    - **Session cookie is paste-from-browser** (D-49). Phase 25 replaces this
      with native `google_sign_in` calling POST /v1/auth/google/mobile.
    - **flutter_secure_storage is NOT exercised** in this spike — `--dart-define
      SESSION_ID=...` sidesteps the storage layer. The production path
      (Plan 04 SecureStorage + AuthInterceptor) is exercised via Plan 04's
      unit tests against an in-memory _FakeBackend; full device-Keychain
      verification is a Phase 25 concern (RESEARCH Pitfall #4 — iOS Simulator
      volatility).
    ```

    Step 2 — Replace every `<DEV-CAPTURED ...>` placeholder with the actual values from Task 0's evidence dump. The frontmatter `verdict: PASS` is fixed (no placeholder).

    Step 3 — Run final grep gates:
    - `verdict: PASS` is present
    - The frontmatter has all 7 fields (date, git_sha, flutter_sdk_version, recipe, model, base_url, target, verdict)
    - `<DEV-CAPTURED` does NOT appear anywhere (all placeholders replaced)
    - The string "OPENROUTER_KEY=sk-or-" does NOT appear (raw key not leaked)
  </action>
  <verify>
    <automated>test -f spikes/flutter-api-roundtrip.md && grep -q "^verdict: PASS$" spikes/flutter-api-roundtrip.md && grep -q "^date: " spikes/flutter-api-roundtrip.md && grep -q "^git_sha: " spikes/flutter-api-roundtrip.md && grep -q "^flutter_sdk_version: " spikes/flutter-api-roundtrip.md && grep -q "^recipe: nullclaw$" spikes/flutter-api-roundtrip.md && grep -q "^model: anthropic/claude-haiku-4-5$" spikes/flutter-api-roundtrip.md && grep -q "^base_url: " spikes/flutter-api-roundtrip.md && grep -q "^target: " spikes/flutter-api-roundtrip.md && ! grep -q "DEV-CAPTURED" spikes/flutter-api-roundtrip.md && ! grep -qE "sk-or-[a-zA-Z0-9]{20,}" spikes/flutter-api-roundtrip.md</automated>
  </verify>
  <done>
    spikes/flutter-api-roundtrip.md committed at repo root with `verdict: PASS`; all 7 frontmatter fields populated; no `<DEV-CAPTURED>` placeholders remain; no raw OPENROUTER_KEY value embedded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spike output → committed markdown | Test stdout / DB rows cross from the dev's machine into the public repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10-01 | Information Disclosure | Raw secret in artifact | mitigate | Grep gate `! grep -qE "sk-or-[a-zA-Z0-9]{20,}"` rejects any markdown that embeds an OpenRouter key. The artifact records ONLY the presence of the env var, never its value. |
| T-24-10-02 | Information Disclosure | session_id leak | mitigate | The session_id is referenced as `<paste-from-browser>` in the reproduction recipe; never as a literal value. Same redaction posture as the spike's RedactingLogInterceptor (Plan 04). |
| T-24-10-03 | Tampering | False PASS | mitigate | The `verdict: PASS` line is paired with the `git_sha` and `agent_instance_id` from the actual run — both forensically traceable. Plan 09's automated assertions are the actual PASS gate; this artifact is the human-readable record of those gates passing. |
</threat_model>

<verification>
- `spikes/flutter-api-roundtrip.md` exists at repo root
- Frontmatter has 7 required fields + `verdict: PASS`
- All `<DEV-CAPTURED>` placeholders replaced with real values
- No raw OPENROUTER_KEY value embedded
</verification>

<success_criteria>
- The artifact materializes Phase 25's plan-checker precondition: `grep -q "verdict: PASS" spikes/flutter-api-roundtrip.md` succeeds
- A future dev reading the artifact can re-run the spike from the recipe
- Phase 24 exit gate (D-53) is fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/24-flutter-foundation/24-10-SUMMARY.md`
</output>
