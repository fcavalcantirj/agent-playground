---
phase: 23-backend-mobile-api-chat-proxy-persistence-auth-shim
plan: 06
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - api_server/src/api_server/auth/oauth.py
  - api_server/src/api_server/routes/auth.py
  - api_server/tests/auth/conftest.py
  - api_server/tests/auth/test_oauth_mobile.py
autonomous: true
requirements:
  - API-05
must_haves:
  truths:
    - "POST /v1/auth/google/mobile accepts {id_token: string}, verifies the JWT against Google JWKS using verify_oauth2_token(audience=settings.oauth_google_mobile_client_ids), upserts the user, mints a session, and returns {session_id, expires_at, user}"
    - "POST /v1/auth/github/mobile accepts {access_token: string}, calls GitHub /user (and /user/emails fallback for private email), upserts the user, mints a session, returns the same shape"
    - "Invalid Google id_token (signature fail, expired, audience mismatch, missing sub or email) → 401 with Stripe-shape envelope"
    - "Invalid GitHub access_token (GitHub /user returns non-200, or no verified email recoverable) → 401 with Stripe-shape envelope"
    - "Browser OAuth callbacks (existing /v1/auth/google/callback + /v1/auth/github/callback) are NOT modified — mobile endpoints are purely additive"
    - "On success, the mobile response body returns the session_id (mobile has no cookie jar; the Flutter app stores it and re-sends as Cookie: ap_session=<uuid>); response does NOT call _set_session_cookie"
    - "9-cell coverage matrix per D-30: 5 Google scenarios + 3 GitHub scenarios + 1 cookie-continuity scenario all green"
  artifacts:
    - path: "api_server/src/api_server/auth/oauth.py"
      provides: "verify_google_id_token(id_token, mobile_client_ids) + verify_github_access_token(access_token, http_client) helpers"
    - path: "api_server/src/api_server/routes/auth.py"
      provides: "POST /v1/auth/google/mobile + POST /v1/auth/github/mobile handlers (additive — appended below the existing browser callbacks)"
    - path: "api_server/tests/auth/conftest.py"
      provides: "authenticated_mobile_session fixture mirroring the existing authenticated_cookie pattern (signs in via the new mobile endpoint, returns a session_id usable as Cookie header)"
    - path: "api_server/tests/auth/test_oauth_mobile.py"
      provides: "9 integration tests covering D-30 matrix"
  key_links:
    - from: "routes/auth.py::google_mobile + github_mobile"
      to: "auth/oauth.py::upsert_user + mint_session"
      via: "100% reuse — mobile path uses identical user-upsert and session-minting logic as browser callbacks"
      pattern: "upsert_user|mint_session"
    - from: "auth/oauth.py::verify_google_id_token"
      to: "google.oauth2.id_token.verify_oauth2_token(audience=mobile_client_ids)"
      via: "asyncio.to_thread wrapper (synchronous library call)"
      pattern: "verify_oauth2_token|asyncio\\.to_thread"
    - from: "config.py::settings.oauth_google_mobile_client_ids (Plan 23-01 Task 5)"
      to: "verify_google_id_token audience= argument"
      via: "list[str] passed verbatim"
      pattern: "oauth_google_mobile_client_ids"
---

<objective>
Implement two mobile credential-exchange endpoints (D-15..D-17, D-23, D-24, D-30 + REQ API-05): `POST /v1/auth/google/mobile` and `POST /v1/auth/github/mobile`. Each accepts a native-SDK-issued credential (Google id_token / GitHub access_token), verifies it server-side, upserts the user via the existing `upsert_user()` helper, mints a session via the existing `mint_session()` helper, and returns `{session_id, expires_at, user}`. The Flutter app stores the session_id and sends it back as `Cookie: ap_session=<uuid>` on subsequent requests — `ApSessionMiddleware` reads it transparently (D-17, no middleware changes).

Purpose: Real OAuth on mobile (D-15 — no dev-mode shim). Reuses 100% of Phase 22c-oauth-google's substrate; only the credential-verification layer is new. This is the load-bearing piece of Phase 23 because every other mobile-facing endpoint depends on session continuity established here.
Output: 2 helper functions in auth/oauth.py (~40 LOC) + 2 endpoint handlers in routes/auth.py (~70 LOC) + 1 fixture extension + 9 integration tests against testcontainers Postgres + respx-mocked OAuth providers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PATTERNS.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Source-of-truth pattern excerpts the executor MUST mirror. -->

auth/oauth.py:44-56 (existing module-level pattern — _log + module-level cache):
```
_log = logging.getLogger("api_server.auth.oauth")
_oauth: OAuth | None = None
_DEV_PLACEHOLDER = "dev-placeholder-not-for-prod"
```

auth/oauth.py existing helpers — verbatim contracts:
- upsert_user(conn, *, provider: str, sub: str, email: str | None, display_name: str, avatar_url: str | None) -> UUID  (returns user_id)
- mint_session(conn, *, user_id: UUID, request: Request) -> str  (returns session_id as UUID string)

routes/auth.py:68-80 (_err helper to reuse — Stripe-shape error envelope):
```
def _err(status, code, message, *, param=None, category=None) -> JSONResponse:
    return JSONResponse(status_code=status, content=make_error_envelope(code, message, param=param, category=category))
```

routes/auth.py:188-198 (Google browser callback — upsert_user + mint_session shape — verbatim from PATTERNS.md):
```
display_name = userinfo.get("name") or userinfo.get("email") or "user"
pool = request.app.state.db
async with pool.acquire() as conn:
    user_id = await upsert_user(conn, provider="google", sub=str(userinfo["sub"]),
                                email=userinfo.get("email"),
                                display_name=display_name,
                                avatar_url=userinfo.get("picture"))
    session_id = await mint_session(conn, user_id=user_id, request=request)
```

routes/auth.py:246-278 (existing GitHub /user + /user/emails fallback — RESEARCH §Pattern 6 mirrors this byte-for-byte for the mobile helper).

routes/auth.py:300+ (the "# Logout" divider — append the 2 new mobile handlers BEFORE this divider).

middleware/session.py:90-98 (ApSessionMiddleware reads ap_session from cookie OR explicit Cookie header — verified D-17 reuse path).

models/users.py::SessionUserResponse (existing Pydantic shape returned in browser-OAuth flow — reuse for the mobile response's `user` field).

config.py::settings.oauth_google_mobile_client_ids (added in Plan 23-01 Task 5 — list[str] CSV-parsed).

tests/conftest.py:590-655 (respx_oauth_providers fixture — extend pattern; mocks Google JWKS + GitHub /user + /user/emails).

tests/conftest.py:510-553 (authenticated_cookie fixture — analog for the new authenticated_mobile_session fixture).

tests/auth/test_google_callback.py + test_github_callback.py (analog test shapes — module markers, fixture composition).

tests/spikes/test_google_auth_multi_audience.py (Wave 0) — re-use the test-JWT helper (mint signed JWT with cryptography + pyjwt + JWKS). PATTERNS.md flags this as the canonical RESEARCH §Pitfall 3 helper.

tests/spikes/test_respx_intercepts_pyjwk_fetch.py OUTCOME banner — informs whether to use `respx.mock` for JWKS or `monkeypatch verify_oauth2_token` (Plan 23-01 Task 3 result).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: verify_google_id_token + verify_github_access_token helpers in auth/oauth.py</name>
  <files>api_server/src/api_server/auth/oauth.py</files>
  <read_first>
    - api_server/src/api_server/auth/oauth.py FULL (locate end of file ~line 238 mint_session; append helpers BELOW)
    - api_server/src/api_server/routes/auth.py:246-278 (existing GitHub /user + /user/emails fallback — mirror byte-for-byte; refactor opportunity to extract a shared helper that BOTH browser callback AND mobile endpoint call)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PATTERNS.md §"auth/oauth.py" lines 255-333 (concrete helper shapes)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-RESEARCH.md §Pattern 5 lines 605-668 (verify_google_id_token canonical implementation; recommended: skip 6h cache for MVP per RESEARCH; rely on google-auth defaults)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-RESEARCH.md §Pattern 6 lines 670-737 (verify_github_access_token canonical implementation)
    - api_server/tests/spikes/test_google_auth_multi_audience.py SUMMARY (Plan 23-01 outcome) — confirms verify_oauth2_token accepts list[str] audience
  </read_first>
  <behavior>
    - verify_google_id_token(id_token, mobile_client_ids): calls google.oauth2.id_token.verify_oauth2_token(token, _GOOGLE_REQUEST, audience=mobile_client_ids) inside asyncio.to_thread; on GoogleAuthError or ValueError raise ValueError with a redacted message; if mobile_client_ids is empty raises ValueError immediately ("no mobile client IDs configured")
    - verify_github_access_token(access_token, http_client): calls https://api.github.com/user with Authorization: Bearer; on non-200 raises ValueError; if profile["email"] is null, falls back to /user/emails and picks the first {primary:true, verified:true, email:non-null}; if still no email raises ValueError; returns {sub, email, display_name, avatar_url}
    - Both helpers raise ValueError on every failure mode; the route-level handler maps ValueError → 401 envelope
  </behavior>
  <action>
APPEND to `api_server/src/api_server/auth/oauth.py` (do NOT modify existing functions). Two new async helpers.

HELPER 1 — verify_google_id_token:

Add the imports at the top of the file (next to existing google imports if present, otherwise add fresh):
- `import asyncio`
- `from google.oauth2 import id_token as _google_id_token`
- `from google.auth.transport import requests as _google_ga_requests`
- `from google.auth import exceptions as _google_exceptions`

Module-level constant (next to existing _DEV_PLACEHOLDER pattern at line 53):
- `_GOOGLE_REQUEST = _google_ga_requests.Request()` (process-wide; google-auth uses requests.Session under the hood, HTTP keep-alive amortizes)

Per RESEARCH recommendation: SKIP the 6h JWKS cache for MVP (rely on google-auth defaults — ~150ms HTTPS round-trip per sign-in is acceptable; the cache complexity could mask key-rotation incidents). Document this decision in a code comment.

Function body (mirror PATTERNS.md §"auth/oauth.py" lines 268-293):
```
async def verify_google_id_token(id_token: str, mobile_client_ids: list[str]) -> dict:
    """Verify a Google-issued ID token against mobile client IDs.

    Raises ValueError on any failure (signature, expiry, audience, missing sub).
    Per D-23, mobile_client_ids is a list (Android + iOS) and verify_oauth2_token
    accepts list[str] audience (verified by Wave 0 spike A1).
    """
    if not mobile_client_ids:
        raise ValueError("no mobile client IDs configured")

    def _verify_sync():
        return _google_id_token.verify_oauth2_token(
            id_token, _GOOGLE_REQUEST, audience=mobile_client_ids,
        )

    try:
        return await asyncio.to_thread(_verify_sync)
    except _google_exceptions.GoogleAuthError as e:
        raise ValueError(f"google id_token rejected: {e}") from e
    except ValueError:
        raise  # already a ValueError from the library — pass through
```

HELPER 2 — verify_github_access_token:

Function body (mirror PATTERNS.md §"auth/oauth.py" verify_github_access_token + RESEARCH §Pattern 6):
```
async def verify_github_access_token(access_token: str, http_client) -> dict:
    """Validate a GitHub access token; return profile dict.

    Mirrors the browser GitHub callback flow at routes/auth.py:246-278:
      1. GET /user with Authorization: Bearer <token>
      2. If email is null, GET /user/emails and pick first primary+verified
      3. Refuse account creation if no verified email is recoverable

    Raises ValueError on any failure mode.

    Returns: {sub: str, email: str, display_name: str, avatar_url: str | None}
    """
    headers = {
        "Authorization": f"Bearer {access_token}",
        "Accept": "application/vnd.github+json",
    }
    try:
        r = await http_client.get(
            "https://api.github.com/user", headers=headers, timeout=10.0,
        )
    except httpx.HTTPError as e:
        raise ValueError(f"github /user fetch failed: {e}") from e
    if r.status_code != 200:
        raise ValueError(f"github access_token rejected (status {r.status_code})")
    profile = r.json()

    if not profile.get("id"):
        raise ValueError("github profile missing id")

    email = profile.get("email")
    if not email:
        try:
            er = await http_client.get(
                "https://api.github.com/user/emails", headers=headers, timeout=10.0,
            )
            er.raise_for_status()
            emails = er.json()
            email = next(
                (e["email"] for e in emails
                 if e.get("primary") and e.get("verified") and e.get("email")),
                None,
            )
        except httpx.HTTPError:
            email = None

    if not email:
        raise ValueError("no verified primary email")

    return {
        "sub": str(profile["id"]),
        "email": email,
        "display_name": profile.get("name") or profile.get("login") or "user",
        "avatar_url": profile.get("avatar_url"),
    }
```

Add `import httpx` at the top of the file if not already imported.

REFACTOR DECISION: The browser GitHub callback at routes/auth.py:246-278 has identical fallback logic. RESEARCH §Pattern 6 recommends extracting a shared helper. EXECUTOR DECISION: extract IF AND ONLY IF the diff to the browser callback is mechanical (replace the inlined logic with a single call to verify_github_access_token). If the existing browser callback uses authlib's `oauth.github.get(...)` (different HTTP path than the mobile endpoint's raw httpx call), do NOT refactor — keeping both paths is fine for MVP. Verify by reading routes/auth.py:246-278 first.

DO NOT touch existing upsert_user, mint_session, or any other helper. The two new helpers are append-only.
  </action>
  <verify>
    <automated>cd api_server && python -c "from api_server.auth.oauth import verify_google_id_token, verify_github_access_token; import inspect; assert inspect.iscoroutinefunction(verify_google_id_token); assert inspect.iscoroutinefunction(verify_github_access_token); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "async def verify_google_id_token" api_server/src/api_server/auth/oauth.py` returns 1 hit.
    - `grep -nE "async def verify_github_access_token" api_server/src/api_server/auth/oauth.py` returns 1 hit.
    - Google helper uses asyncio.to_thread: `grep -nE "asyncio\\.to_thread" api_server/src/api_server/auth/oauth.py` returns ≥1 hit.
    - Google helper passes audience as list: `grep -nE "audience=mobile_client_ids" api_server/src/api_server/auth/oauth.py` returns 1 hit.
    - GitHub helper hits /user/emails fallback: `grep -nE "user/emails" api_server/src/api_server/auth/oauth.py` returns ≥1 hit.
    - GitHub helper picks primary+verified: `grep -nE "primary.*verified|verified.*primary" api_server/src/api_server/auth/oauth.py` returns ≥1 hit.
    - Both helpers raise ValueError on failure: `grep -cE "raise ValueError" api_server/src/api_server/auth/oauth.py` returns ≥3 (existing + 2 new paths from each helper).
    - Helpers are coroutines: smoke command in <verify> exits 0.
    - No regression: `cd api_server && pytest tests/auth/ -x` exits 0 (existing browser tests still green).
  </acceptance_criteria>
  <done>Two new async helpers added to auth/oauth.py; existing functions untouched; helpers raise ValueError on every failure; existing browser-OAuth tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /v1/auth/google/mobile + POST /v1/auth/github/mobile handlers</name>
  <files>api_server/src/api_server/routes/auth.py</files>
  <read_first>
    - api_server/src/api_server/routes/auth.py FULL (locate the existing google_callback at ~147-204, github_callback at ~227-297, the "# Logout" divider at ~300, and the existing _err helper at 68-80; the new handlers go BELOW github_callback and ABOVE the Logout divider)
    - api_server/src/api_server/models/users.py::SessionUserResponse (existing Pydantic class — reuse for the mobile response's `user` field)
    - api_server/src/api_server/auth/oauth.py (the helpers added in Task 1)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PATTERNS.md §"routes/auth.py" lines 199-251 (concrete handler shape with Pydantic body models)
    - api_server/src/api_server/main.py (verify the openrouter_http_client added in Plan 23-05 — reuse this client for the GitHub /user fetch IF the timeout fits; otherwise reuse bot_http_client; the GitHub helper accepts an http_client argument so the route handler picks)
  </read_first>
  <behavior>
    - POST /v1/auth/google/mobile body {id_token: str} → on success 200 {session_id, expires_at, user}; on bad token 401 envelope; on missing sub or email 401 envelope; existing browser callback unaffected
    - POST /v1/auth/github/mobile body {access_token: str} → on success 200 {session_id, expires_at, user}; on bad token 401 envelope; on no recoverable email 401 envelope
    - Both endpoints DO NOT call _set_session_cookie (mobile has no cookie jar — D-17); session_id is returned in the body for the Flutter app to store and re-send as Cookie header
    - Both endpoints reuse upsert_user + mint_session verbatim — same provider/sub key, same 30-day session TTL
    - Pydantic Field(min_length=1) on the credential body fields prevents empty-string DOS
    - Errors do NOT echo the raw token in the response or logs (V7 — log_redact handles this; verify the helpers' ValueError messages don't include the token bytes)
  </behavior>
  <action>
APPEND two handlers to `api_server/src/api_server/routes/auth.py` BELOW the existing `github_callback` and BEFORE the `# Logout` divider.

Add Pydantic body models at the top of the new section (after the github_callback function, before the new handlers):

```
class MobileGoogleAuthRequest(BaseModel):
    id_token: str = Field(..., min_length=1)


class MobileGitHubAuthRequest(BaseModel):
    access_token: str = Field(..., min_length=1)


class MobileSessionResponse(BaseModel):
    session_id: str
    expires_at: datetime
    user: SessionUserResponse
```

Verify imports at the top of the file: `BaseModel`, `Field` from pydantic; `datetime` from datetime; `SessionUserResponse` from `..models.users`; `verify_google_id_token`, `verify_github_access_token` from `..auth.oauth`. Add only what's missing.

GOOGLE HANDLER (mirror PATTERNS.md §"routes/auth.py" lines 215-249):

```
@router.post("/auth/google/mobile", status_code=200)
async def google_mobile(request: Request, body: MobileGoogleAuthRequest):
    """Verify a native-SDK Google id_token, upsert user, mint session.

    Mobile (D-15): no dev-mode shim. The Flutter app sends a JWT issued by
    google_sign_in to one of the configured mobile client IDs. We verify
    server-side, upsert via the existing helper, mint a 30-day session, and
    return session_id for the app to store and re-send as Cookie:ap_session=<uuid>.
    """
    settings = request.app.state.settings
    try:
        claims = await verify_google_id_token(
            body.id_token, settings.oauth_google_mobile_client_ids,
        )
    except ValueError as e:
        return _err(401, ErrorCode.UNAUTHORIZED, str(e), param="id_token")

    if not claims.get("sub") or not claims.get("email"):
        return _err(401, ErrorCode.UNAUTHORIZED,
                    "missing required claims (sub or email)",
                    param="id_token")

    pool = request.app.state.db
    async with pool.acquire() as conn:
        user_id = await upsert_user(
            conn,
            provider="google",
            sub=str(claims["sub"]),
            email=claims["email"],
            display_name=claims.get("name") or claims["email"],
            avatar_url=claims.get("picture"),
        )
        session_id = await mint_session(conn, user_id=user_id, request=request)
        sess_row = await conn.fetchrow(
            "SELECT expires_at FROM sessions WHERE id = $1", UUID(session_id),
        )
        user_row = await conn.fetchrow(
            "SELECT id, email, display_name, avatar_url, provider, created_at "
            "FROM users WHERE id = $1", user_id,
        )

    return JSONResponse(status_code=200, content={
        "session_id": session_id,
        "expires_at": sess_row["expires_at"].isoformat(),
        "user": SessionUserResponse(**dict(user_row)).model_dump(mode="json"),
    })
```

GITHUB HANDLER (analogous; uses verify_github_access_token + bot_http_client OR a dedicated client):

```
@router.post("/auth/github/mobile", status_code=200)
async def github_mobile(request: Request, body: MobileGitHubAuthRequest):
    """Verify a flutter_appauth GitHub access_token, upsert user, mint session."""
    # Reuse openrouter_http_client (10s timeout) for the GitHub /user calls —
    # they should fail fast like the OpenRouter catalog, not hang for 600s.
    http_client = request.app.state.openrouter_http_client

    try:
        profile = await verify_github_access_token(body.access_token, http_client)
    except ValueError as e:
        return _err(401, ErrorCode.UNAUTHORIZED, str(e), param="access_token")

    pool = request.app.state.db
    async with pool.acquire() as conn:
        user_id = await upsert_user(
            conn,
            provider="github",
            sub=profile["sub"],
            email=profile["email"],
            display_name=profile["display_name"],
            avatar_url=profile.get("avatar_url"),
        )
        session_id = await mint_session(conn, user_id=user_id, request=request)
        sess_row = await conn.fetchrow(
            "SELECT expires_at FROM sessions WHERE id = $1", UUID(session_id),
        )
        user_row = await conn.fetchrow(
            "SELECT id, email, display_name, avatar_url, provider, created_at "
            "FROM users WHERE id = $1", user_id,
        )

    return JSONResponse(status_code=200, content={
        "session_id": session_id,
        "expires_at": sess_row["expires_at"].isoformat(),
        "user": SessionUserResponse(**dict(user_row)).model_dump(mode="json"),
    })
```

Critical: do NOT call `_set_session_cookie` on these mobile responses (mobile has no cookie jar). The session_id in the body is the contract; Flutter sends it back via `Cookie: ap_session=<uuid>` header on subsequent requests.

The choice of `openrouter_http_client` for GitHub /user is a reuse — if Plan 23-05's lifespan provisions it correctly with 10s timeout, the GitHub fetch piggy-backs (zero new HTTP client). Verify Plan 23-05 has shipped before this plan executes (depends_on=23-01 transitively through Wave 1; the orchestrator ensures Wave 2 plans run after Wave 1).

If `openrouter_http_client` is NOT yet provisioned at test time (pytest harness may construct app before the lifespan event), the test fixture will need to inject the client. Document this in Task 4.

DO NOT modify google_callback, github_callback, logout, or any other existing handler. New handlers are purely additive.
  </action>
  <verify>
    <automated>cd api_server && python -c "from api_server.routes.auth import google_mobile, github_mobile; import inspect; assert inspect.iscoroutinefunction(google_mobile); assert inspect.iscoroutinefunction(github_mobile); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "@router\\.post\\(.*auth/google/mobile" api_server/src/api_server/routes/auth.py` returns 1 hit.
    - `grep -nE "@router\\.post\\(.*auth/github/mobile" api_server/src/api_server/routes/auth.py` returns 1 hit.
    - `grep -nE "MobileGoogleAuthRequest|MobileGitHubAuthRequest|MobileSessionResponse" api_server/src/api_server/routes/auth.py` returns ≥3 hits.
    - Both handlers reuse upsert_user + mint_session: `grep -cE "upsert_user|mint_session" api_server/src/api_server/routes/auth.py` returns ≥4 (existing 2 in callbacks + 2 in mobile handlers).
    - Mobile handlers do NOT call _set_session_cookie: count occurrences before vs after — `grep -nE "_set_session_cookie" api_server/src/api_server/routes/auth.py` count must NOT increase from the existing browser-callback usage.
    - Both handlers map ValueError to 401: `grep -E "401, ErrorCode\\.UNAUTHORIZED" api_server/src/api_server/routes/auth.py` returns ≥2 hits within the new mobile handlers.
    - Smoke import in <verify> exits 0.
    - No regression: `cd api_server && pytest tests/auth/test_google_callback.py tests/auth/test_github_callback.py -x` exits 0.
  </acceptance_criteria>
  <done>Two mobile handlers appended; existing browser callbacks untouched; reuse of upsert_user + mint_session verified; mobile responses return session_id in body (no cookie set); ValueError → 401 envelope.</done>
</task>

<task type="auto">
  <name>Task 3: authenticated_mobile_session fixture in tests/auth/conftest.py</name>
  <files>api_server/tests/auth/conftest.py</files>
  <read_first>
    - api_server/tests/auth/conftest.py FULL (locate existing fixtures including respx_oauth_providers — extension target)
    - api_server/tests/conftest.py:510-553 (authenticated_cookie fixture — analog shape)
    - api_server/tests/conftest.py:590-655 (respx_oauth_providers fixture — extension pattern)
    - api_server/tests/spikes/test_google_auth_multi_audience.py (Plan 23-01 Task 2 — re-use the test-JWT helper for minting signed JWTs in this fixture)
  </read_first>
  <action>
Add `authenticated_mobile_session` fixture to `api_server/tests/auth/conftest.py`. Mirror the existing `authenticated_cookie` fixture pattern from `tests/conftest.py:510-553` but instead of going through the browser callback flow, this fixture:

1. Mints a test RSA keypair + JWKS (re-use the helper from `tests/spikes/test_google_auth_multi_audience.py` — copy it inline if not yet exposed as a shared fixture; document the duplication with a TODO comment for a future refactor).
2. Mints a signed test JWT with `aud = "test-mobile-client.apps.googleusercontent.com"`, valid `exp`, valid `sub`, valid `email`.
3. Patches `settings.oauth_google_mobile_client_ids` to include the test audience (use `monkeypatch.setattr` on the app's settings, OR pass the audience via env var `AP_OAUTH_GOOGLE_MOBILE_CLIENT_IDS` set in the test fixture's autouse env-setting fixture).
4. Mocks the JWKS endpoint via respx with the test public key.
5. POSTs to `/v1/auth/google/mobile` with `{id_token: <jwt>}`.
6. Asserts 200 response.
7. Yields a dict similar to `authenticated_cookie` shape: `{"Cookie": f"ap_session={session_id}", "_user_id": user_id, "_session_id": session_id}` so subsequent test calls can use the cookie header transparently.

Sample fixture skeleton (concrete):

```
@pytest_asyncio.fixture
async def authenticated_mobile_session(async_client, db_pool, monkeypatch):
    """Mobile-flavored sign-in fixture — mirrors authenticated_cookie but via
    POST /v1/auth/google/mobile."""
    # 1. mint keypair + JWKS
    private_key, jwks = _mint_test_rsa_jwks()  # helper inlined here or imported
    # 2. mint JWT
    test_aud = "test-mobile-client.apps.googleusercontent.com"
    id_token = _mint_test_jwt(
        private_key,
        kid=jwks["keys"][0]["kid"],
        aud=test_aud,
        sub="mobile-sub-1",
        email="mobile@test.example",
    )
    # 3. force settings to include the test audience
    monkeypatch.setattr(
        async_client._app.state.settings,
        "oauth_google_mobile_client_ids",
        [test_aud],
    )
    # 4. respx mock JWKS
    with respx.mock(assert_all_called=False) as m:
        m.get("https://www.googleapis.com/oauth2/v3/certs").mock(
            return_value=httpx.Response(200, json=jwks)
        )
        # 5. POST to mobile endpoint
        r = await async_client.post(
            "/v1/auth/google/mobile",
            json={"id_token": id_token},
        )
        assert r.status_code == 200, r.text
        body = r.json()
        session_id = body["session_id"]
        user_id = body["user"]["id"]

    yield {
        "Cookie": f"ap_session={session_id}",
        "_user_id": user_id,
        "_session_id": session_id,
    }
```

If the OUTCOME banner from Plan 23-01 Task 3 (test_respx_intercepts_pyjwk_fetch.py) recorded that respx does NOT intercept PyJWKClient (scenario B), use the documented fallback instead: `monkeypatch.setattr("api_server.auth.oauth._google_id_token.verify_oauth2_token", lambda *a, **kw: {"sub": "mobile-sub-1", "email": "mobile@test.example", "name": "Test"})`. The fixture's contract is the same regardless of the mocking strategy.

Helper functions `_mint_test_rsa_jwks()` and `_mint_test_jwt(...)` can be COPIED from the spike file. If they're identical and used in 2+ places, consider extracting them to `tests/auth/_jwt_helpers.py` (a shared module). Discretion: copy-paste is acceptable for MVP; refactor is a deferred-ideas item.

DO NOT modify the existing authenticated_cookie or respx_oauth_providers fixtures.
  </action>
  <verify>
    <automated>cd api_server && grep -nE "authenticated_mobile_session" tests/auth/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "def authenticated_mobile_session|async def authenticated_mobile_session" api_server/tests/auth/conftest.py` returns ≥1 hit.
    - Fixture POSTs to the new mobile endpoint: `grep -E "/v1/auth/google/mobile" api_server/tests/auth/conftest.py` returns ≥1 hit.
    - Fixture yields the cookie shape: `grep -E '"Cookie".*ap_session=' api_server/tests/auth/conftest.py` returns ≥1 hit.
    - Fixture is consumable: a smoke test (any test in tests/auth/test_oauth_mobile.py — see Task 4) using the fixture passes.
  </acceptance_criteria>
  <done>authenticated_mobile_session fixture exists; signs in via POST /v1/auth/google/mobile; yields session_id in a Cookie header shape; existing fixtures unmodified.</done>
</task>

<task type="auto">
  <name>Task 4: 9-cell coverage matrix in test_oauth_mobile.py</name>
  <files>api_server/tests/auth/test_oauth_mobile.py</files>
  <read_first>
    - api_server/tests/auth/test_google_callback.py (full — analog test shape)
    - api_server/tests/auth/test_github_callback.py (full — analog for GitHub coverage)
    - api_server/tests/auth/conftest.py (the fixtures added in Task 3 + existing respx_oauth_providers)
    - api_server/tests/spikes/test_google_auth_multi_audience.py (Plan 23-01 — borrow the test-JWT helpers)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-VALIDATION.md per-task entries 23-API05-G-01..05 + 23-API05-GH-01..03 + 23-API05-CK-01 (the 9 cells)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md D-30 (the matrix)
  </read_first>
  <action>
Create `api_server/tests/auth/test_oauth_mobile.py` with 9 tests covering the D-30 matrix:

GOOGLE (5 cells):
- google_happy: valid JWT (correct aud, valid signature, future exp, sub + email present) → 200 + session_id + user; assert sessions row exists in DB; assert users row created/upserted.
- google_invalid_sig: JWT signed with a DIFFERENT private key (or `aud=correct` but signed by an attacker key) → 401 envelope param="id_token".
- google_expired: JWT with `exp` 1h in the past → 401 envelope.
- google_aud_mismatch: JWT with `aud="wrong-client.apps.googleusercontent.com"` not in settings.oauth_google_mobile_client_ids → 401 envelope.
- google_missing_claims: JWT signed correctly but with no `email` claim → 401 envelope (or no `sub` claim).

GITHUB (3 cells):
- github_public_email: respx mock GitHub /user returns `{id: 123, login: "ghuser", email: "pub@example.com", name: "GH User", avatar_url: "..."}` → 200 + session.
- github_private_email: /user returns `email: null` → /user/emails returns `[{email: "priv@example.com", primary: true, verified: true}]` → 200 + session with email="priv@example.com".
- github_invalid_token: /user returns 401 → endpoint returns 401 envelope param="access_token".

COOKIE-CONTINUITY (1 cell):
- cookie_continuity: sign in via POST /v1/auth/google/mobile, capture session_id; subsequent GET /v1/users/me with `Cookie: ap_session=<session_id>` returns 200 + the user's profile (proves D-17: ApSessionMiddleware reads the explicit Cookie header transparently).

Test module skeleton:

```
import pytest
import respx
import httpx
from datetime import datetime, timezone, timedelta
from uuid import UUID

pytestmark = [pytest.mark.api_integration, pytest.mark.asyncio]


# Helpers (copied from spike — see Task 3 conftest for canonical home if extracted)
def _mint_test_rsa_jwks(): ...
def _mint_test_jwt(private_key, *, kid, aud, sub, email, exp_offset=3600): ...


async def test_google_happy_path_returns_session_and_user(
    async_client, db_pool, monkeypatch,
):
    private_key, jwks = _mint_test_rsa_jwks()
    aud = "test-mobile-android.apps.googleusercontent.com"
    monkeypatch.setattr(async_client._app.state.settings,
                        "oauth_google_mobile_client_ids", [aud])
    token = _mint_test_jwt(private_key, kid=jwks["keys"][0]["kid"],
                           aud=aud, sub="g-sub-1", email="alice@test.example")
    with respx.mock(assert_all_called=False) as m:
        m.get("https://www.googleapis.com/oauth2/v3/certs").mock(
            return_value=httpx.Response(200, json=jwks))
        r = await async_client.post("/v1/auth/google/mobile",
                                    json={"id_token": token})
    assert r.status_code == 200, r.text
    body = r.json()
    assert "session_id" in body and "expires_at" in body
    assert body["user"]["email"] == "alice@test.example"
    assert body["user"]["provider"] == "google"
    # DB-level check
    async with db_pool.acquire() as conn:
        row = await conn.fetchrow(
            "SELECT * FROM sessions WHERE id = $1", UUID(body["session_id"]))
        assert row is not None
        user_row = await conn.fetchrow(
            "SELECT * FROM users WHERE provider = 'google' AND sub = 'g-sub-1'")
        assert user_row is not None


async def test_google_invalid_signature_returns_401(async_client, monkeypatch):
    pk_real, jwks = _mint_test_rsa_jwks()
    pk_attacker, _ = _mint_test_rsa_jwks()  # different key
    aud = "test-mobile.apps.googleusercontent.com"
    monkeypatch.setattr(async_client._app.state.settings,
                        "oauth_google_mobile_client_ids", [aud])
    token = _mint_test_jwt(pk_attacker, kid=jwks["keys"][0]["kid"],
                           aud=aud, sub="x", email="x@x")
    with respx.mock(assert_all_called=False) as m:
        m.get("https://www.googleapis.com/oauth2/v3/certs").mock(
            return_value=httpx.Response(200, json=jwks))
        r = await async_client.post("/v1/auth/google/mobile",
                                    json={"id_token": token})
    assert r.status_code == 401, r.text
    assert r.json()["error"]["param"] == "id_token"


async def test_google_expired_returns_401(async_client, monkeypatch): ...
async def test_google_audience_mismatch_returns_401(async_client, monkeypatch): ...
async def test_google_missing_claims_returns_401(async_client, monkeypatch): ...


async def test_github_public_email_returns_session(async_client, db_pool):
    with respx.mock(assert_all_called=False) as m:
        m.get("https://api.github.com/user").mock(return_value=httpx.Response(
            200, json={"id": 12345, "login": "ghuser", "email": "pub@example.com",
                       "name": "GH User", "avatar_url": "https://x"}))
        r = await async_client.post("/v1/auth/github/mobile",
                                    json={"access_token": "valid-token"})
    assert r.status_code == 200, r.text
    assert r.json()["user"]["email"] == "pub@example.com"


async def test_github_private_email_falls_back_to_emails_endpoint(
    async_client, db_pool,
):
    with respx.mock(assert_all_called=False) as m:
        m.get("https://api.github.com/user").mock(return_value=httpx.Response(
            200, json={"id": 999, "login": "private-user", "email": None,
                       "name": "Priv", "avatar_url": None}))
        m.get("https://api.github.com/user/emails").mock(return_value=httpx.Response(
            200, json=[{"email": "priv@example.com", "primary": True,
                        "verified": True}]))
        r = await async_client.post("/v1/auth/github/mobile",
                                    json={"access_token": "valid"})
    assert r.status_code == 200, r.text
    assert r.json()["user"]["email"] == "priv@example.com"


async def test_github_invalid_token_returns_401(async_client):
    with respx.mock(assert_all_called=False) as m:
        m.get("https://api.github.com/user").mock(return_value=httpx.Response(401))
        r = await async_client.post("/v1/auth/github/mobile",
                                    json={"access_token": "bad-token"})
    assert r.status_code == 401, r.text
    assert r.json()["error"]["param"] == "access_token"


async def test_mobile_cookie_header_grants_authenticated_session(
    async_client, authenticated_mobile_session,
):
    """D-17: mobile sends Cookie: ap_session=<uuid>; ApSessionMiddleware reads it."""
    r = await async_client.get(
        "/v1/users/me",
        headers={"Cookie": authenticated_mobile_session["Cookie"]},
    )
    assert r.status_code == 200, r.text
    assert r.json()["id"] == authenticated_mobile_session["_user_id"]
```

Fill in the remaining Google scenarios (expired / audience-mismatch / missing-claims) by tweaking the JWT-mint helper's arguments (`exp_offset=-3600` for expired; different `aud=` for mismatch; pop `email` for missing-claims).

If respx does NOT intercept the PyJWKClient JWKS fetch (Plan 23-01 Task 3 OUTCOME = scenario B), use the monkeypatch fallback instead — patch `api_server.auth.oauth._google_id_token.verify_oauth2_token` to raise the expected library exceptions per scenario. Document this choice in the test module's docstring.

Run: `cd api_server && pytest tests/auth/test_oauth_mobile.py -x`. All 9 tests must pass.

Regression sweep: `cd api_server && pytest tests/auth/ -x` — must remain green (existing google_callback + github_callback tests).
  </action>
  <verify>
    <automated>cd api_server && pytest tests/auth/test_oauth_mobile.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `ls api_server/tests/auth/test_oauth_mobile.py` succeeds.
    - File contains ≥9 test functions: `grep -cE "^async def test_" api_server/tests/auth/test_oauth_mobile.py` returns ≥9.
    - All 5 Google cells: `grep -E "test_google_happy|test_google_invalid|test_google_expired|test_google_audience|test_google_missing" api_server/tests/auth/test_oauth_mobile.py` returns ≥5 hits.
    - All 3 GitHub cells: `grep -E "test_github_public|test_github_private|test_github_invalid" api_server/tests/auth/test_oauth_mobile.py` returns ≥3 hits.
    - Cookie-continuity cell: `grep -E "test_mobile_cookie|cookie_continuity|Cookie.*authenticated_mobile_session" api_server/tests/auth/test_oauth_mobile.py` returns ≥1 hit.
    - `cd api_server && pytest tests/auth/test_oauth_mobile.py -x` exits 0.
    - Regression: `cd api_server && pytest tests/auth/ -x` exits 0 (entire auth test directory green).
  </acceptance_criteria>
  <done>9 tests green covering D-30 matrix; existing browser-OAuth tests still green; mobile cookie continuity verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter app → POST /v1/auth/{google,github}/mobile | untrusted client credential; all verification server-side |
| handler → google-auth library | RS256 signature + audience + iss + exp validation |
| handler → GitHub /user + /user/emails | GitHub validates the access_token; non-200 means invalid |
| handler → upsert_user + mint_session | reuses the audited Phase 22c-oauth-google substrate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-V2-AUTH | S (Spoofing) | forged Google id_token | mitigate | google-auth verifies RS256 + iss allowlist + exp + aud against settings.oauth_google_mobile_client_ids; no path bypasses verification |
| T-23-AUD-CONFUSION | S (Spoofing) | id_token issued for app A used to sign in to our app | mitigate | audience= mobile_client_ids enforces our app's IDs only (Wave 0 spike A1 confirmed multi-audience semantic) |
| T-23-V2-GH-REPLAY | S (Spoofing) | replayed/revoked GitHub access_token | mitigate | per-call /user validation; revoked tokens fail naturally; we never trust client-side claims |
| T-23-V7-TOKEN-LOG-LEAK | I (Info Disclosure) | id_token or access_token logged | mitigate | RESEARCH §"Phase 23-specific security flags" — log_redact middleware redacts these body fields; helpers' ValueError messages don't include token bytes |
| T-23-V3-SESSION | T (Tampering) | session_id stolen from mobile body response | accept | the session_id IS the credential; HTTPS in flight (Flutter app's responsibility); 30-day TTL + revoked_at column matches browser-OAuth posture |
| T-23-V5-EMPTY-TOKEN | D (DoS) | empty-string token causes expensive verify path | mitigate | Pydantic Field(min_length=1) rejects empty strings at the boundary |
| T-23-MIXED-PROVIDER | T (Tampering) | upserting same email across google + github paths creates duplicate users | accept | upsert_user deduplicates on (provider, sub) pair (existing 22c contract); same human signing in via both providers gets two user rows by design |
</threat_model>

<verification>
- `cd api_server && pytest tests/auth/test_oauth_mobile.py -x` exits 0.
- `cd api_server && pytest tests/auth/ -x` exits 0 (no regression in auth tests).
- 2 helpers added to auth/oauth.py: `grep -cE "async def verify_google_id_token|async def verify_github_access_token" api_server/src/api_server/auth/oauth.py` returns 2.
- 2 routes added to routes/auth.py: `grep -cE "auth/google/mobile|auth/github/mobile" api_server/src/api_server/routes/auth.py` returns ≥2.
- Browser-callback handlers untouched (line-count check before vs after, OR diff inspection): existing `google_callback` and `github_callback` function bodies are byte-identical to pre-plan.
</verification>

<success_criteria>
- POST /v1/auth/google/mobile verifies a Google id_token JWT, upserts user, mints session, returns body shape.
- POST /v1/auth/github/mobile validates a GitHub access_token, upserts user, mints session, returns same shape.
- All 9 D-30 matrix cells green.
- Mobile cookie continuity proven (sign in → use Cookie header → /v1/users/me works).
- Existing browser OAuth tests still green.
- No new SQL inlined in routes (single-seam: existing upsert_user + mint_session unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-06-SUMMARY.md` documenting:
- JWKS-mocking strategy used (respx vs monkeypatch fallback) per Plan 23-01 Task 3 OUTCOME
- Whether verify_github_access_token was extracted as a shared helper (browser callback refactored?) or kept duplicated
- Whether bot_http_client or openrouter_http_client was used for GitHub /user (timeout choice)
- Sample response JSON for a Google sign-in (redacted) — useful for Phase 24 typed-client codegen
</output>
