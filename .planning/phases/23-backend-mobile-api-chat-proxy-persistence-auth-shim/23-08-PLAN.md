---
phase: 23-backend-mobile-api-chat-proxy-persistence-auth-shim
plan: 08
type: execute
wave: 3
depends_on:
  - "23-02"
  - "23-03"
  - "23-04"
  - "23-05"
  - "23-06"
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
must_haves:
  truths:
    - "REQUIREMENTS.md API-01 wording matches D-32 exactly: hardens existing POST /v1/agents/:id/messages to require Idempotency-Key (400 if missing); body unchanged {content: string}; response 202 {message_id, status, queued_at}; idempotent retry via existing IdempotencyMiddleware"
    - "REQUIREMENTS.md API-05 wording matches D-32 exactly: backend exposes POST /v1/auth/google/mobile + POST /v1/auth/github/mobile accepting native-SDK credentials, server-side verification, mints sessions rows, mobile sends as Cookie: ap_session=<uuid> header; ApSessionMiddleware unchanged; no dev-mode shim"
    - "REQUIREMENTS.md API-06 is DROPPED with the explicit note: 'Replaced by reuse of existing inapp_messages table per Phase 23 D-01'"
    - "REQUIREMENTS.md API-04 wording is unchanged (per D-32 — existing wording covers /v1/models proxy correctly)"
    - "REQUIREMENTS.md Traceability table reflects the API-06-dropped status"
    - "Phase-exit gate verifier (which scans REQUIREMENTS.md against shipped code) will not fail on API-01/05/06 wording mismatches"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended API-01 + API-05 wording; API-06 marked dropped; traceability table updated"
  key_links:
    - from: ".planning/REQUIREMENTS.md::API-01"
      to: "Phase 23 D-09 + D-32 amendment"
      via: "wording rewrite to 'Idempotency-Key REQUIRED on existing POST /v1/agents/:id/messages'"
      pattern: "Idempotency-Key.*[Rr]equired"
    - from: ".planning/REQUIREMENTS.md::API-05"
      to: "Phase 23 D-15..D-17 + D-32 amendment"
      via: "wording rewrite to '/v1/auth/{google,github}/mobile credential-exchange endpoints'"
      pattern: "auth/google/mobile|auth/github/mobile"
    - from: ".planning/REQUIREMENTS.md::API-06"
      to: "Phase 23 D-01 reuse of inapp_messages"
      via: "DROPPED + note 'Replaced by reuse of existing inapp_messages table per Phase 23 D-01'"
      pattern: "API-06.*DROPPED|Replaced by reuse of existing inapp_messages"
---

<objective>
Per D-32, the REQUIREMENTS.md API-01 / API-05 / API-06 entries are STALE — they reflect the original mobile-mvp-decisions.md assumptions ("planned-not-executed", "dev-mode auth shim", "new messages table") that this phase corrects. Without these amendments the verifier (which scans REQUIREMENTS.md against shipped code) will fail the phase-exit gate.

Purpose: Synchronize REQUIREMENTS.md with the corrected scope so the phase-exit verifier passes. Documentation-only plan — no code changes.
Output: Amended REQUIREMENTS.md with rewritten API-01 + API-05, API-06 marked dropped, traceability table updated to reflect the new status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- The exact existing lines to amend, plus the canonical replacement wording. -->

REQUIREMENTS.md:174 (current API-01 — STALE):
"Backend exposes POST /v1/agents/:id/chat accepting {message: string}. The handler looks up the agent's running container via agent_containers, POSTs to its bridge-IP /v1/chat/completions with the OpenAI shape (stream: false), persists both the user row and the assistant row in the new messages table inside one transaction, and returns {message: string}. Block-and-wait — streaming is a future additive endpoint variant, NOT a rewrite of this handler."

REQUIREMENTS.md:178 (current API-05 — STALE):
"Backend has a dev-mode auth shim. A FastAPI dependency Depends(current_user_id) returns a hardcoded UUID when AP_ENV=dev AND no real auth header is present. Every API-01..04 route consumes this dependency. The same dependency is the integration point for OAuth (Phase 22c-oauth-google) — swapping the impl must NOT require touching call sites."

REQUIREMENTS.md:179 (current API-06 — STALE):
"Alembic migration creates a messages (id UUID PK, agent_id UUID FK→agent_containers.id, user_id UUID, role text CHECK IN ('user','assistant'), content text, created_at timestamptz) table with a btree index on (agent_id, created_at) to support history pagination."

REQUIREMENTS.md:387-394 (current Traceability rows for API-01..07 — all "Pending"):
| API-01 | Phase 23 | Pending |
| API-02 | Phase 23 | Pending |
| API-03 | Phase 23 | Pending |
| API-04 | Phase 23 | Pending |
| API-05 | Phase 23 | Pending |
| API-06 | Phase 23 | Pending |
| API-07 | Phase 23 | Pending |

CONTEXT.md D-32 (the canonical amendment text):
- API-01 → "Backend hardens existing POST /v1/agents/:id/messages to require an Idempotency-Key header (400 if missing). Body unchanged: {content: string}. Response unchanged: 202 {message_id, status, queued_at}. Idempotent retry replays cached response via existing IdempotencyMiddleware."
- API-05 → "Backend exposes POST /v1/auth/google/mobile and POST /v1/auth/github/mobile accepting native-SDK credentials, verifying server-side, and minting sessions rows that mobile sends back as Cookie: ap_session=<uuid> header. ApSessionMiddleware unchanged. No dev-mode shim."
- API-06 → DROPPED. Note: "Replaced by reuse of existing inapp_messages table per Phase 23 D-01."
- API-04 → unchanged.
- API-02, API-03, API-07 → unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend API-01 + API-05 wording, drop API-06, update traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md FULL (locate exact lines: 174 API-01, 178 API-05, 179 API-06; 387-394 Traceability rows; 267 Traceability section header)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md D-32 (the canonical amendment text — copy verbatim)
  </read_first>
  <action>
Apply four targeted edits to `.planning/REQUIREMENTS.md`. Do NOT touch any other line.

EDIT 1 — Replace API-01 (line 174):

OLD: `- [ ] **API-01**: Backend exposes \`POST /v1/agents/:id/chat\` accepting \`{message: string}\`. ... NOT a rewrite of this handler.`

NEW (verbatim from D-32 + retain the markdown checkbox structure of the surrounding entries):

```
- [ ] **API-01**: Backend hardens the existing `POST /v1/agents/:id/messages` to require an `Idempotency-Key` header (400 if missing). Body unchanged: `{content: string}`. Response unchanged: 202 `{message_id, status, queued_at}`. Idempotent retry replays cached response via existing IdempotencyMiddleware. *(Amended Phase 23 per D-32 — original /v1/agents/:id/chat block-and-wait spec superseded by reuse of the existing /messages dispatcher path; client-side polling via SSE on /messages/stream covers the reply-delivery contract.)*
```

EDIT 2 — Replace API-05 (line 178):

OLD: `- [ ] **API-05**: Backend has a dev-mode auth shim. ... swapping the impl must NOT require touching call sites.`

NEW (verbatim from D-32):

```
- [ ] **API-05**: Backend exposes `POST /v1/auth/google/mobile` and `POST /v1/auth/github/mobile` accepting native-SDK credentials (Google id_token JWT / GitHub access_token), verifying server-side, and minting `sessions` rows that mobile sends back as `Cookie: ap_session=<uuid>` header. ApSessionMiddleware unchanged. No dev-mode shim. *(Amended Phase 23 per D-32 — original dev-mode `Depends(current_user_id)` shim spec superseded by real OAuth via Flutter native SDKs; OAuth substrate from Phase 22c-oauth-google is reused 100%.)*
```

EDIT 3 — Mark API-06 as DROPPED (line 179):

OLD: `- [ ] **API-06**: Alembic migration creates a \`messages\` ... support history pagination.`

NEW:

```
- [ ] **API-06**: ~~Alembic migration creates a `messages` table.~~ **DROPPED in Phase 23.** Replaced by reuse of existing `inapp_messages` table per Phase 23 D-01 (single chat-history source; zero new migrations; single-seam at `services/inapp_messages_store.py`). *(Amended Phase 23 per D-32.)*
```

The strikethrough (`~~...~~`) preserves the original wording for traceability while making the dropped status visible. Keep the bullet/checkbox structure consistent.

EDIT 4 — Update Traceability table (lines 388, 392, 393):

OLD:
```
| API-01 | Phase 23 | Pending |
| API-05 | Phase 23 | Pending |
| API-06 | Phase 23 | Pending |
```

NEW:
```
| API-01 | Phase 23 | Pending — amended D-32 (Idempotency-Key REQUIRED) |
| API-05 | Phase 23 | Pending — amended D-32 (mobile credential-exchange endpoints; no dev-mode shim) |
| API-06 | Phase 23 | DROPPED — replaced by inapp_messages reuse per D-01 |
```

Lines 389, 390, 391, 394 (API-02, API-03, API-04, API-07) are UNCHANGED — leave them as-is.

CRITICAL:
- Do NOT touch other requirement IDs (API-02, API-03, API-04, API-07, APP-*, UI-*, BST-*).
- Do NOT touch any line outside the four edits above.
- Preserve the existing markdown formatting (bullets, table syntax, indentation).
- The bullet checkbox `- [ ]` is preserved on API-06 even though it's dropped — this is documentation, not a real "to do"; the strikethrough conveys the dropped status.
- After editing, re-read the file to verify the changes look clean.

FINAL DECISION FLAG: if the executor finds that line 174 is NOT line 174 in the actual file at execute-time (the file may have shifted by edits in other phases), use grep to locate the exact lines:
```
grep -n "API-01" .planning/REQUIREMENTS.md
grep -n "API-05" .planning/REQUIREMENTS.md
grep -n "API-06" .planning/REQUIREMENTS.md
```
And apply the 4 edits at the actual line locations.
  </action>
  <verify>
    <automated>grep -E "Idempotency-Key" .planning/REQUIREMENTS.md && grep -E "auth/google/mobile" .planning/REQUIREMENTS.md && grep -E "DROPPED.*Phase 23|Replaced by reuse of existing inapp_messages" .planning/REQUIREMENTS.md && grep -E "API-06.*DROPPED.*replaced|API-06.*DROPPED" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - API-01 mentions Idempotency-Key REQUIRED: `grep -E "Idempotency-Key.*[Rr]equired|Idempotency-Key header" .planning/REQUIREMENTS.md` returns ≥1 hit (in the API-01 block).
    - API-01 references the existing /messages endpoint, not the old /chat endpoint: `grep -E "/v1/agents/:id/messages" .planning/REQUIREMENTS.md` returns ≥1 hit AND `grep -E "/v1/agents/:id/chat" .planning/REQUIREMENTS.md` (original) returns ZERO hits in the API-01 block (the old wording must be replaced; if the strikethrough trail is kept anywhere it must be tagged as amended).
    - API-05 mentions the new mobile endpoints: `grep -E "auth/google/mobile" .planning/REQUIREMENTS.md` returns ≥1 hit AND `grep -E "auth/github/mobile" .planning/REQUIREMENTS.md` returns ≥1 hit.
    - API-05 says "no dev-mode shim": `grep -E "[Nn]o dev-mode shim|No dev-mode" .planning/REQUIREMENTS.md` returns ≥1 hit.
    - API-06 marked dropped: `grep -E "API-06.*DROPPED|DROPPED in Phase 23" .planning/REQUIREMENTS.md` returns ≥1 hit.
    - API-06 references the replacement: `grep -E "Replaced by reuse of existing inapp_messages" .planning/REQUIREMENTS.md` returns ≥1 hit.
    - Traceability table reflects amendments: `grep -E "amended D-32" .planning/REQUIREMENTS.md` returns ≥2 hits (API-01 + API-05 rows).
    - Traceability table reflects API-06 dropped: `grep -E "API-06.*DROPPED.*inapp_messages|API-06.*DROPPED.*replaced" .planning/REQUIREMENTS.md` returns ≥1 hit.
    - No accidental damage to API-02, API-03, API-04, API-07 wording: `grep -E "GET /v1/agents/:id/messages\\?limit=N" .planning/REQUIREMENTS.md` returns ≥1 hit (API-02 wording preserved); `grep -E "GET /v1/models" .planning/REQUIREMENTS.md` returns ≥1 hit (API-04 wording preserved).
    - File still valid markdown: a quick visual inspection of the table syntax confirms no broken pipes or stray characters.
  </acceptance_criteria>
  <done>API-01 + API-05 rewritten per D-32; API-06 marked DROPPED with replacement note; Traceability table updated; other requirement IDs untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/REQUIREMENTS.md → phase-exit verifier | the verifier scans this file against shipped code; mismatches fail the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-DOC-DRIFT | I (Info Disclosure / Documentation) | stale REQUIREMENTS.md misleads future planners | mitigate | D-32 amendments synchronize the doc with shipped reality; strikethrough preserves history |
| T-23-VERIFIER-MISMATCH | A (Availability / Process) | phase-exit verifier fails on stale wording | mitigate | This plan is the explicit fix; verifier passes after amendment |
</threat_model>

<verification>
- All 4 edits applied verbatim per D-32 wording.
- Other requirement IDs (API-02, API-03, API-04, API-07, APP-*, UI-*) untouched.
- Traceability table consistent with amended status.
- File parses as valid markdown (no broken table rows or stray characters).
</verification>

<success_criteria>
- API-01, API-05, API-06 wording reflects shipped Phase 23 reality.
- Traceability table flags amendments / drops.
- Phase-exit verifier (when run after Plan 23-09) does not fail on stale wording.
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-08-SUMMARY.md` documenting:
- Final line numbers where each amendment landed (for traceability)
- Whether the strikethrough convention was honored (preserves history vs. replaces wholesale)
- Confirmation that no other requirement IDs were touched
</output>
