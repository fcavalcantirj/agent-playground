---
phase: 23-backend-mobile-api-chat-proxy-persistence-auth-shim
plan: 09
type: execute
wave: 3
depends_on:
  - "23-02"
  - "23-03"
  - "23-04"
  - "23-05"
  - "23-06"
  - "23-07"
  - "23-08"
files_modified:
  # Defensive entries (W-03 from plan-checker iteration 1): Plan 23-02 makes
  # Idempotency-Key REQUIRED on POST /v1/agents/:id/messages. The e2e harness
  # may need a small adjustment (~3 LOC) to inject the header. List the most
  # likely edit sites here so the audit trail captures whatever lands. If
  # NONE of these need editing in practice, leave the files untouched and the
  # plan still ships clean (frontmatter is permissive, not prescriptive).
  - api_server/Makefile
  - api_server/tests/e2e/
  - tests/spikes/
autonomous: false
requirements:
  - API-07
must_haves:
  truths:
    - "cd api_server && pytest tests/ exits 0 — every Phase 23 test plus all pre-existing tests pass against testcontainers Postgres + Redis"
    - "cd api_server && make e2e-inapp-docker exits 0 — the live-Docker e2e gate (Phase 22c.3.1's harness) still passes; mobile-API additions did not regress the in-app chat round-trip"
    - "No new mocks were introduced for core substrate (Golden Rule #1) — bot HTTP responses may be respx-stubbed at the upstream boundary only; chat-proxy DB writes + container lookup remain real"
    - "Wave 0 spikes still pass (regression check after all subsequent plans landed)"
  artifacts: []
  key_links:
    - from: "make e2e-inapp-docker"
      to: "api_server bind-mount of /Users/fcavalcanti/dev/agent-playground/api_server (live source)"
      via: "Phase 22c.3.1's docker-compose / make target — bind-mounts main, NOT a worktree"
      pattern: "make e2e-inapp-docker"
---

<objective>
Phase-exit verification gate. NO NEW CODE — this plan runs the full test suite + the live-Docker e2e harness and confirms nothing regressed. Per the live-infra worktree memory, this plan's e2e command bind-mounts `api_server/` from the main checkout, so it MUST run on main (NOT in a worktree). The orchestrator MUST commit Plans 23-01 through 23-08 to main before this plan executes.

Purpose: Closes REQ API-07 ("Integration tests for API-01..06 hit real Postgres via testcontainers and real Docker"). Confirms the Phase 23 deliverables ship cleanly and the Phase 22c.3.1 substrate they extend is still healthy.
Output: Two green commands. No file edits. A SUMMARY documenting the test counts + e2e-report.json status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- The two gating commands and what they exercise. -->

cd api_server && pytest tests/
- Runs the full test suite including testcontainer-backed Postgres + Redis (api_integration marker triggers the testcontainer fixture).
- Should be ~150+ tests after Phase 23 (existing 100+ tests + new tests from Plans 23-01..06: 3 spikes + 4 new test files + ~30 new tests).
- Estimated runtime: 3-4 minutes (per VALIDATION.md).

cd api_server && make e2e-inapp-docker
- Phase 22c.3.1's macOS-parity end-to-end gate.
- Spins up real Docker daemon via testcontainers, runs a live agent recipe (hermes by default), POSTs a real /messages, waits for a real bot reply via SSE, asserts the inapp_messages row reaches status='done', writes e2e-report.json with passed=true.
- IMPORTANT: bind-mounts `api_server/` from main; the worktree does NOT contain the live code.

Per memory `feedback_worktree_breaks_for_live_infra.md`: when a plan's acceptance test runs against live infrastructure that bind-mounts api_server from main, the executor MUST commit to main, not a worktree. Plan 23-09 falls in this category — autonomous: false.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Pre-gate: confirm Plans 23-01..23-08 are committed to main</name>
  <what-built>
Plans 23-01 through 23-08 should now be on the main branch (or in a committable state on a worktree that has been merged/rebased into main). The e2e-inapp-docker target bind-mounts api_server/ from main, so any uncommitted code on a worktree will NOT be exercised.
  </what-built>
  <how-to-verify>
1. Run `git log --oneline -20` and confirm commits for Plans 23-01 through 23-08 are present on the current branch (which should be main).
2. Run `git status` and confirm there are no staged or unstaged Phase 23 production-code changes that haven't been committed.
3. If any Phase 23 plan was executed on a worktree and not yet merged to main, STOP this plan and merge first. Do NOT run `make e2e-inapp-docker` from the worktree.
4. If all Phase 23 commits are on main and the working tree is clean (or only contains Plan 23-09's SUMMARY.md, which is fine), reply "approved" to proceed.
  </how-to-verify>
  <resume-signal>Type "approved" once Plans 23-01..23-08 are confirmed on main, or describe what's missing.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Full pytest suite — Wave 0 spikes + all unit + integration tests</name>
  <files></files>
  <read_first>
    - api_server/Makefile (read the e2e-inapp-docker target to understand what it bind-mounts and what report file it writes)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-VALIDATION.md ("Test Infrastructure" + "Sampling Rate" sections)
  </read_first>
  <action>
Run the full pytest suite against testcontainers infrastructure:

```
cd api_server && pytest tests/ -v
```

Use `-v` to capture per-test pass/fail in the executor's stdout for the SUMMARY artifact. Do NOT use `-x` here — we want the FULL count, not first-failure abort. If failures occur, the executor must investigate root cause (per Golden Rule #4 — "root cause first, never fix-to-pass") and re-run AFTER fix.

If a flaky test surfaces (e.g. respx race, testcontainer port collision), DO NOT mark it xfail or skip. Investigate. The Phase 23 plans were each verified individually; a wave-end regression here means an interaction between plans went undetected during per-plan verification.

Expected outcome: zero failures, zero errors, possibly a small number of warnings (existing baseline). Capture the test count in the SUMMARY.

If a small number of api_integration tests fail because testcontainers cannot pull a Postgres image (network blip), retry once. If still failing, document the root cause in the SUMMARY (network outage / docker daemon down / etc.). Do NOT proceed to Task 2 until pytest tests/ is green.
  </action>
  <verify>
    <automated>cd api_server && pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd api_server && pytest tests/ -v` exits 0.
    - Test count is GREATER than the pre-Phase-23 baseline (Phase 23 added ≥30 new tests across spikes + 5 new test files).
    - Specifically all new test files run: `cd api_server && pytest tests/spikes/ tests/auth/test_oauth_mobile.py tests/routes/test_messages_idempotency_required.py tests/routes/test_agent_messages_get.py tests/routes/test_agents_status_field.py tests/routes/test_models.py -v` exits 0.
    - No `xfail` annotations were added during this plan to make tests pass (root-cause-first).
  </acceptance_criteria>
  <done>Full pytest suite green; all Phase 23 new tests + all existing tests pass against testcontainers.</done>
</task>

<task type="auto">
  <name>Task 2: e2e-inapp-docker live-infra gate</name>
  <files></files>
  <read_first>
    - api_server/Makefile (read the e2e-inapp-docker target — confirm bind-mount of api_server source from main; confirm the report file location is api_server/e2e-report.json or similar)
    - memory/project_phase_22c31_shipped.md (the Phase 22c.3.1 SUMMARY — documents e2e-inapp-docker as the macOS-parity gate; D-27 invariant 3/3 + 5/5 PASS expected)
  </read_first>
  <action>
Run the live-Docker end-to-end gate:

```
cd api_server && make e2e-inapp-docker
```

This target (per Phase 22c.3.1):
- Builds + starts a real Docker container for a live recipe (hermes).
- Boots api_server with bind-mounted source from main.
- POSTs a real /messages, waits for a real bot reply via SSE.
- Asserts the inapp_messages row reaches status='done' within the bot timeout.
- Writes a report file (likely `api_server/e2e-report.json`) with `passed: true`.

Expected outcome: exit code 0; report file shows `passed: true` (or whatever the schema is — read the report file after the run).

If the gate fails:
1. Capture the report file's content + the relevant logs.
2. Investigate root cause. Phase 23 added: Idempotency-Key REQUIRED on /messages (Plan 23-02) — if the e2e harness sends /messages WITHOUT an Idempotency-Key header, this plan's gate WILL fail. The fix is in the harness/test-driver, not in production code (since the production change is correct per D-09).
3. Determine: did Plan 23-02's enforcement break the e2e harness? If yes:
   - Option A: extend the e2e harness to inject an Idempotency-Key header per message (preferred — aligns the harness with the production contract).
   - Option B: confirm the harness was already injecting one (verify by reading the e2e-driver source — likely `tests/e2e/` or `tools/e2e_driver.py`).
4. If the harness needs an update, that's a small fix (~3 LOC). Apply, commit, re-run. This is a legitimate gap-closure fix; it does NOT violate "no fix-to-pass" because the production contract (Idempotency-Key REQUIRED) is correct and intentional.

Other potential root causes:
- GZipMiddleware (Plan 23-05) interfering with SSE → Wave 0 spike already confirmed text/event-stream is excluded. If the e2e gate fails on SSE timing/buffering, re-run the spike: `cd api_server && pytest tests/spikes/test_gzip_sse_compat.py -x`. If the spike now fails, the regression is real and must be fixed in Plan 23-05's middleware ordering (not in this plan).
- Container lookup or `last_activity` SQL changes (Plan 23-04) breaking an existing dispatcher path — verify by reading run_store.py changes; the new LATERAL JOINs are READ-only and should NOT affect the dispatcher.

After a green run, capture:
- Exit code (0).
- Report file path (likely `api_server/e2e-report.json`).
- The `passed` field value (must be true).
- The number of message round-trips covered (typically 5 cells per Phase 22c.3.1 SC-03).
  </action>
  <verify>
    <automated>cd api_server && make e2e-inapp-docker</automated>
  </verify>
  <acceptance_criteria>
    - `cd api_server && make e2e-inapp-docker` exits 0.
    - The e2e report file exists (e.g. `ls api_server/e2e-report.json` succeeds) AND `grep -E '"passed":\s*true' api_server/e2e-report.json` returns ≥1 hit (N-03 tightened — per memory `project_phase_22c31_shipped.md`, the harness emits the JSON literal `"passed": true`).
    - The report file shows the expected number of cells (≥5 per Phase 22c.3.1 baseline).
    - If the e2e harness needed adjustment for Idempotency-Key REQUIRED, the adjustment is committed (small ~3-LOC fix) and the SUMMARY documents it.
    - **Audit-trail rule (W-03):** ANY harness edit MUST land in a separate atomic commit BEFORE Plan 23-09's verification commit; that earlier commit's message MUST reference plan 23-02 (the source of the contract change). The harness fix commit's diff MUST stay within `api_server/Makefile` + `api_server/tests/e2e/` + `tests/spikes/` (the paths declared in this plan's frontmatter `files_modified`); any edit outside those paths means the diagnosis was wrong — STOP and re-investigate before proceeding.
  </acceptance_criteria>
  <done>Live-Docker e2e gate green; report file shows passed=true; Phase 22c.3.1 macOS-parity behavior is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → live Docker daemon | the e2e gate spawns real containers; failure modes include resource exhaustion, daemon down, image-pull rate limits |
| executor → testcontainers Postgres + Redis | spawned per-test or per-session; failure modes include port collision, slow image pull |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-E2E | A (Availability) | e2e gate flake due to Docker / network / image-pull issues | accept | Retry once on infra-flake; document in SUMMARY; do NOT mask via xfail |
| T-23-WORKTREE | T (Tampering) | running e2e from a worktree where api_server source isn't bind-mountable | mitigate | autonomous: false + pre-gate human-verify checkpoint confirms commits are on main |
| T-23-IDEM-HARNESS | T (Tampering) | e2e harness sends /messages without Idempotency-Key after Plan 23-02 enforcement | accept | If failure, harness adjustment is a legitimate gap-closure fix (~3 LOC); production contract is correct per D-09 |
</threat_model>

<verification>
- `cd api_server && pytest tests/ -v` exits 0.
- `cd api_server && make e2e-inapp-docker` exits 0.
- e2e-report.json (or equivalent) exists with `"passed": true` (per memory `project_phase_22c31_shipped.md` — the e2e harness emits `"passed": true` in JSON). Tightened acceptance: `grep -E '"passed":\s*true' api_server/e2e-report.json` returns ≥1 hit (N-03).
- Wave 0 spikes still pass (re-run as a fast regression check at the end if desired): `cd api_server && pytest tests/spikes/ -x` exits 0.
- No new xfail annotations in any test file from Phase 23 plans (root-cause-first invariant).
</verification>

<success_criteria>
- Full pytest suite green.
- e2e-inapp-docker green; e2e-report.json shows passed=true.
- Wave 0 spikes still green at phase end.
- No regression in Phase 22c.3 / 22c.3.1 substrate.
- Phase 23 ships with all 7 API-* requirements verified.
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-09-SUMMARY.md` documenting:
- Total pytest test count (before vs after Phase 23)
- e2e-report.json final state (passed/failed, cells covered)
- Whether the e2e harness needed an Idempotency-Key adjustment (and if so, the commit hash)
- Confirmation that no test was skipped/xfailed during this plan

Then create the phase-level summary `.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PHASE-SUMMARY.md` documenting the overall Phase 23 outcome:
- 9 plans shipped
- 7 API-* requirements green
- D-32 amendments applied
- Wave 0 spikes + live-Docker gate confirm correctness
- Pointer to next phase (Phase 24 Flutter Foundation — depends on this phase per ROADMAP)
</output>
