---
phase: 23-backend-mobile-api-chat-proxy-persistence-auth-shim
plan: 05
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - api_server/src/api_server/services/openrouter_models.py
  - api_server/src/api_server/routes/models.py
  - api_server/src/api_server/main.py
  - api_server/tests/routes/test_models.py
autonomous: true
requirements:
  - API-04
must_haves:
  truths:
    - "GET /v1/models returns OpenRouter's /api/v1/models payload byte-for-byte on cache miss (D-20 passthrough — no JSON re-serialize)"
    - "GET /v1/models 200 responses set Cache-Control: private, max-age=300 (RESEARCH §Open Questions Q2 RESOLVED — overrides upstream no-store so a Flutter app reload within 5min skips the round-trip; complements the 15-min in-process server cache)"
    - "Within the 15min TTL window, subsequent GETs return the cached bytes without an upstream HTTP fetch (D-18)"
    - "After the TTL expires, the next GET refetches and updates the cache"
    - "On upstream fetch failure (network error, 5xx, timeout) AND a stale cache entry exists, the stale payload is served + the failure is logged (stale-while-revalidate per D-18)"
    - "On upstream fetch failure AND no cache exists (cold start failed fetch), the endpoint returns 503 with Stripe-shape envelope"
    - "GZipMiddleware(minimum_size=1024) is registered as the OUTERMOST middleware in main.py (D-25 + Pitfall 1); SSE responses are NOT compressed (Starlette default exclusion verified by Wave 0 spike)"
    - "Concurrent first-fetches are deduped by an asyncio.Lock so only one upstream HTTP call fires under a thundering herd (RESEARCH §6)"
    - "OpenRouter HTTP client lives on app.state with 10s timeout, lifecycle-managed by lifespan; teardown closes it before the pool"
  artifacts:
    - path: "api_server/src/api_server/services/openrouter_models.py"
      provides: "get_models_payload(state) -> bytes — TTL cache + asyncio.Lock dedupe + stale-while-revalidate"
    - path: "api_server/src/api_server/routes/models.py"
      provides: "GET /v1/models handler — calls get_models_payload, returns Response(media_type='application/json')"
    - path: "api_server/src/api_server/main.py"
      provides: "GZipMiddleware registered (outermost) + lifespan provisioning of openrouter_http_client + models_cache + models_cache_lock; teardown closes the client"
    - path: "api_server/tests/routes/test_models.py"
      provides: "Integration tests — cache miss, cache hit, TTL expiry, stale-while-revalidate, cold-start-503, gzip header"
  key_links:
    - from: "routes/models.py"
      to: "services/openrouter_models.py::get_models_payload"
      via: "single call; route is thin"
      pattern: "from \\.\\.services\\.openrouter_models import"
    - from: "main.py lifespan"
      to: "app.state.openrouter_http_client + app.state.models_cache + app.state.models_cache_lock"
      via: "startup creates, teardown closes"
      pattern: "openrouter_http_client|models_cache|models_cache_lock"
    - from: "main.py middleware stack"
      to: "GZipMiddleware as outermost (last add_middleware call per FastAPI semantics)"
      via: "compresses non-SSE responses ≥1024 bytes"
      pattern: "GZipMiddleware|minimum_size=1024"
---

<objective>
Implement `GET /v1/models` (REQ API-04 + D-18..D-20 + D-25) — the OpenRouter passthrough proxy with 15min in-process cache + stale-while-revalidate + asyncio.Lock dedupe. Register Starlette `GZipMiddleware(minimum_size=1024)` as the outermost middleware (D-25). Closes the dumb-client substrate (Golden Rule #2) — once green, the frontend playground-form migration in Plan 23-07 can land.

Purpose: Mobile and web read the OpenRouter catalog through the backend; backend caches it; backend transparently compresses big responses. No new auth (catalog is public per D-19), no new DB tables, no Redis. Pure in-process state on `app.state`.
Output: ~30 LOC service + ~25 LOC route + 3 lifespan additions in main.py + 1 GZipMiddleware registration + integration test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PATTERNS.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Source-of-truth excerpts. -->

main.py:137-140 lifespan-owned httpx pattern (bot_http_client) — analog with 600s timeout. Phase 23 mirrors this with a SEPARATE client at 10s timeout.

main.py:313-317 lifespan teardown pattern — close in try/except, log on failure with `_log.exception(...)`. Phase 23 closes openrouter_http_client analogously.

main.py:384-408 middleware stack — comment line 384 "outermost declared last". The LAST add_middleware call is the OUTERMOST layer in the response path. GZipMiddleware goes AT THE BOTTOM of this block.

main.py:413 — `app.include_router(recipes_route.router, prefix="/v1", tags=["recipes"])` is the analog for the new models router include.

routes/recipes.py — read-only `/v1/recipes` analog: minimal handler, app.state lookup, return raw payload.

PATTERNS.md §"services/openrouter_models.py" lines 122-153 — canonical service module shape with TTL+Lock+SWR.

RESEARCH.md §Pitfall 1 — GZip ordering: place at BOTTOM of add_middleware block to be OUTERMOST.

models/errors.py — locate ErrorCode.INFRA_UNAVAILABLE; if not present, use the closest 5xx-shaped constant (e.g. SERVICE_UNAVAILABLE) — verify by reading the file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Service module + route — get_models_payload + GET /v1/models</name>
  <files>api_server/src/api_server/services/openrouter_models.py, api_server/src/api_server/routes/models.py</files>
  <read_first>
    - api_server/src/api_server/services/ (list directory — match existing service-module conventions: file naming, _log = logging.getLogger("api_server.<module>"), async functions taking state or conn)
    - api_server/src/api_server/routes/recipes.py (full — analog for the route handler shape)
    - api_server/src/api_server/models/errors.py (full — confirm ErrorCode.INFRA_UNAVAILABLE constant; if absent use the closest 5xx-shaped constant)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PATTERNS.md §"openrouter_models.py" lines 122-153 (canonical implementation)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-RESEARCH.md §Pitfall 6, §Pitfall 7 (cache-control no-store hint; multi-replica divergence — both ACCEPT)
  </read_first>
  <behavior>
    - First request after boot: cache miss → upstream HTTP GET to https://openrouter.ai/api/v1/models with NO Authorization header → on 200, stores raw bytes + fetched_at; returns bytes
    - Within 15min TTL: subsequent requests do NOT call upstream; returns the cached bytes
    - After 15min TTL: next request triggers re-fetch
    - Concurrent first-fetches: only one upstream call fires (asyncio.Lock + double-check pattern)
    - Upstream raises (httpx.ConnectError, 5xx via raise_for_status, timeout) AND cache has prior payload: returns stale bytes + logs the failure as openrouter_models.fetch_failed
    - Upstream raises AND cache is empty: route returns 503 with Stripe envelope
    - Returns Response (not JSONResponse) so the bytes are passthrough — no decode/re-encode (D-20)
  </behavior>
  <action>
STEP 1 — Create `api_server/src/api_server/services/openrouter_models.py` with the canonical implementation from PATTERNS.md §"openrouter_models.py". Verbatim shape:

The module starts with a module docstring, imports (`from __future__ import annotations`, `asyncio`, `logging`, `from datetime import datetime, timedelta, timezone`, `import httpx`), the module logger `_log = logging.getLogger("api_server.openrouter_models")`, the URL constant `_OPENROUTER_URL = "https://openrouter.ai/api/v1/models"`, the TTL constant `_CACHE_TTL = timedelta(minutes=15)`, and the single async function `get_models_payload(state) -> bytes`.

The function:
1. Reads `cache = state.models_cache` and `now = datetime.now(timezone.utc)`.
2. Fast-path BEFORE the lock: if `cache.get("fetched_at")` AND `(now - cache["fetched_at"]) < _CACHE_TTL`, return `cache["payload"]`.
3. Slow-path: `async with state.models_cache_lock:` — re-check the same condition INSIDE the lock (double-check pattern; another coroutine may have refreshed).
4. Inside the lock, `try:` block: `r = await state.openrouter_http_client.get(_OPENROUTER_URL); r.raise_for_status()`. On exception (catch `httpx.HTTPError`): `_log.exception("openrouter_models.fetch_failed")`. If `cache.get("payload")` is truthy → log `openrouter_models.serving_stale` and return the stale bytes (SWR). Otherwise re-raise (caller renders 503).
5. On success: `cache["fetched_at"] = now; cache["payload"] = r.content` (raw bytes per D-20). Return `cache["payload"]`.

DO NOT add a JSON parse step. DO NOT add an Authorization header (D-19 — endpoint is public).

STEP 2 — Create `api_server/src/api_server/routes/models.py`. Mirror routes/recipes.py shape exactly:

Imports: `from __future__ import annotations`, `from fastapi import APIRouter, Request`, `from fastapi.responses import JSONResponse, Response`, `from ..models.errors import ErrorCode, make_error_envelope`, `from ..services.openrouter_models import get_models_payload`.

`router = APIRouter()` at module top.

Single handler `@router.get("/models")` async def `list_models(request: Request)`. Body wraps the call in try/except. On any exception, return `JSONResponse(status_code=503, content=make_error_envelope(ErrorCode.INFRA_UNAVAILABLE, "OpenRouter catalog temporarily unavailable"))`. On success, return `Response(content=payload, media_type="application/json", headers={"Cache-Control": "private, max-age=300"})` — Response, NOT JSONResponse, so the bytes are passthrough; the explicit `Cache-Control` header overrides whatever upstream OpenRouter sent (per RESEARCH §Open Questions Q2 RESOLVED — OpenRouter sends `private, no-store`, but we want mobile reload UX within 5min). Add a 1-line docstring on the handler citing "RESEARCH §Q2: Cache-Control: private, max-age=300 — 5min mobile-side cache complements 15min in-process server cache".

If `ErrorCode.INFRA_UNAVAILABLE` is absent in models/errors.py, use the closest 5xx-shaped constant. If no suitable constant exists, ADD one in this task (single-line addition to the ErrorCode enum/constants block).

NO auth check — this is a public catalog (D-19 + per CONTEXT.md "no per-user filter, no auth"). Mirror `routes/recipes.py::list_recipes` which is also unauthenticated.

DO NOT register the router in main.py here — that happens in Task 2.
  </action>
  <verify>
    <automated>cd api_server && python -c "from api_server.services.openrouter_models import get_models_payload; from api_server.routes.models import router; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `ls api_server/src/api_server/services/openrouter_models.py` succeeds.
    - File exists: `ls api_server/src/api_server/routes/models.py` succeeds.
    - Service module has TTL constant: `grep -E "_CACHE_TTL = timedelta\\(minutes=15\\)" api_server/src/api_server/services/openrouter_models.py` returns 1 hit.
    - Service uses asyncio.Lock: `grep -E "async with state\\.models_cache_lock" api_server/src/api_server/services/openrouter_models.py` returns 1 hit.
    - Service hits the canonical URL: `grep -E "openrouter\\.ai/api/v1/models" api_server/src/api_server/services/openrouter_models.py` returns 1 hit.
    - Service does NOT set Authorization header: `grep -nE "Authorization|Bearer" api_server/src/api_server/services/openrouter_models.py` returns ZERO hits (D-19 — public endpoint).
    - Service implements stale-while-revalidate: `grep -E "stale|cache\\.get" api_server/src/api_server/services/openrouter_models.py` returns ≥1 hit.
    - Route uses Response (not JSONResponse) for passthrough success: `grep -E "Response\\(content=payload, media_type=" api_server/src/api_server/routes/models.py` returns 1 hit.
    - Route sets Cache-Control header (RESEARCH §Q2 RESOLVED): `grep -E "Cache-Control.*private.*max-age=300|\"Cache-Control\":\s*\"private, max-age=300\"" api_server/src/api_server/routes/models.py` returns ≥1 hit.
    - Route returns 503 envelope on failure: `grep -E "status_code=503" api_server/src/api_server/routes/models.py` returns 1 hit.
    - Imports validate: `cd api_server && python -c "from api_server.services.openrouter_models import get_models_payload; from api_server.routes.models import router"` exits 0.
  </acceptance_criteria>
  <done>Service module + route module created; TTL+Lock+SWR pattern implemented; passthrough bytes; D-18/D-19/D-20 enforced; Cache-Control: private, max-age=300 set on 200 responses (RESEARCH §Q2 RESOLVED).</done>
</task>

<task type="auto">
  <name>Task 2: Wire lifespan + register router + add GZipMiddleware in main.py</name>
  <files>api_server/src/api_server/main.py</files>
  <read_first>
    - api_server/src/api_server/main.py FULL (locate: lifespan startup ~117-141, lifespan teardown ~313-317, middleware stack ~384-408, router includes ~413)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PATTERNS.md §"main.py" lines 474-514 (concrete lifespan additions + GZip placement)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-RESEARCH.md §Pitfall 1 (GZip ordering: outermost = bottom of add_middleware block per Starlette/FastAPI semantics)
  </read_first>
  <action>
Edit `api_server/src/api_server/main.py`. FIVE concrete edits.

EDIT 1 — Add new imports at top of file:
- `import asyncio` — verify with grep first; likely already imported.
- `from starlette.middleware.gzip import GZipMiddleware` — NEW.
- `from .routes import models as models_route` — NEW (alongside the existing `from .routes import recipes as recipes_route` or whatever the existing convention is; verify by grep).

EDIT 2 — Lifespan startup additions, IMMEDIATELY AFTER the existing `app.state.bot_http_client = httpx.AsyncClient(...)` block (around line 137-140):

Add three new state attributes:
- `app.state.openrouter_http_client = httpx.AsyncClient(timeout=httpx.Timeout(10.0, connect=5.0), limits=httpx.Limits(max_connections=10, max_keepalive_connections=5))`
- `app.state.models_cache = {}` (will hold `{fetched_at: datetime, payload: bytes}`)
- `app.state.models_cache_lock = asyncio.Lock()`

Critical: timeout is 10s (NOT 600s like bot_http_client). Catalog fetch must fail fast. Per RESEARCH §"Reuse app.state.bot_http_client?" rejection rationale, this MUST be a SEPARATE client.

EDIT 3 — Lifespan teardown additions, mirroring the existing bot_http_client teardown at lines 313-317. Place IMMEDIATELY ABOVE or BELOW the bot_http_client close (matching the "close HTTP first, DB last" ordering):

Use a try/except getattr pattern identical to the bot_http_client teardown. Call `await app.state.openrouter_http_client.aclose()`. On exception, log `_log.exception("phase23.lifespan.openrouter_http_client_close_failed")`. Match the existing log-tag convention (phase22c3.lifespan.* precedent → use phase23.lifespan.*).

EDIT 4 — Register the new router, alongside the existing recipes_route include around line 413:

Add `app.include_router(models_route.router, prefix="/v1", tags=["models"])`.

EDIT 5 — Add GZipMiddleware as the OUTERMOST middleware (D-25 + Pitfall 1). In the existing add_middleware block at lines 384-408, the comment says "outermost declared last". Add the new line AT THE BOTTOM of the block (AFTER `CorrelationIdMiddleware` — the current last entry):

`app.add_middleware(GZipMiddleware, minimum_size=1024)`

Add a one-line comment above explaining: "Phase 23 (D-25) — GZip outermost; Starlette ≥0.46.0 default-excludes text/event-stream (verified by Wave 0 spike)."

Critical: the existing "outermost declared last" semantic means the LAST add_middleware call is the OUTERMOST layer of the response. Adding GZip AT THE BOTTOM of the block is the right placement. Adding at the TOP would put it INNERMOST (cannot see the final outgoing response).

SMOKE CHECKS after all edits:
1. `cd api_server && python -c "from api_server.main import create_app; app = create_app(); print([m.cls.__name__ for m in app.user_middleware])"` — the LAST entry in the printed list should be GZipMiddleware. (FastAPI/Starlette stores middlewares in declaration order; the LAST declared is the outermost.)
2. `cd api_server && python -c "from api_server.main import create_app; app = create_app(); routes = [r.path for r in app.routes]; assert any('/v1/models' in p for p in routes)"` exits 0.

DO NOT modify any other middleware or route registration. DO NOT change the bot_http_client timeout or limits. DO NOT remove any existing middleware.
  </action>
  <verify>
    <automated>cd api_server && grep -nE "openrouter_http_client|models_cache_lock|GZipMiddleware|models_route" src/api_server/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "from starlette\\.middleware\\.gzip import GZipMiddleware" api_server/src/api_server/main.py` returns 1 hit.
    - `grep -nE "from \\.routes import models as models_route" api_server/src/api_server/main.py` returns 1 hit.
    - `grep -nE "app\\.state\\.openrouter_http_client = httpx\\.AsyncClient" api_server/src/api_server/main.py` returns 1 hit.
    - `grep -nE "app\\.state\\.models_cache = \\{\\}" api_server/src/api_server/main.py` returns 1 hit.
    - `grep -nE "app\\.state\\.models_cache_lock = asyncio\\.Lock\\(\\)" api_server/src/api_server/main.py` returns 1 hit.
    - `grep -nE "openrouter_http_client.*aclose|aclose.*openrouter_http_client" api_server/src/api_server/main.py` returns ≥1 hit (teardown close call present).
    - `grep -nE "include_router\\(models_route\\.router" api_server/src/api_server/main.py` returns 1 hit.
    - `grep -nE "GZipMiddleware, minimum_size=1024" api_server/src/api_server/main.py` returns 1 hit.
    - GZip is the LAST add_middleware call: a manual diff inspection (or `awk` over the file) confirms no other `add_middleware` line appears AFTER the `GZipMiddleware` line.
    - Smoke import: `cd api_server && python -c "from api_server.main import create_app; app = create_app(); m = [x.cls.__name__ for x in app.user_middleware]; assert 'GZipMiddleware' in m"` exits 0.
    - Route registered: `cd api_server && python -c "from api_server.main import create_app; app = create_app(); paths = [r.path for r in app.routes]; assert any('/v1/models' in p for p in paths)"` exits 0.
  </acceptance_criteria>
  <done>Lifespan starts/stops openrouter_http_client + models_cache + models_cache_lock; models router registered at /v1; GZipMiddleware is outermost.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — cache miss/hit, SWR, 503, gzip header</name>
  <files>api_server/tests/routes/test_models.py</files>
  <read_first>
    - api_server/tests/spikes/test_respx_authlib.py (respx fixture pattern)
    - api_server/tests/conftest.py (locate `async_client` fixture; confirm it's the FastAPI ASGITransport client used by other route tests)
    - api_server/src/api_server/services/openrouter_models.py (the implementation under test — confirm cache shape `{fetched_at: datetime, payload: bytes}` for direct manipulation in tests)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-VALIDATION.md per-task entries 23-API04-01..23-API04-04
  </read_first>
  <action>
Create `api_server/tests/routes/test_models.py` covering 4 scenarios from VALIDATION.md.

Module-level: `import pytest`, `pytestmark = [pytest.mark.api_integration, pytest.mark.asyncio]` (or per-test decorators matching other route tests). Imports: `httpx`, `respx`, `datetime, timedelta, timezone`.

Use respx to mock the upstream OpenRouter URL. The `async_client` fixture provides the FastAPI ASGITransport client that exercises the full middleware stack including the new GZipMiddleware.

Test 1 — cache miss fetches upstream and caches:

```
@respx.mock
async def test_get_models_cache_miss_fetches_and_caches(async_client):
    fake_payload = b'{"data":[{"id":"a","name":"alpha"}]}'
    route = respx.get("https://openrouter.ai/api/v1/models").mock(
        return_value=httpx.Response(200, content=fake_payload)
    )
    # ensure cold cache
    async_client._app.state.models_cache.clear()  # access via httpx client's app
    r = await async_client.get("/v1/models")
    assert r.status_code == 200
    assert r.content == fake_payload  # passthrough bytes, byte-equal
    assert r.headers["content-type"].startswith("application/json")
    assert route.call_count == 1
```

If the test framework doesn't expose `_app` directly, fetch state via the FastAPI app object — likely `async_client.app.state` or a helper fixture. Inspect `tests/conftest.py` to find the right access path. If the existing tests use a different mechanism (e.g. dependency injection of state), follow that.

Test 2 — cache hit within TTL skips upstream:

```
@respx.mock
async def test_get_models_cache_hit_within_ttl_skips_upstream(async_client):
    fake_payload = b'{"data":[{"id":"a"}]}'
    route = respx.get("https://openrouter.ai/api/v1/models").mock(
        return_value=httpx.Response(200, content=fake_payload)
    )
    state = async_client._app.state
    state.models_cache["payload"] = fake_payload
    state.models_cache["fetched_at"] = datetime.now(timezone.utc)
    r = await async_client.get("/v1/models")
    assert r.status_code == 200
    assert r.content == fake_payload
    assert route.call_count == 0  # upstream NOT called
```

Test 3 — stale-while-revalidate on fetch failure:

```
@respx.mock
async def test_get_models_swr_on_upstream_failure(async_client):
    stale_payload = b'{"data":[{"id":"stale"}]}'
    state = async_client._app.state
    state.models_cache["payload"] = stale_payload
    # fetched_at older than TTL → forces re-fetch attempt
    state.models_cache["fetched_at"] = datetime.now(timezone.utc) - timedelta(minutes=20)
    respx.get("https://openrouter.ai/api/v1/models").mock(
        return_value=httpx.Response(503, content=b"upstream gone")
    )
    r = await async_client.get("/v1/models")
    assert r.status_code == 200  # SWR — served stale
    assert r.content == stale_payload
```

Test 4 — cold-start failure returns 503:

```
@respx.mock
async def test_get_models_cold_start_failure_returns_503(async_client):
    state = async_client._app.state
    state.models_cache.clear()  # no prior payload
    respx.get("https://openrouter.ai/api/v1/models").mock(
        return_value=httpx.Response(500)
    )
    r = await async_client.get("/v1/models")
    assert r.status_code == 503, r.text
    body = r.json()
    assert body["error"]["code"] in ("INFRA_UNAVAILABLE", "SERVICE_UNAVAILABLE")  # whichever the route picked
```

Test 5 — gzip header set when client sends Accept-Encoding: gzip:

```
@respx.mock
async def test_get_models_gzip_header_when_requested(async_client):
    # Build a >1024-byte payload so GZipMiddleware kicks in
    large_payload = b'{"data":[' + b','.join(b'{"id":"x"}' for _ in range(200)) + b']}'
    assert len(large_payload) > 1024
    respx.get("https://openrouter.ai/api/v1/models").mock(
        return_value=httpx.Response(200, content=large_payload)
    )
    state = async_client._app.state
    state.models_cache.clear()
    r = await async_client.get("/v1/models", headers={"Accept-Encoding": "gzip"})
    assert r.status_code == 200
    # httpx auto-decodes gzip — assert content-encoding was used
    assert r.headers.get("content-encoding") == "gzip"
```

Test 6 — Cache-Control: private, max-age=300 header on 200 responses (RESEARCH §Q2 RESOLVED):

```
@respx.mock
async def test_get_models_cache_control_header(async_client):
    """Q2 RESOLVED: response sets Cache-Control: private, max-age=300 so mobile
    reload within 5min can skip the round-trip. Complements the 15min in-process
    server cache."""
    fake_payload = b'{"data":[{"id":"a"}]}'
    respx.get("https://openrouter.ai/api/v1/models").mock(
        return_value=httpx.Response(
            200, content=fake_payload,
            # Simulate OpenRouter's upstream Cache-Control to prove we OVERRIDE it:
            headers={"Cache-Control": "private, no-store"},
        )
    )
    state = async_client._app.state
    state.models_cache.clear()
    r = await async_client.get("/v1/models")
    assert r.status_code == 200
    # The route handler MUST override upstream Cache-Control with its own value:
    cc = r.headers.get("cache-control", "")
    assert "private" in cc.lower(), f"missing private directive: {cc!r}"
    assert "max-age=300" in cc.lower(), f"missing max-age=300: {cc!r}"
    # And critically, NOT the upstream no-store:
    assert "no-store" not in cc.lower(), f"upstream no-store leaked: {cc!r}"
```

This test name (`test_get_models_cache_control_header`) is ALSO the grep-checkable acceptance criterion below — `pytest tests/routes/test_models.py -x -k cache_control_header` runs exactly this case in isolation.

Note on `async_client._app`: if accessing the app via `_app` is private/brittle, inspect `tests/conftest.py` for how other tests reach `app.state`. Many test setups expose `started_api_server` or a similar fixture that yields both the client and the app object. Use whichever is idiomatic in the project.

After file creation: `cd api_server && pytest tests/routes/test_models.py -x`. All 5 tests must PASS.

Regression sweep: `cd api_server && pytest tests/routes/ tests/spikes/ -x` — must remain green.
  </action>
  <verify>
    <automated>cd api_server && pytest tests/routes/test_models.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `ls api_server/tests/routes/test_models.py` succeeds.
    - File contains ≥6 test functions: `grep -cE "^async def test_" api_server/tests/routes/test_models.py` returns ≥6.
    - File mocks the canonical URL: `grep -E "openrouter\\.ai/api/v1/models" api_server/tests/routes/test_models.py` returns ≥1 hit.
    - File asserts byte-equal passthrough: `grep -E "r\\.content == fake_payload|r\\.content == stale_payload|byte-equal" api_server/tests/routes/test_models.py` returns ≥1 hit.
    - File asserts gzip header: `grep -E "content-encoding.*gzip" api_server/tests/routes/test_models.py` returns ≥1 hit.
    - File asserts Cache-Control header (RESEARCH §Q2): `grep -E "cache_control_header|Cache-Control.*private|max-age=300" api_server/tests/routes/test_models.py` returns ≥1 hit.
    - Cache-Control test runs in isolation: `cd api_server && pytest tests/routes/test_models.py -x -k cache_control_header` exits 0.
    - File asserts 503 envelope on cold failure: `grep -E "status_code == 503" api_server/tests/routes/test_models.py` returns ≥1 hit.
    - `cd api_server && pytest tests/routes/test_models.py -x` exits 0.
    - Regression: `cd api_server && pytest tests/routes/ tests/spikes/ -x` exits 0.
  </acceptance_criteria>
  <done>6 tests green; cache miss/hit/SWR/503/gzip/cache-control behaviors verified (RESEARCH §Q2 closed); no regression in routes or spikes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /v1/models | public; no auth required (D-19) |
| handler → upstream OpenRouter HTTPS | TLS termination at openrouter.ai |
| handler → in-process cache | per-replica state; multi-replica divergence accepted |
| GZipMiddleware → outgoing response | compresses non-SSE payloads ≥1024 bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-OR-PASSTHRU | T (Tampering) | malicious payload from compromised CDN | accept | TLS to openrouter.ai; we passthrough bytes; if bytes contain HTML/script, neither web nor mobile clients execute it (Flutter doesn't execute HTML; web treats it as JSON) |
| T-23-GZIP-COMPRESS | I (Info Disclosure) | BREACH-style attack via response compression | mitigate | GZipMiddleware excludes text/event-stream by default (Wave 0 spike verified); /v1/models is non-sensitive (public catalog) so BREACH is N/A here |
| T-23-OR-DOS | D (DoS) | OpenRouter rate-limits us under thundering herd | mitigate | asyncio.Lock dedupes concurrent first-fetches → ~1 fetch per 15min per replica; well under any reasonable rate limit |
| T-23-OR-COLD-START | A (Availability) | OpenRouter down at boot, no cache → endpoint 503 | accept | Documented behavior; 503 envelope is honest; SWR mitigates after first successful fetch |
| T-23-MULTI-REPLICA | I (Info Disclosure) | replicas serve different snapshots within TTL | accept | Documented in RESEARCH §Pitfall 7; ~one fetch / 15min / replica is trivial; eventual consistency on catalog updates is fine for MVP |
</threat_model>

<verification>
- `cd api_server && pytest tests/routes/test_models.py -x` exits 0.
- `cd api_server && pytest tests/routes/ tests/spikes/ -x` exits 0 (no regression).
- `grep -nE "openrouter_http_client|models_cache" api_server/src/api_server/main.py` returns ≥3 hits.
- `grep -nE "GZipMiddleware, minimum_size=1024" api_server/src/api_server/main.py` returns 1 hit.
- `grep -nE "include_router\\(models_route" api_server/src/api_server/main.py` returns 1 hit.
- Manual smoke (only if api_server is running): `curl -s -o /dev/null -w "%{http_code} %{size_download}\\n" http://localhost:8000/v1/models` returns `200 <bytes>`; second call within 15min returns same bytes faster (cache hit).
</verification>

<success_criteria>
- GET /v1/models works on cache miss, cache hit, SWR, and 503 cold-start scenarios.
- GZipMiddleware is the outermost middleware; SSE responses unaffected (Wave 0 spike re-runs as regression check).
- No Authorization header on the upstream fetch (D-19).
- Bytes are passthrough (D-20 — no JSON re-serialize).
- All Plan 23-05 tests green; no regression in routes/spikes.
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-05-SUMMARY.md` documenting:
- Final ErrorCode constant used (INFRA_UNAVAILABLE / SERVICE_UNAVAILABLE / etc.)
- Method used to access app.state in tests (async_client._app.state vs fixture-exposed)
- Confirmed: GZipMiddleware is the LAST add_middleware call (outermost on response)
- Sample compressed-vs-uncompressed payload sizes (manual measurement with `wc -c` if api_server is running locally — optional)
</output>
