---
phase: 23-backend-mobile-api-chat-proxy-persistence-auth-shim
plan: 07
type: execute
wave: 3
depends_on:
  - "23-05"
files_modified:
  - frontend/components/playground-form.tsx
autonomous: true
requirements:
  - API-04
must_haves:
  truths:
    - "frontend/components/playground-form.tsx no longer fetches https://openrouter.ai/api/v1/models directly"
    - "The new fetch uses apiGet('/api/v1/models') — the same dumb-client pattern used by the existing recipes fetch 15 lines above"
    - "The model list still renders correctly in the playground form (no UI regression)"
    - "GZipMiddleware on the backend transparently compresses the response (Plan 23-05); the frontend code does NOT need to manually request gzip — the browser does it automatically"
  artifacts:
    - path: "frontend/components/playground-form.tsx"
      provides: "5-LOC change at line ~169 — replace direct OpenRouter fetch with apiGet"
  key_links:
    - from: "frontend/components/playground-form.tsx"
      to: "GET /v1/models (Plan 23-05)"
      via: "apiGet from frontend/lib/api.ts"
      pattern: "apiGet.*\\/v1\\/models"
---

<objective>
Migrate `frontend/components/playground-form.tsx` from a direct `fetch("https://openrouter.ai/api/v1/models")` to `apiGet("/api/v1/models")` (D-21). Closes the second remaining Golden Rule #2 violation (the first was hardcoded `defaultClones` already removed; the third — mocked chat page — is deferred per CONTEXT.md). Once Plan 23-05 is green, this plan flips the consumer to the new server-side proxy.

Purpose: Web stops fetching OpenRouter directly; backend is the single source of truth for the model catalog; mobile (Phase 25) gets the same behavior for free.
Output: A 5-LOC change at one site in one file. No new tests required (existing playground UI already exercises this code path; manual smoke verification confirms parity).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The exact analog already exists 15 lines above the migration site. -->

frontend/components/playground-form.tsx:150-163 (existing apiGet pattern for /v1/recipes — verbatim from PATTERNS.md):
```
useEffect(() => {
  let cancelled = false;
  (async () => {
    try {
      const data = await apiGet<{ recipes: RecipeSummary[] }>("/api/v1/recipes");
      if (cancelled) return;
      const sorted = [...data.recipes].sort((a, b) => a.name.localeCompare(b.name));
      setRecipes(sorted);
    } catch (e) {
      if (!cancelled) setUiError(parseApiError(e));
    }
  })();
  return () => { cancelled = true; };
}, []);
```

frontend/components/playground-form.tsx:165-179 (current OpenRouter direct-fetch site to replace):
```
useEffect(() => {
  let cancelled = false;
  (async () => {
    try {
      const r = await fetch("https://openrouter.ai/api/v1/models");  // LINE 169 — DIRECT FETCH
      if (!r.ok) throw new Error(`HTTP ${r.status}`);
      const d = (await r.json()) as { data: OpenRouterModel[] };
      if (cancelled) return;
      setOrModels(d.data ?? []);
    } catch (e) {
      if (!cancelled) setOrError(e instanceof Error ? e.message : "load failed");
    }
  })();
  return () => { cancelled = true; };
}, []);
```

frontend/lib/api.ts (existing apiGet — already imported at playground-form.tsx line 23: `import { apiGet, apiPost } from "@/lib/api";`).

next.config.ts — `/api/*` rewrites to the Go/FastAPI backend (per the comment in frontend/lib/api.ts:3); apiGet handles the URL prefixing transparently.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace direct OpenRouter fetch with apiGet('/api/v1/models')</name>
  <files>frontend/components/playground-form.tsx</files>
  <read_first>
    - frontend/components/playground-form.tsx FULL (locate: line 23 imports for apiGet; line ~150-163 the recipes useEffect (analog); line ~165-179 the OpenRouter direct-fetch useEffect (migration site))
    - frontend/lib/api.ts (verify apiGet's signature and how it handles the response shape; check if it returns the body parsed as JSON or raw)
    - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-PATTERNS.md §"frontend/components/playground-form.tsx:169" (concrete migration patch)
  </read_first>
  <action>
Edit `frontend/components/playground-form.tsx`. Replace the OpenRouter direct-fetch useEffect at lines ~165-179 with the apiGet shape mirroring the recipes useEffect 15 lines above.

CONCRETE BEFORE-AND-AFTER (the only change):

BEFORE:
```
useEffect(() => {
  let cancelled = false;
  (async () => {
    try {
      const r = await fetch("https://openrouter.ai/api/v1/models");
      if (!r.ok) throw new Error(`HTTP ${r.status}`);
      const d = (await r.json()) as { data: OpenRouterModel[] };
      if (cancelled) return;
      setOrModels(d.data ?? []);
    } catch (e) {
      if (!cancelled) setOrError(e instanceof Error ? e.message : "load failed");
    }
  })();
  return () => { cancelled = true; };
}, []);
```

AFTER:
```
useEffect(() => {
  let cancelled = false;
  (async () => {
    try {
      const d = await apiGet<{ data: OpenRouterModel[] }>("/api/v1/models");
      if (cancelled) return;
      setOrModels(d.data ?? []);
    } catch (e) {
      if (!cancelled) setOrError(e instanceof Error ? e.message : "load failed");
    }
  })();
  return () => { cancelled = true; };
}, []);
```

Notes:
- `apiGet` is already imported at line 23 — do NOT add a new import.
- The URL path is `/api/v1/models` (the next.config.ts rewrite forwards /api/* to the FastAPI backend at /v1/models). Do NOT use `/v1/models` directly in the frontend — that bypasses the rewrite.
- The response shape `{data: OpenRouterModel[]}` is preserved verbatim because Plan 23-05's GET /v1/models is a byte-passthrough of OpenRouter's payload (D-20).
- Removed: `if (!r.ok) throw new Error(\`HTTP ${r.status}\`)` — apiGet handles non-2xx responses by throwing already (verify by reading frontend/lib/api.ts; if apiGet does NOT throw on 4xx/5xx, keep an explicit check). Per the analog at lines 150-163, the recipes fetch does NOT include a manual !r.ok check, so apiGet does throw — match that pattern.
- The `OpenRouterModel` TypeScript type is already imported in the file; do NOT change it.

DO NOT modify any other useEffect, state setter, JSX rendering, or import. The only diff is the body of THIS ONE useEffect (lines ~166-178 inclusive of the try block).

After edit, run a smoke check:
1. `cd frontend && pnpm tsc --noEmit` (or `npx tsc --noEmit` if pnpm not in scope) — TypeScript compile must pass.
2. `cd frontend && pnpm build` (optional but recommended) — Next.js build must succeed.

If a linter is configured (`eslint`), run it on the file: `cd frontend && pnpm lint` or `pnpm lint --filter=frontend` per the project's tooling. Errors must be zero.

If frontend tooling is broken/unavailable in the executor's environment, document in the SUMMARY that the type-check could not run locally and rely on CI for the gate.
  </action>
  <verify>
    <automated>cd frontend && grep -E 'apiGet.*"/api/v1/models"' components/playground-form.tsx && (grep -E 'fetch\("https://openrouter\.ai/api/v1/models"' components/playground-form.tsx; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - The direct OpenRouter fetch is GONE: `grep -E 'fetch\\("https://openrouter\\.ai/api/v1/models"' frontend/components/playground-form.tsx` returns ZERO hits (the load-bearing acceptance criterion for D-21).
    - The new apiGet call exists: `grep -E 'apiGet<.*>\\("/api/v1/models"\\)' frontend/components/playground-form.tsx` returns 1 hit.
    - No new imports added: `git diff frontend/components/playground-form.tsx | grep -E "^\\+import "` returns ZERO hits (apiGet was already imported per PATTERNS.md).
    - `setOrModels(d.data ?? [])` is preserved: `grep -E "setOrModels\\(d\\.data" frontend/components/playground-form.tsx` returns 1 hit.
    - TypeScript compile passes: `cd frontend && pnpm tsc --noEmit` (or `npx tsc --noEmit`) exits 0. If `pnpm` unavailable in executor environment, document in SUMMARY.
    - Other useEffects untouched: `grep -cE "useEffect\\(\\(\\) =>" frontend/components/playground-form.tsx` count is identical to pre-plan (the count of useEffect calls did not change — we replaced the BODY of one, not added/removed the call).
  </acceptance_criteria>
  <done>The direct OpenRouter fetch is replaced by apiGet at the same site; setOrModels still receives the model list; TypeScript compiles; no new imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → frontend bundle | the bundle's hardcoded OpenRouter URL is the leak being closed |
| frontend → backend /v1/models | now goes through Same-Origin (next.config.ts rewrite) → FastAPI proxy (Plan 23-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-GR2-DUMB-CLIENT | I (Info Disclosure / Architecture) | client-side hardcoded catalog source URL | mitigate | Replace direct OpenRouter URL with apiGet to same-origin /api/v1/models; backend now owns the catalog source |
| T-23-CORS-LEAK | I (Info Disclosure) | direct browser → openrouter.ai exposes the user's IP + browser identity to OpenRouter | mitigate | Same-origin /api/v1/models keeps OpenRouter blind to the end-user's browser; backend's outbound HTTP is the only consumer |
</threat_model>

<verification>
- `grep -E "openrouter\\.ai/api/v1/models" frontend/components/playground-form.tsx` returns ZERO hits (the load-bearing gate from VALIDATION.md 23-API04-05).
- `grep -E "apiGet" frontend/components/playground-form.tsx` returns ≥2 hits (existing recipes call + new models call).
- `cd frontend && pnpm tsc --noEmit` exits 0.
- Manual smoke (only if backend is running locally with Plan 23-05 shipped): start frontend dev server, open the playground form, verify the model dropdown still populates with the OpenRouter catalog.
</verification>

<success_criteria>
- frontend/components/playground-form.tsx no longer references openrouter.ai directly.
- The new code uses apiGet to the same-origin /api/v1/models path.
- TypeScript compiles cleanly.
- No regression in the playground form's UX (manual smoke if locally runnable).
</success_criteria>

<output>
After completion, create `.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-07-SUMMARY.md` documenting:
- The exact line numbers before/after the change (for diff traceability)
- Whether the TypeScript compile + Next.js build were runnable locally
- Whether a manual UI smoke was performed
</output>
