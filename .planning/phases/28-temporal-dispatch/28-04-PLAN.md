---
phase: 28-temporal-dispatch
plan: 04
type: execute
wave: 3
depends_on: [28-03]
files_modified:
  - api_server/src/api_server/temporal/activities/check_container_ready.py
  - api_server/src/api_server/temporal/activities/forward_to_agent.py
  - api_server/src/api_server/temporal/activities/record_usage.py
  - api_server/src/api_server/temporal/activities/emit_inapp_outbound.py
  - api_server/src/api_server/temporal/activities/mark_message_done.py
  - api_server/src/api_server/temporal/activities/mark_message_failed.py
  - api_server/src/api_server/temporal/worker.py
autonomous: true
requirements:
  - D-04
  - D-05
  - D-07
  - D-10
  - D-11
  - D-15
must_haves:
  truths:
    - "Each activity stub from Plan 03 is replaced with a real implementation, EXCEPT debit_balance which keeps its Decimal('0') no-op body per D-22"
    - "forward_to_agent activity owns its own [1s, 2s, 4s] retry loop on httpx.ConnectError + httpx.ReadTimeout per D-11; HTTP 4xx/5xx + RuntimeError fail terminal via ApplicationError(non_retryable=True)"
    - "forward_to_agent reuses the existing services/inapp_dispatcher._dispatch_http_localhost 3-way contract switch verbatim — no contract logic re-implemented"
    - "emit_inapp_outbound + mark_message_done are combined into a single transactional pair (or run inside one DB tx) per RESEARCH §7 R2 — they MUST be atomic so the outbox pump never sees torn state"
    - "Activities are class-bound where they need shared deps (db_pool, bot_http_client, recipe_index) per RESEARCH §4 — top-level functions where they don't"
    - "worker.py is a runnable entrypoint: `python -m api_server.temporal.worker` boots the worker, registers DispatchMessageWorkflow + all 7 activities on task queue ap-messages, drains gracefully on SIGTERM"
    - "Worker concurrency: max_concurrent_activities=10, max_activities_per_second=5 (mirroring MSV)"
    - "deploy-temporal-worker-1 container reaches Up state after Plan 04 ships and the api_server lifespan boots, with no restart loop"
  artifacts:
    - path: "api_server/src/api_server/temporal/worker.py"
      provides: "python -m api_server.temporal.worker entrypoint"
      exports: ["main"]
    - path: "api_server/src/api_server/temporal/activities/forward_to_agent.py"
      provides: "ForwardActivities class with bot HTTP call + activity-internal [1s,2s,4s] retry"
      exports: ["forward_to_agent", "ForwardActivities"]
    - path: "api_server/src/api_server/temporal/activities/mark_message_done.py"
      provides: "Atomic mark_done + emit_inapp_outbound pair (RESEARCH §7 R2)"
      exports: ["mark_message_done"]
  key_links:
    - from: "deploy-temporal-worker-1 container"
      to: "Temporal frontend"
      via: "Client.connect(temporal:7233) + Worker.run() on task_queue=ap-messages"
      pattern: "Client.connect"
    - from: "forward_to_agent activity"
      to: "existing _dispatch_http_localhost in services/inapp_dispatcher.py"
      via: "import + call (verbatim 3-way contract switch)"
      pattern: "_dispatch_http_localhost"
    - from: "mark_message_done activity"
      to: "agent_events INSERT for outbox pump"
      via: "single asyncpg transaction with both writes"
      pattern: "BEGIN.*mark.*emit"
---

<objective>
Wave 3 — fill in the activity bodies + ship the worker entrypoint. After this plan, the worker container in `deploy/docker-compose.prod.yml` boots cleanly, registers the workflow + 7 activities on `ap-messages`, and is ready to consume `start_workflow` calls (which Plan 06 wires from the route handler).

This plan does NOT touch the workflow file (Plan 03 owned that), does NOT delete the legacy `inapp_dispatcher.py` (Plan 06 owns the cutover), and does NOT add tests (Plan 05 owns those). Plan 04 only adds the bodies + the worker process.

Critical decision absorbed from RESEARCH §7 R2: `mark_message_done` is implemented as an ATOMIC pair (mark + emit_agent_event) inside a single asyncpg transaction, NOT as two separate activities. The workflow still calls both `emit_inapp_outbound` and `mark_message_done` activities for shape parity with MSV, but the BODY of `mark_message_done` performs both writes; `emit_inapp_outbound` becomes a no-op for the success path (RESEARCH §7 R2 + R7 mitigation: outbox pump owns the Redis publish; the activity's job is the DB write only). Same for failure path: `mark_message_failed` writes BOTH the inapp_messages status update AND the agent_events row in one tx.

Purpose: convert the 7-activity scaffold into runnable code; bring up the worker.
Output: 6 activity files filled in + 1 new worker.py + a green `deploy-temporal-worker-1` container in `make dev-api-local`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-temporal-dispatch/28-CONTEXT.md
@.planning/phases/28-temporal-dispatch/28-RESEARCH.md
@CLAUDE.md
@api_server/src/api_server/services/inapp_dispatcher.py
@api_server/src/api_server/services/inapp_recipe_index.py
@api_server/src/api_server/services/inapp_messages_store.py
@api_server/src/api_server/services/usage_recorder.py
@api_server/src/api_server/services/event_store.py
@api_server/src/api_server/temporal/workflows/dispatch_message.py
@api_server/src/api_server/temporal/workflows/types.py

<interfaces>
Settings (from Plan 02): temporal_host, temporal_namespace, temporal_task_queue, bot_timeout_seconds, redis_url, recipes_dir, docker_network_name, database_url

Existing services consumed by activities:
- services/inapp_dispatcher._dispatch_http_localhost(http, row, inapp, container_ip, timeout_seconds) -> (reply: str, raw: dict)
- services/inapp_recipe_index.InappRecipeIndex.get_inapp_block(recipe_name) -> InappChannelConfig | None
- services/inapp_recipe_index.InappRecipeIndex.get_container_ip(container_id) -> str  (raises RuntimeError on dead/missing)
- services/inapp_messages_store.mark_done(conn, message_id, bot_response)
- services/inapp_messages_store.mark_failed(conn, message_id, reason)
- services/event_store.insert_agent_event(conn, kind, agent_container_id, payload, correlation_id=None) -> int  (seq)
- services/usage_recorder.record_usage(conn, *, user_id, agent_id, container_id, recipe_name, model, ...) -> None

Plan 03 stubs (signatures locked):
  check_container_ready(inp: DispatchMessageInput) -> bool
  forward_to_agent(inp: DispatchMessageInput) -> ForwardResult
  record_usage(usage_input: dict) -> None
  emit_inapp_outbound(event_input: dict) -> None
  mark_message_done(mark_done_input: dict) -> None
  mark_message_failed(args: tuple[str, str]) -> None
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill activity bodies — readiness + forward + record_usage + emit_inapp_outbound (no-op success-path) + mark_message_done (atomic) + mark_message_failed (atomic)</name>
  <files>api_server/src/api_server/temporal/activities/check_container_ready.py, api_server/src/api_server/temporal/activities/forward_to_agent.py, api_server/src/api_server/temporal/activities/record_usage.py, api_server/src/api_server/temporal/activities/emit_inapp_outbound.py, api_server/src/api_server/temporal/activities/mark_message_done.py, api_server/src/api_server/temporal/activities/mark_message_failed.py</files>
  <read_first>
    - api_server/src/api_server/services/inapp_dispatcher.py (full file — particularly `_handle_row`, `_dispatch_http_localhost`, the readiness query around line 405, and `_KNOWN_ERROR_TYPES` around line 325)
    - api_server/src/api_server/services/inapp_recipe_index.py (InappRecipeIndex API: get_inapp_block, get_container_ip)
    - api_server/src/api_server/services/inapp_messages_store.py (mark_done, mark_failed signatures + the asyncpg connection contract)
    - api_server/src/api_server/services/usage_recorder.py:record_usage (full signature — the workflow's usage_input dict keys must match this kwarg list)
    - api_server/src/api_server/services/event_store.py:insert_agent_event (signature + payload schema)
    - .planning/phases/28-temporal-dispatch/28-RESEARCH.md §3 (forward_to_agent skeleton lines 367-505), §7 R2 + R7 (atomic dual-write rationale)
    - .planning/phases/28-temporal-dispatch/28-CONTEXT.md D-07, D-10, D-11, D-15
  </read_first>
  <action>
**File 1 — check_container_ready.py:**

Class-bound `ReadinessActivities`:

```
class ReadinessActivities:
    def __init__(self, *, db_pool):
        self.db_pool = db_pool

    @activity.defn(name="check_container_ready")
    async def check_container_ready(self, inp: DispatchMessageInput) -> bool:
        # Query: SELECT 1 FROM agent_containers WHERE id=$1 AND container_status='running' AND ready_at IS NOT NULL AND stopped_at IS NULL
        # Returns True if row found, False otherwise.
        # IMPORTANT: ApplicationError NOT raised on "not ready" — return False so the workflow's [250ms,...,4s] retry budget kicks in.
        # ApplicationError(type='container_dead', non_retryable=True) IS raised if the container row was DELETED entirely (a hard terminal — workflow should not retry that).
```

Wire it: the file's top-level export `check_container_ready` must point at the bound method on a worker-owned instance. Since Temporal needs a function-shaped registration target, the worker file (Task 2) instantiates `ReadinessActivities(db_pool=...)` then registers `instance.check_container_ready` directly. So the activity FILE exports BOTH:
- `class ReadinessActivities` (the class)
- A module-level `async def check_container_ready(inp: DispatchMessageInput) -> bool: raise NotImplementedError("call ReadinessActivities.check_container_ready instead")` — kept ONLY for the workflow file's import statement and Plan 03's smoke test.

The workflow registers the bound method (worker.py decides which); the module-level function is a NO-OP placeholder for the import path. Document this clearly in the file's module docstring.

**File 2 — forward_to_agent.py:**

This is the largest activity. Verbatim shape from RESEARCH §3 lines 367-505. Class-bound `ForwardActivities`:

```
class ForwardActivities:
    def __init__(self, *, db_pool, bot_http_client, recipe_index):
        self.db_pool = db_pool
        self.bot_http_client = bot_http_client
        self.recipe_index = recipe_index

    @activity.defn(name="forward_to_agent")
    async def forward_to_agent(self, inp: DispatchMessageInput) -> ForwardResult:
        attempt = activity.info().attempt
        activity.logger.info("phase28.forward_to_agent.attempt", extra={"message_id": inp.message_id, "attempt": attempt})

        # 1. Recipe lookup
        inapp = self.recipe_index.get_inapp_block(inp.recipe_name)
        if inapp is None:
            raise ApplicationError("recipe_lacks_inapp_channel", type="recipe_lacks_inapp_channel", non_retryable=True)

        # 2. Container IP discovery
        try:
            container_ip = self.recipe_index.get_container_ip(inp.container_id)
        except RuntimeError:
            raise ApplicationError("container_dead", type="container_dead", non_retryable=True)

        # 3. Build dict-like row matching _dispatch_http_localhost's existing input shape
        from uuid import UUID
        row = {
            "id": UUID(inp.message_id),
            "user_id": UUID(inp.user_id),
            "agent_id": UUID(inp.agent_id),
            "container_row_id": UUID(inp.container_row_id),
            "container_id": inp.container_id,
            "recipe_name": inp.recipe_name,
            "agent_model": inp.agent_model,
            "content": inp.content,
            "inapp_auth_token": inp.inapp_auth_token,
            "attempts": attempt,
        }

        # 4. Activity-internal retry on transport errors only (D-11)
        for retry_idx, backoff_s in enumerate([0.0, 1.0, 2.0, 4.0]):
            if backoff_s > 0:
                await asyncio.sleep(backoff_s)
            try:
                reply, raw = await _dispatch_http_localhost(self.bot_http_client, row, inapp, container_ip, timeout_seconds=inp.bot_timeout_seconds)
                # Success — build ForwardResult with the dicts the downstream activities expect
                return ForwardResult(
                    reply=reply,
                    raw_response=raw,
                    usage_input={
                        "user_id": inp.user_id,
                        "agent_id": inp.agent_id,
                        "container_row_id": inp.container_row_id,
                        "recipe_name": inp.recipe_name,
                        "agent_model": inp.agent_model,
                        "raw_response": raw,
                        "request_text": inp.content,
                        "reply_text": reply,
                    },
                    event_input={
                        "container_row_id": inp.container_row_id,
                        "agent_id": inp.agent_id,
                        "user_id": inp.user_id,
                        "reply": reply,
                        "message_id": inp.message_id,
                    },
                    mark_done_input={
                        "message_id": inp.message_id,
                        "container_row_id": inp.container_row_id,
                        "user_id": inp.user_id,
                        "agent_id": inp.agent_id,
                        "bot_response": reply,
                    },
                )
            except (httpx.ConnectError, httpx.ReadTimeout):
                if retry_idx == 3:
                    raise ApplicationError("bot_timeout", type="bot_timeout", non_retryable=True)
                continue
            except httpx.HTTPStatusError as e:
                raise ApplicationError(f"bot_5xx:{e.response.status_code}", type="bot_5xx", non_retryable=True)
            except RuntimeError as e:
                raise ApplicationError(str(e), type=str(e).split(":", 1)[0], non_retryable=True)
            except (httpx.RequestError, ValueError):
                raise ApplicationError("bot_invalid_response", type="bot_invalid_response", non_retryable=True)

        raise ApplicationError("bot_timeout", type="bot_timeout", non_retryable=True)  # unreachable but keeps type-checker happy
```

Same module-level `forward_to_agent` placeholder pattern as Task 1 file 1.

**File 3 — record_usage.py:**

Class-bound `RecordUsageActivities`:

```
class RecordUsageActivities:
    def __init__(self, *, db_pool):
        self.db_pool = db_pool

    @activity.defn(name="record_usage")
    async def record_usage(self, usage_input: dict) -> None:
        # Open a connection from db_pool, call the existing services/usage_recorder.record_usage with kwargs unpacked from usage_input.
        # Drop the savepoint pattern (per CONTEXT D-07: activity owns its own tx context).
        from ..services.usage_recorder import record_usage as legacy_record_usage
        async with self.db_pool.acquire() as conn:
            async with conn.transaction():
                await legacy_record_usage(conn, **usage_input)
```

Note: the keyword expansion `**usage_input` requires the workflow's `usage_input` dict keys to match the legacy function's kwargs verbatim. Cross-reference services/usage_recorder.record_usage's signature when reading; if the kwarg list is different from the dict shape in File 2 (forward_to_agent), update File 2's `usage_input` builder to match.

If `record_usage` raises ANY exception, the activity propagates it to the workflow as `ApplicationError`. The workflow's outer try/except (D-15) swallows it and continues. **Do NOT catch + log inside the activity** — let the workflow's swallow-and-continue handle it. The activity's only job is to attempt the write.

**File 4 — emit_inapp_outbound.py (success-path no-op):**

Per RESEARCH §7 R2 + R7: the success-path emit is folded into mark_message_done (next file). This activity becomes a NO-OP for success — it logs and returns:

```
@activity.defn(name="emit_inapp_outbound")
async def emit_inapp_outbound(event_input: dict) -> None:
    """Phase 28 — RESEARCH §7 R2 + R7 atomicity.

    The success-path agent_events row is written by mark_message_done in
    the SAME transaction as the inapp_messages status flip. This activity
    is preserved as a no-op for shape parity with MSV's send_message.go;
    Plan 04 may opt to delete it AND the corresponding workflow.execute_activity
    call entirely if MSV-shape parity becomes a maintenance cost.
    """
    activity.logger.info("phase28.emit_inapp_outbound.noop", extra={"message_id": event_input.get("message_id")})
    return None
```

Document the design rationale in the file's module docstring. The workflow file does NOT change as part of this plan — the no-op activity is faster than removing the call from the workflow + breaking the test contract Plan 05 expects.

**File 5 — mark_message_done.py (atomic mark + emit):**

Class-bound `MarkActivities` with atomic write:

```
class MarkActivities:
    def __init__(self, *, db_pool):
        self.db_pool = db_pool

    @activity.defn(name="mark_message_done")
    async def mark_message_done(self, mark_done_input: dict) -> None:
        """Atomic dual-write per RESEARCH §7 R2: mark_done + insert_agent_event in
        ONE asyncpg transaction. Outbox pump (services/inapp_outbox.py) reads the
        agent_events row with published=false and fans out to Redis.
        """
        from uuid import UUID
        from ...services.inapp_messages_store import mark_done
        from ...services.event_store import insert_agent_event
        from ...models.events import InappOutboundPayload

        message_id = UUID(mark_done_input["message_id"])
        container_row_id = UUID(mark_done_input["container_row_id"])
        bot_response = mark_done_input["bot_response"]

        async with self.db_pool.acquire() as conn:
            async with conn.transaction():
                await mark_done(conn, message_id=message_id, bot_response=bot_response)
                payload = InappOutboundPayload(content=bot_response, source="agent", captured_at=...)  # use workflow.now() ISO string passed via event_input or compute via activity_now
                await insert_agent_event(conn, kind="inapp_outbound", agent_container_id=container_row_id, payload=payload.model_dump())
```

Caveat: `captured_at` must be computed in activity context (not workflow). The activity is unsandboxed so `from datetime import datetime, timezone; datetime.now(timezone.utc).isoformat()` is fine. Document this clearly.

**File 6 — mark_message_failed.py (atomic mark_failed + emit failure):**

Class-bound — same pattern as File 5:

```
@activity.defn(name="mark_message_failed")
async def mark_message_failed(self, args: tuple[str, str]) -> None:
    """Atomic dual-write per RESEARCH §7 R2: mark_failed + insert_agent_event
    (kind=inapp_outbound_failed) in ONE asyncpg transaction.
    """
    from uuid import UUID
    from datetime import datetime, timezone
    from ...services.inapp_messages_store import mark_failed, fetch_by_id
    from ...services.event_store import insert_agent_event
    from ...models.events import InappOutboundFailedPayload

    message_id_str, error_type = args
    message_id = UUID(message_id_str)

    async with self.db_pool.acquire() as conn:
        async with conn.transaction():
            await mark_failed(conn, message_id=message_id, reason=error_type)
            # Resolve container_row_id from the message row (fetch_by_id returns it)
            row = await fetch_by_id(conn, message_id=message_id, user_id=...)
            if row is None:
                activity.logger.warning("phase28.mark_message_failed.row_missing", extra={"message_id": message_id_str})
                return
            payload = InappOutboundFailedPayload(error_type=error_type, message="", retry_count=1, captured_at=datetime.now(timezone.utc).isoformat())
            await insert_agent_event(conn, kind="inapp_outbound_failed", agent_container_id=row["agent_container_id"], payload=payload.model_dump())
```

Note: `fetch_by_id` requires `user_id` for defense-in-depth; the args tuple does NOT include user_id. EITHER (a) extend the args tuple to `(message_id, error_type, user_id)` and update Plan 03's workflow file's `_fail` call site (DO NOT — Plan 03 is sealed); OR (b) add a NEW `fetch_by_id_no_user_check(conn, message_id)` function in inapp_messages_store.py + use it here. Recommendation: option (b) — add the function, document it's `[INTERNAL — for failure-path activity only — bypasses user-scope filter]`. This is a Plan 04 addition to the store module.

If option (b) feels like scope creep, alternatively look up `agent_container_id` via direct SQL inside the activity (`SELECT agent_id FROM inapp_messages WHERE id=$1` + `SELECT id FROM agent_containers WHERE agent_instance_id=$1 ORDER BY created_at DESC LIMIT 1`) — same shape the reaper uses.

Pick option (b) for cleanliness; the function is 3 lines + 1 docstring.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/api_server &amp;&amp; python -c "
from api_server.temporal.activities.check_container_ready import ReadinessActivities
from api_server.temporal.activities.forward_to_agent import ForwardActivities
from api_server.temporal.activities.record_usage import RecordUsageActivities
from api_server.temporal.activities.mark_message_done import MarkActivities
from api_server.temporal.activities.emit_inapp_outbound import emit_inapp_outbound
print('all activity classes + functions importable')
"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 activity files have a class-bound implementation OR top-level function with the real body
    - `grep -c "class ReadinessActivities" api_server/src/api_server/temporal/activities/check_container_ready.py` returns 1
    - `grep -c "class ForwardActivities" api_server/src/api_server/temporal/activities/forward_to_agent.py` returns 1
    - `grep -c "_dispatch_http_localhost" api_server/src/api_server/temporal/activities/forward_to_agent.py` returns at least 2 (import + call)
    - `grep -c "for retry_idx, backoff_s in enumerate(\[0.0, 1.0, 2.0, 4.0\])" api_server/src/api_server/temporal/activities/forward_to_agent.py` returns 1 (D-11 retry loop verbatim)
    - `grep -c "ApplicationError" api_server/src/api_server/temporal/activities/forward_to_agent.py` is at least 5 (recipe_lacks_inapp_channel, container_dead, bot_timeout, bot_5xx, bot_invalid_response)
    - `grep -c "non_retryable=True" api_server/src/api_server/temporal/activities/forward_to_agent.py` is at least 5
    - `grep -c "conn.transaction()" api_server/src/api_server/temporal/activities/mark_message_done.py` returns 1 (atomic dual-write)
    - `grep -c "insert_agent_event" api_server/src/api_server/temporal/activities/mark_message_done.py` returns at least 1
    - `grep -c "conn.transaction()" api_server/src/api_server/temporal/activities/mark_message_failed.py` returns 1
    - `grep -c "noop\|no-op" api_server/src/api_server/temporal/activities/emit_inapp_outbound.py` is at least 1 (success-path no-op design rationale)
    - debit_balance.py is UNCHANGED from Plan 03 (still returns Decimal('0'))
    - Pre-existing 113+ pytest suite still green: `cd api_server && pytest -x` exit 0 (Plan 04 does NOT touch the legacy dispatcher; the new activity files are not yet in any executor path)
  </acceptance_criteria>
  <done>6 activity bodies filled in; atomic mark+emit landed; legacy dispatcher untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Worker entrypoint — python -m api_server.temporal.worker</name>
  <files>api_server/src/api_server/temporal/worker.py</files>
  <read_first>
    - api_server/src/api_server/main.py:80-220 (existing FastAPI lifespan — db pool + redis client + recipe index + bot http client patterns)
    - .planning/phases/28-temporal-dispatch/28-RESEARCH.md §4 (worker boot pattern, lines 545-700)
    - api_server/src/api_server/temporal/client.py (make_client factory)
    - api_server/src/api_server/log.py (configure_logging — same logging setup the api_server uses)
    - api_server/src/api_server/db.py (create_pool / close_pool helpers)
  </read_first>
  <action>
Create `api_server/src/api_server/temporal/worker.py` matching RESEARCH §4 verbatim. Key structural elements:

1. Module docstring: "Phase 28 — Temporal worker process. Mirrors MSV messaging/cmd/worker/main.go:288-327. Runs as `python -m api_server.temporal.worker`."

2. Imports (top of file):
   - asyncio, logging, signal
   - contextlib.AsyncExitStack
   - docker (`import docker`)
   - httpx (`import httpx`)
   - redis.asyncio as redis_async
   - temporalio.client.Client (or via `..client.make_client`)
   - temporalio.worker.Worker
   - `from ..config import get_settings`
   - `from ..db import create_pool, close_pool`
   - `from ..log import configure_logging`
   - `from ..services.inapp_recipe_index import InappRecipeIndex`
   - `from .client import make_client`
   - The 5 activity classes (ReadinessActivities, ForwardActivities, RecordUsageActivities, MarkActivities) + the 2 standalone activity functions (debit_balance, emit_inapp_outbound)
   - `from .workflows.dispatch_message import DispatchMessageWorkflow`

3. `async def main() -> None:` body:

```
configure_logging()
settings = get_settings()
log = logging.getLogger("api_server.temporal.worker")
log.info("phase28.worker.boot", extra={"task_queue": settings.temporal_task_queue, "namespace": settings.temporal_namespace, "host": settings.temporal_host})

async with AsyncExitStack() as stack:
    db_pool = await create_pool(settings.database_url)
    stack.push_async_callback(close_pool, db_pool)

    redis_client = redis_async.from_url(settings.redis_url, decode_responses=False, max_connections=20)
    await redis_client.ping()
    stack.push_async_callback(redis_client.aclose)

    bot_http = httpx.AsyncClient(
        timeout=httpx.Timeout(600.0, connect=5.0),
        limits=httpx.Limits(max_connections=50, max_keepalive_connections=20),
    )
    stack.push_async_callback(bot_http.aclose)

    docker_client = docker.from_env()
    recipe_index = InappRecipeIndex(
        recipes_dir=settings.recipes_dir,
        docker_client=docker_client,
        network_name=settings.docker_network_name,
    )

    forward_acts = ForwardActivities(db_pool=db_pool, bot_http_client=bot_http, recipe_index=recipe_index)
    ready_acts = ReadinessActivities(db_pool=db_pool)
    usage_acts = RecordUsageActivities(db_pool=db_pool)
    mark_acts = MarkActivities(db_pool=db_pool)

    # Outer connect retry loop (RESEARCH §7 R5)
    client = None
    for attempt in range(5):
        try:
            client = await make_client(settings)
            break
        except Exception as e:
            if attempt == 4:
                raise
            log.warning("phase28.worker.connect.retry", extra={"attempt": attempt + 1, "error": str(e)})
            await asyncio.sleep(5.0)

    worker = Worker(
        client,
        task_queue=settings.temporal_task_queue,
        workflows=[DispatchMessageWorkflow],
        activities=[
            ready_acts.check_container_ready,
            forward_acts.forward_to_agent,
            usage_acts.record_usage,
            emit_inapp_outbound,           # standalone no-op
            mark_acts.mark_message_done,
            mark_acts.mark_message_failed,
            debit_balance,                  # standalone D-22 no-op
        ],
        max_concurrent_activities=10,
        max_activities_per_second=5,
    )

    loop = asyncio.get_running_loop()
    stop_event = asyncio.Event()

    def _shutdown() -> None:
        log.info("phase28.worker.shutdown_signal")
        stop_event.set()

    loop.add_signal_handler(signal.SIGTERM, _shutdown)
    loop.add_signal_handler(signal.SIGINT, _shutdown)

    run_task = asyncio.create_task(worker.run())
    stop_task = asyncio.create_task(stop_event.wait())
    done, pending = await asyncio.wait({run_task, stop_task}, return_when=asyncio.FIRST_COMPLETED)
    if stop_task in done:
        await worker.shutdown()
        run_task.cancel()


if __name__ == "__main__":
    asyncio.run(main())
```

4. Import the 2 standalone activity functions at the top: `from .activities.emit_inapp_outbound import emit_inapp_outbound` and `from .activities.debit_balance import debit_balance`.

5. The Settings field `recipes_dir` already exists; verify it points at `/app/recipes` inside compose (which the volume mount exposes).

6. After writing, smoke-test from inside the deploy compose stack:

   `make dev-api-local` brings up postgres + redis + temporal + temporal-ui + temporal-worker + api_server. Run `docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml logs --tail=30 temporal-worker` — expect to see `phase28.worker.boot` followed by `Worker successfully started` (or equivalent SDK boot line). If the worker container is still in `Restarting` state, capture the logs and debug.

7. After verification, run `docker compose ... ps temporal-worker` — expect `Up` (not `Restarting`).
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground &amp;&amp; grep -q "max_concurrent_activities=10" api_server/src/api_server/temporal/worker.py &amp;&amp; grep -q "max_activities_per_second=5" api_server/src/api_server/temporal/worker.py &amp;&amp; grep -q "DispatchMessageWorkflow" api_server/src/api_server/temporal/worker.py &amp;&amp; grep -q "loop.add_signal_handler" api_server/src/api_server/temporal/worker.py</automated>
  </verify>
  <acceptance_criteria>
    - File `api_server/src/api_server/temporal/worker.py` exists
    - `python -m api_server.temporal.worker --help` does not need to work, but `python -c "from api_server.temporal.worker import main; print(callable(main))"` prints `True`
    - `grep -c "Worker(" api_server/src/api_server/temporal/worker.py` returns 1
    - `grep -c "task_queue=settings.temporal_task_queue" api_server/src/api_server/temporal/worker.py` returns 1
    - `grep -c "max_concurrent_activities=10" api_server/src/api_server/temporal/worker.py` returns 1
    - `grep -c "max_activities_per_second=5" api_server/src/api_server/temporal/worker.py` returns 1
    - `grep -c "loop.add_signal_handler(signal.SIGTERM" api_server/src/api_server/temporal/worker.py` returns 1
    - `grep -c "make_client(settings)" api_server/src/api_server/temporal/worker.py` returns 1
    - After `make dev-api-local`, `docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml ps temporal-worker --format json | python -c "import sys, json; [print(r['State']) for r in json.loads(sys.stdin.read())]"` prints `running` (NOT `restarting`)
    - `docker compose ... logs --tail=20 temporal-worker | grep -c "phase28.worker.boot"` returns at least 1
    - The api_server lifespan starts cleanly: `curl -fsS http://127.0.0.1:8000/healthz` returns 200
  </acceptance_criteria>
  <done>Worker process boots, connects to Temporal, registers workflow + 7 activities, runs in `Up` state. The full local stack is green.</done>
</task>

</tasks>

<verification>
- All 7 activity bodies real (with the 2 documented no-op exceptions: emit_inapp_outbound success path and debit_balance D-22 stub)
- forward_to_agent owns its [1s, 2s, 4s] retry loop and reuses _dispatch_http_localhost verbatim
- mark_message_done + mark_message_failed are atomic dual-writes per RESEARCH §7 R2
- Worker container in Up state on `make dev-api-local`
- 113+ pytest suite still green (legacy dispatcher unchanged in this plan)
</verification>

<success_criteria>
The Temporal worker is a runnable process. Plan 06 can wire `start_workflow` from the route handler and the workflow will execute end-to-end against real activities. Plan 05's tests can run against either WorkflowEnvironment (unit) or the live worker (integration).
</success_criteria>

<output>
After completion, create `.planning/phases/28-temporal-dispatch/28-04-SUMMARY.md` documenting which activities are class-bound vs standalone, the atomic dual-write design, and the worker boot wall time.
</output>
