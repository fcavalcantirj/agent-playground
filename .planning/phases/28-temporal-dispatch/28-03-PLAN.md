---
phase: 28-temporal-dispatch
plan: 03
type: execute
wave: 2
depends_on: [28-02]
files_modified:
  - api_server/src/api_server/temporal/__init__.py
  - api_server/src/api_server/temporal/client.py
  - api_server/src/api_server/temporal/workflows/__init__.py
  - api_server/src/api_server/temporal/workflows/dispatch_message.py
  - api_server/src/api_server/temporal/workflows/types.py
  - api_server/src/api_server/temporal/activities/__init__.py
  - api_server/src/api_server/temporal/activities/check_container_ready.py
  - api_server/src/api_server/temporal/activities/forward_to_agent.py
  - api_server/src/api_server/temporal/activities/record_usage.py
  - api_server/src/api_server/temporal/activities/debit_balance.py
  - api_server/src/api_server/temporal/activities/emit_inapp_outbound.py
  - api_server/src/api_server/temporal/activities/mark_message_done.py
  - api_server/src/api_server/temporal/activities/mark_message_failed.py
autonomous: true
requirements:
  - D-08
  - D-09
  - D-10
  - D-13
  - D-15
  - D-22
must_haves:
  truths:
    - "DispatchMessageWorkflow @workflow.defn class exists in api_server/src/api_server/temporal/workflows/dispatch_message.py"
    - "Workflow body is deterministic: imports limited to temporalio.workflow, temporalio.common, temporalio.exceptions, datetime (timedelta only), dataclasses, and the local types module"
    - "Workflow uses workflow.unsafe.imports_passed_through() to import the activity function references"
    - "Workflow body does NOT directly import asyncpg, httpx, redis, docker, subprocess, os, time, random, uuid"
    - "DispatchMessageInput + DispatchMessageResult + ForwardResult dataclasses are frozen and primitive-typed"
    - "RetryPolicy on each activity matches D-10 + D-11 retry budgets verbatim"
    - "make_client(settings) factory exists in api_server/src/api_server/temporal/client.py"
    - "All 7 activity stub modules exist with @activity.defn decorators; 6 raise NotImplementedError; debit_balance returns Decimal('0') per D-22"
    - "5-minute execution_timeout (D-13) is enforced via DEFENSE-IN-DEPTH: defn-level/worker-level default AND start_workflow override (RESEARCH §8 Q5)"
  artifacts:
    - path: "api_server/src/api_server/temporal/__init__.py"
      provides: "package marker"
    - path: "api_server/src/api_server/temporal/client.py"
      provides: "make_client(settings) factory"
      exports: ["make_client"]
    - path: "api_server/src/api_server/temporal/workflows/dispatch_message.py"
      provides: "DispatchMessageWorkflow + DispatchMessageInput + DispatchMessageResult"
      exports: ["DispatchMessageWorkflow", "DispatchMessageInput", "DispatchMessageResult"]
    - path: "api_server/src/api_server/temporal/workflows/types.py"
      provides: "ForwardResult dataclass"
      exports: ["ForwardResult"]
    - path: "api_server/src/api_server/temporal/activities/debit_balance.py"
      provides: "D-22 no-op stub returning Decimal('0')"
      exports: ["debit_balance"]
  key_links:
    - from: "DispatchMessageWorkflow.run"
      to: "the 7 activity stubs (Plan 04 fills bodies)"
      via: "workflow.execute_activity calls under workflow.unsafe.imports_passed_through()"
      pattern: "workflow.execute_activity"
---

<objective>
Wave 2a — workflow scaffolding. Define the DispatchMessageWorkflow's deterministic body + the temporal_client factory + the shared input/output dataclasses + activity stub modules (so the workflow file can import the activities at registration time without circular-import or NameError failures). Ships BEFORE Plan 04 because:

1. The workflow declares what activities it expects. Plan 04 implements those exact signatures.
2. Workflow files have strict sandbox rules — getting imports right is its own concern, separated from Plan 04's heavier surface.
3. Plan 05's tests can register the workflow with mock activities AS SOON AS this plan ships.

This plan does NOT register a worker process or run the workflow end-to-end. The worker process lands in Plan 04. Plan 03's activity stubs raise NotImplementedError; Plan 04 swaps in real bodies (and `debit_balance` keeps its Decimal('0') no-op stub per D-22).

Purpose: define workflow body + client factory + shared dataclasses + activity stub contract with verbatim MSV-Go shape per D-09.
Output: 5 new module files in `temporal/workflows/` + `temporal/client.py` + 8 activity stub files in `temporal/activities/`. Workflow registers cleanly under WorkflowEnvironment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-temporal-dispatch/28-CONTEXT.md
@.planning/phases/28-temporal-dispatch/28-RESEARCH.md
@.planning/phases/28-temporal-dispatch/28-01-SPIKE-EVIDENCE.md
@CLAUDE.md
@api_server/src/api_server/services/inapp_dispatcher.py

<interfaces>
Settings (from Plan 02):
  temporal_host: str            # default "temporal:7233"
  temporal_namespace: str       # default "default"
  temporal_task_queue: str      # default "ap-messages"
  bot_timeout_seconds: float    # default 60.0

Activity functions Plan 04 will fill in (names locked here, stubs land in Task 3):
  check_container_ready.check_container_ready(inp: DispatchMessageInput) -> bool
  forward_to_agent.forward_to_agent(inp: DispatchMessageInput) -> ForwardResult
  record_usage.record_usage(usage_input: dict) -> None
  debit_balance.debit_balance(inp: DispatchMessageInput) -> Decimal  (D-22 stub — keeps no-op body)
  emit_inapp_outbound.emit_inapp_outbound(event_input: dict) -> None
  mark_message_done.mark_message_done(mark_done_input: dict) -> None
  mark_message_failed.mark_message_failed(args: tuple[str, str]) -> None
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create temporal package skeleton, client.py, workflows/types.py</name>
  <files>api_server/src/api_server/temporal/__init__.py, api_server/src/api_server/temporal/client.py, api_server/src/api_server/temporal/workflows/__init__.py, api_server/src/api_server/temporal/workflows/types.py</files>
  <read_first>
    - api_server/src/api_server/config.py (verify Settings has temporal_host + temporal_namespace from Plan 02)
    - .planning/phases/28-temporal-dispatch/28-RESEARCH.md sections 4 (client factory) and 3 (determinism rules table)
    - .planning/phases/28-temporal-dispatch/28-CONTEXT.md D-04, D-08
  </read_first>
  <action>
File 1 — `api_server/src/api_server/temporal/__init__.py`. Module-docstring-only file declaring the package's purpose: this package owns the workflow + activities + worker entry point that replaces `services/inapp_dispatcher.py`. List the four subpackages (client, workflows, activities, worker) with one-line descriptions each.

File 2 — `api_server/src/api_server/temporal/client.py`. Single async function `make_client(settings: Settings) -> Client` that calls `await Client.connect(settings.temporal_host, namespace=settings.temporal_namespace)` and returns the connected Client. NO retry loop in this factory — Plan 04's worker.py wraps with an outer 5×5s backoff if needed. Imports: `from temporalio.client import Client` and `from ..config import Settings`. Module docstring documents that both api_server lifespan (Plan 06) and the worker process (Plan 04) consume this.

File 3 — `api_server/src/api_server/temporal/workflows/__init__.py`. Module-docstring-only file documenting the determinism rules: workflow files must NOT import httpx, asyncpg, redis, docker, subprocess. Allowed imports: temporalio.workflow, temporalio.common, temporalio.exceptions, datetime (timedelta only), dataclasses, typing, uuid (type hints only — never uuid.uuid4()).

File 4 — `api_server/src/api_server/temporal/workflows/types.py`. Defines `ForwardResult` frozen dataclass per RESEARCH §3 + the `<interfaces>` block above. Fields:

- `reply: str` — the bot's reply text. Empty/whitespace-only triggers `bot_empty` terminal failure in workflow.
- `raw_response: dict[str, Any] | None = None` — for ops debugging only; downstream activities ignore.
- `usage_input: dict[str, Any]` with `field(default_factory=dict)` — opaque dict consumed by record_usage activity.
- `event_input: dict[str, Any]` with `field(default_factory=dict)` — consumed by emit_inapp_outbound.
- `mark_done_input: dict[str, Any]` with `field(default_factory=dict)` — consumed by mark_message_done.

Module docstring states: "Temporal serializes activity inputs/outputs as JSON. All cross-boundary fields must be primitive-or-dict — UUID/datetime/Decimal are stringified before crossing the wire."

Verify with one-shot import:
  cd api_server
  python -c "from api_server.temporal.client import make_client; from api_server.temporal.workflows.types import ForwardResult; r = ForwardResult(reply='ok', usage_input={'a':1}); assert r.reply == 'ok' and r.usage_input == {'a':1}; print('OK')"
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/api_server &amp;&amp; python -c "from api_server.temporal.client import make_client; from api_server.temporal.workflows.types import ForwardResult; r = ForwardResult(reply='ok', usage_input={'a':1}); assert r.reply == 'ok' and r.usage_input == {'a':1}; assert hash(r) is not None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - 4 files exist: temporal/__init__.py, temporal/client.py, temporal/workflows/__init__.py, temporal/workflows/types.py
    - `python -c "from api_server.temporal.client import make_client"` returns exit 0
    - `python -c "from api_server.temporal.workflows.types import ForwardResult"` returns exit 0
    - `grep -c "Client.connect" api_server/src/api_server/temporal/client.py` returns 1
    - `grep -c "@dataclass(frozen=True)" api_server/src/api_server/temporal/workflows/types.py` returns 1
    - `grep -E "^(import|from) (httpx|asyncpg|redis|docker|subprocess)" api_server/src/api_server/temporal/client.py api_server/src/api_server/temporal/workflows/types.py api_server/src/api_server/temporal/workflows/__init__.py api_server/src/api_server/temporal/__init__.py | wc -l` returns 0
  </acceptance_criteria>
  <done>Package skeleton importable; client factory + types ready for workflow body to consume.</done>
</task>

<task type="auto">
  <name>Task 2: DispatchMessageWorkflow body — deterministic, MSV-Go-portable shape</name>
  <files>api_server/src/api_server/temporal/workflows/dispatch_message.py</files>
  <read_first>
    - .planning/phases/28-temporal-dispatch/28-RESEARCH.md §3 (Workflow code patterns) — full code skeleton lines 215-365 plus the determinism rules table
    - .planning/phases/28-temporal-dispatch/28-RESEARCH.md §8 Q5 (defense-in-depth recommendation: defn-level default + start_workflow override)
    - .planning/phases/28-temporal-dispatch/28-CONTEXT.md D-08, D-09, D-10, D-11, D-12, D-13, D-15, D-22
    - /Users/fcavalcanti/dev/meusecretariovirtual/messaging/workflows/send_message.go lines 16-198 (verbatim MSV reference)
    - api_server/src/api_server/temporal/workflows/types.py (ForwardResult shape from Task 1)
    - api_server/src/api_server/services/inapp_dispatcher.py around line 325-335 (the _KNOWN_ERROR_TYPES taxonomy — workflow's _classify_forward_failure must produce these strings)
    - .planning/phases/28-temporal-dispatch/28-01-SPIKE-EVIDENCE.md Spike B section (confirmed parameter naming for defn-level default, if any)
  </read_first>
  <action>
Create `api_server/src/api_server/temporal/workflows/dispatch_message.py`. Body verbatim from RESEARCH §3 with these specific clarifications:

1. Imports at top of file (in this exact order):
   - `from __future__ import annotations`
   - `from dataclasses import dataclass`
   - `from datetime import timedelta`
   - `from temporalio import workflow`
   - `from temporalio.common import RetryPolicy`
   - `from temporalio.exceptions import ApplicationError`
   - `from .types import ForwardResult`
   - Then the activity references inside `with workflow.unsafe.imports_passed_through(): from ..activities import (check_container_ready, debit_balance, emit_inapp_outbound, forward_to_agent, mark_message_done, mark_message_failed, record_usage)`

2. The `@workflow.defn` decorator passes `name="DispatchMessageWorkflow"`. The 5-minute cap (D-13) is enforced via DEFENSE-IN-DEPTH per RESEARCH §8 Q5 — BOTH at the defn-level (or worker-level default) AND at the route handler's `start_workflow(execution_timeout=timedelta(minutes=5))` call in Plan 06.

   Defense-in-depth rationale: if a future caller forgets `execution_timeout`, the workflow MUST still terminate within 5 minutes. Single-defense (start_workflow only) is NOT acceptable.

   Implementation procedure:
   a. Check Wave 0 spike B evidence (28-01-SPIKE-EVIDENCE.md) for confirmed parameter naming. The Python SDK has historically exposed `default_execution_timeout` on `@workflow.defn` in some versions; verify against the pinned 1.27.x SDK.
   b. If `@workflow.defn(name=..., default_execution_timeout=timedelta(minutes=5))` is supported by 1.27.x: pass it. Document in class docstring.
   c. If NOT supported at the decorator level: pass `default_workflow_execution_timeout=timedelta(minutes=5)` to the `Worker(...)` constructor in Plan 04's worker.py. Document this fallback in the workflow class docstring AND create a follow-up note in this plan's SUMMARY directing Plan 04 to set the worker-level default.
   d. Either way, Plan 06's route handler ALSO passes `execution_timeout=timedelta(minutes=5)` to `start_workflow` — this is the second defense line.
   e. Document the chosen path in the class docstring with a "Defense-in-depth (D-13 + RESEARCH §8 Q5):" note.

3. `DispatchMessageInput` (`@dataclass(frozen=True)`) fields, in order:
   - `message_id: str` (UUID stringified — workflow ID = `f"msg-{user_id}-{message_id}"` per D-08, user-scoped)
   - `user_id: str`
   - `agent_id: str`
   - `container_row_id: str`
   - `container_id: str`
   - `recipe_name: str`
   - `agent_model: str`
   - `content: str`
   - `inapp_auth_token: str | None`
   - `bot_timeout_seconds: float`

4. `DispatchMessageResult` (`@dataclass(frozen=True)`):
   - `success: bool`
   - `error_type: str | None = None`
   - `reply: str | None = None`

5. `DispatchMessageWorkflow.run` body — the 7 activity calls in this exact sequence:

   - Step 1 — check_container_ready: start_to_close=10s, RetryPolicy(initial=250ms, backoff=2.0, max_interval=4s, max_attempts=5). On `not ready`: call `_fail` then return `error_type="container_not_ready"`.
   - Step 2 — forward_to_agent: start_to_close=`inp.bot_timeout_seconds + 30.0`s (D-12 buffer), RetryPolicy(maximum_attempts=1). Wrap in try/except ApplicationError; on exception call `_classify_forward_failure(e)`, then `_fail`, then return.
   - After Step 2 success, check `forward_result.reply` — empty/whitespace-only triggers `_fail(inp, "bot_empty")` then return.
   - Step 3a — record_usage: start_to_close=10s, RetryPolicy(initial=1s, max_attempts=3). Best-effort per D-15 — wrap in try/except ApplicationError; on exception call `workflow.logger.warning("phase28.record_usage.swallowed", extra={"error": str(e), "message_id": inp.message_id})` and continue.
   - Step 3b — debit_balance: start_to_close=5s, RetryPolicy(maximum_attempts=1). Best-effort per D-22. Same pattern.
   - Step 3c — emit_inapp_outbound: start_to_close=5s, RetryPolicy(max_attempts=3). Best-effort per D-15. Same pattern.
   - Step 3d — mark_message_done: start_to_close=5s, RetryPolicy(initial=250ms, max_attempts=5). Load-bearing per D-10 — NO try/except. Failure here propagates as workflow failure.
   - On Step 3d success, return `DispatchMessageResult(success=True, reply=forward_result.reply)`.

6. `_fail(self, inp, error_type)` private method — calls `mark_message_failed.mark_message_failed` with `(inp.message_id, error_type)` tuple, start_to_close=5s, RetryPolicy(max_attempts=5). Best-effort.

7. Module-level `_classify_forward_failure(e: ApplicationError) -> str`:
   - Check `getattr(e, "type", None)` first — return as string if truthy.
   - Else check `e.args` — return `str(e.args[0])` if truthy.
   - Else return `"internal_error"`.

After writing, smoke-test workflow registration (this requires Task 3's stub activities to exist; if Task 3 hasn't run yet, the smoke fails with ImportError, which is the expected ordering signal).
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/api_server &amp;&amp; grep -q "@workflow.defn(name=\"DispatchMessageWorkflow\"" src/api_server/temporal/workflows/dispatch_message.py &amp;&amp; grep -q "imports_passed_through" src/api_server/temporal/workflows/dispatch_message.py &amp;&amp; grep -q "RetryPolicy(maximum_attempts=1)" src/api_server/temporal/workflows/dispatch_message.py</automated>
  </verify>
  <acceptance_criteria>
    - File `api_server/src/api_server/temporal/workflows/dispatch_message.py` exists
    - `grep -c "@workflow.defn(name=\"DispatchMessageWorkflow\"" api_server/src/api_server/temporal/workflows/dispatch_message.py` returns 1 (decorator may have additional kwargs like default_execution_timeout — match prefix only)
    - `grep -c "with workflow.unsafe.imports_passed_through" api_server/src/api_server/temporal/workflows/dispatch_message.py` returns 1
    - `grep -c "workflow.execute_activity" api_server/src/api_server/temporal/workflows/dispatch_message.py` returns 7 (one per activity: check_container_ready, forward_to_agent, record_usage, debit_balance, emit_inapp_outbound, mark_message_done, mark_message_failed via _fail)
    - `grep -c "RetryPolicy(maximum_attempts=1)" api_server/src/api_server/temporal/workflows/dispatch_message.py` returns 2 (forward_to_agent + debit_balance)
    - `grep -c "RetryPolicy" api_server/src/api_server/temporal/workflows/dispatch_message.py` returns 7 (one per activity call)
    - `grep -E "^(import|from) (httpx|asyncpg|redis|docker|subprocess|os|time|random)" api_server/src/api_server/temporal/workflows/dispatch_message.py | wc -l` returns 0
    - `grep -E "datetime\.now|datetime\.utcnow|time\.time|random\.|uuid\.uuid4" api_server/src/api_server/temporal/workflows/dispatch_message.py | wc -l` returns 0
    - `grep "^from datetime" api_server/src/api_server/temporal/workflows/dispatch_message.py` shows ONLY `from datetime import timedelta`
    - Defense-in-depth (D-13 + RESEARCH §8 Q5) is documented in the class docstring AND either (a) the @workflow.defn decorator carries `default_execution_timeout=timedelta(minutes=5)`, OR (b) the class docstring explicitly states the worker-level fallback that Plan 04 will apply
  </acceptance_criteria>
  <done>Workflow body landed; deterministic guards verified; 5-min defense-in-depth recorded; full registration pending Task 3's activity stubs.</done>
</task>

<task type="auto">
  <name>Task 3: Activity stub modules to unblock workflow file import</name>
  <files>api_server/src/api_server/temporal/activities/__init__.py, api_server/src/api_server/temporal/activities/check_container_ready.py, api_server/src/api_server/temporal/activities/forward_to_agent.py, api_server/src/api_server/temporal/activities/record_usage.py, api_server/src/api_server/temporal/activities/debit_balance.py, api_server/src/api_server/temporal/activities/emit_inapp_outbound.py, api_server/src/api_server/temporal/activities/mark_message_done.py, api_server/src/api_server/temporal/activities/mark_message_failed.py</files>
  <read_first>
    - api_server/src/api_server/temporal/workflows/dispatch_message.py (Task 2's import block — the activity module names there are the contract this task fulfills)
    - .planning/phases/28-temporal-dispatch/28-CONTEXT.md D-22 (debit_balance no-op stub stays a no-op even after Plan 04)
  </read_first>
  <action>
Create activity stub modules — NAME-ONLY placeholders to satisfy the workflow file's `from ..activities import (...)` line at import time. Plan 04 replaces each stub body with the real implementation (except `debit_balance` per D-22).

Each stub file uses `from __future__ import annotations` + `TYPE_CHECKING` guard to break the circular import (the workflow file imports activities; activities reference DispatchMessageInput from the workflow file).

File 1 — `activities/__init__.py`. Module-docstring-only stating "Phase 28 activity stubs — Plan 03 places NotImplementedError-raising stubs; Plan 04 replaces with real implementations. Exception: debit_balance keeps its Decimal('0') no-op body per D-22."

Files 2-8 — each activity stub:

  ```
  from __future__ import annotations
  from typing import TYPE_CHECKING
  from temporalio import activity

  if TYPE_CHECKING:
      from ..workflows.dispatch_message import DispatchMessageInput

  @activity.defn(name="check_container_ready")
  async def check_container_ready(inp: "DispatchMessageInput") -> bool:
      raise NotImplementedError("Plan 28-04 implements this activity body")
  ```

Specific signatures per `<interfaces>`:
- `check_container_ready.py`: takes `DispatchMessageInput` returns `bool`
- `forward_to_agent.py`: takes `DispatchMessageInput` returns `ForwardResult` (import from `..workflows.types` directly — no TYPE_CHECKING needed since types.py has no circular import)
- `record_usage.py`: takes `usage_input: dict` returns `None`
- `debit_balance.py`: takes `DispatchMessageInput` returns `Decimal` — body returns `Decimal("0")` per D-22 (NO NotImplementedError); imports `from decimal import Decimal`
- `emit_inapp_outbound.py`: takes `event_input: dict` returns `None`
- `mark_message_done.py`: takes `mark_done_input: dict` returns `None`
- `mark_message_failed.py`: takes `args: tuple[str, str]` returns `None`

After all 8 files exist, smoke-import the workflow + activities:

  cd api_server
  python -c "
  import asyncio
  from temporalio.testing import WorkflowEnvironment
  from temporalio.worker import Worker
  from api_server.temporal.workflows.dispatch_message import DispatchMessageWorkflow
  from api_server.temporal.activities import check_container_ready, debit_balance, emit_inapp_outbound, forward_to_agent, mark_message_done, mark_message_failed, record_usage

  async def smoke():
      async with await WorkflowEnvironment.start_time_skipping() as env:
          async with Worker(
              env.client, task_queue='smoke',
              workflows=[DispatchMessageWorkflow],
              activities=[
                  check_container_ready.check_container_ready,
                  debit_balance.debit_balance,
                  emit_inapp_outbound.emit_inapp_outbound,
                  forward_to_agent.forward_to_agent,
                  mark_message_done.mark_message_done,
                  mark_message_failed.mark_message_failed,
                  record_usage.record_usage,
              ],
          ):
              pass

  asyncio.run(smoke())
  print('OK')
  "

If this prints `OK`, Plan 03 is complete and Plan 04 has its contract.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/api_server &amp;&amp; python -c "
import asyncio
from temporalio.testing import WorkflowEnvironment
from temporalio.worker import Worker
from api_server.temporal.workflows.dispatch_message import DispatchMessageWorkflow
from api_server.temporal.activities import check_container_ready, debit_balance, emit_inapp_outbound, forward_to_agent, mark_message_done, mark_message_failed, record_usage

async def smoke():
    async with await WorkflowEnvironment.start_time_skipping() as env:
        async with Worker(env.client, task_queue='smoke', workflows=[DispatchMessageWorkflow], activities=[check_container_ready.check_container_ready, debit_balance.debit_balance, emit_inapp_outbound.emit_inapp_outbound, forward_to_agent.forward_to_agent, mark_message_done.mark_message_done, mark_message_failed.mark_message_failed, record_usage.record_usage]):
            pass

asyncio.run(smoke())
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - 8 activity files exist under `api_server/src/api_server/temporal/activities/`
    - Each non-init activity file has exactly one `@activity.defn(name="...")` decorator
    - 6 of 7 activity stubs raise `NotImplementedError("Plan 28-04 implements this activity body")` (only `debit_balance` returns `Decimal("0")`)
    - `grep -c "NotImplementedError" api_server/src/api_server/temporal/activities/check_container_ready.py` returns 1
    - `grep -c "Decimal" api_server/src/api_server/temporal/activities/debit_balance.py` is at least 2 (import + return)
    - `grep -c "NotImplementedError" api_server/src/api_server/temporal/activities/debit_balance.py` returns 0 (D-22 — debit_balance is the exception)
    - The smoke-import command above prints `OK`
    - No circular import error at runtime
  </acceptance_criteria>
  <done>Activity stub contract in place; workflow file imports cleanly under WorkflowEnvironment; Plan 04 has a clear "fill in the bodies" target.</done>
</task>

</tasks>

<verification>
- 5 module files (workflows package + client) + 8 stub activity files = 13 new files under `api_server/src/api_server/temporal/`
- Workflow registers cleanly under WorkflowEnvironment with all 7 stub activities
- Determinism guards: no httpx/asyncpg/redis/docker imports in workflow file, no datetime.now / random / uuid.uuid4
- D-22 debit_balance stub returns Decimal('0') — phase-end no-op preserved
- Defense-in-depth (D-13 + RESEARCH §8 Q5): defn-level/worker-level default + start_workflow override (Plan 06 sets the second line of defense)
</verification>

<success_criteria>
The workflow + types + client + stub activities form a complete contract. Plan 04 can replace each stub's body with the real implementation without touching the workflow file. Plan 05's tests can register the workflow and execute it against test-mocked activities immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/28-temporal-dispatch/28-03-SUMMARY.md` documenting the workflow shape + the 7 activity contracts + sandbox-import verification result + the chosen defense-in-depth path (decorator kwarg vs worker-level default) so Plan 04 can mirror.
</output>
