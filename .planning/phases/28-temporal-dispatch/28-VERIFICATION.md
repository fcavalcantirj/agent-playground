---
phase: 28-temporal-dispatch
verified: 2026-05-06T01:01:40Z
status: passed
score: 14/14 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 28: Temporal-backed message dispatch + dispatcher hardening — Phase-Level Verification Report

**Phase Goal:** Replace the in-process `dispatcher_loop` with a Temporal-backed durable message dispatch workflow. POST /v1/agents/:id/messages persists the message and starts a `DispatchMessageWorkflow` (id=`msg-{user_id}-{message_id}`). Activities handle container readiness, forward to bot, record usage, emit SSE outbound + mark done atomically, debit balance (no-op for v0.2). Replace yanked `UsageTickerWidget` with `Consumer`-wrapped re-mount in Dashboard + Chat AppBars.

**Verified:** 2026-05-06T01:01:40Z
**Status:** passed
**Re-verification:** No — initial phase-level verification orthogonal to per-plan exit gate `28-09-VERIFICATION.md`

## Goal Achievement

### Observable Truths (must_haves)

| #  | Must-Have                                                                                                                                       | Status     | Evidence                                                                                                                                                                                                                                                                                          |
|----|-------------------------------------------------------------------------------------------------------------------------------------------------|------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1  | `temporalio==1.27.x` SDK pinned in api_server runtime + worker image                                                                            | VERIFIED   | `api_server/pyproject.toml` declares `"temporalio>=1.27.0,<1.28"`. Worker image is the same Python image as api_server (shared deps per D-04).                                                                                                                                                     |
| 2  | Compose stack has `temporal` (auto-setup:1.29.2) + `temporal-ui` (2.40.0) + `temporal-worker` services on `deploy_default`                     | VERIFIED   | `deploy/docker-compose.prod.yml` lines 59 (`temporal:` `image: temporalio/auto-setup:1.29.2`), 95 (`temporal-ui:` `image: temporalio/ui:2.40.0`), 109 (`temporal-worker:`). Default compose project network is `deploy_default` (project dir is `deploy`); `docker compose ps` shows all 3 services Up. |
| 3  | `DispatchMessageWorkflow` exists, deterministic, sandbox-passthrough activities                                                                  | VERIFIED   | File `api_server/src/api_server/temporal/workflows/dispatch_message.py` defines `@workflow.defn(name="DispatchMessageWorkflow")` (line 86); imports activities under `workflow.unsafe.imports_passed_through()` (line 41); `grep -E "(datetime\.now|random\.)"` returns 0 matches (D-09 Go-portable). |
| 4  | 7 activities implemented (debit_balance is Decimal('0') no-op per D-22)                                                                          | VERIFIED — see Note | 7 activity files exist in `api_server/src/api_server/temporal/activities/`: `check_container_ready.py`, `forward_to_agent.py`, `record_usage.py`, `emit_inapp_outbound.py`, `mark_message_done.py`, `mark_message_failed.py`, `debit_balance.py`. NOTE: `debit_balance.py:40` returns `"0"` (string) NOT `Decimal('0')` — documented Plan 28-06 Rule-1 deviation: Temporal's default JSON payload converter rejected `Decimal` (`TypeError: Object of type Decimal is not JSON serializable`); the string-encoded form `"0"` is recoverable losslessly via `Decimal(<str>)`, preserves the no-op intent (D-22), and locks the activity contract for Phase B's Stripe debit substitution. Workflow swallows exceptions from this activity (best-effort per D-22). |
| 5  | Worker entrypoint registers workflow + 7 activities on `ap-messages` queue, drains on SIGTERM                                                    | VERIFIED   | `api_server/src/api_server/temporal/worker.py:186-201` builds `Worker(client, task_queue=settings.temporal_task_queue, workflows=[DispatchMessageWorkflow], activities=[ready_acts.check_container_ready, forward_acts.forward_to_agent, usage_acts.record_usage, emit_inapp_outbound, mark_acts.mark_message_done, mark_failed_acts.mark_message_failed, debit_balance], max_concurrent_activities=10, max_activities_per_second=5)`. Lines 212-213 attach `loop.add_signal_handler(signal.SIGTERM, _shutdown)` + SIGINT; line 228 calls `await worker.shutdown()` for graceful drain. Compose env sets `AP_TEMPORAL_TASK_QUEUE=ap-messages`. |
| 6  | Alembic 011 added `workflow_id` column + `(user_id, idempotency_key)` partial UNIQUE index; reversible                                            | VERIFIED   | `api_server/alembic/versions/011_phase28_workflow_id_idempotency.py` upgrade(): `op.add_column("inapp_messages", sa.Column("workflow_id", sa.Text(), nullable=True))` + `op.create_index("ix_inapp_messages_workflow_id", ...)` + `op.add_column("inapp_messages", sa.Column("idempotency_key", sa.Text(), nullable=True))` + `op.create_index("ix_inapp_messages_idempotency_key_unique", "inapp_messages", ["user_id", "idempotency_key"], unique=True, postgresql_where=sa.text("idempotency_key IS NOT NULL"))`. downgrade() reverses all 4 ops. Revision id `011_phase28_workflow_id_idem` (28 chars — fits varchar(32) alembic_version cap; documented deviation). |
| 7  | Cutover sweep script (`tools/migrate_phase28_stuck_rows.py`) idempotent + safety-capped                                                          | VERIFIED   | File exists at `tools/migrate_phase28_stuck_rows.py`. Lines 39-42 docstring asserts idempotency ("re-running the non-dry-run path after the sweep is a no-op") and safety cap ("`--limit` (default 10000) aborts the run if more rows match than the cap"). `argparse` `--limit` flag at line 99 with default 10000; safety cap fires BEFORE any mutation runs (line 149).                                          |
| 8  | POST /messages calls `start_workflow` with `id=msg-{user_id}-{message_id}`, `execution_timeout=5min`, REJECT_DUPLICATE; catches `WorkflowAlreadyStartedError` → 202 | VERIFIED   | `api_server/src/api_server/routes/agent_messages.py:251` `workflow_id = f"msg-{user_id}-{message_id}"`; lines 253-271 call `await temporal_client.start_workflow(...)` with `id_reuse_policy=WorkflowIDReusePolicy.REJECT_DUPLICATE` + `execution_timeout=timedelta(minutes=5)` (D-13); lines 281-291 catch `WorkflowAlreadyStartedError` → log + continue to Step 7/8 → returns 202 (line 313, `JSONResponse(status_code=202)`). |
| 9  | Legacy `dispatcher_loop` + `inapp_dispatcher._handle_row` deleted; lifespan no longer creates `dispatcher_loop` background task                  | VERIFIED   | `grep "async def _handle_row"` in `services/inapp_dispatcher.py` → 0 matches. `services/inapp_dispatcher.py` reduced to docstring + `_dispatch_http_localhost` + error-taxonomy helpers (lines 1-82 documents the deletion). `main.py` lines 120 + 242 carry comments noting "Phase 28 DELETED dispatcher_loop"; no `dispatcher_loop` task creation in lifespan. ImportError reproducible: `from api_server.services.inapp_dispatcher import _handle_row` raises ImportError (recorded in `28-09-VERIFICATION.md` lines 36-41). |
| 10 | `UsageTickerWidget` mounted via `Consumer` wrapper in Dashboard + Chat AppBars; widget itself unchanged; D-21 trigger #3 deferred                | VERIFIED   | `mobile/lib/features/dashboard/dashboard_screen.dart:103-104` `Consumer(builder: (context, ref, _) => const UsageTickerWidget())`. `mobile/lib/features/chat/chat_screen.dart:165-166` same pattern. Widget body in `mobile/lib/features/usage/usage_ticker_widget.dart` unchanged from Phase 27 Wave 3 (still `class UsageTickerWidget extends ConsumerWidget` at line 33 — Plan 08 only modified mount sites). D-21 trigger #3 verified deferred: `grep "ref.invalidate(usageSummaryProvider)" chat_providers.dart` → 1 match inside a documentation comment, zero callable invocations. |
| 11 | `pytest -x` green (322 PASS / 15 skip / 13 pre-existing failures documented in `deferred-items.md`)                                              | VERIFIED — see Note | Plan 09 verification re-ran the suite: **332 passed, 4 skipped, 1 failed (pre-existing 22c.3.1 flake `test_data_dir_hoisted_before_pre_start`), 61 deselected** (28-09-VERIFICATION.md line 80). Re-validated independently during this verification: `uv run pytest tests/temporal/ --tb=no -q` → `19 passed, 1 warning in 56.43s`. The Plan 07 SUMMARY snapshot quoted in must_have (322 PASS / 15 skip) is older; net delta is +10 PASS, well above D-25 baseline (113+). Pre-existing failures #1-#9 in `deferred-items.md`; #10-#12 are operational (e2e harness rewrite, 22c.3.1 flake, worktree bind drift). |
| 12 | `28-09-VERIFICATION.md` committed with `PHASE-28-EXIT-GATE-PASSED` marker                                                                        | VERIFIED   | File exists at `.planning/phases/28-temporal-dispatch/28-09-VERIFICATION.md`. Line 199: `PHASE-28-EXIT-GATE-PASSED 2026-05-06`. Committed at `364e2c8` (`docs(28-09): exit-gate verification — PHASE-28-EXIT-GATE-PASSED`).                                                                          |
| 13 | All 25 D-XX decisions evidenced in the verification ledger                                                                                       | VERIFIED   | `28-09-VERIFICATION.md` `## D-XX → evidence map` table covers D-01 through D-25, no blank cells. Independently confirmed: D-12 (`bot_timeout_seconds + 30` at workflow line 149), D-16 (`temporal-ui` Up on 8088), D-17 (no Loki/Grafana service in compose), D-23 (`@workflow.signal` count = 0). Note: PLAN frontmatter `requirements:` fields aggregate to 21/25 D-XX (omits D-12, D-16, D-17, D-23) — the four omitted decisions are still verified in the per-plan ledger and re-verified here. |
| 14 | End-to-end smoke PASS against 2 distinct recipes (Wave 5 cutover SUMMARY: openai_compat + zeroclaw_native)                                       | VERIFIED   | `28-06-SUMMARY.md` lines 88-130 documents 2 live curl tests: openai_compat (nanobot → claude-haiku, 2.81s round-trip, `status='done'`, workflow_id back-filled) + zeroclaw_native (zeroclaw → mimo). Plan 09 added 3 more live smokes (hermes 19.2s, nullclaw 11.1s, hermes bullseye cold-boot 83.6s — all `WORKFLOW_EXECUTION_STATUS_COMPLETED`). Live deploy stack still up at verification time: `docker compose ps` shows all 7 services (api_server / postgres / redis / temporal / temporal-ui / temporal-worker / caddy) running with healthy status. |

**Score:** 14/14 must-haves verified.

### Required Artifacts (re-verified independently of per-plan ledger)

| Artifact                                                                                                | Expected                                                            | Status     | Details                                                                                                                                                                                                                          |
|---------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------|------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `api_server/pyproject.toml`                                                                             | `temporalio>=1.27.0,<1.28` pin                                      | VERIFIED   | Line found via grep: `"temporalio>=1.27.0,<1.28"`.                                                                                                                                                                                |
| `deploy/docker-compose.prod.yml`                                                                        | 3 new services (temporal, temporal-ui, temporal-worker)              | VERIFIED   | Services declared at lines 59, 95, 109. Pinned image tags `temporalio/auto-setup:1.29.2` + `temporalio/ui:2.40.0`. AP_TEMPORAL_HOST passed as env to worker.                                                                       |
| `api_server/src/api_server/temporal/__init__.py`                                                        | Package init                                                         | VERIFIED   | Exists; documents subpackages + temporalio version pin.                                                                                                                                                                          |
| `api_server/src/api_server/temporal/client.py`                                                          | `make_client(settings)` factory                                      | VERIFIED   | Imported by both `worker.py` and `routes/agent_messages.py` (via lifespan).                                                                                                                                                      |
| `api_server/src/api_server/temporal/worker.py`                                                          | Boot worker, register, drain on SIGTERM                              | VERIFIED   | 240 lines; Sec 1 logging, Sec 2-4 deps (db_pool, redis, httpx, recipe_index, docker), Sec 5 activity binding, Sec 6 5×5s connect retry, Sec 7 Worker, Sec 8 SIGTERM/SIGINT, Sec 9 graceful run+drain.                                |
| `api_server/src/api_server/temporal/workflows/dispatch_message.py`                                      | `@workflow.defn`, deterministic body                                 | VERIFIED   | 307 lines. Imports activities under `imports_passed_through()` (line 41). 5 activity execute_activity calls + `_fail` helper. Retry policies match D-10 spec. `_classify_forward_failure` walks cause chain for ApplicationError. |
| `api_server/src/api_server/temporal/workflows/types.py`                                                 | Shared dataclasses                                                   | VERIFIED   | Provides `ForwardResult`, etc.                                                                                                                                                                                                    |
| `api_server/src/api_server/temporal/activities/check_container_ready.py`                                | Activity 1 of 7                                                     | VERIFIED   | 6075 bytes. `ReadinessActivities` class.                                                                                                                                                                                          |
| `api_server/src/api_server/temporal/activities/forward_to_agent.py`                                     | Activity 2 of 7                                                     | VERIFIED   | 10512 bytes. `ForwardActivities` class with `[0.0, 1.0, 2.0, 4.0]` sleep budgets (D-11) at line 145.                                                                                                                              |
| `api_server/src/api_server/temporal/activities/record_usage.py`                                         | Activity 3 of 7                                                     | VERIFIED   | 3994 bytes. `RecordUsageActivities` class wraps existing `services/usage_recorder.py::record_usage` (D-07).                                                                                                                       |
| `api_server/src/api_server/temporal/activities/emit_inapp_outbound.py`                                  | Activity 4 of 7                                                     | VERIFIED   | 2189 bytes. Standalone no-op pattern (per D-15 idempotency / outbox-pump owns Redis fan-out).                                                                                                                                     |
| `api_server/src/api_server/temporal/activities/mark_message_done.py`                                    | Activity 5 of 7                                                     | VERIFIED   | 3983 bytes. `MarkActivities` class — load-bearing per D-10.                                                                                                                                                                       |
| `api_server/src/api_server/temporal/activities/mark_message_failed.py`                                  | Activity 6 of 7                                                     | VERIFIED   | 5435 bytes. `MarkFailedActivities` class.                                                                                                                                                                                         |
| `api_server/src/api_server/temporal/activities/debit_balance.py`                                        | Activity 7 of 7 (D-22 no-op)                                        | VERIFIED — Note | 1765 bytes. Returns `"0"` (string-encoded decimal) due to JSON serialization; intent preserved per D-22.                                                                                                                      |
| `api_server/alembic/versions/011_phase28_workflow_id_idempotency.py`                                    | Migration 011                                                        | VERIFIED   | Adds `workflow_id` + `idempotency_key` columns + 2 partial indexes (one UNIQUE on `(user_id, idempotency_key)`); reversible.                                                                                                      |
| `tools/migrate_phase28_stuck_rows.py`                                                                   | Cutover sweep, idempotent, safety-capped                            | VERIFIED   | Asyncpg-based UPDATE with WHERE filter on `status='forwarded'` + 11min reaper threshold; `--limit` safety cap default 10000; `--dry-run` flag.                                                                                    |
| `mobile/lib/features/dashboard/dashboard_screen.dart`                                                   | Consumer-wrapped UsageTickerWidget                                   | VERIFIED   | Lines 103-104: `Consumer(builder: (context, ref, _) => const UsageTickerWidget())`.                                                                                                                                              |
| `mobile/lib/features/chat/chat_screen.dart`                                                             | Consumer-wrapped UsageTickerWidget                                   | VERIFIED   | Lines 165-166: same pattern.                                                                                                                                                                                                      |
| `mobile/lib/features/usage/usage_ticker_widget.dart`                                                    | Widget body unchanged from Phase 27 Wave 3                          | VERIFIED   | Still `class UsageTickerWidget extends ConsumerWidget` (line 33).                                                                                                                                                                 |

### Key Link Verification

| From                                                  | To                                                  | Via                                                          | Status |
|-------------------------------------------------------|-----------------------------------------------------|--------------------------------------------------------------|--------|
| POST /v1/agents/:id/messages handler                  | `DispatchMessageWorkflow.run`                        | `temporal_client.start_workflow(...)` (agent_messages.py:253) | WIRED  |
| `DispatchMessageWorkflow`                             | 7 activities                                         | `workflow.execute_activity(...)` calls + `imports_passed_through` | WIRED  |
| Worker process                                        | Temporal frontend (gRPC 7233)                        | `make_client(settings)` with 5×5s outer retry                | WIRED  |
| Worker                                                | DB pool / Redis / httpx / recipe_index               | `AsyncExitStack` deps in `main()`                            | WIRED  |
| Worker                                                | `ap-messages` task queue                             | `Worker(..., task_queue=settings.temporal_task_queue)`       | WIRED  |
| Workflow                                              | mark_message_done activity                            | direct execute_activity, no try/except (load-bearing per D-10) | WIRED  |
| Workflow failure paths                                | mark_message_failed activity                          | `self._fail(inp, error_type)` helper                          | WIRED  |
| Compose `temporal-worker` container                   | `python -m api_server.temporal.worker`                | `command:` directive in compose                              | WIRED  |
| Migration 011 `workflow_id`                           | route handler back-fill                               | `ims.set_workflow_id(...)` (route Step 7)                    | WIRED  |
| Dashboard AppBar                                      | UsageTickerWidget                                    | `Consumer(builder: ...)`                                     | WIRED  |
| Chat AppBar                                           | UsageTickerWidget                                    | `Consumer(builder: ...)`                                     | WIRED  |

### Data-Flow Trace (Level 4)

| Artifact                                              | Data Variable          | Source                                           | Produces Real Data | Status   |
|-------------------------------------------------------|------------------------|--------------------------------------------------|--------------------|----------|
| POST /v1/agents/:id/messages                          | workflow execution     | `temporal_client.start_workflow(...)`            | YES — 5+ live `WORKFLOW_EXECUTION_STATUS_COMPLETED` workflows observed via Temporal UI REST | FLOWING  |
| `forward_to_agent` activity                           | bot reply              | `_dispatch_http_localhost(...)` → bot HTTP       | YES — 2.81s nanobot reply, 8s zeroclaw reply, 19.2s hermes reply, 11.1s nullclaw reply, 83.6s hermes cold-boot reply (all stored in `inapp_messages.bot_response`) | FLOWING  |
| `record_usage` activity                                | usage_logs row          | `record_usage(...)` against asyncpg pool         | YES — wraps existing UsageRecorder body, idempotent at DB layer (D-15) | FLOWING  |
| `mark_message_done` activity                           | inapp_messages.status   | UPDATE … WHERE id=$1 AND status != 'done'        | YES — terminal status confirmed in 5 live smokes                | FLOWING  |
| Migration 011 `workflow_id` column                    | workflow_id audit row   | route handler `set_workflow_id(...)` back-fill    | YES — Plan 06 SUMMARY confirms back-fill in cutover smoke      | FLOWING  |
| `UsageTickerWidget` (Dashboard / Chat AppBars)        | usage USD              | `usageSummaryProvider` via Consumer subscription | DEFERRED VERIFY — visual increment + tap-navigation is the residual manual gate (`28-09-VERIFICATION.md` `## Auto-mode Checkpoint Handling`); structural defunct-element fix covered by `mobile/test/features/usage/usage_ticker_widget_remount_test.dart` | STATIC (data path unchanged from Phase 27 Wave 3; widget mount surface verified) |

### Behavioral Spot-Checks

| Behavior                                              | Command                                                                                  | Result            | Status |
|-------------------------------------------------------|------------------------------------------------------------------------------------------|-------------------|--------|
| `pytest tests/temporal/` passes 19/19                 | `cd api_server && uv run pytest tests/temporal/ --tb=no -q`                              | `19 passed, 1 warning in 56.43s` | PASS   |
| Worker module imports cleanly                          | `python -c "from api_server.temporal.worker import main"`                               | implicit pass via worker container booting `phase28.worker.running` log | PASS   |
| Live deploy stack — all 7 services                     | `docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml ps` | 7/7 services Up; temporal/temporal-ui/temporal-worker present | PASS   |
| Workflow body has zero non-deterministic primitives    | `grep -E "(datetime\\.now|random\\.)" .../workflows/dispatch_message.py`                 | 0 matches         | PASS   |
| Workflow has zero signal handlers (D-23)               | `grep -c "@workflow.signal" .../workflows/dispatch_message.py`                           | 0                 | PASS   |
| Migration 011 has UNIQUE partial index                 | grep `ix_inapp_messages_idempotency_key_unique` + `unique=True` + partial WHERE          | found at line 131-137 | PASS   |
| `_handle_row` is gone                                  | `grep "async def _handle_row" services/inapp_dispatcher.py`                              | 0 matches         | PASS   |

### Requirements Coverage (D-XX vs PLAN frontmatter aggregation)

PLAN frontmatter `requirements:` fields union: **D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-13, D-14, D-15, D-18, D-19, D-20, D-21, D-22, D-24, D-25** (21 of 25).

PLAN frontmatter omits: **D-12, D-16, D-17, D-23**. These four are still part of phase scope and are evidenced in `28-09-VERIFICATION.md` ledger AND independently re-verified here:

| Decision | Description                                                  | Plan claim | Status     | Evidence                                                                                                                                |
|----------|--------------------------------------------------------------|------------|------------|-----------------------------------------------------------------------------------------------------------------------------------------|
| D-12     | Activity timeout = bot_timeout + 30s buffer                   | none       | SATISFIED  | `dispatch_message.py:149` — `seconds=inp.bot_timeout_seconds + 30.0,  # D-12 buffer`. Empirically validated by 83.5s cold-boot smoke. |
| D-16     | Temporal UI for dev observability on localhost:8088           | none       | SATISFIED  | `temporal-ui` service Up + 5 workflows visible via REST API.                                                                            |
| D-17     | No Loki/Grafana wiring this phase                             | none       | SATISFIED  | `grep -n "Loki\|Grafana" deploy/docker-compose.prod.yml` → 0 matches. Confirmed by absence.                                              |
| D-23     | No workflow signal handlers                                   | none       | SATISFIED  | `grep -c "@workflow.signal" .../workflows/dispatch_message.py` → 0.                                                                      |

All 25 D-XX SATISFIED. None ORPHANED. None BLOCKED.

| Decision | Source Plan(s)                | Status     | Evidence                                                                                                            |
|----------|-------------------------------|------------|---------------------------------------------------------------------------------------------------------------------|
| D-01     | 28-01, 28-02                  | SATISFIED  | `temporal:1.29.2` + `temporal-ui:2.40.0` in compose; spike evidence `28-01-SPIKE-EVIDENCE.md`.                         |
| D-02     | 28-01, 28-02                  | SATISFIED  | Dedicated `temporal` + `temporal_visibility` DBs created in `deploy-postgres-1` by auto-setup bootstrap.            |
| D-03     | 28-01, 28-02                  | SATISFIED  | gRPC 7233 + UI 8088 in compose; per-plan VERIFICATION.md `curl http://127.0.0.1:8088/` → 200.                        |
| D-04     | 28-01, 28-02, 28-04           | SATISFIED  | `deploy-temporal-worker-1` Up; runs `python -m api_server.temporal.worker`.                                          |
| D-05     | 28-02, 28-04                  | SATISFIED  | Worker registers on `ap-messages` queue; `temporal_task_queue` defaults to `ap-messages`.                            |
| D-06     | 28-06                         | SATISFIED  | `_handle_row` gone; lifespan no longer creates `dispatcher_loop` task; documented in module docstring.               |
| D-07     | 28-04                         | SATISFIED  | `record_usage_activity` wraps `services/usage_recorder.py::record_usage` body.                                       |
| D-08     | 28-03, 28-06                  | SATISFIED  | `f"msg-{user_id}-{message_id}"` in route handler; user-scoped IDs visible in 5 live workflows.                       |
| D-09     | 28-01, 28-03                  | SATISFIED  | Workflow body grep: 0 `datetime.now`/`random.` matches; sandbox-passthrough explicit.                                |
| D-10     | 28-03, 28-04                  | SATISFIED  | 7 activities + 5 retry policies in `dispatch_message.py` mirror MSV `SendMessageWorkflow`.                          |
| D-11     | 28-01, 28-04, 28-07           | SATISFIED  | `[0.0, 1.0, 2.0, 4.0]` sleep budgets in `forward_to_agent.py:145`.                                                    |
| D-12     | (no PLAN — see above)         | SATISFIED  | `bot_timeout_seconds + 30.0` at workflow line 149.                                                                   |
| D-13     | 28-03, 28-06, 28-07           | SATISFIED  | `execution_timeout=timedelta(minutes=5)` at `agent_messages.py:270`.                                                 |
| D-14     | 28-05, 28-06                  | SATISFIED  | Migration 011 + UNIQUE partial index + IdempotencyMiddleware (24h TTL) two-layer defense.                            |
| D-15     | 28-03, 28-04, 28-07           | SATISFIED  | 4 try/except blocks in workflow swallow non-fatal failures (lines 153, 184, 201, 215).                              |
| D-16     | (no PLAN — see above)         | SATISFIED  | Temporal UI reachable HTTP 200 on 8088.                                                                              |
| D-17     | (no PLAN — see above)         | SATISFIED  | No Loki/Grafana service in compose.                                                                                   |
| D-18     | 28-08                         | SATISFIED  | Consumer-wrapped mount in Dashboard + Chat (lines 103, 165).                                                          |
| D-19     | 28-08                         | SATISFIED  | Both AppBars mount.                                                                                                  |
| D-20     | 28-08                         | SATISFIED  | Widget body unchanged from Phase 27 Wave 3.                                                                          |
| D-21     | 28-08                         | SATISFIED  | `ref.invalidate(usageSummaryProvider)` only inside doc comment, zero callable invocations.                          |
| D-22     | 28-03                         | SATISFIED — note | `debit_balance.py:40` returns `"0"` (string, not Decimal — Plan 06 Rule-1 deviation; intent preserved). |
| D-23     | (no PLAN — see above)         | SATISFIED  | Zero `@workflow.signal` handlers.                                                                                   |
| D-24     | 28-07                         | SATISFIED  | 19 tests in `tests/temporal/` use real `WorkflowEnvironment` + `ActivityEnvironment` + testcontainers PG17.        |
| D-25     | 28-01, 28-05, 28-07, 28-09    | SATISFIED  | 332 PASS / 4 skip / 1 pre-existing fail / 61 deselected — well above 113+ baseline.                                  |

### Anti-Patterns Found

None blocking. The known operational and stale-test items are all logged in `deferred-items.md`:

| File / Surface                                                           | Pattern                                                  | Severity | Impact                                                                                                                                       |
|--------------------------------------------------------------------------|----------------------------------------------------------|----------|----------------------------------------------------------------------------------------------------------------------------------------------|
| `tests/e2e/_helpers.py::drive_dispatcher_once`                           | imports deleted symbol `_handle_row`                      | INFO     | `make e2e-inapp-docker` BLOCKED at fixture setup. Replacement coverage in `tests/temporal/` (19 PASS) + 3 live curl smokes. Logged as deferred item #10. |
| `tests/test_run_recipe_persistent_inapp.py::test_data_dir_hoisted_*`     | Docker container teardown timing flake                   | INFO     | Pre-existing 22c.3.1 flake (commit `ca9bb19`); not Phase 28 regression. Logged as deferred item #11.                                         |
| `temporal-worker` bind-mount stale-worktree drift                         | volume path can point at deleted executor worktree       | INFO     | Operational only; mitigated by `--force-recreate` from canonical project root. Logged as deferred item #12.                                |
| `debit_balance.py:40` returns `"0"` (string) not `Decimal('0')`           | typed-value deviation from D-22 wording                  | INFO     | Documented Plan 06 Rule-1 fix — Temporal default JSON converter rejected `Decimal`. Lossless via `Decimal(<str>)`; no-op intent preserved.   |
| 9 pre-existing test failures (deferred-items #1-9)                       | author-marked allowlist drift, fixture wiring gaps       | INFO     | Pre-existing across 22c.3.1 → 28; not Phase 28 regressions; documented per item with remediation hints.                                       |

### Human Verification Required

None for the phase exit gate — the per-plan verification (`28-09-VERIFICATION.md`) auto-approved Plan 09's `checkpoint:human-verify` under auto-mode and recorded the residual manual smoke as a follow-up item. Re-quoted here for visibility:

- **Residual manual smoke (acknowledged, not blocking ship):** iOS-simulator chat → ticker increments end-to-end against the Phase 28 stack to empirically validate triggers #1 (mount) + #2 (resume) with a Temporal-dispatched assistant reply driving the ticker. Structural defunct-element guarantee already covered by `mobile/test/features/usage/usage_ticker_widget_remount_test.dart`. The visual increment + tap-navigation case is the residual manual gate; per Plan 09 frontmatter and 28-09-VERIFICATION.md `## Auto-mode Checkpoint Handling`, the user owns this final visual smoke at their discretion.

### Gaps Summary

**No gaps.** Every must_have in the brief has been verified against the codebase, the deploy stack, and the per-plan ledger. The two minor textual deviations (debit_balance returns string `"0"` not `Decimal('0')`; current pytest count is 332 not 322) are both materially in keeping with the underlying intent (D-22 no-op preserved, D-25 baseline 113+ exceeded) and are documented as deviations in the per-plan SUMMARYs (28-06-SUMMARY.md "Deviations" section, 28-07-SUMMARY.md test gate evidence). Four D-XX decisions (D-12, D-16, D-17, D-23) are missing from PLAN frontmatter `requirements:` fields but are evidenced in `28-09-VERIFICATION.md` and re-verified here — they are SATISFIED, not ORPHANED.

The phase delivers exactly what the goal said:
- A new user message starts a `DispatchMessageWorkflow` (live, observed in 5+ Temporal UI executions).
- Activities handle readiness → forward → record_usage → emit_outbound → mark_done atomically with documented retry budgets.
- Legacy asyncpg dispatcher is gone (`_handle_row` deleted, lifespan task creation removed).
- Mobile ticker is back via Consumer-scoped mounts in Dashboard + Chat AppBars.
- The bullseye `container_not_ready` mitigation ships through both the readiness retry budget and (empirically validated) the `bot_timeout_seconds + 30s` D-12 buffer.

Phase 28 ships. PHASE-28-EXIT-GATE-PASSED.

---

_Verified: 2026-05-06T01:01:40Z_
_Verifier: Claude (gsd-verifier) — phase-level orthogonal verification_
_Per-plan exit gate: `28-09-VERIFICATION.md` (Plan 09, commit 364e2c8)_
