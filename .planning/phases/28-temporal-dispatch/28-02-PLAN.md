---
phase: 28-temporal-dispatch
plan: 02
type: execute
wave: 1
depends_on: [28-01]
files_modified:
  - api_server/pyproject.toml
  - api_server/src/api_server/config.py
  - tools/Dockerfile.api
  - deploy/docker-compose.prod.yml
  - deploy/docker-compose.local.yml
  - deploy/.env.prod.example
  - deploy/README.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
must_haves:
  truths:
    - "temporalio==1.27.x SDK pinned in api_server/pyproject.toml runtime deps with floor 1.27.0 ceiling <1.28"
    - "tools/Dockerfile.api pip install chain includes the same temporalio pin so the worker image carries the SDK"
    - "deploy/docker-compose.prod.yml has a `temporal` service (image temporalio/auto-setup:1.29.2) with healthcheck, depends_on postgres healthy, NO host-published 7233 port"
    - "deploy/docker-compose.prod.yml has a `temporal-ui` service (image temporalio/ui:2.40.0) publishing 127.0.0.1:8088:8080"
    - "deploy/docker-compose.prod.yml has a `temporal-worker` service that builds from tools/Dockerfile.api with command `python -m api_server.temporal.worker`"
    - "api_server service `depends_on` includes `temporal: { condition: service_healthy }`"
    - "Pydantic Settings exposes 4 new fields: temporal_host (AP_TEMPORAL_HOST, default temporal:7233), temporal_namespace (AP_TEMPORAL_NAMESPACE, default default), temporal_task_queue (AP_TEMPORAL_TASK_QUEUE, default ap-messages), bot_timeout_seconds (AP_BOT_TIMEOUT_SECONDS, default 60.0)"
    - "deploy/.env.prod.example lists AP_TEMPORAL_HOST + AP_TEMPORAL_NAMESPACE + AP_TEMPORAL_TASK_QUEUE entries with comments"
    - "make dev-api-local brings the full stack up green including all 3 temporal services"
    - "deploy/README.md carries a Phase 28 stanza documenting that the temporal-worker MUST run inside compose on macOS (host-venv path is broken; cross-references make dev-api-local) per RESEARCH §7 R6"
  artifacts:
    - path: "deploy/docker-compose.prod.yml"
      provides: "temporal + temporal-ui + temporal-worker service blocks on deploy_default"
      contains: "temporalio/auto-setup:1.29.2"
    - path: "api_server/pyproject.toml"
      provides: "temporalio runtime dep pin"
      contains: "temporalio>=1.27.0,<1.28"
    - path: "tools/Dockerfile.api"
      provides: "worker image carries temporalio SDK"
      contains: "temporalio"
    - path: "api_server/src/api_server/config.py"
      provides: "Settings exposes 4 new temporal_* + bot_timeout_seconds fields"
      contains: "temporal_host"
    - path: "deploy/README.md"
      provides: "Phase 28 macOS-worker-in-compose constraint documented"
      contains: "temporal-worker"
  key_links:
    - from: "api_server service in docker-compose.prod.yml"
      to: "temporal service"
      via: "depends_on.temporal.condition: service_healthy"
      pattern: "temporal:\\s*\\n\\s*condition:\\s*service_healthy"
    - from: "temporal-worker service"
      to: "deploy_default network"
      via: "default network attachment + AP_DOCKER_NETWORK=deploy_default env"
      pattern: "AP_DOCKER_NETWORK"
---

<objective>
Wave 1: lay the infrastructure that Wave 2's workflow + activities will land on. Add the Temporal cluster (frontend + UI + worker) to `deploy/docker-compose.prod.yml`, pin the Python SDK, extend Settings with the 4 new env vars, and confirm `make dev-api-local` boots the whole stack green. This plan ships ZERO Python application code under `api_server/src/api_server/temporal/` — that's Plan 03 + Plan 04. Wave 1's only Python touch is to `config.py` (Settings fields) so Wave 2's worker.py + client.py can read settings without re-touching config.

Purpose: bring up the Temporal cluster + worker container shell on deploy_default; later waves write the actual workflow/activity code that the worker process executes.
Output: a green local stack (`make dev-api-local`) that includes 3 new services (`temporal`, `temporal-ui`, `temporal-worker`), with the worker container in a healthy `restart: always` loop pending Wave 2's `python -m api_server.temporal.worker` entrypoint actually existing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-temporal-dispatch/28-CONTEXT.md
@.planning/phases/28-temporal-dispatch/28-RESEARCH.md
@.planning/phases/28-temporal-dispatch/28-01-SPIKE-EVIDENCE.md
@CLAUDE.md
@api_server/pyproject.toml
@tools/Dockerfile.api
@deploy/docker-compose.prod.yml
@deploy/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin temporalio==1.27.x in pyproject.toml + Dockerfile + add Settings fields</name>
  <files>api_server/pyproject.toml, tools/Dockerfile.api, api_server/src/api_server/config.py</files>
  <read_first>
    - api_server/pyproject.toml (current dependencies block, especially how respx>=0.22,<0.24 is pinned — match the floor/ceiling discipline)
    - tools/Dockerfile.api (current pip install chain — Phase 22c-09 lesson: pyproject changes alone don't bake into the image; Dockerfile must mirror)
    - api_server/src/api_server/config.py (Settings class shape, validation_alias pattern, recipes_dir + docker_network_name fields)
    - .planning/phases/28-temporal-dispatch/28-RESEARCH.md §1 "Python SDK pinning"
    - .planning/phases/28-temporal-dispatch/28-CONTEXT.md D-04, D-05, D-12
  </read_first>
  <action>
**Step 1 — pyproject.toml runtime deps:**

Add the line `"temporalio>=1.27.0,<1.28",` to the `dependencies = [...]` array in `api_server/pyproject.toml`. Place it alphabetically (after `structlog` and before any letter past `t`). Match the same string-with-trailing-comma format the existing entries use.

**Step 2 — tools/Dockerfile.api:**

Find the existing `pip install --prefix=/install` chain (Phase 22c-09 + 22c.3-03 added authlib + httpx + redis + sse-starlette there). Append `"temporalio>=1.27.0,<1.28"` to the same explicit list. The Dockerfile MUST carry the SDK at image-build time so the worker container can `import temporalio.client` immediately on boot.

If the Dockerfile uses a `requirements.txt`-style flow rather than inline pip args, append `temporalio>=1.27.0,<1.28` to that file. Inspect first; mirror the established pattern.

**Step 3 — config.py — add 4 Pydantic Settings fields:**

Inside the `Settings(BaseSettings)` class, AFTER the existing `docker_network_name` field, add:

```python
    # Phase 28 — Temporal client/worker config (D-01..D-05).
    # The worker container reads these to dial the cluster on boot.
    # api_server reads `temporal_host` + `temporal_namespace` to mint
    # the Client used for `start_workflow` calls from POST /messages.
    temporal_host: str = Field(
        "temporal:7233",
        validation_alias="AP_TEMPORAL_HOST",
        description="Temporal frontend host:port (Docker DNS in compose; localhost:7233 if running outside compose).",
    )
    temporal_namespace: str = Field(
        "default",
        validation_alias="AP_TEMPORAL_NAMESPACE",
        description="Temporal namespace. Single namespace per CONTEXT D-03.",
    )
    temporal_task_queue: str = Field(
        "ap-messages",
        validation_alias="AP_TEMPORAL_TASK_QUEUE",
        description="Workflow task queue name. Single queue per CONTEXT D-05.",
    )
    bot_timeout_seconds: float = Field(
        60.0,
        validation_alias="AP_BOT_TIMEOUT_SECONDS",
        description="Per-call bot HTTP timeout. Phase 28 D-12: forward_to_agent activity start_to_close = this + 30s buffer.",
    )
```

Do NOT remove or rename any existing field. The `bot_timeout_seconds` field replaces the hardcoded `600.0` referenced in `inapp_dispatcher._dispatch_http_localhost` — Plan 03's activity reuses this setting.

**Step 4 — verify locally (do not commit until verification passes):**

Run `cd api_server && uv sync` (or `pip install -e .` depending on the project's local convention; `uv sync` is the canonical path per pyproject's `uv.lock` presence — Phase 22c-09 used uv). Confirm no resolver conflicts.

Run `cd api_server && python -c "from api_server.config import Settings; s = Settings(); print(s.temporal_host, s.temporal_namespace, s.temporal_task_queue, s.bot_timeout_seconds)"` — expect `temporal:7233 default ap-messages 60.0`.

Then build the Dockerfile to confirm temporalio is baked in: `docker build -f tools/Dockerfile.api -t ap-test-build .` — image tag local. Verify with `docker run --rm ap-test-build python -c "import temporalio; print(temporalio.__version__)"` — expect a 1.27.x version string.

Tear-down the test image: `docker rmi ap-test-build`.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/api_server &amp;&amp; uv sync &amp;&amp; python -c "from api_server.config import Settings; s = Settings(); assert s.temporal_host == 'temporal:7233' and s.temporal_namespace == 'default' and s.temporal_task_queue == 'ap-messages' and s.bot_timeout_seconds == 60.0, 'settings defaults wrong'" &amp;&amp; grep -q 'temporalio>=1.27.0,&lt;1.28' pyproject.toml &amp;&amp; grep -q 'temporalio' ../tools/Dockerfile.api</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "temporalio>=1.27.0,<1.28" api_server/pyproject.toml` returns 1
    - `grep -c "temporalio" tools/Dockerfile.api` returns at least 1
    - `python -c "from api_server.config import Settings; s = Settings(); print(s.temporal_host)"` from `api_server/` prints `temporal:7233`
    - `python -c "from api_server.config import Settings; s = Settings(); print(s.bot_timeout_seconds)"` prints `60.0`
    - `python -c "import os; os.environ['AP_TEMPORAL_HOST']='custom:1234'; from api_server.config import Settings; print(Settings().temporal_host)"` prints `custom:1234` (env alias works)
    - No resolver conflict during `uv sync` (exit 0)
    - All 4 new Settings fields have docstring + validation_alias
  </acceptance_criteria>
  <done>SDK pinned at runtime + image level; Settings exposes the 4 new fields; uv resolver clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add temporal + temporal-ui + temporal-worker services to deploy/docker-compose.prod.yml + document macOS constraint in deploy/README.md</name>
  <files>deploy/docker-compose.prod.yml, deploy/docker-compose.local.yml, deploy/.env.prod.example, deploy/README.md</files>
  <read_first>
    - deploy/docker-compose.prod.yml (full file — see existing service shape, AP_DOCKER_NETWORK env, postgres healthcheck)
    - deploy/docker-compose.local.yml (full file — see local-only port overrides shape)
    - deploy/.env.prod.example (find the `# Phase XX —` stanza pattern to follow)
    - deploy/README.md (find the macOS dev-mode section, if present; otherwise locate where boot ordering is documented)
    - .planning/phases/28-temporal-dispatch/28-RESEARCH.md §2 "Compose recipe — copy-paste-ready service blocks"
    - .planning/phases/28-temporal-dispatch/28-RESEARCH.md §7 R6 (worker MUST run inside compose on macOS)
    - .planning/phases/28-temporal-dispatch/28-CONTEXT.md D-01, D-02, D-03, D-04
    - /Users/fcavalcanti/dev/meusecretariovirtual/infra/docker-compose.yml lines 86-122 (MSV reference)
    - .planning/phases/28-temporal-dispatch/28-01-SPIKE-EVIDENCE.md (confirm WAVE-0-CLOSED before proceeding)
  </read_first>
  <action>
**Step 1 — Add 3 services to `deploy/docker-compose.prod.yml`.**

Insert the three service blocks AFTER the existing `redis:` block and BEFORE the `api_server:` block. Use the EXACT YAML from RESEARCH §2 — do not paraphrase. Specifically:

```yaml
  temporal:
    # Phase 28 (D-01, D-02, D-03): self-hosted Temporal cluster, single
    # namespace `default`, Postgres backend pointing at the existing
    # `postgres` service. auto-setup bootstraps `temporal` and
    # `temporal_visibility` databases inside the same Postgres on first boot.
    # NO host-bound port for 7233 — internal-only. UI exposes 8088 below.
    image: temporalio/auto-setup:1.29.2
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB: postgres12
      DB_PORT: "5432"
      POSTGRES_SEEDS: postgres
      POSTGRES_USER: ap
      POSTGRES_PWD: ${POSTGRES_PASSWORD}
      DBNAME: temporal
      VISIBILITY_DBNAME: temporal_visibility
      DYNAMIC_CONFIG_FILE_PATH: config/dynamicconfig/development-sql.yaml
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CLI_ADDRESS: temporal:7233
    healthcheck:
      test: ["CMD", "temporal", "operator", "cluster", "health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  temporal-ui:
    # Phase 28 (D-03, D-16): web UI on 127.0.0.1:8088 (loopback only — never
    # publicly bind; ops access via SSH tunnel to the Hetzner box).
    image: temporalio/ui:2.40.0
    restart: always
    depends_on:
      temporal:
        condition: service_healthy
    ports:
      - "127.0.0.1:8088:8080"
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CORS_ORIGINS: http://localhost:8088

  temporal-worker:
    # Phase 28 (D-04): worker process for the `ap-messages` task queue.
    # Same image as api_server — shared deps, separate `command` invoking
    # the worker entrypoint that lands in Plan 28-04. Inherits the same
    # env_file so DB pool + Redis + recipe dir are available.
    build:
      context: ..
      dockerfile: tools/Dockerfile.api
      args:
        DOCKER_GID: ${DOCKER_GID:-999}
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      temporal:
        condition: service_healthy
    command: ["python", "-m", "api_server.temporal.worker"]
    environment:
      AP_ENV: prod
      AP_RECIPES_DIR: /app/recipes
      AP_REDIS_URL: "redis://redis:6379/0"
      AP_DOCKER_NETWORK: "deploy_default"
      AP_TEMPORAL_HOST: "temporal:7233"
      AP_TEMPORAL_NAMESPACE: "default"
      AP_TEMPORAL_TASK_QUEUE: "ap-messages"
      DATABASE_URL: postgresql+asyncpg://ap:${POSTGRES_PASSWORD}@postgres:5432/agent_playground_api
      AP_CHANNEL_MASTER_KEY: ${AP_CHANNEL_MASTER_KEY}
    env_file:
      - .env.prod
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ../recipes:/app/recipes:ro
```

**Step 2 — Extend `api_server` service's `depends_on:` block.**

Find the existing `api_server:` service. Inside its `depends_on:` map, ADD:

```yaml
      temporal:
        condition: service_healthy
```

(Adding after the existing `redis: { condition: service_healthy }` entry.)

This wires the boot ordering: api_server starts AFTER Temporal frontend is ready, so the lifespan's `temporal_client.connect()` (Plan 28-04) doesn't error with connection-refused.

**Step 3 — `deploy/docker-compose.local.yml` — local-only override.**

Inspect the file. If it provides host-port overrides (similar to redis 127.0.0.1:6379:6379 added in Phase 22c.3-03), append a `temporal:` override that publishes `127.0.0.1:7233:7233` so a host-venv pytest can dial the cluster directly. The override block should look like:

```yaml
services:
  temporal:
    ports:
      - "127.0.0.1:7233:7233"
```

Do NOT add an override for `temporal-worker` — the worker MUST stay inside compose per RESEARCH §7 R6 (macOS bridge limitation). Document this in a comment above the temporal override stanza.

**Step 4 — `deploy/.env.prod.example`.**

Append a new stanza (mirroring the existing `# Phase XX — ...` patterns):

```ini
# --- Phase 28 — Temporal-backed message dispatch ---
# Internal Docker DNS hostnames; override only if running services elsewhere.
AP_TEMPORAL_HOST=temporal:7233
AP_TEMPORAL_NAMESPACE=default
AP_TEMPORAL_TASK_QUEUE=ap-messages
# Per-call bot HTTP timeout (seconds). forward_to_agent activity uses this
# + 30s buffer for its start_to_close timeout (D-12).
AP_BOT_TIMEOUT_SECONDS=60
```

Do NOT touch `deploy/.env.prod` (the actual file with secrets). Only `.env.prod.example`.

**Step 4.5 — `deploy/README.md` — document the macOS-worker-in-compose constraint (RESEARCH §7 R6).**

Read `deploy/README.md`. Append a new section at an appropriate location (near other macOS / dev-mode notes if they exist; otherwise append to the end before any "License" or boilerplate footer):

```markdown
## Phase 28 — Temporal worker on macOS

The Phase 28 dispatch architecture introduces a `temporal-worker` container
that consumes the `ap-messages` task queue. **On macOS, the worker MUST run
inside Docker Compose** — running it from a host venv is broken in two ways
that have already been observed in Phase 22c.3.1 (see
`memory/feedback_check_msv_when_stuck.md` and the dockerized e2e harness):

1. The host venv (`uv sync` outside Docker) does not consistently install
   `temporalio` against the same Python ABI as the api_server container.
2. The worker resolves agent containers by Docker bridge IP (`172.18.0.x`).
   macOS Docker Desktop does not route from host to the bridge network,
   so a host-venv worker cannot reach agent containers — symptom is
   `inapp_dispatcher.ip_lookup_failed` (or its Phase 28 successor
   `phase28.forward_to_agent.container_dead`).

**Canonical local boot path:**

```
make dev-api-local
```

This brings the entire compose stack up — postgres, redis, temporal,
temporal-ui, temporal-worker, and api_server — so the worker container
joins the same `deploy_default` bridge as agent containers and the
in-process `temporal:7233` DNS name resolves correctly.

**Do NOT** attempt `python -m api_server.temporal.worker` from a host venv
on macOS. If you need to debug the worker outside compose, use a Linux
host or run inside an interactive shell on the existing worker container
(`docker exec -it deploy-temporal-worker-1 bash`). Linux dev hosts CAN
run the worker from a host venv (the bridge route works there) but the
canonical path remains `make dev-api-local`.

The Temporal UI is reachable at <http://127.0.0.1:8088> after
`make dev-api-local` finishes booting.
```

The exact heading level + nearby section anchors should match the file's existing markdown style. If the file has no existing structure, the inserted section becomes a new top-level (`##`) section.

**Step 5 — Verify locally.**

Run `cd /Users/fcavalcanti/dev/agent-playground && make dev-api-local` (after Plan 28-04 lands the worker entrypoint, this will boot all three services). For THIS plan, the worker container will fail to boot because `python -m api_server.temporal.worker` doesn't exist yet — that's EXPECTED and acceptable for Plan 02. Verify ONLY that `temporal` reaches healthy status and `temporal-ui` returns 200 on `http://127.0.0.1:8088`. The worker's restart loop is fine — Plan 04 closes it.

Use `docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml ps` to confirm `deploy-temporal-1` is `Up (healthy)`. Use `curl -fsS http://127.0.0.1:8088 | head -30` to confirm UI serves HTML.

If healthcheck fails to go green within 90s, capture `docker compose ... logs temporal` and re-investigate Spike A's assumptions. (Spike A passed against an isolated stack — re-run if integrating against the existing deploy postgres surfaces a divergence.)
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground &amp;&amp; grep -q "temporalio/auto-setup:1.29.2" deploy/docker-compose.prod.yml &amp;&amp; grep -q "temporalio/ui:2.40.0" deploy/docker-compose.prod.yml &amp;&amp; grep -q "ap-messages" deploy/docker-compose.prod.yml &amp;&amp; grep -q "AP_TEMPORAL_HOST" deploy/.env.prod.example &amp;&amp; grep -q "temporal-worker" deploy/README.md &amp;&amp; docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml config &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "image: temporalio/auto-setup:1.29.2" deploy/docker-compose.prod.yml` returns 1
    - `grep -c "image: temporalio/ui:2.40.0" deploy/docker-compose.prod.yml` returns 1
    - `grep -c "command: \[\"python\", \"-m\", \"api_server.temporal.worker\"\]" deploy/docker-compose.prod.yml` returns 1
    - `grep -c "AP_TEMPORAL_HOST" deploy/.env.prod.example` returns 1
    - `grep -c "AP_TEMPORAL_HOST" deploy/docker-compose.prod.yml` returns 1 (only on temporal-worker, NOT on api_server — api_server reads from env_file)
    - `grep -c "temporal-worker" deploy/README.md` returns at least 1 (Phase 28 macOS stanza added per RESEARCH §7 R6)
    - `grep -c "make dev-api-local" deploy/README.md` returns at least 1 (canonical-path cross-reference)
    - `docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml config` exits 0 (yaml validates)
    - `docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml up -d temporal temporal-ui` brings both services to healthy within 90s (verify with `docker compose ... ps` showing `Up (healthy)`)
    - `curl -fsS http://127.0.0.1:8088 | head -1` returns HTML (HTTP 200, even if the body is the SPA shell)
    - `deploy/docker-compose.prod.yml`'s api_server service `depends_on` block contains `temporal: { condition: service_healthy }`
    - NO `ports:` block on the `temporal:` service in `docker-compose.prod.yml` (host-bind only via local override; verify with `grep -A3 "^  temporal:" deploy/docker-compose.prod.yml | grep -c "ports:"` returns 0)
  </acceptance_criteria>
  <done>Compose stack including Temporal cluster + UI brings up green; worker container in restart loop pending Plan 04; local override + .env.prod.example documented; deploy/README.md Phase 28 macOS stanza added.</done>
</task>

</tasks>

<verification>
- `temporalio>=1.27.0,<1.28` pin in pyproject AND Dockerfile.api
- 4 new Settings fields with env aliases default-to-correct values
- 3 new compose services with healthchecks + correct depends_on
- temporal-ui reachable on `http://127.0.0.1:8088`
- temporal-worker container in restart loop is EXPECTED (Plan 04 closes it)
- deploy/README.md carries the Phase 28 macOS-worker-in-compose constraint per RESEARCH §7 R6
</verification>

<success_criteria>
The Temporal infrastructure layer is in place. Wave 2 can implement the workflow + activities knowing the cluster is reachable at `temporal:7233` from inside compose, and the worker shell exists as a service ready to consume the entrypoint Plan 04 provides. Operators reading deploy/README.md know they MUST use `make dev-api-local` on macOS — no host-venv worker.
</success_criteria>

<output>
After completion, create `.planning/phases/28-temporal-dispatch/28-02-SUMMARY.md` documenting the SDK pin commit + compose service additions + healthcheck wall time + the deploy/README.md stanza added.
</output>
