---
phase: 20-frontend-alicerce
plan: 05
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03, 20-04]
files_modified:
  - frontend/app/playground/page.tsx
autonomous: false
requirements:
  - SC-01
  - SC-02
  - SC-03
  - SC-04
  - SC-05
  - SC-06
  - SC-09
  - SC-10
  - SC-11
must_haves:
  truths:
    - "frontend/app/playground/page.tsx imports <PlaygroundForm> from @/components/playground-form and mounts it inside the <div className='mx-auto max-w-2xl'> slot"
    - "Chrome is preserved byte-for-byte: <Navbar>, <ParticleBackground>, <Footer>, the h1 'Agent Playground' heading, the subtitle, and the outer max-w-7xl container (D-11)"
    - "pnpm build AND pnpm lint both exit 0 against the fully-wired page (final SC-09 gate)"
    - "A manual smoke run against the local stack completes end-to-end: recipes load from API, Deploy POSTs with Authorization: Bearer, a verdict card renders, run persists to postgres (SC-01..SC-06 + SC-10)"
    - "The SC-11 checkpoint confirms the Phase 19 Hetzner deploy is unblocked; the SUMMARY documents this explicitly so the next agent knows to proceed with Phase 19 Plan 07 Task 3"
  artifacts:
    - path: "frontend/app/playground/page.tsx"
      provides: "<PlaygroundForm> mounted inside the existing chrome; no other change"
      contains: "PlaygroundForm"
    - path: ".planning/phases/20-frontend-alicerce/20-05-SUMMARY.md"
      provides: "Records the manual smoke-run results (SC-01..SC-10), the SC-11 unblock confirmation, any D-14 follow-up items"
      contains: "SC-01"
  key_links:
    - from: "frontend/app/playground/page.tsx"
      to: "frontend/components/playground-form.tsx"
      via: "import + mount inside the max-w-2xl wrapper"
      pattern: "<PlaygroundForm />"
    - from: "Phase 20 exit gate"
      to: "Phase 19 Plan 07 Task 3 (Hetzner deploy)"
      via: "SC-11 confirmation in 20-05-SUMMARY.md unblocks STATE.md's deploy_blocked_pending_dumb_playground"
      pattern: "SC-11"
---

<objective>
Wire `<PlaygroundForm>` into `/playground` and run the full SC-01..SC-11 manual smoke pass that unblocks Phase 19's Hetzner deployment.

Purpose: This is the phase exit gate. Every prior plan can pass every automated check and still leave the user blocked if the whole tree doesn't round-trip a real run. The smoke checkpoint IS the Phase 20 hypothesis proof — the same pattern Phase 2 used ("API-driven agent start without Telegram is the hypothesis proof"). Until this runs green against the containerized API, golden rule #3 ("ship when the stack works locally end-to-end") stays violated and the Hetzner deploy stays blocked.

Output: One two-line edit to `app/playground/page.tsx`, one manual checkpoint with a documented runbook, one SUMMARY file that the next agent reads to resume Phase 19 Plan 07 Task 3.

**D-14 planner decision (DEFER):** Per RESEARCH Q10, no test framework is installed in this phase. Grep-based structural assertions (SC-07, SC-08) cover the mock deletion; manual SC-11 smoke covers the end-to-end round-trip. Phase 20.1 is the natural place to add Vitest + Playwright. The SUMMARY records this as a follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-frontend-alicerce/20-CONTEXT.md
@.planning/phases/20-frontend-alicerce/20-RESEARCH.md
@.planning/phases/20-frontend-alicerce/20-UI-SPEC.md

@.planning/STATE.md

@frontend/app/playground/page.tsx

<interfaces>
<!-- After Plan 20-02 landed, the page looks like this (AgentConfigurator gone, empty slot present): -->

```typescript
"use client"

import { Navbar } from "@/components/navbar"
import { ParticleBackground } from "@/components/particle-background"
import { Footer } from "@/components/footer"

export default function PlaygroundPage() {
  return (
    <main className="relative min-h-screen overflow-x-hidden bg-background">
      <ParticleBackground />
      <Navbar isLoggedIn={true} user={{ name: "Alex Chen", email: "alex@example.com" }} />

      <div className="relative z-10 mx-auto max-w-7xl px-4 pb-16 pt-24 sm:px-6 sm:pb-20 sm:pt-28 lg:px-8">
        <div className="mb-8 sm:mb-12">
          <h1 className="text-2xl font-bold text-foreground sm:text-3xl lg:text-4xl">
            Agent <span className="text-primary">Playground</span>
          </h1>
          <p className="mt-2 text-sm text-muted-foreground sm:text-base">
            Configure, deploy, and manage your autonomous agents with any model from OpenRouter.
          </p>
        </div>

        <div className="mx-auto max-w-2xl">
          {/* PlaygroundForm is wired in Plan 20-05 */}
        </div>
      </div>

      <Footer />
    </main>
  )
}
```

After this plan lands, the page imports and mounts `<PlaygroundForm />` inside that wrapper. Plan 20-03 exports:

```typescript
// frontend/components/playground-form.tsx
export function PlaygroundForm(): JSX.Element
```

No other change to the page is permitted (D-11 chrome preservation).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire <PlaygroundForm> into app/playground/page.tsx</name>
  <read_first>
    - frontend/app/playground/page.tsx (full file, post-Plan 20-02 state)
    - .planning/phases/20-frontend-alicerce/20-CONTEXT.md §D-11 — chrome preserved byte-for-byte
    - .planning/phases/20-frontend-alicerce/20-UI-SPEC.md §Form + result container (lines 94-103) — `<div className="mx-auto max-w-2xl">` wrapper already present from Plan 20-02
  </read_first>
  <action>
Apply a two-line edit to `frontend/app/playground/page.tsx`:

1. Add the import alongside the existing imports (alphabetized with the others):
```typescript
import { PlaygroundForm } from "@/components/playground-form"
```

2. Replace the placeholder comment inside the `<div className="mx-auto max-w-2xl">` wrapper with the component mount:
```diff
         <div className="mx-auto max-w-2xl">
-          {/* PlaygroundForm is wired in Plan 20-05 */}
+          <PlaygroundForm />
         </div>
```

Preserve EVERY other line byte-for-byte. Specifically:
- `"use client"` directive stays on line 1
- Navbar/ParticleBackground/Footer imports stay
- The `Navbar` props (`isLoggedIn={true}`, the "Alex Chen" fake user) stay — CONTEXT D-11 explicit
- The outer `<main className="relative min-h-screen overflow-x-hidden bg-background">` stays
- The orange `<h1>Agent <span className="text-primary">Playground</span></h1>` heading stays
- The subtitle `<p>` stays
- The container `<div className="relative z-10 mx-auto max-w-7xl px-4 pb-16 pt-24 sm:px-6 sm:pb-20 sm:pt-28 lg:px-8">` stays
- The inner `mb-8 sm:mb-12` heading wrapper stays

Run `pnpm tsc --noEmit` and `pnpm lint` and `pnpm build` after the edit. ALL must exit 0. This is the FINAL SC-09 gate.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/frontend && pnpm tsc --noEmit 2>&1 | tee /tmp/ts-check-20-05.log; ! grep -q "error TS" /tmp/ts-check-20-05.log && pnpm lint 2>&1 | tee /tmp/lint-20-05.log; ! grep -qE "^\\s*[0-9]+:[0-9]+\\s+error" /tmp/lint-20-05.log && pnpm build 2>&1 | tee /tmp/build-20-05.log; grep -qE "(Compiled successfully|Build completed|Generating static pages|✓ Compiled)" /tmp/build-20-05.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^import \\{ PlaygroundForm \\} from "@/components/playground-form"' frontend/app/playground/page.tsx` returns 1
    - `grep -cE "<PlaygroundForm />" frontend/app/playground/page.tsx` returns 1
    - `grep -cE "AgentConfigurator|PlaygroundSection" frontend/app/playground/page.tsx` returns 0 (verifies Plan 20-02's clean state persists)
    - `grep -cE "Alex Chen" frontend/app/playground/page.tsx` returns 1 (D-11 navbar prop preserved)
    - `grep -cE "Agent <span className=\"text-primary\">Playground</span>" frontend/app/playground/page.tsx` returns 1 (chrome h1 preserved)
    - `grep -cE "Configure, deploy, and manage your autonomous agents with any model from OpenRouter\\." frontend/app/playground/page.tsx` returns 1 (chrome subtitle preserved)
    - `grep -cE "mx-auto max-w-7xl" frontend/app/playground/page.tsx` returns 1 (outer container preserved)
    - `cd frontend && pnpm tsc --noEmit` exits 0
    - `cd frontend && pnpm lint` exits 0 (errors only; warnings acceptable)
    - `cd frontend && pnpm build` exits 0
  </acceptance_criteria>
  <done>
    `/playground` page imports and mounts `<PlaygroundForm />` inside the UI-SPEC wrapper; chrome is preserved byte-for-byte; all three quality gates (tsc, lint, build) are green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual SC-11 smoke-run (the Phase 20 exit gate)</name>
  <what-built>
    The full Phase 20 tree is now assembled:
    - `frontend/lib/api.ts` — extended with `ApiError.headers` and `apiPost(path, body?, headers?)` (Plan 20-01)
    - `frontend/lib/api-types.ts` — TS mirrors + `UiError` + `parseApiError` + `parseRetryAfter` + `useRetryCountdown` (Plan 20-01)
    - The 5 mock components are deleted; `/playground` has an empty slot waiting for the form (Plan 20-02)
    - `frontend/components/playground-form.tsx` — the 4-field form with recipe fetch, BYOK transport, 6 error states (Plan 20-03)
    - `frontend/components/run-result-card.tsx` — the pure verdict display with category badge map + stderr accordion (Plan 20-04)
    - `frontend/app/playground/page.tsx` — mounts `<PlaygroundForm>` inside the preserved chrome (Task 1 of this plan)
    - Automated `pnpm tsc --noEmit` + `pnpm lint` + `pnpm build` are all green (this is what Task 1 just verified)

    What is NOT built: no frontend test framework (D-14 deferred to Phase 20.1 per planner decision). No SSE streaming (Phase 21). No auth (Phase 22+). No mobile polish (Phase 20.1). No BYOK persistence (Phase 20.2).
  </what-built>
  <how-to-verify>
    **You will drive a live run against the local stack. This is the SC-01..SC-11 acceptance gate.** Take your time.

    PRE-SETUP:
    1. Kill any previously-running dev processes (user rule: always kill before start):
       ```bash
       pkill -f "next dev" 2>/dev/null; pkill -f "uvicorn api_server" 2>/dev/null; true
       docker compose -f docker-compose.dev.yml down 2>/dev/null; true
       ```
    2. Have an OpenRouter API key ready (or Anthropic / OpenAI). This is your BYOK.
    3. Open a NEW private browser window (to guarantee zero cached state) AND the browser DevTools (Console + Network tabs visible).

    SC-01 — Local stack boots:
    - Run `make dev-api-local` — wait for `curl -sf http://127.0.0.1:8000/healthz` to return 200
    - Run `make dev-frontend` in a separate shell — wait for "Local: http://localhost:3000" in the output
    - ✅ PASS if both come up cleanly.

    SC-02 — Recipes populate from the API:
    - Open `http://localhost:3000/playground` in the private browser window
    - In DevTools Network tab, filter by `recipes`. Confirm a single `GET /api/v1/recipes` request returns 200 with a JSON body containing 5 recipes: `hermes`, `nanobot`, `nullclaw`, `openclaw`, `picoclaw` (in any order).
    - Visually confirm the Recipe `<select>` is populated with those 5 options in **alphabetical order** (Plan 20-03's sort).
    - Confirm the `<select>` placeholder transitioned from "Loading recipes…" to "Select a recipe…" after the fetch.
    - ✅ PASS if the list has exactly 5 real recipes and NONE of `defaultClones` / `openRouterModels` / channel literals appear in the page.

    SC-03 + SC-04 — Deploy round-trips to a real verdict:
    - Select a recipe (`picoclaw` works; try `hermes` second)
    - Type model: `openai/gpt-4o-mini` (or any model your key supports)
    - Paste your BYOK key into the API key field (masked as `•••••`)
    - Prompt: `hello, what is your name?`
    - Click Deploy.
    - In the Network tab, find the `POST /api/v1/runs` request. Click it.
    - ✅ PASS Request tab shows `Authorization: Bearer sk-...` in request headers.
    - ✅ PASS Request payload body shows `{"recipe_name":"...","model":"...","prompt":"..."}` — NOT containing the key.
    - Wait for the response (typically 8–30s depending on model latency).
    - ✅ PASS A verdict card appears below the form containing: colored verdict badge, category pill, `exit_code`, `wall_time` (e.g., `11.80s`), `run_id` (26-char ULID), and a `stderr tail (N lines)` accordion (expanded if non-PASS, collapsed if PASS).

    SC-05 — BYOK key never leaks:
    - Note the first few characters of your BYOK key (e.g., `sk-or-v1-abc…`).
    - Browser Console tab: type `$$('input')` and visually confirm no element's `.value` contains the key.
    - Network tab: open the `POST /v1/runs` request → Preview tab → confirm the key value is in the `Authorization` header only, never in the request JSON body.
    - React DevTools (install if needed): select the `PlaygroundForm` component and check the `byok` state var — ✅ PASS if it's the empty string `""` (cleared post-submit per Plan 20-03's finally block).
    - In a shell: `docker compose -f docker-compose.dev.yml logs api_server 2>&1 | grep -F "$(your key's first 16 chars)" | wc -l` → MUST return `0` (Plan 19-06's log redaction plus client discipline).
    - ✅ PASS if every check above holds.

    SC-06 — Error paths render (provoke 4 of the 6):
    - **422 validation**: clear the prompt field, click Deploy while it's empty → button stays disabled (validation is client-gated too; acceptable). Alternative: open DevTools, type a truly invalid model like `🦄/bad-model-🦄` and click Deploy. If the server 422s, the inline error renders under the field with red text.
    - **401 unauthorized**: type garbage into the BYOK field (`sk-invalid-foo`), deploy → red destructive alert `Invalid or missing API key` appears. Request ID is present. ✅ PASS.
    - **Network**: stop the api_server container (`docker compose -f docker-compose.dev.yml stop api_server`), click Deploy → `Could not reach API` alert appears with a Retry button. Restart the container (`... start api_server`) and click Retry → Deploy proceeds. ✅ PASS.
    - **429 rate-limit**: (optional — hard to provoke locally; can be skipped if you confirmed Plan 19-05 rate-limit middleware is installed). If hit in a later deploy, the banner should show `Rate limited` with a live countdown.
    - ✅ PASS if at least 3 of 4 observable error paths render per UI-SPEC §Error State Visual Specs.

    SC-10 — Multi-run replaces the old verdict, persists as separate DB rows:
    - Deploy once, note the `run_id` (copy it via the button — ✅ the icon swaps Copy → Check for ~1.5s).
    - Change prompt to `count to three`, Deploy again.
    - ✅ The old verdict card is REPLACED by the new one (not accumulated). Prior `run_id` is gone from the UI.
    - In a shell:
      ```
      docker compose -f docker-compose.dev.yml exec postgres psql -U temporal -d agent_playground -c "SELECT id, created_at FROM runs ORDER BY created_at DESC LIMIT 2;"
      ```
    - ✅ PASS if 2 distinct rows with different `id` values and different `created_at` timestamps appear.

    SC-07 / SC-08 — mock catalog / configurator absent:
    - Shell: `grep -RE "defaultClones|openRouterModels" frontend/app/playground/ frontend/components/ 2>/dev/null | wc -l` → MUST return 0.
    - Shell: `test ! -f frontend/components/agent-configurator.tsx && echo OK` → MUST print `OK`.
    - ✅ PASS if both.

    SC-09 — Build + lint:
    - ✅ Already verified in Task 1's automated gate (`pnpm tsc --noEmit && pnpm lint && pnpm build` green).

    SC-11 — Unblock Hetzner deploy:
    - If every SC-01..SC-10 above is green, the Phase 19 Hetzner deploy is unblocked.
    - In your approval message, list every SC you verified and any that were skipped with reason.

    AFTER VERIFICATION (whether pass or fail):
    - Shut down cleanly:
      ```bash
      pkill -f "next dev" 2>/dev/null; true
      docker compose -f docker-compose.dev.yml down
      ```
  </how-to-verify>
  <resume-signal>
    Type `approved` and paste a short bullet list of which SCs passed (SC-01 through SC-11) OR describe any failure with enough detail for the next agent to reproduce + fix. If a plan change is required (e.g., a copy string needs adjustment, an error state renders wrong), describe it and the orchestrator will kick it back for revision instead of continuing to Task 3.
  </resume-signal>
  <action>
See the full step-by-step runbook in `<how-to-verify>` above. This is a human-driven checkpoint — the executor cannot proceed past this gate without a human returning the `approved` resume-signal plus a SC-by-SC pass/skip record. The executor's job is to (1) confirm Plan 20-05 Task 1 landed and `pnpm build` is green, (2) surface the runbook in `<how-to-verify>` to the human, (3) wait.
  </action>
  <verify>
    <automated>echo "BLOCKING CHECKPOINT — requires human approval per resume-signal. Executor must surface <how-to-verify> runbook and wait."; exit 0</automated>
  </verify>
  <done>
Human returns `approved` in the resume-signal with a SC-01..SC-11 pass/skip record. If any SC fails, the checkpoint returns control to the orchestrator for replanning instead of advancing to Task 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 20-05-SUMMARY.md with the SC-11 unblock record and update STATE.md</name>
  <read_first>
    - .planning/STATE.md — the `status: deploy_blocked_pending_dumb_playground` marker and the resume-anchor at "📍 RESUME ANCHOR"
    - .planning/phases/19-api-foundation/RESUME-TOMORROW.md (if present) — the anchor that documented why deploy was cancelled
    - .planning/phases/20-frontend-alicerce/20-CONTEXT.md §SC-11 — "After SC-01..SC-10 are green locally, the Phase 19 Hetzner deploy is unblocked"
    - The human verification output captured during Task 2's checkpoint
  </read_first>
  <action>
Only runs AFTER Task 2's checkpoint returned `approved`. If the checkpoint reported failures, STOP and return control to the orchestrator — do NOT write the SUMMARY as if it passed.

Two artifacts to produce:

**Artifact 1 — `.planning/phases/20-frontend-alicerce/20-05-SUMMARY.md`:**

Markdown file with these sections:

```markdown
# Phase 20 Plan 05 — Summary

**Completed:** {ISO date from Task 2 approval}
**Exit gate:** SC-11 PASSED — Phase 19 Hetzner deploy is UNBLOCKED

## Changes landed in this plan

- `frontend/app/playground/page.tsx` — added `import { PlaygroundForm } from "@/components/playground-form"` and replaced the empty-slot placeholder with `<PlaygroundForm />`.
- Chrome preserved byte-for-byte per D-11.
- `pnpm tsc --noEmit` / `pnpm lint` / `pnpm build` all exit 0.

## SC-01..SC-11 verification record

Paste the user's approval bullet list verbatim here, one line per SC, with PASS/SKIP/N-A and any notes.

## D-14 disposition (planner decision)

DEFERRED to Phase 20.1 per RESEARCH Q10. No frontend test framework installed in Phase 20. Grep-based structural assertions (SC-07, SC-08) + manual SC-11 smoke pass cover the phase's acceptance. Phase 20.1 is the natural place to stand up Vitest + Playwright once the UI shape is stable.

## Follow-ups for future phases

- **Phase 20.1 (polish):** mobile-first refinement (FND-04), test framework, focus-management audit.
- **Phase 20.2:** BYOK persistence via settings page once auth lands.
- **Phase 21:** SSE streaming upgrade (`GET /v1/runs/{id}/events`) — replaces the blocking Deploy with progressive render.
- **Open:** the comment at the top of `frontend/lib/api.ts` still says "Go API" — stale after the Python pivot. Deliberately left unchanged this phase (CONTEXT D-01 scope discipline). Fix in Phase 20.1 or a quick-task.

## Next action for the orchestrator

Phase 20's SC-11 is green. The next command is `/gsd-execute-phase 19 --wave 4` (to run the previously-cancelled 19-07 Task 3 Hetzner deploy), with STATE.md updated to reflect the new state.
```

**Artifact 2 — `.planning/STATE.md` update:**

Edit these three fields in the frontmatter:
```yaml
status: ready_for_hetzner_deploy
stopped_at: "Phase 20 shipped; 19-07 Task 3 Hetzner deploy is now unblocked"
last_updated: "{current ISO timestamp}"
```

Also in the body, under `## 📍 RESUME ANCHOR — READ THIS FIRST AFTER /clear`, change the "**The next command is:**" line from the Phase 20 discuss-phase suggestion to:
```
**The next command is:** `/gsd-execute-phase 19 --wave 4` — run the previously-cancelled Hetzner deploy now that the dumb playground is live.
```

DO NOT rewrite the rest of STATE.md. Do NOT add new top-level sections. The minimum edit keeps the handoff legible.
  </action>
  <verify>
    <automated>test -f /Users/fcavalcanti/dev/agent-playground/.planning/phases/20-frontend-alicerce/20-05-SUMMARY.md && grep -qE "SC-11 PASSED" /Users/fcavalcanti/dev/agent-playground/.planning/phases/20-frontend-alicerce/20-05-SUMMARY.md && grep -q "ready_for_hetzner_deploy" /Users/fcavalcanti/dev/agent-playground/.planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/20-frontend-alicerce/20-05-SUMMARY.md` exists
    - `grep -E "^## SC-01\\.\\.SC-11 verification record$" .planning/phases/20-frontend-alicerce/20-05-SUMMARY.md` returns 1 match
    - `grep -E "SC-11 PASSED" .planning/phases/20-frontend-alicerce/20-05-SUMMARY.md` returns >= 1 match
    - `grep -E "D-14 disposition" .planning/phases/20-frontend-alicerce/20-05-SUMMARY.md` returns 1 match
    - `.planning/STATE.md` frontmatter `status:` is now `ready_for_hetzner_deploy` (grep -E "^status: ready_for_hetzner_deploy$" .planning/STATE.md returns 1 match)
    - `.planning/STATE.md` contains "`/gsd-execute-phase 19 --wave 4`" — the next-command pointer is updated
    - No other sections in STATE.md are rewritten beyond the three frontmatter fields + the one resume-anchor line
  </acceptance_criteria>
  <done>
    The phase summary records the full SC verification table + the D-14 disposition + the orchestrator's next step. STATE.md reflects that deploy is unblocked. A fresh agent after `/clear` reads this file and knows to resume with Phase 19 Plan 07 Task 3.
  </done>
</task>

</tasks>

<verification>
After all three tasks:
- `frontend/app/playground/page.tsx` mounts `<PlaygroundForm>` with chrome preserved
- `pnpm build` + `pnpm lint` + `pnpm tsc --noEmit` are green
- A manual SC-01..SC-10 smoke pass confirmed the end-to-end round-trip
- `20-05-SUMMARY.md` documents the SC-11 unblock
- `.planning/STATE.md` reflects the new `ready_for_hetzner_deploy` state
</verification>

<success_criteria>
Plan 20-05 is complete when:
1. `<PlaygroundForm>` is mounted on `/playground` with D-11 chrome preserved.
2. All automated quality gates (tsc, lint, build) are green.
3. The human smoke-pass checkpoint returned `approved` with SC-01..SC-10 confirmed.
4. `20-05-SUMMARY.md` documents SC-11 PASSED and the D-14 defer decision.
5. `.planning/STATE.md` is updated so the next command is `/gsd-execute-phase 19 --wave 4`.
</success_criteria>

<output>
The `20-05-SUMMARY.md` artifact IS the phase exit record. No other SUMMARY for this plan.
</output>
