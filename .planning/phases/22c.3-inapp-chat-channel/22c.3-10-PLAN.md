---
phase: 22c.3-inapp-chat-channel
plan: 10
type: execute
wave: 4
depends_on: [22c.3-09]
files_modified:
  - recipes/hermes.yaml
files_created: []
autonomous: true
worktree_safe: false  # the recipe is consumed by the live api_server (mounted via :ro volume in docker-compose)
requirements: [D-03, D-20, D-21, D-36, D-37]
must_haves:
  truths:
    - "recipes/hermes.yaml has a new top-level channels.inapp block: transport=http_localhost, port=8642, endpoint=/v1/chat/completions, contract=openai_compat, auth_header references INAPP_AUTH_TOKEN"
    - "channels.inapp.activation_env declares the 4 hermes-specific env vars: API_SERVER_ENABLED=true, API_SERVER_KEY=${INAPP_AUTH_TOKEN}, API_SERVER_PORT=8642, API_SERVER_HOST=127.0.0.1"
    - "channels.inapp.ready_log_regex matches the empirically-verified hermes log line `[Api_Server] API server listening on http://127.0.0.1:8642`"
    - "channels.inapp.verified_cells contains a 2026-04-29 entry referencing spikes/recipe-hermes.md verbatim PASS evidence + Plan 22c.3-01's wave-0 revalidation matrix"
    - "After api_server reload (or recipe SIGHUP), hermes recipe is parseable + GET /v1/recipes/hermes shows the new channels.inapp block (or its sanitized projection)"
    - "Existing channels.telegram block is UNTOUCHED — additive change only (D-20: inapp + telegram coexist on the schema; v1 is single-channel-per-instance)"
  artifacts:
    - path: "recipes/hermes.yaml"
      provides: "channels.inapp block declaring native HTTP server activation"
      contains: "transport: http_localhost"
  key_links:
    - from: "recipes/hermes.yaml channels.inapp.ready_log_regex"
      to: "spikes/recipe-hermes.md verbatim log line at line 33"
      via: "regex must match `[Api_Server] API server listening on http://127.0.0.1:8642`"
      pattern: "Api_Server\\\\] API server listening"
---

<objective>
Add a `channels.inapp` block to `recipes/hermes.yaml`. Hermes is the **easiest of the 5** — it ships a built-in OpenAI-compat HTTP server (`gateway.platforms.api_server`) activated purely by env vars (`API_SERVER_ENABLED=true`, `API_SERVER_KEY`, `API_SERVER_PORT`, `API_SERVER_HOST`). No persistent_argv_override needed; no sidecar; no upstream patch.

The block declares the 4 env vars + the loopback port (8642) + the endpoint path + the auth header template + the empirically-verified ready_log_regex. The runner (Plan 22c.3-09 wired the lifespan; per-channel start in `routes/agent_lifecycle.py::start_persistent` already handles channel_inputs — but minting the per-session `INAPP_AUTH_TOKEN` and persisting it to `agent_containers.inapp_auth_token` is OUT OF SCOPE for this plan; that's a small extension to start_persistent which Wave 5's e2e exercises end-to-end).

Per D-20: the block is **additive** with the existing `channels.telegram` block. v1 is single-channel-per-agent-instance; the schema already accepts both blocks coexisting on the recipe.

Purpose: First of 5 recipes to opt-in to inapp channel.
Output: hermes.yaml with channels.inapp; recipe still parses + GET /v1/recipes/hermes works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/22c.3-inapp-chat-channel/22c.3-CONTEXT.md
@.planning/phases/22c.3-inapp-chat-channel/22c.3-RESEARCH.md
@.planning/phases/22c.3-inapp-chat-channel/22c.3-PATTERNS.md
@.planning/phases/22c.3-inapp-chat-channel/spikes/recipe-hermes.md
@recipes/hermes.yaml

<interfaces>
**From spikes/recipe-hermes.md (verbatim):**
- Hermes container exposes `POST /v1/chat/completions` on port 8642 when `API_SERVER_ENABLED=true` + `API_SERVER_KEY=<bearer>` env vars are set.
- Activation: env-only. NO config-file edits required.
- Ready log line: `INFO gateway.platforms.api_server: [Api_Server] API server listening on http://127.0.0.1:8642 (model: hermes-agent)`.
- Auth: `Authorization: Bearer <API_SERVER_KEY>` required for /v1/*.
- Documented endpoints: POST /v1/chat/completions, POST /v1/responses, GET /v1/models, POST /v1/runs, GET /v1/runs/{id}/events, GET /health.

**Recipe block (to ADD verbatim) per RESEARCH per-recipe matrix line 851 + spikes/recipe-hermes.md verdict block (lines 60-74):**

```yaml
channels:
  # ... existing telegram block stays as-is ...

  # ============================================================================
  # Phase 22c.3 — in-app chat channel (D-20: additive with telegram).
  # Empirically verified 2026-04-29 (spikes/recipe-hermes.md): hermes ships a
  # native OpenAI-compatible HTTP server in gateway.platforms.api_server.
  # Activation is env-only — no upstream patch, no sidecar.
  # ============================================================================
  inapp:
    transport: http_localhost           # D-04: only verb in v1 (5/5 recipes uniform)
    port: 8642                          # API_SERVER_PORT default
    endpoint: /v1/chat/completions      # OpenAI-compat
    contract: openai_compat
    contract_model_name: "hermes-agent" # the bot answers when client sends model=hermes-agent
    auth_header: "Authorization: Bearer ${INAPP_AUTH_TOKEN}"
    ready_log_regex: "\\[Api_Server\\] API server listening on http://127\\.0\\.0\\.1:8642"
    response_routing: per_session       # 1:1 with the agent_instance
    activation_env:
      # Runner injects ${INAPP_AUTH_TOKEN} as a per-session opaque token at
      # container start time + persists it to agent_containers.inapp_auth_token
      # so the dispatcher (Plan 22c.3-05) can forward it as the Bearer.
      API_SERVER_ENABLED: "true"
      API_SERVER_KEY: "${INAPP_AUTH_TOKEN}"
      API_SERVER_PORT: "8642"
      API_SERVER_HOST: "127.0.0.1"
    verified_cells:
      - date: "2026-04-29"
        boot_wall_s: 14
        first_reply_wall_s: 0           # local test = N/A (real e2e in Plan 22c.3-15)
        verdict: PASS
        notes: |
          Empirically verified via spikes/recipe-hermes.md (2026-04-29):
          docker run with API_SERVER_ENABLED=true + API_SERVER_KEY=<token>
          starts the api_server platform on port 8642 within ~14s. /health
          returns 200 unauthenticated; /v1/models returns 200 with the
          hermes-agent model entry on Bearer-authenticated requests.
          Wave-0 revalidation (Plan 22c.3-01) confirmed no regression.
        category: PASS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add channels.inapp block to recipes/hermes.yaml + verify parse</name>
  <files>recipes/hermes.yaml</files>
  <read_first>
    - recipes/hermes.yaml (full file — find the existing `channels:` key + the `telegram:` block; the new `inapp:` is a sibling key)
    - .planning/phases/22c.3-inapp-chat-channel/spikes/recipe-hermes.md (verbatim ready log line; activation env vars; bearer auth)
    - .planning/phases/22c.3-inapp-chat-channel/spikes/wave-0-revalidation.md (Plan 22c.3-01 confirmation that hermes still PASSes)
  </read_first>
  <action>
**File: `recipes/hermes.yaml`** — locate the `channels:` top-level key (around line 263). Below the existing `telegram:` block, ADD a sibling `inapp:` block with the EXACT YAML from `<interfaces>` block above.

Important details:
- **Indentation:** `inapp:` is 2 spaces (sibling of `telegram:`). All keys under `inapp:` are 4 spaces.
- **Regex escaping:** the ready_log_regex uses YAML double-quoted form so `\\[` and `\\.` are literal regex backslashes (not YAML escapes).
- **Do NOT remove** the existing telegram block, direct_interface, persistent block, or any other keys — additive change.
- **Do NOT remove** the existing comment block above `channels:` referring to deferred channels.

After edit, verify the YAML parses + the recipe loader accepts it:

```bash
# 1. Pure YAML parse check.
python -c "import yaml; d = yaml.safe_load(open('recipes/hermes.yaml')); print(list(d.get('channels', {}).keys()))"
# Expected: ['telegram', 'inapp']

# 2. Recipe loader accepts it (the api_server's loader has no `inapp` block
#    in its current schema — but the loader is permissive; unknown blocks
#    are ignored). Verify by hitting the live /v1/recipes endpoint:
curl -sf http://localhost:8000/v1/recipes/hermes | python -c "import sys, json; d = json.load(sys.stdin); print(d.get('channels', {}).keys())"
# Expected: dict_keys with telegram + inapp (or whatever sanitized projection
# the recipes route returns; minimum bar: api_server boot didn't fail).

# 3. api_server still healthy after the recipe change (recipes are loaded
#    at boot per main.py line 80; need a restart to re-load):
docker compose --env-file deploy/.env.prod \
  -f deploy/docker-compose.prod.yml \
  -f deploy/docker-compose.local.yml \
  restart api_server
sleep 8
curl -sf http://localhost:8000/healthz
# Expected: {"ok":true}

docker logs deploy-api_server-1 2>&1 | tail -50 | grep -E "load_all_recipes|recipe.*hermes" | tail -5
# Expected: at most 0 errors related to hermes.
```

**Recipe-loader compatibility note:** if the existing recipe loader at `services/recipes_loader.py` has STRICT schema validation (rejects unknown top-level keys under `channels`), the api_server will refuse to boot. In that case:
- Read `services/recipes_loader.py`. If it strict-validates `channels.*` against a closed enum, RELAX the validator to PASS-THROUGH any unknown channel names (treat them as forward-compat extension points, since recipe authors define the schema per D-03).
- A 2-line fix to the loader: skip strict validation for `channels.*` blocks; preserve the dict as-is for downstream consumers.
- This is a SCOPE EXPANSION of Plan 22c.3-10. Document the deviation in the SUMMARY.

If the loader DOES NOT strict-validate (likely — recipes/openclaw.yaml etc. already have varied custom blocks), no loader change is needed.
  </action>
  <verify>
    <automated>python -c "import yaml; d = yaml.safe_load(open('recipes/hermes.yaml')); assert 'inapp' in d.get('channels', {}), 'inapp not in channels'; assert d['channels']['inapp']['transport'] == 'http_localhost'; assert d['channels']['inapp']['port'] == 8642" && curl -sf http://localhost:8000/healthz | grep -q '"ok":true'</automated>
  </verify>
  <done>recipes/hermes.yaml has the 8-key channels.inapp block. YAML parses. api_server still boots + /healthz green after recipe reload. If recipes_loader needed relaxation, that is documented in SUMMARY.</done>
</task>

</tasks>

<verification>
- `python -c "import yaml; ..."` parse check passes.
- api_server `/healthz` returns ok after `docker compose restart api_server`.
- `grep -q "transport: http_localhost" recipes/hermes.yaml`.
- `grep -q "API_SERVER_ENABLED" recipes/hermes.yaml` (activation env present).
- Existing telegram block UNTOUCHED — confirmed by `wc -l recipes/hermes.yaml` showing strictly more lines than before AND `grep -c "ready_log_regex" recipes/hermes.yaml` returning 2 (one telegram, one inapp).
</verification>

<success_criteria>
- channels.inapp block authoritatively declared on hermes recipe.
- ready_log_regex matches the spike's verified log line.
- activation_env declares all 4 hermes-specific env vars.
- verified_cells references the 2026-04-29 evidence.
- D-20 honored: telegram coexists with inapp on the schema.
</success_criteria>

<output>
After completion, create `.planning/phases/22c.3-inapp-chat-channel/22c.3-10-SUMMARY.md` documenting:
- The added YAML block (verbatim).
- Recipe-loader compatibility verdict (no change needed / 2-line relaxation applied).
- Verbatim `curl /v1/recipes/hermes` projection showing channels.inapp surfaced.
- Verbatim docker logs after restart showing no recipe-load errors.
- The need for `start_persistent` (in agent_lifecycle.py) to mint and inject INAPP_AUTH_TOKEN — flagged as a Wave 4/5 prerequisite for the e2e gate (Plan 22c.3-15 will need it).
</output>
