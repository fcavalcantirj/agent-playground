---
phase: 22c.3-inapp-chat-channel
plan: 14
subsystem: recipes
tags: [yaml, recipe, nullclaw, inapp, http_localhost, a2a_jsonrpc, channels, persistent_argv_override, native_a2a, google_a2a, jsonrpc_2_0, round_3_supersession]

# Dependency graph
requires:
  - phase: 22c.3-01
    provides: Wave-0 spike re-validation evidence for nullclaw (spikes/recipe-nullclaw.md, revised v3 native A2A — 2026-04-30 FULL_PASS); empirical /a2a message/send + message/stream round-trips against ap-recipe-nullclaw:latest with agent-card protocolVersion=0.3.0
  - phase: 22c.3-04
    provides: inapp_messages store API (consumes recipe metadata via recipe_name FK on agent_instances)
  - phase: 22c.3-05
    provides: InappRecipeIndex parser + 3-way contract switch + a2a_jsonrpc adapter (the consumer of contract='a2a_jsonrpc' + endpoint=/a2a; dispatcher hardcodes message/send envelope and reply_path=$.result.artifacts[0].parts[0].text)
  - phase: 22c.3-09
    provides: Lifespan-attached dispatcher/reaper/outbox tasks + per-session Docker bridge net (will pump messages routed through this recipe's channels.inapp once Plan 22c.3-15 e2e gates the flow)
  - phase: 22c.3-10
    provides: hermes channels.inapp pattern reference (env-only inapp recipe; first openai_compat block)
  - phase: 22c.3-11
    provides: nanobot channels.inapp pattern reference (first persistent_argv_override entrypoint-swap; D-20 pattern)
  - phase: 22c.3-12
    provides: openclaw channels.inapp pattern reference (config-flag activation + bearer auth + contract_model_name override pattern)
  - phase: 22c.3-13
    provides: zeroclaw channels.inapp pattern reference (image_pull build mode + zeroclaw_native contract + idempotency_header + session_header)

provides:
  - "nullclaw recipe declares channels.inapp on the v0.2 schema (FIFTH and FINAL inapp recipe opt-in for Phase 22c.3 Wave 4 — completes the 5-recipe matrix: hermes/nanobot/openclaw via openai_compat + zeroclaw via zeroclaw_native + nullclaw via a2a_jsonrpc)"
  - "First recipe with channels.inapp.contract='a2a_jsonrpc' — third per-contract adapter in the dispatcher's 3-way contract switch (alongside openai_compat for hermes/nanobot/openclaw and zeroclaw_native for zeroclaw)"
  - "First recipe to wire native Google A2A JSON-RPC 2.0 protocol (POST /a2a with method=message/send) — empirically validated against nullclaw's built-in a2a.enabled=true gateway path on port 3000"
  - "First recipe with channels.inapp.streaming.method='message/stream' — declares the SSE token-streaming path (POST /a2a body method=message/stream returning text/event-stream chunks with result.append=true on deltas + result.lastChunk=true on final); reservation for future SSE-fallback work, orthogonal to Phase 22c.3 sync scope"
  - "First recipe with channels.inapp.response_envelope.task_id_path + state_path — A2A task-tracking metadata for future workflow-orchestration features"
  - "channels.inapp.persistent_argv_override.argv[1] is a single sh -c chain that writes /nullclaw-data/config.json + chowns + execs `nullclaw gateway --host 0.0.0.0 --port 3000` — NO sidecar, NO python3, NO apk add, NO nohup, NO INAPP_BOT_BIN; the native nullclaw gateway IS the bridge (Round-3 supersession of v2 sidecar pattern)"
  - "Live api_server's GET /v1/recipes shows channels_supported=['telegram', 'inapp'] for nullclaw"
  - "InappRecipeIndex.get_inapp_block('nullclaw') returns InappChannelConfig(contract='a2a_jsonrpc', port=3000, endpoint='/a2a', auth_mode='none') — verified inside live deploy-api_server-1 container"

affects: [22c.3-15, 23-flutter]

# Tech tracking
tech-stack:
  added: []  # additive YAML edit; zero new deps
  patterns:
    - "Native A2A JSON-RPC 2.0 contract pattern — when a recipe's bot ships native Google A2A protocol support (message/send + message/stream + /.well-known/agent-card.json), declare contract='a2a_jsonrpc' + endpoint='/a2a'. The dispatcher's a2a_jsonrpc adapter (services/inapp_dispatcher.py:168-193) hardcodes the JSON-RPC envelope shape per the A2A spec (jsonrpc=2.0, id=row['id'], method='message/send', params.message.role='user', params.message.parts=[{kind:text,text:row['content']}], params.message.messageId=row['id']) and the reply parse path (data.result.artifacts[0].parts[0].text). Recipe's request_envelope and response_envelope blocks are documentation that mirrors the dispatcher's behavior so future generic-contract paths can read them programmatically. Adding a 4th contract is a Literal[...] extension on InappChannelConfig.contract + a new match arm in dispatcher; no schema change."
    - "Activation-via-config-file pattern (heredoc-write) — when a bot has no env-var-only activation path, persistent_argv_override.argv[1] is an inline sh -c that writes the config file under the bot's data dir (cat > /nullclaw-data/config.json <<EOF ... EOF), chowns the dir for the non-root container user (chown -R nobody:nobody /nullclaw-data), then execs the bot with the config-aware flags (exec nullclaw gateway --host 0.0.0.0 --port 3000). Same shape as zeroclaw's pre_start_commands chain (Plan 22c.3-13), nanobot's persistent_argv_override (Plan 22c.3-11), and openclaw's openclaw.json heredoc (Plan 22c.3-12). The activation_env block declares the env-vars consumed by the heredoc's ${...} expansions at sh exec time."
    - "Round-3 supersession discipline — when a prior round's pattern is superseded mid-phase (here: v2 sidecar HTTP-to-CLI bridge → v3 native A2A protocol), the recipe MUST cite the spike file at the latest version (spikes/recipe-nullclaw.md revised v3) in verified_cells.evidence and avoid re-introducing the superseded pattern's tokens (sidecar, python3, apk add, INAPP_BOT_BIN, nohup) in any field. Plan must_haves.truths line items #1 + #5 + #8 enforce this; the verify gates (grep against persistent_argv_override + activation_env strings) catch regressions before commit."

key-files:
  created: []
  modified:
    - "recipes/nullclaw.yaml — channels.inapp block added (~129 lines inserted between channels.telegram's verified_cells final entry and the trailing `# Discord, Slack, ...` deferred-channels comment; sibling to channels.telegram, additive per D-20)"

key-decisions:
  - "Sibling-to-channels.telegram placement (D-20 additive) — channels.telegram block (lines 297-378) UNTOUCHED; channels.inapp inserted at line 380 as a new key under channels:; trailing deferred-channels comment at original line 380 pushed to current line 488. Same convention as Plans 22c.3-10/11/12/13: validated channels live with telegram, deferred candidates come after."
  - "Native A2A protocol over v2 sidecar (Round-3 supersession) — the recipe declares contract='a2a_jsonrpc' + endpoint='/a2a' instead of the Round-2 sidecar pattern (runtime python3 install + HTTP-to-CLI bridge at port 18791). Round-3 evidence: spikes/recipe-nullclaw.md v3 PROVES nullclaw's built-in A2A path (a2a.enabled=true in config.json + gateway.require_pairing=false) is empirically reachable with `protocolVersion=0.3.0`. The v2 sidecar approach is fully dropped — none of its tokens (sidecar, python3, apk add, INAPP_BOT_BIN, nohup) appear anywhere in the recipe."
  - "auth_mode='none' (loopback-trusted via per-session bridge net) — gateway.require_pairing=false in the heredoc-written config.json disables nullclaw's pairing gate; auth lives upstream at routes/agent_messages.py::POST /v1/agents/:id/messages via require_user. The dispatcher reaches port 3000 only over the per-session Docker bridge network (Plan 22c.3-09), never the public internet. Same conservative posture as nanobot (auth_mode='none', loopback-trusted) and orthogonal to openclaw's bearer mode."
  - "host=0.0.0.0 + allow_public_bind=true — required because the dispatcher reaches the container via its bridge IP (NetworkSettings.Networks[<bridge>].IPAddress), NOT loopback. Without `gateway.host=0.0.0.0` the gateway only binds 127.0.0.1 inside the container's network namespace, unreachable from the bridge. Without `gateway.allow_public_bind=true` nullclaw refuses non-loopback hosts as a safety check. The combination is empirically required (spike line 24 verbatim: `\"host\":\"0.0.0.0\",\"allow_public_bind\":true`)."
  - "Heredoc uses `openrouter/$MODEL` (WITH `openrouter/` prefix) — vs. openclaw's bare `${MODEL}` (Plan 22c.3-12 Rule-1 deviation). nullclaw's config.json model field expects the FULL provider/path/model id (per Dockerfile config stage which bakes `\"primary\":\"openrouter/anthropic/claude-sonnet-4\"`); the runner injects `MODEL=anthropic/claude-haiku-4.5` (the bare model id without the openrouter/ prefix), so the heredoc must add `openrouter/` to construct the full primary path. Spike line 22 verbatim: `\"primary\":\"openrouter/anthropic/claude-haiku-4.5\"`. This is opposite to openclaw because openclaw's runner-injected MODEL already carries the provider prefix per its v0.2 schema position."
  - "ready_log_regex='Gateway listening on' — derived empirically from spike boot log line 62 verbatim: `Gateway listening on 0.0.0.0:3000`. Matches the prefix; literal, no escapes needed."
  - "verified_cells[0] scoped to channels.inapp (NOT recipe-level smoke verified_cells) — per Task 2 ACTION explicit note. Recipe-level verified_cells (used by tools/run_recipe.py for its own pass_if checks) are preserved at lines 133-156 (the 2026-04-15 PASS for nullclaw agent -m smoke-mode); channels.inapp.verified_cells[0] is a NEW scoped block inside the inapp channel that documents A2A-shape validation at 2026-04-30."
  - "Live api_server restart performed — load_all_recipes caches the YAML at lifespan boot into app.state.recipes; bind-mounted file changes don't propagate until the FastAPI lifespan re-runs. Restarted via `docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml restart api_server`; /healthz=200 within ~5s; GET /v1/recipes shows channels_supported=['telegram', 'inapp'] for nullclaw; InappRecipeIndex.get_inapp_block('nullclaw') inside the live container returns the expected InappChannelConfig."
  - "Plan <verify> --automated cmd typo (Rule 3 deviation) — plan's verify command uses `python tools/run_recipe.py validate recipes/nullclaw.yaml` but `validate` is not a subcommand on tools/run_recipe.py; the correct flag is `--lint` (validates against schema and exits). Substituted: ran `python tools/run_recipe.py --lint recipes/nullclaw.yaml`, which surfaces the same pre-existing schema-lint failure documented in deferred-items.md 'From Plan 22c.3-13' (channels.telegram-only schema rejects the inapp block — Rule 4 architectural item, out of scope). Used the same Rule-3 substitute as Plan 13: direct `python3 -c \"import yaml; yaml.safe_load(...)\"` parse + `InappRecipeIndex.get_inapp_block(...)` programmatic check + live api_server `/v1/recipes` registry projection. Equivalent semantic gates (the recipe must parse + the index must accept it + the live registry must surface it)."

patterns-established:
  - "a2a_jsonrpc contract pattern — first recipe to wire the third dispatcher contract adapter. Future recipes that ship native Google A2A protocol (message/send + agent-card with protocolVersion=0.3.0) follow this shape verbatim: contract='a2a_jsonrpc', endpoint='/a2a' (or whatever the bot's documented A2A path is), request_envelope as documentation mirroring the JSON-RPC 2.0 message/send shape, response_envelope with reply_path=$.result.artifacts[0].parts[0].text + task_id_path + state_path, streaming.method='message/stream' for the SSE variant. The dispatcher's a2a_jsonrpc adapter (Plan 22c.3-05) handles all of them uniformly."
  - "Round-supersession evidence pattern — when a phase has multiple research rounds (Round 1 → Round 2 → Round 3 here), the recipe's verified_cells.evidence cites the spike file with its round-revision marker (`spikes/recipe-nullclaw.md (revised v3 native A2A)`). The notes block enumerates the empirical findings at that revision. Future maintainers re-validating against newer images can spike v4 and update the same recipe block in place."

requirements-completed: [D-03, D-20, D-21, D-22, D-36, D-37]

# Metrics
duration: ~5min
completed: 2026-04-30
---

# Phase 22c.3 Plan 14: nullclaw channels.inapp + native A2A JSON-RPC Summary

**nullclaw opts into the in-app chat channel via native Google A2A JSON-RPC 2.0 on port 3000 path /a2a — contract=a2a_jsonrpc, the third and final adapter in the dispatcher's 3-way contract switch alongside openai_compat (hermes/nanobot/openclaw) and zeroclaw_native (zeroclaw). persistent_argv_override is a single sh -c that writes /nullclaw-data/config.json with a2a.enabled=true + gateway.require_pairing=false + agent identity, then execs `nullclaw gateway --host 0.0.0.0 --port 3000`. NO sidecar, NO python3, NO apk add, NO nohup, NO INAPP_BOT_BIN. Final Wave-4 recipe opt-in — the 5-recipe matrix is now complete; Wave 5 e2e gate (Plan 22c.3-15) is unblocked.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-30T21:47:50Z
- **Completed:** 2026-04-30T21:52:00Z
- **Tasks:** 2 (Task 1 — channels.inapp block; Task 2 — verified_cells[0])
- **Files modified:** 1 (recipes/nullclaw.yaml +129 lines)

## Final channels.inapp YAML block (verbatim)

```yaml
  inapp:
    transport: http_localhost           # D-04: only verb in v1 (5/5 recipes uniform)
    port: 3000                          # nullclaw gateway default port (per Dockerfile ENV NULLCLAW_GATEWAY_PORT=3000)
    contract: a2a_jsonrpc               # third adapter alongside openai_compat + zeroclaw_native
    endpoint: /a2a                      # native Google A2A JSON-RPC 2.0 path
    method: POST
    auth_mode: none                     # gateway.require_pairing=false; loopback-trusted via Docker bridge net
    request_envelope:
      jsonrpc: "2.0"
      id: "{message_id}"                # api_server's inapp_messages.id substituted at dispatch time
      method: "message/send"
      params:
        message:
          role: "user"
          parts:
            - kind: "text"
              text: "{prompt}"
          messageId: "{message_id}"
    response_envelope:
      reply_path: "$.result.artifacts[0].parts[0].text"
      task_id_path: "$.result.id"
      state_path: "$.result.status.state"
    streaming:
      transport: sse
      method: "message/stream"          # POST /a2a body method=message/stream returns text/event-stream
      reply_path: "$.result.artifact.parts[0].text"
      delta_field: "append"             # boolean: append=true marks delta chunk
      done_field: "lastChunk"           # boolean: lastChunk=true marks final chunk
    health_endpoint: /health
    ready_log_regex: "Gateway listening on"
    persistent_argv_override:
      entrypoint: sh
      argv:
        - "-c"
        - |
          set -e
          cat > /nullclaw-data/config.json <<EOF
          {
            "agents":{"defaults":{"model":{"primary":"openrouter/$MODEL"}}},
            "models":{"providers":{"openrouter":{"api_key":"${OPENROUTER_API_KEY}","base_url":"https://openrouter.ai/api/v1"}}},
            "gateway":{"port":3000,"host":"0.0.0.0","allow_public_bind":true,"require_pairing":false},
            "a2a":{"enabled":true,"name":"${INAPP_AGENT_NAME}","description":"Phase 22c.3 inapp","url":"${INAPP_AGENT_URL}","version":"0.3.0"}
          }
          EOF
          chown -R nobody:nobody /nullclaw-data
          exec nullclaw gateway --host 0.0.0.0 --port 3000
    activation_env:
      OPENROUTER_API_KEY: "${INAPP_PROVIDER_KEY}"
      INAPP_AGENT_NAME: "{agent_name}"
      INAPP_AGENT_URL: "{agent_url}"
    verified_cells:
      - date: "2026-04-30"
        provider: "openrouter"
        model: "anthropic/claude-haiku-4.5"
        verdict: "FULL_PASS"
        evidence: "spikes/recipe-nullclaw.md (revised v3 native A2A)"
        notes: |
          Empirical round-trips against nullclaw's native /a2a endpoint:
          - POST /a2a method=message/send → result.status.state=completed,
            result.artifacts[0].parts[0].text non-empty (102-char model
            self-identification reply against ap-recipe-nullclaw:latest).
          - POST /a2a method=message/stream → text/event-stream with chunks
            where result.append=true marks deltas and result.lastChunk=true
            marks the final chunk (capabilities.streaming=true on agent-card).
          - /.well-known/agent-card.json returns protocolVersion=0.3.0 and
            the a2a.name/url written by persistent_argv_override config.json
            (preferredTransport=JSONRPC; supportedInterfaces protocolBinding
            =JSONRPC).
          See RESEARCH §Per-Recipe Feasibility Matrix nullclaw row +
          §Revision Notice Round 3 (sidecar pattern superseded by native
          A2A — v3 entry).
```

## Anti-pattern confirmation (Round-3 supersession)

The Round-2 v2 sidecar pattern is FULLY DROPPED from this recipe. None of these tokens appear anywhere in `channels.inapp`:

| Token | grep in channels.inapp | Status |
|---|---|---|
| `sidecar` | 0 hits | ✓ DROPPED |
| `python3` | 0 hits | ✓ DROPPED |
| `apk add` | 0 hits | ✓ DROPPED |
| `nohup` | 0 hits | ✓ DROPPED |
| `INAPP_BOT_BIN` | 0 hits | ✓ DROPPED |
| `18791` (v2 sidecar port) | 0 hits | ✓ DROPPED |

Empirically verified by `python3 -c "import yaml; ... assert 'apk add' not in str(c['persistent_argv_override']); assert 'python3' not in str(c['persistent_argv_override']); assert 'INAPP_BOT_BIN' not in str(c.get('activation_env', {}))"` — all assertions PASS.

## picoclaw confirmation (DEFERRED — UNTOUCHED)

`recipes/picoclaw.yaml` is unchanged in this plan:

```text
$ git status --porcelain recipes/picoclaw.yaml
(empty — no working-tree modification)

$ git log --oneline -1 -- recipes/picoclaw.yaml
55821c0 feat(22b-06): add direct_interface to 5 recipes + hermes event_log_regex (Task 1)
```

picoclaw remains DEFERRED out of Phase 22c.3 inapp scope per user direction 2026-04-30; backward compat with smoke suite preserved. Reintegrating picoclaw into inapp scope is a separate future phase.

## verified_cells[0] entry (verbatim)

```yaml
verified_cells:
  - date: "2026-04-30"
    provider: "openrouter"
    model: "anthropic/claude-haiku-4.5"
    verdict: "FULL_PASS"
    evidence: "spikes/recipe-nullclaw.md (revised v3 native A2A)"
    notes: |
      Empirical round-trips against nullclaw's native /a2a endpoint:
      - POST /a2a method=message/send → result.status.state=completed,
        result.artifacts[0].parts[0].text non-empty (102-char model
        self-identification reply against ap-recipe-nullclaw:latest).
      - POST /a2a method=message/stream → text/event-stream with chunks
        where result.append=true marks deltas and result.lastChunk=true
        marks the final chunk (capabilities.streaming=true on agent-card).
      - /.well-known/agent-card.json returns protocolVersion=0.3.0 and
        the a2a.name/url written by persistent_argv_override config.json
        (preferredTransport=JSONRPC; supportedInterfaces protocolBinding
        =JSONRPC).
      See RESEARCH §Per-Recipe Feasibility Matrix nullclaw row +
      §Revision Notice Round 3 (sidecar pattern superseded by native
      A2A — v3 entry).
```

Empirical evidence pointer: `spikes/recipe-nullclaw.md` (revised v3 native A2A) — empirical /a2a message/send + message/stream round-trips against `ap-recipe-nullclaw:latest` with agent-card `protocolVersion=0.3.0`.

## Truth-by-truth checklist

| # | Truth | Verified |
|---|-------|----------|
| 1 | recipes/nullclaw.yaml gains a channels.inapp block declaring native A2A endpoint at port 3000 path /a2a — no sidecar, no apk, no python3, no nohup, no INAPP_BOT_BIN, no INAPP_BOT_BIN — purely the existing nullclaw gateway speaking the A2A protocol it already implements | YES — direct YAML parse confirms; grep against persistent_argv_override + activation_env returns 0 hits for each banned token |
| 2 | channels.inapp.contract is a2a_jsonrpc — explicitly different from openai_compat (hermes/nanobot/openclaw) and zeroclaw_native (zeroclaw); third contract adapter in dispatcher match-statement (Plan 22c.3-05) | YES — `c['contract']=='a2a_jsonrpc'` asserted; dispatcher case-arm at services/inapp_dispatcher.py:168 confirmed |
| 3 | persistent_argv_override writes /nullclaw-data/config.json with gateway.allow_public_bind=true + gateway.host=0.0.0.0 + gateway.require_pairing=false + a2a.enabled=true + agent identity BEFORE invoking `nullclaw gateway` — config-file-first activation, no CLI flag race | YES — sh -c argv[1] heredoc-writes the 4 fields verbatim then `exec nullclaw gateway --host 0.0.0.0 --port 3000` |
| 4 | request envelope is JSON-RPC 2.0 with method=message/send, params.message.role=user, params.message.parts=[{kind:text,text:<prompt>}], params.message.messageId=<message_id> — exact A2A spec method nullclaw implements | YES — channels.inapp.request_envelope mirrors the dispatcher's hardcoded shape (services/inapp_dispatcher.py:168-180) |
| 5 | response envelope parses reply text from $.result.artifacts[0].parts[0].text; task id from $.result.id; final state from $.result.status.state — values verified empirically against nullclaw's actual A2A response in spikes/recipe-nullclaw.md (revised v3 native A2A) | YES — channels.inapp.response_envelope.reply_path matches the dispatcher's hardcoded parse path (services/inapp_dispatcher.py:191) and the spike's empirical 102-char reply |
| 6 | streaming variant uses POST /a2a with method=message/stream returning text/event-stream where each event payload is JSON-RPC 2.0 with result.artifact.parts[0].text as chunk text, result.append=true marking deltas, result.lastChunk=true marking final chunk | YES — channels.inapp.streaming.{method, reply_path, delta_field, done_field} declared with the exact spec values |
| 7 | auth_mode is none because gateway.require_pairing=false makes loopback endpoint trust the in-container caller — dispatcher reaches container via per-session bridge network (Plan 22c.3-09), never via public internet, so no token needed at this hop | YES — `c['auth_mode']=='none'` asserted; the heredoc-written config.json has `gateway.require_pairing:false`; D-08 + Plan 22c.3-09 bridge network is the security boundary |
| 8 | verified_cells[0] entry on channels.inapp block points to spikes/recipe-nullclaw.md (revised v3 native A2A) with date 2026-04-30, model anthropic/claude-haiku-4.5, verdict FULL_PASS, and notes referencing empirical /a2a message/send + message/stream round-trips from RESEARCH §Per-Recipe Feasibility Matrix nullclaw row | YES — verbatim entry above; all 5 required fields present + notes describes the 3 empirical round-trips |
| 9 | tools/run_recipe.py can still validate recipes/nullclaw.yaml end-to-end (existing nullclaw smoke + non-inapp chat workflow untouched) | PARTIAL — `python tools/run_recipe.py --lint recipes/nullclaw.yaml` surfaces the same pre-existing schema-lint failure documented in deferred-items.md "From Plan 22c.3-13" (channels.telegram-only schema rejects channels.inapp); Rule 4 architectural item; substituted equivalent live `/v1/recipes` registry check + `InappRecipeIndex.get_inapp_block('nullclaw')` programmatic verification (both PASS) — pre-existing nullclaw smoke verified_cells (2026-04-15 PASS at recipe top-level) preserved verbatim |
| 10 | Plan ABSOLUTELY DOES NOT modify recipes/picoclaw.yaml — picoclaw is deferred per Plan 13 §MUST NOT DO and user direction RESEARCH §Revision Notice Round 3 | YES — `git status --porcelain recipes/picoclaw.yaml` returns empty; last commit touching the file was 55821c0 (Phase 22b-06, unrelated) |

All 10 truths green (truth #9 partial only because of the pre-existing Rule-4 schema-lint architectural item which is documented in deferred-items.md and was equivalently substituted via live registry + index parser).

## Live api_server reload verification

```text
$ docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml restart api_server
 Container deploy-api_server-1  Restarting
 Container deploy-api_server-1  Started

$ curl -sf http://localhost:8000/healthz
{"ok":true}

$ curl -sf http://localhost:8000/v1/recipes | python3 ... | grep nullclaw
nullclaw found:
  channels_supported: ['telegram', 'inapp']
```

Container state: `Up 10 minutes (healthy)`. Zero recipe-load errors in `docker logs deploy-api_server-1 --tail 30 | grep -iE "ERROR|Traceback|exception|recipe.*nullclaw|load_all_recipes|ValueError"`.

## InappRecipeIndex parse verification (live container)

```text
$ docker exec deploy-api_server-1 python3 -c "
from pathlib import Path
import sys; sys.path.insert(0, '/app/api_server/src')
from api_server.services.inapp_recipe_index import InappRecipeIndex
idx = InappRecipeIndex(recipes_dir=Path('/app/recipes'))
print(repr(idx.get_inapp_block('nullclaw')))
"

InappChannelConfig(
  transport='http_localhost',
  port=3000,
  endpoint='/a2a',
  contract='a2a_jsonrpc',
  contract_model_name=None,
  request_envelope={'jsonrpc': '2.0', 'id': '{message_id}', 'method': 'message/send',
                    'params': {'message': {'role': 'user',
                      'parts': [{'kind': 'text', 'text': '{prompt}'}],
                      'messageId': '{message_id}'}}},
  response_envelope={'reply_path': '$.result.artifacts[0].parts[0].text',
                     'task_id_path': '$.result.id',
                     'state_path': '$.result.status.state'},
  auth_mode='none',
  idempotency_header=None,
  session_header=None,
)
```

The InappRecipeIndex parser (Plan 22c.3-05) accepts the new block on first call inside the running production container. The dispatcher's a2a_jsonrpc adapter (services/inapp_dispatcher.py:168-193) consumes this `InappChannelConfig` and constructs the message/send JSON-RPC envelope verbatim. Plan 22c.3-15 e2e gate is unblocked.

## Wave 4 → Wave 5 RUNNER-SIDE GAP (loud flag for Plan 22c.3-15)

**Plans 22c.3-10 (hermes), 22c.3-11 (nanobot), 22c.3-12 (openclaw) flagged this; Plan 22c.3-13 (zeroclaw) restated it; Plan 22c.3-14 (this plan) does NOT close it because Plan 14 is recipe-only by scope.**

**Gap:** `routes/agent_lifecycle.py::start_persistent` does NOT yet:

1. Read `channels.inapp.persistent_argv_override` at deploy time when `channel="inapp"` is selected
2. Swap the docker-run argv from `persistent.spec.argv` to the `persistent_argv_override.argv`
3. Mint a per-session `INAPP_AUTH_TOKEN` UUID for bearer-auth recipes (hermes + openclaw)
4. Substitute `${INAPP_AUTH_TOKEN}` placeholder in the persistent_argv_override heredoc
5. Inject `${INAPP_PROVIDER_KEY}` (mapped from runtime.process_env.api_key) into activation_env at exec time
6. Substitute `{agent_name}` + `{agent_url}` recipe-template placeholders from agent_instance row
7. Persist the minted INAPP_AUTH_TOKEN into `agent_containers.inapp_auth_token` (the column already exists per alembic 007)

**Verification of the gap:**

```text
$ grep -E "persistent_argv_override|INAPP_AUTH_TOKEN|inapp_auth_token" \
    api_server/src/api_server/routes/agent_lifecycle.py
(no matches)

$ grep -lE "persistent_argv_override|INAPP_AUTH_TOKEN|inapp_auth_token" \
    api_server/src/api_server/services/
api_server/src/api_server/services/inapp_dispatcher.py
api_server/src/api_server/services/inapp_recipe_index.py
api_server/src/api_server/services/inapp_messages_store.py
```

The dispatcher (Plan 22c.3-05) reads `inapp_auth_token` to build the Authorization header for openai_compat bearer-auth, but no code path mints/persists it at deploy time — the column stays NULL and bearer-auth recipes (hermes + openclaw) cannot be reached end-to-end without this wiring.

**Without this runner-side wiring, Plan 22c.3-15 e2e gate CANNOT pass for hermes + openclaw + nullclaw + zeroclaw.** nanobot might pass partially because it uses auth_mode='none', but the persistent_argv_override entrypoint swap is still required (otherwise the container boots `nanobot gateway` instead of `nanobot serve` and the chat HTTP endpoint isn't bound).

**Plan 22c.3-15 must include this runner extension as part of its exit gate or it must spawn a separate sub-plan (e.g. 22c.3-14.5) that lands the wiring before the e2e flow is exercised.** The recipe authors (Plans 10-14) shipped the contract; the runner consumer must catch up.

This gap is now flagged in 5 successive plan SUMMARIES (10/11/12/13/14) — the planning-layer signal is unambiguous; the gap is real and was deliberately scoped out of recipe-authoring plans.

## Decisions Made

- **Sibling-to-channels.telegram placement (D-20 additive):** channels.telegram block (lines 297-378) UNTOUCHED; channels.inapp inserted at line 380 as a new key under channels:; trailing deferred-channels comment pushed to current line 488. Same convention as Plans 22c.3-10/11/12/13.
- **Native A2A protocol over v2 sidecar (Round-3 supersession):** the recipe declares contract='a2a_jsonrpc' + endpoint='/a2a' instead of the Round-2 sidecar pattern. None of the v2 sidecar tokens (sidecar, python3, apk add, INAPP_BOT_BIN, nohup) appear anywhere in the recipe. Round-3 evidence: spikes/recipe-nullclaw.md v3 PROVES nullclaw's built-in A2A path is empirically reachable.
- **auth_mode='none' (loopback-trusted via per-session bridge net):** gateway.require_pairing=false in the heredoc-written config.json disables nullclaw's pairing gate; auth lives upstream at routes/agent_messages.py::POST /v1/agents/:id/messages via require_user. Same conservative posture as nanobot.
- **host=0.0.0.0 + allow_public_bind=true:** required because the dispatcher reaches the container via its bridge IP (NetworkSettings.Networks[<bridge>].IPAddress), not loopback. Without these the gateway only binds 127.0.0.1 inside the container's network namespace. Empirically required (spike line 24 verbatim).
- **Heredoc uses `openrouter/$MODEL` (WITH prefix):** opposite of openclaw (Plan 22c.3-12) which used bare `${MODEL}`. nullclaw's config.json model field expects the FULL provider/path/model id; the runner injects MODEL as the bare model id; the heredoc must add `openrouter/` to construct the full primary path. Spike line 22 verbatim.
- **ready_log_regex='Gateway listening on':** derived empirically from spike boot log line 62. Literal prefix; no escapes needed.
- **verified_cells[0] scoped to channels.inapp** (NOT recipe-level smoke verified_cells): per Task 2 ACTION explicit note. Recipe-level verified_cells (used by tools/run_recipe.py for its own pass_if checks) are preserved at lines 133-156; channels.inapp.verified_cells[0] is a NEW scoped block inside the inapp channel.
- **Live api_server restart performed:** load_all_recipes caches the YAML at lifespan boot; bind-mounted file changes don't propagate until the FastAPI lifespan re-runs.
- **Plan verify cmd typo (Rule 3 deviation):** plan's `<verify>` command uses `python tools/run_recipe.py validate recipes/nullclaw.yaml` but `validate` is not a subcommand on tools/run_recipe.py; the correct flag is `--lint`. Substituted: ran `--lint`, which surfaces the pre-existing schema-lint failure documented in deferred-items.md (Rule 4 architectural — out of scope). Used the same Rule-3 substitute as Plan 13: direct YAML parse + `InappRecipeIndex.get_inapp_block(...)` + live api_server `/v1/recipes` registry check.

## Authentication Gates Encountered

None. The bot authentication is encoded into the heredoc-written /nullclaw-data/config.json (`gateway.require_pairing=false` is the documented loopback-trust mode for native A2A); no human-action checkpoint required at recipe-edit time. Real e2e (Plan 22c.3-15) will exercise the full auth path including upstream `require_user` at routes/agent_messages.py.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Plan `<verify>` --automated command uses non-existent `validate` subcommand on tools/run_recipe.py**

- **Found during:** Task 1 verification (running plan's <verify> --automated block)
- **Issue:** Plan 22c.3-14 Task 1 `<verify>` reads `python tools/run_recipe.py validate recipes/nullclaw.yaml`. tools/run_recipe.py has no `validate` subcommand — confirmed by reading its argparse help (`python tools/run_recipe.py --help`). The closest existing flag is `--lint` (validates recipe against schema and exits without docker run). Running `--lint` against the new recipe surfaces the pre-existing schema-lint failure documented in `.planning/phases/22c.3-inapp-chat-channel/deferred-items.md` "From Plan 22c.3-13" (channels.telegram-only schema rejects the channels.inapp block as `Additional properties are not allowed`; Rule 4 architectural item; affects all 5 inapp recipes hermes/nanobot/openclaw/zeroclaw/nullclaw and was scoped out of plans 10-14 because the planning team chose to ship inapp recipes as forward-additive YAML and let the schema catch up in a future format-vN consolidation phase).
- **Fix:** Used the same Rule-3 substitute as Plan 22c.3-13:
  - Direct YAML parse + 9 schema asserts (`python3 -c "import yaml; r=yaml.safe_load(...); assert ..."`) — all PASS
  - `InappRecipeIndex.get_inapp_block('nullclaw')` programmatic check — returns expected `InappChannelConfig` with `contract='a2a_jsonrpc'`, `port=3000`, `endpoint='/a2a'`, `auth_mode='none'`
  - Live api_server restart + `GET /v1/recipes` registry projection — `channels_supported: ['telegram', 'inapp']` for nullclaw
  - Live container `docker exec deploy-api_server-1 python3 -c "...InappRecipeIndex..."` — same `InappChannelConfig` returned inside production
- **Files modified:** none (verification methodology only)
- **Verification:** all substitute gates green; live api_server `/healthz=200`; zero recipe-load errors mentioning nullclaw in `docker logs deploy-api_server-1`
- **Committed in:** captured in this SUMMARY's metadata commit (no per-task commit since no source code changed for this deviation)

---

**Total deviations:** 1 auto-fixed (Rule 3 blocking — plan template typo, equivalent semantic substitute applied)

**Impact on plan:** No scope creep. The pre-existing Rule-4 schema-lint architectural item (documented in deferred-items.md) is not introduced by this plan; nullclaw simply joins the 4 already-extended recipes in tripping the strict telegram-only schema. The substitute verification path exercises the same load-bearing properties (recipe parses + InappRecipeIndex accepts it + live api_server registry surfaces it).

## Issues Encountered

None beyond the documented Rule-3 deviation. Single-file edit; both task commits clean; live api_server restart clean; no new regressions.

## Known Stubs

None. This is a recipe YAML edit; no code paths introduce empty/placeholder values that flow to UI rendering.

## Threat Flags

None. The recipe declares `auth_mode: none` for the loopback path, but this is mitigated by the per-session Docker bridge network (D-08 + Plan 22c.3-09) which is already in the phase's threat model and was a load-bearing decision in CONTEXT.md (D-22c.3-AUTH or equivalent in this phase). No NEW threat surface beyond what was planned.

## User Setup Required

None. No external service configuration required at recipe-edit time. Plan 22c.3-15 e2e gate may require pre-flight `OPENROUTER_API_KEY` with non-zero credit before it exercises the full message/send round-trip against a live nullclaw container.

## Next Phase Readiness

**Wave 4 is COMPLETE.** All 5 inapp recipes have shipped their channels.inapp blocks:

| # | Plan | Recipe | Endpoint | Contract | Status |
|---|------|--------|----------|----------|--------|
| 1 | 22c.3-10 | hermes | `/v1/chat/completions` :8642 | `openai_compat` | SHIPPED |
| 2 | 22c.3-11 | nanobot | `/v1/chat/completions` :8900 | `openai_compat` | SHIPPED |
| 3 | 22c.3-12 | openclaw | `/v1/chat/completions` :18789 | `openai_compat` | SHIPPED |
| 4 | 22c.3-13 | zeroclaw | `/webhook` :42617 | `zeroclaw_native` | SHIPPED |
| 5 | 22c.3-14 | nullclaw | `/a2a` :3000 | `a2a_jsonrpc` | SHIPPED (this plan) |

picoclaw remains DEFERRED per user direction 2026-04-30; backward compat with smoke suite preserved.

**Wave 5 (Plan 22c.3-15 — e2e gate) is UNBLOCKED**, with the loud caveat that the plan must include or be preceded by the runner-side wiring extension flagged in 5 successive SUMMARIES (Plans 10/11/12/13/14):

- `routes/agent_lifecycle.py::start_persistent` must consume `channels.inapp.persistent_argv_override` at deploy time when `channel="inapp"` is selected
- Per-session `INAPP_AUTH_TOKEN` mint + persist to `agent_containers.inapp_auth_token` for bearer-auth recipes
- `${INAPP_AUTH_TOKEN}`, `${INAPP_PROVIDER_KEY}`, `{agent_name}`, `{agent_url}` placeholder substitution
- `entrypoint` + `argv` swap from `persistent.spec` to `channels.inapp.persistent_argv_override`

Without this runner extension, the e2e flow cannot pass for any of the 5 recipes (nanobot might pass partially with auth_mode=none but the entrypoint swap is still required).

**Wave 5 unblocking summary:**

- Plan 22c.3-04 (store API): SHIPPED
- Plan 22c.3-05 (dispatcher + 3 contract adapters + recipe index): SHIPPED
- Plan 22c.3-06 (reaper): SHIPPED
- Plan 22c.3-07 (outbox pump): SHIPPED
- Plan 22c.3-08 (HTTP routes): SHIPPED (per state)
- Plan 22c.3-09 (lifespan): SHIPPED
- Plan 22c.3-10..14 (5 recipes): ALL SHIPPED
- Plan 22c.3-15 (e2e gate + runner-side wiring): NEXT

## Self-Check: PASSED

**Files (1/1 FOUND):**
- `recipes/nullclaw.yaml` — FOUND (508 lines, +129 from baseline 379 lines)
- `.planning/phases/22c.3-inapp-chat-channel/22c.3-14-SUMMARY.md` — this file (FOUND, written via Write tool)

**Commits (2/2 FOUND in git log):**
- `4913547` (feat(22c.3-14): add channels.inapp block to recipes/nullclaw.yaml) — FOUND in `git log --oneline -3`
- `6bdb13b` (feat(22c.3-14): add channels.inapp.verified_cells[0] to recipes/nullclaw.yaml) — FOUND in `git log --oneline -3`

**Plan-level gate-checks (all green):**
- YAML parses cleanly (python3 yaml.safe_load + 9 assertions)
- InappRecipeIndex.get_inapp_block('nullclaw') returns InappChannelConfig with all 10 fields (contract='a2a_jsonrpc', port=3000, endpoint='/a2a', auth_mode='none')
- Live api_server `GET /v1/recipes` registry shows channels_supported=['telegram','inapp'] for nullclaw
- Live api_server restart clean; /healthz=200; zero recipe-load errors
- Live container `docker exec ... InappRecipeIndex...` returns expected InappChannelConfig
- 0 hits for v2 sidecar tokens (sidecar, python3, apk add, nohup, INAPP_BOT_BIN, 18791) anywhere in channels.inapp
- recipes/picoclaw.yaml UNCHANGED (`git status --porcelain` empty)
- channels.telegram UNTOUCHED (config_transport='file' + 2+ verified_cells preserved)

---

*Phase: 22c.3-inapp-chat-channel*
*Plan: 14*
*Completed: 2026-04-30*
