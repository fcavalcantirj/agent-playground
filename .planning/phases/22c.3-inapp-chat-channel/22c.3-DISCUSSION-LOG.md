# Phase 22c.3 — In-App Chat Channel - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `22c.3-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-29
**Phase:** 22c.3-inapp-chat-channel
**Areas discussed:** Stream protocol · Bot-side inbound transport · Outbound fan-out

---

## Pre-discussion empirical work

User signal: *"dissecate msv and understand. and, you ARE allowed to make tests b4 comitting to the plan for all agents."*

Two empirical artifacts were generated before the gray areas were even narrowed:

### MSV deep recon (subagent ae6a633d20205f9f4, ~83s)

Findings cited inline in CONTEXT.md `<canonical_refs>`. Key takeaways:
- Inbound IPC: HTTP POST to `localhost:{port}/v1/chat/completions` (OpenAI-compat envelope)
- Outbound: Redis Pub/Sub channel `sse:notifications:<user_id>` → SSE handler with 30s heartbeat
- Persistence: single `user_messages` table with `direction` discriminator
- Auth: web POST has Bearer middleware; SSE has zero auth (we improve)
- Workflow orchestration: Temporal SendMessageWorkflow with 8 activities (we simplify to direct asyncio)

### FIFO bind-mount empirical spike (negative result)

Tested `mkfifo /tmp/spike-fifo + docker run -v /tmp/spike-fifo:/inbox alpine`. Container did NOT receive host-written data. Confirmed: FIFO inode read/write semantics don't propagate across Docker Desktop's gRPC-FUSE/virtiofs boundary on macOS. **STRIKES FIFO from the inbound transport menu.**

---

## Stream protocol

| Option | Description | Selected |
|--------|-------------|----------|
| Long-poll only (existing) | Reuse Phase 22b `GET /v1/agents/:id/events` as-is | |
| Long-poll + SSE wrapper | Keep long-poll, add SSE endpoint that wraps the same plumbing | ✓ |
| Replace long-poll with SSE | Migrate to SSE; breaks Phase 22b SC-03 Gate B harness | |

**User's choice:** "how is msv? also, use temporal" — interpreted as: use SSE (mirror MSV); raise Temporal as separate question.

**Notes:** SSE wrapper is the cleanest path — keeps Phase 22b long-poll endpoint working for the existing test harness, adds SSE for browser EventSource + Flutter SSE clients, both share the same `event_poll_signals` plumbing.

---

## Bot-side inbound transport

| Option | Description | Selected |
|--------|-------------|----------|
| Per-recipe HTTP server | Each recipe ships its own HTTP /inapp endpoint | |
| Generic sidecar adapter | One wrapper image; recipes pick stdin/exec/http | |
| OpenAI-compat /v1/chat/completions on each bot | Mandate OpenAI-compat shape for all recipes | |
| Recipe declares; executor implements (user override) | YAML-declared transport + endpoint contract; runner reads + dispatches | ✓ |

**User's choice:** *"wtf man. RECIPE DEFINES HOW. EXECUTOR IMPLEMENTS. the agent is already running right? we already have, in theory, an api, right? so, whats the diff from msv dude?"*

**Notes:** User reframed the question correctly — Claude was overcomplicating. The MSV pattern IS recipe-declared (each recipe's bot declares its IPC shape; MSV's Temporal activity reads + executes). Same shape for us. The Claude-proposed sidecar pattern was dismissed as unnecessary middle-layer.

---

## Outbound fan-out

| Option | Description | Selected |
|--------|-------------|----------|
| Keep in-process (existing event_poll_signals) | Reuse Phase 22b's asyncio.Event dict; works in single-process | |
| Add Redis Pub/Sub now | Match MSV exactly; new Redis container in deploy stack | ✓ |

**User's choice:** Add Redis Pub/Sub now.

**Notes:** Redis is also reusable downstream (rate limiting at scale, session cache, ephemeral state). CLAUDE.md tech stack already lists Redis as a v1 dep — adding it now isn't a single-purpose dep.

---

## Temporal — separate question raised mid-discussion

| Option | Description | Selected |
|--------|-------------|----------|
| Defer Temporal — direct asyncio for now | Use existing patterns; Temporal added in dedicated future phase | ✓ |
| Split: 22c.3a Temporal substrate + 22c.3b chat workflow | Stand up Temporal first, then chat on top | |
| Add Temporal inline (one phase) | Both in one phase; ~3× scope | |

**User's choice:** Defer Temporal.

**User's notes:** *"temporal is not only for multiserver, but for robustness and reliance. do you think we should stop and refac api + temporal b4 add this"*

**Claude's response (recorded for context):** Recommended NOT pausing for full refactor. Reasoning:
1. Pre-refactor risk is real — Temporal touches all workflows
2. Robustness comes from boundaries (persist-before-action), not orchestrator
3. Validate Temporal on a real multi-step workflow first (likely Phase 24 candidate: `runs.create_run` build→run→verdict)
4. Chat is sub-second — Temporal earns its keep on long-running checkpointed operations

User accepted defer. Pre-refactor flagged as deferred idea (Phase 24 — "Temporal Substrate").

---

## Claude's Discretion

- Exact SSE event envelope shape (e.g., `event: inapp_outbound\ndata: {...}` vs `event: message`)
- Exponential backoff parameters (base, max, jitter)
- Redis client library choice (`redis` vs `aioredis`)
- Two-step persist-before-publish transaction boundaries

---

## Deferred Ideas

(Captured in CONTEXT.md `<deferred>` section — see for full list.)

Highlights:
- Phase 24 — Temporal Substrate (when a real multi-step workflow earns it)
- Phase 23 — Flutter Native App (depends on this phase shipping)
- Multi-channel-on-one-agent (run telegram AND inapp on same container) — v2
- Web chat UI at `/dashboard/agents/:id` — separate phase
- Pre-Temporal-refactor of all api_server workflows (user mused; declined for now)
