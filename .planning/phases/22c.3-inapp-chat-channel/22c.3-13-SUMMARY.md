---
phase: 22c.3-inapp-chat-channel
plan: 13
subsystem: recipes
tags: [yaml, recipe, zeroclaw, inapp, http_localhost, zeroclaw_native, image_pull, channels, persistent_argv_override, picoclaw_substitute, distroless, rust]

# Dependency graph
requires:
  - phase: 22c.3-09
    provides: Lifespan-attached dispatcher/reaper/outbox tasks (consume the channels.inapp recipe metadata via InappRecipeIndex; will pump messages routed through this recipe's channels.inapp once Plan 22c.3-15 e2e gates the flow)
  - phase: 22c.3-05
    provides: InappRecipeIndex parser + 3-way contract switch + zeroclaw_native adapter (the consumer of contract='zeroclaw_native' + idempotency_header + session_header + response_envelope.reply_path)
  - phase: 22c.3-04
    provides: inapp_messages store API + 3 Pydantic event payloads (consumed downstream by the dispatcher when a zeroclaw-recipe agent_instance receives a chat POST)
  - phase: 22c.3-12
    provides: openclaw channels.inapp pattern reference (auth_mode/contract_model_name/persistent_argv_override conventions inside channels.inapp)
  - phase: 22c.3-11
    provides: nanobot channels.inapp pattern reference (first persistent_argv_override use; D-20 entrypoint swap pattern)
  - phase: 22c.3-10
    provides: hermes channels.inapp pattern reference (env-only inapp recipe; auth_header convention)
  - phase: 22c.3-01
    provides: Wave-0 spike re-validation evidence for zeroclaw (spikes/recipe-zeroclaw.md, 2026-04-30 FULL_PASS — empirical /webhook + idempotency + WS streaming + headless against ghcr.io/zeroclaw-labs/zeroclaw:latest)

provides:
  - "zeroclaw recipe declares channels.inapp on the v0.2 schema (FIFTH and final inapp recipe opt-in for Phase 22c.3 — substitutes picoclaw per user direction 2026-04-30)"
  - "First recipe with build.mode=image_pull (per RECIPE-SCHEMA.md §3.2; ghcr.io/zeroclaw-labs/zeroclaw:latest distroless ~50 MB) — zero Dockerfile build path"
  - "First recipe with channels.inapp.contract='zeroclaw_native' — third per-contract adapter in the dispatcher's contract dispatch table (alongside openai_compat for hermes/nanobot/openclaw and a2a_jsonrpc for nullclaw)"
  - "First recipe with channels.inapp.idempotency_header + channels.inapp.session_header set — built-in to ZeroClaw via X-Idempotency-Key + X-Session-Id (the dispatcher forwards inapp_messages.id as the idempotency key for end-to-end dedup beyond the api-server's IdempotencyMiddleware)"
  - "First recipe with channels.inapp.streaming declared (path=/ws/chat WebSocket — token-streaming reservation for future SSE-fallback work; orthogonal to Phase 22c.3 scope)"
  - "channels.inapp.persistent_argv_override.pre_start_commands — 4-step distroless bootstrap path: onboard --quick + 3× config set + daemon — demonstrates the no-shell variant of the D-20 entrypoint-swap pattern"
  - "Live api_server's GET /v1/recipes/zeroclaw surfaces channels.inapp verbatim with channels_supported=['inapp']"
  - "InappRecipeIndex.get_inapp_block('zeroclaw') returns InappChannelConfig(contract='zeroclaw_native', port=42617, endpoint='/webhook', auth_mode='none', idempotency_header='X-Idempotency-Key', session_header='X-Session-Id')"
  - "picoclaw remains untouched — recipes/picoclaw.yaml unchanged (DEFERRED per user direction 2026-04-30; backward compat with smoke suite preserved)"

affects: [22c.3-14, 22c.3-15, 23-flutter]

# Tech tracking
tech-stack:
  added: []  # additive YAML edit; zero new deps
  patterns:
    - "Distroless image_pull bootstrap: when an upstream image has NO shell (ghcr.io/zeroclaw-labs/zeroclaw:latest is a distroless build), multi-step config bootstraps cannot use sh-heredoc (the nanobot/openclaw/nullclaw pattern). Instead, the recipe's persistent_argv_override.pre_start_commands chains separate `docker run --entrypoint zeroclaw <args>` invocations sharing the named volume — each command is a single-shot CLI subcommand against the same /zeroclaw-data mount; daemon then starts long-running. This is the canonical pattern for any future distroless-image inapp recipe."
    - "channels.inapp.idempotency_header + session_header — first inapp recipe to declare bot-side idempotency. ZeroClaw's built-in X-Idempotency-Key replays a deterministic {idempotent: true, status: duplicate} envelope when the same key is replayed (verified spike 2026-04-30); the dispatcher's zeroclaw_native adapter forwards inapp_messages.id as the idempotency key so retries get deduped at the bot layer in addition to the api-server's IdempotencyMiddleware (defense-in-depth across two independent stores)."
    - "image_pull mode with pre_start_commands — first recipe combining build.mode=image_pull with a multi-step bootstrap. The 4 pre_start_commands run BEFORE the main daemon container starts; the runner sequences them as separate ephemeral docker run invocations sharing the per-session named volume. This avoids the Rust cold-build path (~10min) that would otherwise dominate provisioning latency."

key-files:
  created: []
  modified:
    - "recipes/zeroclaw.yaml — channels.inapp.persistent_argv_override + channels.inapp.activation_env added (34 lines inserted between channels.inapp.optional_user_input and channels.inapp.verified_cells; sibling to existing top-level persistent.spec block which stays for the smoke / non-inapp path)"
    - ".planning/phases/22c.3-inapp-chat-channel/deferred-items.md — appended 2 items: (1) pre-existing recipe-schema lint failure across all 4 inapp-extended recipes (Rule 4 architectural — schema needs an `inapp` extension; out of scope per plan must_haves); (2) plan verify cmd #3 references non-existent --list-recipes flag (Rule 3 deviation; substituted live /v1/recipes endpoint check)"

key-decisions:
  - "channels.inapp.persistent_argv_override SCOPE — placed under channels.inapp (not at top-level persistent.spec). The recipe shipped at the planning commit (c98fd95) had top-level persistent.spec.pre_start_commands but lacked the channels.inapp-scoped persistent_argv_override block required by must_have #4 + the plan's <interfaces> shape. D-20 single-channel-per-instance dictates that the runner SWAPS persistent argv at deploy time based on channel selection — channels.inapp.persistent_argv_override IS that swap declaration. Following the precedent set by 22c.3-10/11/12 (hermes/nanobot/openclaw), this lives inside channels.inapp."
  - "Top-level persistent.spec.pre_start_commands kept (NOT removed). The recipe's existing top-level persistent block (lines 195-237) declares the same 4 pre_start_commands as a default/non-inapp path. Per CLAUDE.md golden rule #4 (root cause first, never fix-to-pass) the existing block is left alone since: (a) it doesn't contradict the plan, (b) it provides a default invocation path for tools/run_recipe.py --mode persistent without requiring the runner to look at channels.inapp, and (c) removing it would be an unprompted code change outside this plan's scope."
  - "auth_mode='none' on channels.inapp — gateway.require-pairing=false in the pre_start_commands disables ZeroClaw's pairing gate; loopback-only inside the docker bridge net is the security boundary. Same conservative posture as nanobot (auth_mode='none', loopback-trusted) and orthogonal to openclaw's bearer mode (which is required by openclaw's gateway when chatCompletions HTTP is enabled)."
  - "activation_env declares OPENROUTER_API_KEY + ZEROCLAW_WORKSPACE — the ZeroClaw daemon consumes OPENROUTER_API_KEY at both onboard time AND at runtime for upstream LLM calls. ZEROCLAW_WORKSPACE points the daemon at the bind-mounted workspace dir (/zeroclaw-data/workspace) to keep state inside the per-session ephemeral volume."
  - "ready_log_regex='ZeroClaw Gateway listening on' — derived empirically from the spike boot log (spikes/recipe-zeroclaw.md line 69 verbatim: '🦀 ZeroClaw Gateway listening on http://0.0.0.0:42617'). Matches the verbatim banner; no escapes needed since the regex is literal."
  - "Live api_server restart performed — load_all_recipes caches the YAML at lifespan boot into app.state.recipes; the bind-mounted file change isn't visible to running middleware until the FastAPI lifespan re-runs. Restarted via `docker compose -f docker-compose.prod.yml -f docker-compose.local.yml restart api_server`; /healthz=200 within ~5s; GET /v1/recipes/zeroclaw now returns the new persistent_argv_override + activation_env blocks."
  - "Verify cmd #3 substitution — the plan's <verify> step #3 (`python3 tools/run_recipe.py --list-recipes 2>&1 | grep -q zeroclaw`) references a non-existent flag. tools/run_recipe.py argparse setup at lines 1470-1579 has no --list-recipes; the closest is --lint-all which would fail on the strict v0.2 schema (pre-existing, affects all 4 inapp recipes). Substituted live `curl -s http://localhost:8000/v1/recipes` registry check — equivalent semantic gate (zeroclaw must appear in the recipe registry). Rule 3 deviation."

patterns-established:
  - "Distroless inapp recipe pattern — when the upstream image has no shell, channels.inapp.persistent_argv_override.pre_start_commands chains separate ephemeral docker run invocations against the same named volume INSTEAD of sh-c heredoc bootstraps. Each pre_start_command is a single-shot CLI subcommand; the runner sequences them; the named volume is the shared state between calls. Subsequent inapp recipes adding distroless agents follow this shape verbatim."
  - "Built-in idempotency declaration pattern — recipes whose bots ship native X-Idempotency-Key (or equivalent) declare channels.inapp.idempotency_header. The dispatcher's per-contract adapter forwards inapp_messages.id as that header value so the bot's storage dedupes retries in addition to the api-server's IdempotencyMiddleware. Defense-in-depth across two independent storage layers — adopters: zeroclaw (this plan); future-candidates: any bot exposing a documented idempotency contract."
  - "image_pull + per_session ephemeral volume pattern — distroless image (no build path) + per_session_tmpdir bind-mount at /zeroclaw-data + 4 pre_start_commands that materialize the workspace state on cold mount. Cold-boot ~5s wall (Rust + distroless + SQLite session-store init); subsequent dispatches reuse the running container. Sets the floor for build-mode=image_pull recipes in v0.2."

requirements-completed: [D-03, D-20, D-21, D-22, D-36, D-37]

# Metrics
duration: ~25min
completed: 2026-04-30
---

# Phase 22c.3 Plan 13: zeroclaw channels.inapp + persistent_argv_override + activation_env Summary

**ZeroClaw substitutes picoclaw in the Phase 22c.3 5-recipe matrix via image_pull (ghcr.io/zeroclaw-labs/zeroclaw:latest, distroless ~50 MB), contract=zeroclaw_native, native POST /webhook returning {response, model}, X-Idempotency-Key + X-Session-Id headers built-in, and a 4-command distroless bootstrap chained as separate docker run invocations sharing a named volume.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-30T21:16:00Z
- **Completed:** 2026-04-30T21:41:23Z
- **Tasks:** 1 (Task 1 — recipe edit + verify + commit)
- **Files modified:** 1 (recipes/zeroclaw.yaml +34 lines) + 1 deferred-items.md append

## Accomplishments

- Added `channels.inapp.persistent_argv_override` block to `recipes/zeroclaw.yaml` with `entrypoint: zeroclaw`, `argv: [daemon]`, and 4 `pre_start_commands` chaining `onboard --quick --provider openrouter --api-key ${OPENROUTER_API_KEY} --model $MODEL` + 3× `config set gateway.{allow-public-bind, host, require-pairing}` — the verbatim 4-command boot sequence the spike empirically PROVED 2026-04-30
- Added `channels.inapp.activation_env` block declaring `OPENROUTER_API_KEY` + `ZEROCLAW_WORKSPACE`
- Verified the recipe parses cleanly: `python3 -c "import yaml; yaml.safe_load(open('recipes/zeroclaw.yaml'))"` exits 0
- Verified all schema-presence asserts: apiVersion='ap.recipe/v0.2', name='zeroclaw', build.mode='image_pull', build.image starts with 'ghcr.io/zeroclaw-labs/zeroclaw', channels.inapp.transport='http_localhost', channels.inapp.port=42617, channels.inapp.endpoint='/webhook', channels.inapp.contract='zeroclaw_native', channels.inapp.ready_log_regex contains 'ZeroClaw Gateway listening on'
- Verified all 8 acceptance grep gates: ZeroClaw Gateway listening on, image_pull, ghcr.io/zeroclaw-labs/zeroclaw:latest, contract: zeroclaw_native, X-Idempotency-Key, X-Session-Id, /ws/chat, spikes/recipe-zeroclaw.md
- Verified `recipes/picoclaw.yaml` is unchanged (`git status --porcelain recipes/picoclaw.yaml` is empty) — picoclaw deferred per user direction
- Restarted live `deploy-api_server-1` container so `load_all_recipes` re-reads the bind-mounted recipes/ dir; `/healthz=200`; `GET /v1/recipes/zeroclaw` now surfaces the new `persistent_argv_override` (entrypoint+argv+4 pre_start_commands) + `activation_env` blocks
- Verified `InappRecipeIndex.get_inapp_block('zeroclaw')` returns `InappChannelConfig(contract='zeroclaw_native', port=42617, endpoint='/webhook', auth_mode='none', idempotency_header='X-Idempotency-Key', session_header='X-Session-Id')` — the dispatcher's zeroclaw_native adapter parse path (Plan 22c.3-05) is reachable end-to-end against the new recipe shape

## Task Commits

1. **Task 1: zeroclaw channels.inapp.persistent_argv_override + activation_env** — `1f01d67` (feat)

## Files Created/Modified

- `recipes/zeroclaw.yaml` — channels.inapp.persistent_argv_override (entrypoint=zeroclaw, argv=[daemon], 4 pre_start_commands) + channels.inapp.activation_env (OPENROUTER_API_KEY + ZEROCLAW_WORKSPACE) added between channels.inapp.optional_user_input and channels.inapp.verified_cells. The existing top-level `persistent.spec.pre_start_commands` (lines 195-237) is left intact (default/non-inapp invocation path; CLAUDE.md golden rule #4 — don't touch unrelated code).
- `.planning/phases/22c.3-inapp-chat-channel/deferred-items.md` — appended 2 items documenting the pre-existing schema-lint failure across inapp-extended recipes + the plan verify-cmd-#3 --list-recipes flag substitution.

## Decisions Made

- **persistent_argv_override placement** — placed inside `channels.inapp` (not at top-level `persistent.spec`) per must_have #4 + D-20 single-channel-per-instance + the precedent set by Plans 22c.3-10/11/12 (hermes/nanobot/openclaw). The runner SWAPS the persistent argv at deploy time when channel="inapp" is selected; channels.inapp.persistent_argv_override IS that swap declaration.
- **Top-level persistent.spec.pre_start_commands kept** — the recipe shipped at planning-commit c98fd95 already had the 4 commands at top-level persistent.spec; per CLAUDE.md golden rule #4 (root cause first, never fix-to-pass) and "no unprompted code changes outside the plan's scope" principle, the existing block was left intact. It serves as the default invocation path for `tools/run_recipe.py --mode persistent` without forcing the runner to descend into channels.inapp.
- **auth_mode='none' on channels.inapp** — gateway.require-pairing=false in the pre_start_commands disables ZeroClaw's pairing gate; loopback-only inside the docker bridge net is the security boundary (D-08 + nanobot precedent).
- **activation_env: OPENROUTER_API_KEY + ZEROCLAW_WORKSPACE** — declared per the plan's <interfaces> shape (lines 158-160 in PLAN.md). The ZeroClaw daemon consumes OPENROUTER_API_KEY at both onboard time AND at runtime for upstream LLM calls; ZEROCLAW_WORKSPACE points the daemon at the bind-mounted workspace dir.
- **Live api_server restart** — required to refresh `app.state.recipes` (set once at FastAPI lifespan boot via `load_all_recipes`); bind-mount alone doesn't propagate to in-memory cache. Restarted via `docker compose -f docker-compose.prod.yml -f docker-compose.local.yml restart api_server`; healthy within ~5s.
- **Verify cmd #3 substitution (Rule 3)** — `--list-recipes` is not a real flag on `tools/run_recipe.py`. Substituted `curl -s http://localhost:8000/v1/recipes | grep zeroclaw` against the live api_server registry — equivalent semantic gate (the recipe must appear in the registry).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Recipe shipped at planning-commit lacked the channels.inapp.persistent_argv_override block required by must_have #4**
- **Found during:** Verification phase (immediately after reading the existing recipe and validating against must_haves)
- **Issue:** `recipes/zeroclaw.yaml` was authored at the planning commit (c98fd95) with the 4 pre_start_commands placed under top-level `persistent.spec.pre_start_commands` ONLY. The plan's must_have #4 + the <interfaces> block (lines 121-161) explicitly require the same shape under `channels.inapp.persistent_argv_override.pre_start_commands` because D-20 single-channel-per-instance dictates that the runner SWAPS persistent argv at deploy time based on channel selection — and channels.inapp.persistent_argv_override IS the swap declaration the runner reads. Without it, a deploy with channel="inapp" would fall back to the top-level persistent.spec which is the smoke/default path; the contract is that channels.inapp owns the inapp-specific argv.
- **Fix:** Inserted `channels.inapp.persistent_argv_override` block (entrypoint=zeroclaw, argv=[daemon], 4 pre_start_commands matching the plan's <interfaces> shape verbatim) + `channels.inapp.activation_env` block (OPENROUTER_API_KEY + ZEROCLAW_WORKSPACE) between `optional_user_input: []` and `verified_cells:`. Followed the placement convention established by Plans 22c.3-10/11/12 (sibling-to-verified_cells, after optional_user_input).
- **Files modified:** `recipes/zeroclaw.yaml`
- **Verification:** All 8 plan-verify gates pass (yaml parse OK; schema OK including persistent_argv_override + 4 pre_start_commands + activation_env asserted; all 8 grep gates green); live api_server `GET /v1/recipes/zeroclaw` after restart surfaces persistent_argv_override.entrypoint='zeroclaw' + argv=['daemon'] + pre_start_commands.length=4 + activation_env.OPENROUTER_API_KEY + activation_env.ZEROCLAW_WORKSPACE; `InappRecipeIndex.get_inapp_block('zeroclaw')` returns the expected InappChannelConfig.
- **Committed in:** `1f01d67` (Task 1 commit)

**2. [Rule 3 - Blocking] Plan verify cmd #3 references non-existent --list-recipes flag**
- **Found during:** Verification phase (running plan's <verify> block sequentially)
- **Issue:** Plan 22c.3-13 <verify> step #3 reads `python3 tools/run_recipe.py --list-recipes 2>&1 | grep -q zeroclaw`. `tools/run_recipe.py` has no `--list-recipes` flag — confirmed by reading its argparse setup at lines 1470-1579. Closest existing flag is `--lint-all` which iterates `recipes/*.yaml` but reports lint verdicts (and would fail on the pre-existing strict-schema mismatch affecting hermes/nanobot/openclaw/zeroclaw — Rule 4 territory, out of scope for this plan).
- **Fix:** Substituted `curl -s http://localhost:8000/v1/recipes | python3 ... | grep zeroclaw` against the live api_server registry — equivalent semantic gate. The api_server's `load_all_recipes` is the same code path the runner would exercise; appearance in `GET /v1/recipes` proves both load-side parse success AND that the registry sees the new recipe with `channels_supported=['inapp']`.
- **Files modified:** none (verification methodology only); deferred-items.md appended for future plan-template fix.
- **Verification:** `curl -s http://localhost:8000/v1/recipes` shows zeroclaw with `channels_supported=['inapp']` after the live api_server restart.
- **Committed in:** captured in this SUMMARY's metadata commit; no per-task commit since no source code changed.

---

**Total deviations:** 2 auto-fixed (1 Rule 1 bug, 1 Rule 3 blocking)
**Impact on plan:** Both auto-fixes necessary for correctness. Rule 1 closes the must_have #4 gap (the recipe authored at planning time didn't match the plan's binding spec). Rule 3 closes the verify-cmd gap (typo'd flag in a verify command — the load-bearing semantic gate was preserved via an equivalent substitute). No scope creep; picoclaw remains untouched per user direction.

## Issues Encountered

- The recipe already existed in the working tree (committed at c98fd95 as part of the planning-time spike artifact) but was structurally incomplete vs. the plan's must_haves. Recognized this immediately, applied Rule 1 to fix in-place, kept the existing top-level persistent.spec block (golden rule #4 — no unprompted changes), and kept the recipe parse-clean throughout the edit.
- Live api_server's `app.state.recipes` is loaded once at FastAPI lifespan boot via `load_all_recipes(settings.recipes_dir)` — bind-mounted file changes aren't visible to running middleware until the lifespan re-runs. Restart via docker-compose was clean (~5s to healthy); `/healthz=200`; new fields visible in `GET /v1/recipes/zeroclaw`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 22c.3-14 (nullclaw channels.inapp opt-in)** unblocked — the only remaining Wave 4 plan.
- **Plan 22c.3-15 (Wave 5 e2e gate)** unblocked once Plan 22c.3-14 lands. The 5-recipe matrix is now: hermes (openai_compat) + nanobot (openai_compat) + openclaw (openai_compat) + zeroclaw (zeroclaw_native) + nullclaw (a2a_jsonrpc — Plan 22c.3-14). Plan 22c.3-15 will exercise the full POST /v1/agents/:id/messages → dispatcher → zeroclaw_native adapter → POST /webhook → reply parse path against a real running zeroclaw container.
- **picoclaw remains untouched** — `recipes/picoclaw.yaml` unchanged in working tree; backward compat with smoke suite preserved. Reintegrating picoclaw into inapp scope is a separate future phase per user direction 2026-04-30.
- **Pre-existing recipe-schema lint failure** logged to deferred-items.md as a Rule 4 architectural item (schema needs an `inapp` extension; affects hermes/nanobot/openclaw/zeroclaw; out of scope for Plans 22c.3-10..14 since the plan must_haves explicitly fall back to "at minimum YAML parses + required fields present").

## Self-Check: PASSED

- `recipes/zeroclaw.yaml` exists with the new channels.inapp.persistent_argv_override + activation_env blocks (verified by `Read`).
- Commit `1f01d67` exists in `git log --oneline --all` (verified by `git log`).
- `.planning/phases/22c.3-inapp-chat-channel/deferred-items.md` exists and contains the 2 appended items (verified by trailing append).

---

*Phase: 22c.3-inapp-chat-channel*
*Plan: 13*
*Completed: 2026-04-30*
