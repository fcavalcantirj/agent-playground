---
phase: 22c.3-inapp-chat-channel
plan: 11
type: execute
wave: 4
depends_on: [22c.3-09]
files_modified:
  - recipes/nanobot.yaml
files_created: []
autonomous: true
worktree_safe: false  # recipe consumed by live api_server
requirements: [D-03, D-20, D-21, D-36, D-37]
must_haves:
  truths:
    - "recipes/nanobot.yaml has a new channels.inapp block: transport=http_localhost, port=8900, endpoint=/v1/chat/completions, contract=openai_compat"
    - "channels.inapp.persistent_argv_override switches the entrypoint to `nanobot serve --port 8900 --host 127.0.0.1 --timeout 600` per D-20 single-channel-per-instance + D-40 600s timeout"
    - "channels.inapp.auth_header is set per the Wave-0 A5 finding (Plan 22c.3-01): if A5=auth_required, declare the bearer; if A5=no_auth, set auth_header=none and document v1 acceptable risk on loopback-only"
    - "channels.inapp.ready_log_regex matches `Endpoint : http://127.0.0.1:8900` (verified in spikes/recipe-nanobot.md line 30)"
    - "Existing channels.telegram block (if present) UNTOUCHED — additive change (D-20)"
  artifacts:
    - path: "recipes/nanobot.yaml"
      provides: "channels.inapp declaring native nanobot serve mode"
      contains: "nanobot serve"
  key_links:
    - from: "recipes/nanobot.yaml channels.inapp.persistent_argv_override"
      to: "spikes/recipe-nanobot.md verbatim docker run command at line 10"
      via: "argv mirrors the spike's invocation; --timeout 600 added per D-40"
      pattern: "nanobot serve --port 8900"
---

<objective>
Add a `channels.inapp` block to `recipes/nanobot.yaml`. Nanobot ships a native OpenAI-compat HTTP server activated via the alternative `nanobot serve` subcommand (vs `nanobot gateway` used for telegram). Per D-20 single-channel-per-instance, when a recipe is deployed in `channel: "inapp"` mode, the runner uses `persistent_argv_override` to swap the binary entrypoint from `nanobot gateway` to `nanobot serve --port 8900 --host 127.0.0.1 --timeout 600`.

The 600s timeout flag honors D-40 (10min bot-side per-request budget; matches dispatcher's 600s httpx timeout).

The auth_header value depends on Wave 0 (Plan 22c.3-01) Open Question A5:
- If A5 answer = `auth_required` (e.g., `nanobot serve` enforces auth via env var or flag) — declare the activation_env that sets the bearer + use `auth_header: "Authorization: Bearer ${INAPP_AUTH_TOKEN}"`.
- If A5 answer = `no_auth` (per spike's MEDIUM-flag finding) — declare `auth_header: "none"` and document the v1 acceptable risk: bot HTTP is loopback-only inside the container's network namespace; the dispatcher (also in-network via deploy_default bridge) hits 127.0.0.1:8900 from the api_server side via Docker DNS; external access blocked.

Per D-20 + spike finding: nanobot recipe's existing `channels.telegram` (if any) coexists with `channels.inapp` in the schema; the runner picks ONE channel per agent instance.

Purpose: Second of 5 recipes (native HTTP, alternative entrypoint).
Output: nanobot.yaml with channels.inapp; recipe parses; api_server boots clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/22c.3-inapp-chat-channel/22c.3-CONTEXT.md
@.planning/phases/22c.3-inapp-chat-channel/22c.3-RESEARCH.md
@.planning/phases/22c.3-inapp-chat-channel/22c.3-PATTERNS.md
@.planning/phases/22c.3-inapp-chat-channel/spikes/recipe-nanobot.md
@.planning/phases/22c.3-inapp-chat-channel/22c.3-01-SUMMARY.md  # has the A5 answer
@recipes/nanobot.yaml

<interfaces>
**From spikes/recipe-nanobot.md verbatim:**
- `nanobot serve --port 8900 --host 127.0.0.1` exposes `/v1/chat/completions` + `/v1/models`.
- Default port: 8900.
- CLI flags: `--port`, `--host`, `--timeout` (per-request; default 120s).
- Activation: NOT `nanobot gateway`; the entrypoint must be swapped to `nanobot serve` per D-20.
- Ready log: `🐈 Starting OpenAI-compatible API server` followed by `Endpoint : http://127.0.0.1:8900/v1/chat/completions`.

**From spike risk flags (line 73-76):**
- MEDIUM: `--timeout` default 120s. D-40 needs 600s. **Override `--timeout 600`** in persistent_argv.
- LOW (now resolved by Plan 22c.3-01 Wave 0 A5 spike): does `nanobot serve` enforce auth? Read `22c.3-01-SUMMARY.md` for the answer.

**Recipe block (template — adjust auth_header per A5):**

```yaml
channels:
  # ... existing channels (if any) ...

  # Phase 22c.3 — in-app chat channel (D-20: additive).
  # Empirically verified 2026-04-29 (spikes/recipe-nanobot.md): native
  # OpenAI-compat HTTP server via `nanobot serve` (alt entrypoint to
  # `nanobot gateway`; per D-20 single-channel-per-instance, the recipe
  # swaps entrypoint at deploy time via persistent_argv_override).
  inapp:
    transport: http_localhost
    port: 8900
    endpoint: /v1/chat/completions
    contract: openai_compat
    contract_model_name: "agent"   # nanobot accepts the configured model id;
                                     # "agent" is the safe placeholder string
                                     # the dispatcher sends (see RESEARCH §
                                     # _dispatch_http_localhost body block).
    # === A5-DEPENDENT: pick ONE based on Plan 22c.3-01 SUMMARY ===
    # If A5 = auth_required:
    #   auth_header: "Authorization: Bearer ${INAPP_AUTH_TOKEN}"
    # If A5 = no_auth (likely):
    auth_header: "none"
    # === END A5 ===
    ready_log_regex: "Endpoint : http://127\\.0\\.0\\.1:8900"
    response_routing: per_session
    persistent_argv_override:
      entrypoint: nanobot
      argv:
        - "serve"
        - "--port"
        - "8900"
        - "--host"
        - "127.0.0.1"
        - "--timeout"
        - "600"        # D-40: 10min per-request budget
    activation_env:
      # Same OPENROUTER_API_KEY (or other provider key) the gateway path uses
      # is required by `nanobot serve` for upstream LLM calls. The runner's
      # existing channel-input mechanism injects them.
      # Per A5: if auth_required, ADD the activation env var that sets the bearer.
      # Example (only if A5=auth_required):
      #   NANOBOT_API_KEY: "${INAPP_AUTH_TOKEN}"
      # If A5=no_auth, no extra activation env is needed beyond what the
      # gateway path already requires.
      {}
    verified_cells:
      - date: "2026-04-29"
        boot_wall_s: 8
        first_reply_wall_s: 0
        verdict: PASS
        notes: |
          Empirically verified via spikes/recipe-nanobot.md (2026-04-29):
          docker run with `nanobot serve --port 8900` produces the
          OpenAI-compat HTTP server. /v1/models returned 200 with the
          configured model entry within 8s of boot. A5 (auth requirement)
          resolved by Plan 22c.3-01 Wave-0 spike — see 22c.3-01-SUMMARY.md.
        category: PASS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read A5 finding from Plan 22c.3-01 SUMMARY + add channels.inapp to nanobot.yaml</name>
  <files>recipes/nanobot.yaml</files>
  <read_first>
    - .planning/phases/22c.3-inapp-chat-channel/22c.3-01-SUMMARY.md (the A5 answer — auth_required vs no_auth)
    - recipes/nanobot.yaml (full file — find existing channels block)
    - .planning/phases/22c.3-inapp-chat-channel/spikes/recipe-nanobot.md (verbatim spike command + ready log + risk flags)
  </read_first>
  <action>
**Step 1 — Resolve A5:**
Read `22c.3-01-SUMMARY.md`. Locate the line `A5 answer: auth_required` or `A5 answer: no_auth`. Use it to pick the correct `auth_header` + `activation_env` shape.

**Step 2 — Edit `recipes/nanobot.yaml`:**
Locate the top-level `channels:` key. ADD a sibling `inapp:` block per `<interfaces>` template above. Use the resolved A5 answer:

- **If A5=no_auth (likely):** set `auth_header: "none"` and the dispatcher (Plan 22c.3-05) skips the Authorization header. Document in the recipe block's notes that on-the-network risk is bounded by Docker network isolation + loopback-only bot HTTP (the deploy_default bridge is a private 172.x.x.x/16 — bot's port 8900 is NOT exposed to host, only to api_server's container).
- **If A5=auth_required:** set `auth_header: "Authorization: Bearer ${INAPP_AUTH_TOKEN}"` + add an `activation_env` entry mapping the appropriate nanobot env var (TBD per spike — the spike's risk flag says 5-10 LOC of aiohttp auth proxy might be needed; document if the actual env var name was discovered).

**Step 3 — Verify:**
```bash
python -c "import yaml; d = yaml.safe_load(open('recipes/nanobot.yaml')); ch = d.get('channels', {}); assert 'inapp' in ch, 'inapp missing'; ip = ch['inapp']; assert ip['port'] == 8900; assert ip['transport'] == 'http_localhost'; assert ip['persistent_argv_override']['entrypoint'] == 'nanobot'; print('ok:', ip)"
```

```bash
docker compose --env-file deploy/.env.prod \
  -f deploy/docker-compose.prod.yml \
  -f deploy/docker-compose.local.yml \
  restart api_server
sleep 8
curl -sf http://localhost:8000/healthz
docker logs deploy-api_server-1 2>&1 | tail -50 | grep -E "load_all_recipes|recipe.*nanobot" | tail -5
```

Expect: api_server boots clean; no recipe-load errors mentioning nanobot.
  </action>
  <verify>
    <automated>python -c "import yaml; d = yaml.safe_load(open('recipes/nanobot.yaml')); assert 'inapp' in d.get('channels', {}), 'inapp not in channels'; assert d['channels']['inapp']['transport'] == 'http_localhost'; assert d['channels']['inapp']['port'] == 8900; assert d['channels']['inapp']['persistent_argv_override']['argv'][0] == 'serve'" && curl -sf http://localhost:8000/healthz | grep -q '"ok":true'</automated>
  </verify>
  <done>recipes/nanobot.yaml has channels.inapp block with persistent_argv_override switching to `nanobot serve --port 8900 --timeout 600`. YAML parses. A5 finding from Plan 22c.3-01 honored. api_server still healthy after restart.</done>
</task>

</tasks>

<verification>
- YAML parse check passes.
- /healthz still ok after restart.
- `grep -q "nanobot serve" recipes/nanobot.yaml` (override declared).
- `grep -q "timeout.*600\|--timeout" recipes/nanobot.yaml` (D-40 600s honored).
- A5 answer (auth_required vs no_auth) reflected in auth_header value.
</verification>

<success_criteria>
- channels.inapp authoritatively declared on nanobot recipe with persistent_argv_override swapping to `nanobot serve` mode.
- 600s timeout flag honors D-40.
- auth_header reflects empirically-determined A5 result.
- D-20 honored: existing channels (if any) untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/22c.3-inapp-chat-channel/22c.3-11-SUMMARY.md` documenting:
- A5 answer used + the auth_header value chosen.
- The added YAML block (verbatim).
- Verbatim docker logs after restart showing no recipe-load errors.
- v1 acceptable risk note (if A5=no_auth): the bot's HTTP listener is loopback-only inside Docker network namespace; api_server reaches it via the deploy_default bridge; external access blocked.
</output>
