# Plan 02.5-08 Task 1 scratch: SHA pin + A7 resolution + Phase 2 template body

## Pinned SHA

Pinned SHA: `773a94c41437d21c7cb1fcc429cee1ac605dd509`

Source: `git ls-remote https://github.com/sipeed/picoclaw HEAD` at plan-execute time (2026-04-14).

Phase 2 pin was `c7461f9e963496c4471336642ac6a8d91a456978` (2026-03-31). Current HEAD is newer; per D-42 ("pinned at plan-WRITE time") we adopt the newer commit. The Phase 2 Dockerfile's `jq`-based model-name fix may or may not still be required at this SHA — the Dockerfile keeps the step defensively (idempotent `jq` rewrite is harmless if the catalog already has the hyphenated name).

## A7 outcome

A7 outcome: green

Evidence (local clone at /Users/fcavalcanti/dev/picoclaw, grepped 2026-04-14):

- `pkg/providers/factory_provider.go:27` — `"openrouter": {defaultAPIBase: "https://openrouter.ai/api/v1"}` — first-class entry in the provider protocol table.
- `pkg/providers/factory_provider.go:103` — comment: "Supported protocol families include OpenAI-compatible prefixes (e.g., openai, openrouter, groq, gemini)".
- `pkg/providers/factory_provider.go:199` — `case "litellm", "lmstudio", "openrouter", "groq", ...` — OpenRouter handled as an OpenAI-compatible HTTP provider.
- `pkg/providers/openai_compat/provider.go:53` — `"openrouter": {}` in the compat protocol set.
- `pkg/providers/openai_compat/provider.go:412` — `if strings.Contains(strings.ToLower(apiBase), "openrouter.ai")` — dedicated special-case for OpenRouter's URL normalization.
- `pkg/agent/instance_test.go:113-115` — test fixture using `openrouter/stepfun/step-3.5-flash:free` with `api_base: https://openrouter.ai/api/v1` and `wantProvider: "openrouter"`.
- `pkg/providers/fallback_test.go:465,474` — fallback provider tests naming "openrouter" as a legitimate fallback target.
- `README.it.md:383` — OpenRouter listed in the supported provider table ("200+ modelli, API unificata").

**Key mechanism:** picoclaw reads API keys from `~/.picoclaw/.security.yml` keyed by `<model_name>:<index>`, NOT from env vars. Grep for `os.Getenv.*OPENROUTER` or `os.Getenv.*ANTHROPIC_API_KEY` returns zero matches in real source (test files do NOT count). So:

- `api_key_env_var` in the recipe schema is pure metadata for the matrix test framework — picoclaw itself does not consume `OPENROUTER_API_KEY` or `ANTHROPIC_API_KEY` env vars.
- The BYOK flow for picoclaw is: BYOK key → template → `.security.yml` → picoclaw's config loader → provider factory. The **template IS the injection surface**.

**Implication for this plan:** Picoclaw supports OpenRouter. However, the 02.5 `.security.yml.tmpl` in this plan only renders the Anthropic key (matching the Phase 2 body verbatim). Supporting OpenRouter in picoclaw's Gate A cell would require a template variant or a merged-model-list rendering step — out of scope for this plan. Per D-47 + the plan's A7-conditional guidance, we declare openrouter in `providers[]` (schema signal for the matrix runner) and add `openrouter-api-key` to `secrets_schema` as optional. Gate A's OpenRouter coverage is carried by aider (Plan 07).

## Phase 2 template body

Source: `api/internal/recipes/recipes.go` lines 237–250 — function `renderPicoclawSecurityYAML`.

Verbatim body (from `fmt.Sprintf` call, after `strings.ReplaceAll` escaping):

```yaml
channels:
  telegram: {}
model_list:
  claude-sonnet-4.6:0:
    api_keys:
      - "<KEY>"
web:
  brave: {}
  tavily: {}
```

Where `<KEY>` is the backslash-escaped BYOK key (double quotes only — `strings.ReplaceAll(key, `"`, `\"`)`).

Port to text/template: replace the string-quoted `<KEY>` with `{{ quote .secrets.anthropic_key }}`, which uses `strconv.Quote` per Plan 02's template FuncMap — strictly safer than `ReplaceAll` because it handles all control characters, not just double quotes.

## Phase 2 Dockerfile step inventory

From `agents/picoclaw/Dockerfile` (Phase 2, inherits `ap-base:v0.1.0`):

1. `ARG PICOCLAW_SHA=c7461f9e963496c4471336642ac6a8d91a456978` — global ARG
2. **Stage 1:** `FROM golang:1.25-alpine AS builder` — Go 1.25 Alpine
3.   `ARG PICOCLAW_SHA` — stage-scope re-declaration (Docker ARG scoping quirk)
4.   `RUN apk add --no-cache git make` — build-time tools
5.   `WORKDIR /src`
6.   `RUN git clone … && git checkout … && go mod download && make build` — source build
7. **Stage 2:** `FROM ap-base:v0.1.0` → needs to become `FROM ap-runtime-node:v0.1.0-22`
8.   `USER root`
9.   `COPY --from=builder /src/build/picoclaw /usr/local/bin/picoclaw`
10.  `RUN chmod +x /usr/local/bin/picoclaw`
11.  `USER agent` + `WORKDIR /home/agent`
12.  `RUN /usr/local/bin/picoclaw onboard` — seed `~/.picoclaw/`
13.  `RUN jq '(.model_list[] | select(.model_name == "claude-sonnet-4.6") | .model) = "anthropic/claude-sonnet-4-6"' …` — model-name wire fix
14.  `RUN mkdir -p /home/agent/.picoclaw/workspace/sessions` — tmpfs mount target
15.  `USER root` + `WORKDIR /home/agent`
16.  `ENV AP_AGENT_CMD="picoclaw agent --session cli:default"` — entrypoint hint for ap-base tmux launcher

## What changes in 02.5 Dockerfile

**Preserved (verbatim):** Steps 1-6 (build stage), 8-11 (binary copy + chmod + user switch), 12 (`picoclaw onboard`), 13 (jq fix, idempotent for safety), 14 (session dir), 15 (USER root at end), 16 (AP_AGENT_CMD env).

**Changed:** Step 7 — `FROM ap-base:v0.1.0` → `FROM ap-runtime-node:v0.1.0-22` (D-22).

**Not added:** No new install steps — the runtime base provides node/npm (unused by picoclaw, but present for the shared pattern). Picoclaw itself is a Go binary requiring zero runtime dependencies.
