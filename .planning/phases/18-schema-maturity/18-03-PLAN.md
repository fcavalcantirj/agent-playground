---
phase: 18-schema-maturity
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - docs/RECIPE-SCHEMA.md
autonomous: true
requirements: [D-11]
tags: [docs, spec, narrative, versioning, license, annotations]

must_haves:
  truths:
    - "`docs/RECIPE-SCHEMA.md` header names the version as `ap.recipe/v0.1.1` and explicitly states it is additive over v0.1."
    - "A new narrative section documents the `oneOf` + `$defs.v0_1` versioning seam (D-01) and explains how v0.2 will land by appending a branch — no wire-format break."
    - "The category enum is described as living at `$defs.category` and referenced by both `verified_cells[].category` and `known_incompatible_cells[].category` (D-02)."
    - "The `known_incompatible_cells[].verdict` enum constraint is documented (D-03 — WR-05 close-out)."
    - "The `source.ref` allowlist pattern is documented with rationale (D-04 — git fetch option-as-value attack)."
    - "The `name.maxLength` and all three `timeout_s` maxima are documented in their respective sections (D-05, D-06)."
    - "The `volumes[].owner_uid` `maximum: 4294967295` is documented with a line noting values > 65535 typically indicate userns-remap (D-07)."
    - "A new section documents the `annotations` escape valve — where it lives (the 7 sites), what it's for, and that `build.observed` is deprecated in favor of `build.annotations` for new recipes (D-08)."
    - "A new section documents `metadata.license` and `metadata.maintainer` as optional-today-required-later, with SPDX convention for license (D-09)."
    - "The `## 10. Out of scope for v0.1` section is preserved but renamed / annotated to reflect v0.1.1 deferrals from CONTEXT.md (capability advertisement, runtime.limits, GPU, known_* collapse, richer cells, required license/maintainer)."
    - "JSON Schema is authoritative — the markdown is narrative. This precedence statement exists verbatim somewhere in the doc."
  artifacts:
    - path: "docs/RECIPE-SCHEMA.md"
      provides: "v0.1.1 narrative spec — oneOf seam, annotations escape valve, license/maintainer advisory, bounds documented"
      contains: "v0.1.1"
  key_links:
    - from: "docs/RECIPE-SCHEMA.md narrative"
      to: "tools/ap.recipe.schema.json"
      via: "cross-reference with 'JSON Schema is authoritative' precedence rule"
      pattern: "authoritative"
---

<objective>
Update `docs/RECIPE-SCHEMA.md` to match the v0.1.1 schema shape landed in Plan 02, per D-11 in `18-CONTEXT.md`. The markdown is the narrative spec; the JSON Schema is authoritative. After this plan, a recipe author (human or agent) who reads the markdown end-to-end can write a valid v0.1.1 recipe without opening the JSON Schema file, and every claim in the markdown is consistent with what Plan 02's schema enforces.

Purpose: Close the doc-runner-sync gap Phase 17 will one day enforce automatically. Until then, the markdown must be kept in manual sync with the schema. Phase 18's CONTEXT explicitly splits "markdown narrative" (this plan) from "JSON Schema tightening" (Plan 02) so a reader of the doc diff sees prose intent, and a reader of the schema diff sees declarative enforcement.

Output: One modified markdown file (`docs/RECIPE-SCHEMA.md`). No code, no test, no schema edits. No recipe edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-schema-maturity/18-CONTEXT.md
@.planning/phases/18-schema-maturity/18-01-PLAN.md
@.planning/phases/18-schema-maturity/18-02-PLAN.md
@docs/RECIPE-SCHEMA.md
@tools/ap.recipe.schema.json

<interfaces>
<!-- Current doc structure (docs/RECIPE-SCHEMA.md). The executor is editing narrative sections, -->
<!-- not the file's skeleton. The section headers below are the anchors to update or append under. -->

Current table of contents (as of line 1-285):
- `# Agent Playground Recipe Schema — ap.recipe/v0.1` (H1 — UPDATE to v0.1.1)
- `## File shape at a glance` (YAML sketch — UPDATE to reflect optional license/maintainer on metadata)
- `## 1. Top-level identity` (UPDATE apiVersion row + name row for maxLength)
- `## 2. source — upstream repository` (UPDATE ref row for allowlist pattern)
- `## 3. build — how to produce the image` (UPDATE for timeout maxima; annotations escape valve note)
- `## 4. runtime — container environment contract` (UPDATE owner_uid row; annotations escape valve note)
- `## 5. invoke — how to call the agent` (annotations escape valve note)
- `## 6. smoke — how to verify the recipe works` (UPDATE timeout max; verdict enum for known_incompatible_cells; annotations on cell items)
- `## 7. metadata` (ADD license + maintainer rows as optional)
- `## 8. Runner interface (tools/run_recipe.py)` (unchanged — this plan does not touch runner semantics)
- `## 9. Compatibility with ap.recipe/v0` (UPDATE to note v0.1.1 is additive over v0.1)
- `## 10. Out of scope for v0.1` (ADD v0.1.1-era deferrals from CONTEXT §Out of scope)

Current §1 apiVersion row (line 40):
  `| apiVersion | string | yes | Must be exactly ap.recipe/v0.1 for this schema. Older ap.recipe/v0 recipes are still accepted by the runner but treated as v0.1-compatible (all new fields optional). |`

The doc currently does NOT mention:
- The `$defs` discriminator structure (D-01)
- The `annotations` escape valve (D-08)
- `metadata.license` / `metadata.maintainer` (D-09)
- Upper bounds on any timeout (D-06)
- maxLength on `name` (D-05)
- The `source.ref` allowlist pattern (D-04)
- Full uid_t range on `owner_uid` (D-07)
- The WR-05 verdict enum tightening (D-03)
- "JSON Schema is authoritative" precedence rule

All of those are narrative gaps this plan closes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/RECIPE-SCHEMA.md to v0.1.1 narrative — versioning seam, bounds, annotations escape valve, license/maintainer advisory, out-of-scope refresh</name>
  <read_first>
    - docs/RECIPE-SCHEMA.md (FULL — you are threading narrative updates across §1-§10, not rewriting)
    - .planning/phases/18-schema-maturity/18-CONTEXT.md (D-01 through D-11 — every narrative addition must cite the decision it reflects; the §Out of scope section of CONTEXT lists exactly which items must be documented as deferred)
    - .planning/phases/18-schema-maturity/18-02-PLAN.md (the schema refactor — the markdown must describe what Plan 02 actually landed, not what was proposed)
    - tools/ap.recipe.schema.json (post-Plan-02 state — cross-check every row you change against the authoritative schema)
  </read_first>
  <files>docs/RECIPE-SCHEMA.md</files>
  <action>
    Thread updates through the existing structure. Do NOT reorganize the document top-to-bottom. Do NOT remove existing sections. Keep the prose voice (authoritative, present tense, example-driven) consistent with what's already there.

    ### Edit 1 — File header (H1 + first paragraph)

    Replace the H1 from `# Agent Playground Recipe Schema — ap.recipe/v0.1` to `# Agent Playground Recipe Schema — ap.recipe/v0.1.1`.

    After the existing opening paragraph (which names runner + orchestrator as consumers), ADD a new paragraph:

    > **Version policy.** `ap.recipe/v0.1.1` is additive over `ap.recipe/v0.1`: every field in a valid v0.1 recipe remains valid, and new recipe authors see tightened bounds (ref allowlist, name length, timeout maxima), an `annotations` escape valve on every section, optional `metadata.license` and `metadata.maintainer`, and a `$defs`-based versioning seam (§10.1). **The JSON Schema at `tools/ap.recipe.schema.json` is authoritative.** When this markdown and the schema disagree, the schema wins and this document is the bug.

    ### Edit 2 — §File shape at a glance

    Keep the YAML sketch but update the `metadata:` line from:
      `metadata: { recon_date, recon_by, source_citations[] }`
    to:
      `metadata: { recon_date, recon_by, source_citations[], license?, maintainer? }`

    Add a trailing line after the sketch:
      `Each major section (build, runtime, invoke, smoke, metadata) may also carry an optional annotations: { ... } escape-valve block — see §11.`

    ### Edit 3 — §1 Top-level identity

    In the `apiVersion` row, replace the cell text with:

    > Must be exactly `ap.recipe/v0.1`. The JSON Schema uses a `$defs.v0_1` discriminator branch to enforce this (see §10.1 "Versioning seam"). Older `ap.recipe/v0` recipes are accepted by the runner but treated as v0.1-compatible.

    In the `name` row, append to the existing cell text:

    > Maximum length 64 characters; used as the image-tag suffix (`ap-recipe-<name>`) where the Docker tag limit of 128 leaves headroom for the `ap-recipe-` prefix (D-05).

    ### Edit 4 — §2 source

    In the `source.ref` row, replace the existing cell with:

    > Git ref — SHA (preferred, reproducible), tag, or branch name. Must match the allowlist pattern `^[a-zA-Z0-9._/-]{1,255}$` (D-04). The pattern admits plain ref characters including `/` (for `refs/heads/main`) and `.` (for `v1.2.3`) but rejects shell metacharacters and `--upload-pack=<cmd>`-style option-as-value injections that `git fetch` would otherwise accept. Shallow-clone friendly: the runner `git clone --depth=1`, then attempts a pinned fetch; if the fetch fails it falls back to shallow HEAD with a warning.

    ### Edit 5 — §3 build

    After §3.1 "build.mode: upstream_dockerfile" table, ADD these two new rows to the same table:

    | `build.timeout_s` | int | no | Max wall time for `docker build` (upstream_dockerfile) or `docker pull` (image_pull). Range `[1, 10800]` seconds (3h ceiling — cold builds for image-heavy agents like hermes or openclaw routinely exceed 15min; 3h is ample). Default 900 when absent. |
    | `build.clone_timeout_s` | int | no | Max wall time for git clone + checkout in upstream_dockerfile mode. Range `[1, 1800]` seconds (30min ceiling for large repos). Default 300 when absent. |

    (If these rows already exist from Phase 10, UPDATE their range cell to include the new maxima rather than adding duplicates.)

    At the bottom of §3, add:

    > Each `build:` block may carry an optional `annotations: { ... }` object — see §11. New recipes should prefer `annotations` over `observed` for recon metadata; `observed` remains accepted for back-compat.

    ### Edit 6 — §4 runtime

    In §4.3 `volumes[]`, the `owner_uid` row replace with:

    > UID the container runs as, documented so operators know what the bind mount must be writable for. Range `[0, 4294967295]` — full Linux `uid_t`. Values greater than 65535 typically indicate a userns-remapped container. Values seen in the committed catalog: 0 (alpine root), 1000 (node/nanobot), 10000 (hermes), 65534 (nullclaw release target) (D-07).

    At the end of §4, add:

    > Each `runtime:` block may carry an optional `annotations: { ... }` object — see §11.

    ### Edit 7 — §5 invoke

    At the end of §5, add:

    > Each `invoke:` block may carry an optional `annotations: { ... }` object — see §11.

    ### Edit 8 — §6 smoke

    In §6.1 "Default probe", the `smoke.timeout_s` row replace with:

    > Max wall time for the container run. Range `[1, 3600]` seconds (1h ceiling — smoke probes should not run longer than a human attention span). Default 180 (D-06).

    In §6.3 "Cell matrices", update the `smoke.verified_cells[]` row to note the required shape per-cell: `model`, `verdict ∈ {PASS, FAIL}`, `category ∈ $defs.category` (11 values), `detail` (string).

    For `smoke.known_incompatible_cells[]`, update the row to explicitly state: `verdict` is constrained to the same enum as `verified_cells[].verdict` (`{PASS, FAIL}`). This closes the silent-typo gap (formerly unconstrained — see Phase 10 review WR-05; D-03).

    At the end of §6, add:

    > Each `smoke:` block, and each item in `verified_cells[]` and `known_incompatible_cells[]`, may carry an optional `annotations: { ... }` object — see §11.

    ### Edit 9 — §7 metadata

    Append two new rows to the existing table:

    | `metadata.license` | string | no (today) | SPDX identifier (e.g. `MIT`, `Apache-2.0`). **Optional in v0.1.1; required before external contribution lands in phase 19+.** Documented here as the optional-today-required-later pattern (D-09). |
    | `metadata.maintainer` | object `{ name, url? }` | no (today) | `name` is a string (maintainer handle or full name). `url` is an optional string (maintainer homepage or contact URL, `format: uri`). **Optional in v0.1.1; required before external contribution lands in phase 19+.** (D-09) |

    After the table, add:

    > Each `metadata:` block may also carry an optional `annotations: { ... }` object — see §11.

    ### Edit 10 — §9 Compatibility (update existing section)

    Replace the single paragraph with:

    > All 5 recipes committed at the v0.1 freeze (`hermes`, `openclaw`, `picoclaw`, `nullclaw`, `nanobot`) declare `apiVersion: ap.recipe/v0.1`. v0.1.1 is additive over v0.1: every v0.1 recipe remains a valid v0.1.1 recipe. The runner accepts both `v0` and `v0.1`/`v0.1.1` and applies identical validation — fields added in v0.1.1 (bounds, `annotations`, optional `license`/`maintainer`) are all optional, so an unmodified v0 or v0.1 recipe sweeps green against the v0.1.1 schema. This is the regression gate enforced by `tools/tests/test_schema_selfcheck.py` (Phase 18 D-10).

    > New recipes should declare `apiVersion: ap.recipe/v0.1`. The minor-version bump to v0.1.1 reflects schema maturation, not a wire-format change.

    ### Edit 11 — §10 Out of scope for v0.1 (update existing section)

    Rename the heading to `## 10. Out of scope for v0.1.1`.

    Replace the existing bulleted list with a superset that adds the CONTEXT.md §"Out of scope (explicitly deferred)" items:

    **Deferred to a future `ap.recipe/v0.2` or later:**
    - `runtime.external_services[]` — sidecar containers a recipe depends on (needed for NanoClaw's OneCLI Agent Vault pattern).
    - `setup.interactive: true` — AI-native fork-and-customize install flow.
    - `invoke.spec.response_source: trajectory_file` — agents that write their response to a file instead of stdout (OpenHands pattern).
    - `invoke.mode: http_post` — long-running service + REST API invocation.
    - `invoke.mode: repl_pty` — REPL-driven agents that can't be driven from a single argv.
    - Compound `pass_if` logic (AND/OR of multiple verbs).
    - `stdout_filter.engine` values other than `awk`.
    - Multi-host orchestration fields.
    - **Capability advertisement block** (MCP-style `capabilities.{streaming, trajectories, multi_turn}`) — deferred to Phase 22 or later.
    - **`runtime.limits`** with token/turn/cost budgets (currently smuggled into `argv`) — deferred to Phase 23.
    - **GPU / hardware declaration** (`build.gpu`, `build.cuda`) — deferred until the first GPU-requiring backlog agent triggers it.
    - **Collapse of the 3 `known_*` arrays into `known_issues[]` with a typed discriminator** — shape change, deferred to `v0.2`.
    - **Richer `verified_cells[]`** with `probe_id`, typed metrics (`tokens_in/out`, `cost_usd`), env fingerprint — deferred to Phase 24.
    - **Making `metadata.license` + `metadata.maintainer` required** — flips when external contribution lands, not today.

    If a new agent needs one of these, it is **blocked by format** until the relevant phase — not a v0.1.1 patch.

    ### Edit 12 — ADD new §10.1 "Versioning seam" (new subsection right before §11)

    Between §10 and what will become §11, add a new subsection that documents D-01 in prose. Draft:

    `## 10.1 Versioning seam`

    > The JSON Schema's root uses `oneOf: [ { "$ref": "#/$defs/v0_1" } ]` as a discriminator branch keyed on `apiVersion`. The full v0.1/v0.1.1 body lives under `$defs.v0_1`. Adding `ap.recipe/v0.2` will append a second `$defs.v0_2` branch and extend the `oneOf` array — a purely additive schema change that does not break v0.1 recipes. Recipes declaring `apiVersion: ap.recipe/v0.1` continue to match the v0_1 branch exactly because each branch body carries its own `apiVersion: { "const": "ap.recipe/v0.N" }` constraint. This is the Kubernetes CRD versioning idiom applied to recipes.
    >
    > The 11-value category enum (`PASS`, `ASSERT_FAIL`, `INVOKE_FAIL`, `BUILD_FAIL`, `PULL_FAIL`, `CLONE_FAIL`, `TIMEOUT`, `LINT_FAIL`, `INFRA_FAIL`, `STOCHASTIC` reserved, `SKIP` reserved) lives once at `$defs.category` and is referenced via `$ref` from both `verified_cells[].category` and `known_incompatible_cells[].category` (D-02). Adding a category is a one-line edit in a single place.

    ### Edit 13 — ADD new §11 "Annotations escape valve"

    At the end of the document, after the (renamed) §10 and the new §10.1, add:

    `## 11. Annotations escape valve`

    > Every major section — `build`, `runtime`, `invoke`, `smoke`, `metadata` — and every item in `verified_cells[]` and `known_incompatible_cells[]` may carry an optional `annotations: { ... }` object. Keys inside `annotations` are open (`additionalProperties: true`); keys at any other level remain strictly enumerated (`additionalProperties: false`). This pattern matches OpenAPI's `x-*` extensions and Kubernetes' `annotations` field: a strict known shape with an explicit extension point.
    >
    > Use `annotations` for recon observations, tool-specific metadata, experimental fields that might become first-class in a future version, and anything that would otherwise fight the schema's strict-by-default stance. Example:
    >
    > ```yaml
    > build:
    >   mode: upstream_dockerfile
    >   annotations:
    >     recon.image_size_gb: 5.19
    >     recon.wall_time_s: 451
    >     recon.host_os: darwin 25.3.0 arm64
    > ```
    >
    > The legacy `build.observed` object is preserved for back-compat with the 5 committed recipes but is **deprecated in favor of `build.annotations` for new recipes**.

    ### Formatting + validation

    - Keep Markdown table formatting consistent with the rest of the file (pipe-separated, Header/---/rows).
    - Keep blockquote voice where cells become multi-sentence — match existing style.
    - Preserve all existing anchors unless renamed (§10 heading changes from "v0.1" to "v0.1.1"; all others stay).

    After editing, verify the markdown renders as a valid document: run `python3 -c "import pathlib; t=pathlib.Path('docs/RECIPE-SCHEMA.md').read_text(); assert 'v0.1.1' in t; assert '$defs.v0_1' in t; assert 'annotations' in t.lower(); assert 'maxLength' in t or 'maximum length' in t.lower(); assert 'maintainer' in t; assert 'license' in t; assert 'authoritative' in t.lower(); print('markdown landmarks present')"`.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && python3 -c "import pathlib; t=pathlib.Path('docs/RECIPE-SCHEMA.md').read_text(); assert 'v0.1.1' in t, 'version bump missing'; assert 'authoritative' in t.lower(), 'precedence rule missing'; assert '\$defs' in t or 'defs.v0_1' in t, 'versioning seam missing'; assert 'annotations' in t.lower(), 'annotations section missing'; assert 'maintainer' in t, 'maintainer row missing'; assert 'license' in t.lower(), 'license row missing'; assert 'owner_uid' in t and '4294967295' in t, 'owner_uid bound doc missing'; assert 'maxLength' in t or 'maximum length' in t.lower() or 'max length' in t.lower() or '64' in t, 'name maxLength not documented'; assert '3600' in t, 'smoke timeout max not documented'; assert '10800' in t, 'build timeout max not documented'; assert '1800' in t, 'clone timeout max not documented'; assert 'a-zA-Z0-9' in t or 'allowlist' in t.lower(), 'ref allowlist not documented'; assert 'WR-05' in t or 'PASS' in t and 'known_incompatible_cells' in t, 'verdict enum tightening not clearly documented'; assert 'userns' in t.lower() or '65535' in t, 'uid remap note missing'; print('all landmarks present')"</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && python3 -c "import pathlib; t=pathlib.Path('docs/RECIPE-SCHEMA.md').read_text(); ooc=t.split('Out of scope')[1] if 'Out of scope' in t else ''; deferred=['capabilities','runtime.limits','GPU','known_issues','license','maintainer']; missing=[d for d in deferred if d.lower() not in ooc.lower()]; assert not missing, f'Out of scope section missing deferred items: {missing}'; print('out-of-scope refresh OK')"</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && git diff --stat docs/RECIPE-SCHEMA.md 2>/dev/null | tail -2 || echo "diff check pending"</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/tools && python3 -m pytest -q 2>&1 | tail -5</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && git diff --stat tools/ recipes/ 2>&1 | tail -3</automated>
  </verify>
  <done>
    - `docs/RECIPE-SCHEMA.md` H1 says `ap.recipe/v0.1.1`.
    - Precedence rule ("JSON Schema is authoritative") appears verbatim.
    - New §10.1 "Versioning seam" explains the `oneOf`/`$defs.v0_1` pattern and the `$defs.category` extraction.
    - New §11 "Annotations escape valve" documents where `annotations` may appear and notes `build.observed` deprecation.
    - §1 name row mentions maxLength 64.
    - §1 apiVersion row references §10.1.
    - §2 source.ref row documents the allowlist pattern.
    - §3 build table has `timeout_s` (max 10800) and `clone_timeout_s` (max 1800).
    - §4 volumes.owner_uid row documents the `[0, 4294967295]` range + userns-remap note.
    - §6 smoke.timeout_s documents max 3600.
    - §6 known_incompatible_cells row documents the `{PASS, FAIL}` verdict enum.
    - §7 metadata table has new optional rows for `license` and `maintainer`.
    - §9 Compatibility section explicitly says v0.1.1 is additive over v0.1 and references the self-check test.
    - §10 Out of scope section renamed to v0.1.1 and includes the CONTEXT-listed deferrals (capabilities, runtime.limits, GPU, known_issues collapse, required license/maintainer).
    - No other file was edited (`git diff` shows only `docs/RECIPE-SCHEMA.md`).
    - Full `pytest -q` still green (markdown edits do not affect tests; this is a safety net).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe author ↔ spec prose | Human-authored Markdown is read by recipe authors. No input-validation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Repudiation | doc-schema drift | mitigate | Precedence rule ("JSON Schema is authoritative; when the markdown and schema disagree, schema wins") is stated in the doc header. Phase 17's doc-runner sync check will later enforce this automatically via pytest. |
| T-18-10 | Information Disclosure | narrative examples | accept | Example YAML blocks do not contain secrets. The `annotations` example uses recon metadata (`recon.image_size_gb` etc.) which is already present in committed recipes' `build.observed`. |
</threat_model>

<verification>
- `grep -c "v0.1.1" docs/RECIPE-SCHEMA.md` returns at least 3 (H1 + header paragraph + compatibility section).
- `grep -c "annotations" docs/RECIPE-SCHEMA.md` returns at least 5 (per-section references + §11 definition).
- `grep -c "maintainer" docs/RECIPE-SCHEMA.md` returns at least 2.
- `grep -c "authoritative" docs/RECIPE-SCHEMA.md` returns at least 1.
- `grep -c '4294967295' docs/RECIPE-SCHEMA.md` returns at least 1.
- `grep -c '3600\|10800\|1800' docs/RECIPE-SCHEMA.md` returns at least 3 (one per timeout bound).
- `git diff --name-only` returns ONLY `docs/RECIPE-SCHEMA.md`.
- Full `pytest -q` still green.
</verification>

<success_criteria>
1. `docs/RECIPE-SCHEMA.md` updated to describe the v0.1.1 schema shape landed in Plan 02.
2. Every bound (D-04, D-05, D-06, D-07), every enum tightening (D-03), the annotations escape valve (D-08), and the license/maintainer advisory (D-09) are narratively documented.
3. The `oneOf`/`$defs.v0_1` versioning seam (D-01) is explained in a new §10.1.
4. The category extraction (D-02) is explained in §10.1.
5. The precedence rule ("JSON Schema is authoritative") is stated.
6. The out-of-scope section is refreshed with CONTEXT's v0.1.1 deferrals.
7. `git diff` touches only `docs/RECIPE-SCHEMA.md`.
8. All pre-existing tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/18-schema-maturity/18-03-SUMMARY.md` following the standard template. Record:
- List of section additions (new §10.1, new §11) and section updates (§1, §2, §3, §4, §6, §7, §9, §10).
- Confirmation that no file outside `docs/RECIPE-SCHEMA.md` was edited.
- The 8 landmark-grep checks from the verification block, with their actual counts.
</output>
