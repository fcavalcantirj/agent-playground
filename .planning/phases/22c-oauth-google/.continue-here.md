---
context: phase
phase: 22c-oauth-google
task: 3
total_tasks: 4
status: paused_blocked_on_docker
last_updated: 2026-04-20T02:29:10.840Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Dockerfile drift from pyproject.toml** — `tools/Dockerfile.api` hardcodes its pip install list and DOES NOT read pyproject.toml. When Phase 22c added `authlib` + `itsdangerous` to `api_server/pyproject.toml`, the Docker image built without them and the api_server container crashed at boot with `ModuleNotFoundError: No module named 'itsdangerous'`. FIX APPLIED: commit `4f7d8b0` added both deps to the Dockerfile's pip install line. STRUCTURAL PREVENTION: any future phase that adds a production runtime dependency to `pyproject.toml` MUST also update `tools/Dockerfile.api` (or, long-term, drive the image's pip install from pyproject.toml).

- [ ] CONSTRAINT: **Phase 22c is OAuth-identity-only scope (AMD-02)** — Do NOT expand 22c to address the 3 out-of-scope UX findings surfaced during smoke (Alex Chen on /playground, /#playground fragment bug, Telegram conditional fields). These belong in 22c.1 gap closure. Scope discipline per user feedback: don't half-finish things by piling on.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fabricate smoke PASS | Wave 5 Task 3 is a D-22c-TEST-02 manual gate requiring real browser OAuth consent. Do NOT mark it complete without a human operator confirming all 6 scenarios. Scenarios 5 + 6 are already PASS (curl-automatable). Scenarios 1-4 require human eyeballs. | blocking | Orchestrator must present `test/22c-manual-smoke.md` to the user and receive explicit PASS/FAIL per scenario. Auto_advance=true does NOT apply to human-action checkpoints. |
| Docker bind-mount assumption | Earlier in the session I assumed `api_server/src/` was bind-mounted (would auto-refresh). It is NOT. The image bakes the source at build time via `COPY api_server/src /src/api_server/src`. Only `../recipes:/app/recipes:ro` is bind-mounted. | advisory | Rebuild `api_server` image after every backend code change intended to exercise the live stack: `docker compose --env-file deploy/.env.prod -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml build api_server && ... up -d api_server`. |

<current_state>
Phase 22c-oauth-google — Wave 5 (the final wave) is mid-execution, blocked mid-smoke on a Docker daemon hang.

All 9 plans are committed. All automated tests pass. The only open gate is the D-22c-TEST-02 manual browser smoke — 4 of its 6 scenarios still need human action.

The user hit Docker daemon unresponsiveness mid-browser-test and has to restart the whole machine.
</current_state>

<completed_work>

## All 9 Phase 22c plans shipped and committed

| Plan | Wave | Commit (last of chain) | One-liner |
|------|------|------------------------|-----------|
| 22c-01 | 0 | 0cc2c23 | SPIKE-A (respx×authlib 1.6.11) + SPIKE-B (TRUNCATE 7-table Mode B) — both green |
| 22c-02 | 1 | 29e0f7c | alembic 005 live-applied; sessions table + users.{sub,avatar_url,last_login_at} + partial unique index |
| 22c-03 | 1 | 1be3dda | 7 Pydantic env fields + authlib registry (Google OIDC + GitHub non-OIDC) + fail-loud-in-prod |
| 22c-04 | 2 | 3a6de74 | Custom ASGI SessionMiddleware (PG lookup + 60s throttle + PG-outage fail-open) + log_redact extension |
| 22c-05 | 3 | 5747e83 | auth/deps.require_user (inline JSONResponse \| UUID) + 5 OAuth routes + /v1/users/me + 20 tests green (32 total incl pre-existing) |
| 22c-06 | 4 | 8fec917 | alembic 006 live; 8-table TRUNCATE + ANONYMOUS purge; 4 routes rewired; agent_status Bearer gap closed; 108 tests pass |
| 22c-07 | 4 | 55a2572 | Frontend login + dashboard + navbar rewritten; zero setTimeout; real window.location.href OAuth buttons; sonner error toasts; useUser hook |
| 22c-08 | 4 | 043f65f | Next 16.2 proxy.ts server-side gate (307 before React); middleware.ts deleted; next.config.mjs redirects /signup + /forgot-password |
| 22c-09 T1+T2 | 5 | ecca249 | Cross-user isolation integration test (1 passed in 4.60s) + manual smoke checklist file + R8 Layer 3 belt-and-suspenders (8-table COUNT=0 pre-assertion) |

## Smoke scenarios already automated-verified

| # | Scenario | Status | Evidence |
|---|----------|--------|----------|
| 5 | `/dashboard` server-side 307 gate before React renders | ✅ PASS | `curl -sI http://localhost:3000/dashboard` → `HTTP/1.1 307 Temporary Redirect` + `location: /login` |
| 6a | `/signup` → `/login` 307 | ✅ PASS | Same curl pattern |
| 6b | `/forgot-password` → `/login` 307 | ✅ PASS | Same curl pattern |

## Out-of-scope plan gap patched

Commit `4f7d8b0` — `fix(22c-09): Dockerfile missing authlib + itsdangerous`. Added both deps to the `RUN pip install --no-cache-dir --prefix=/install \` chain in `tools/Dockerfile.api`. Without this the api_server container crashed at boot with `ModuleNotFoundError: itsdangerous`.
</completed_work>

<remaining_work>

## Wave 5 remaining

- **22c-09 T3 checkpoint** (manual smoke gate — human action):
  - Scenario 1: Google happy path — browser click through Google consent → land on /dashboard with real name (not "Alex Chen") + ap_session cookie Max-Age ~2592000
  - Scenario 2: GitHub happy path — browser click through GitHub → same shape, private-email fallback via /user/emails verified in PG
  - Scenario 3: access_denied — click Cancel on Google consent → /login?error=access_denied + sonner toast
  - Scenario 4: logout invalidation — click Log out → curl replay of copied cookie → HTTP 401 Stripe-envelope

- **22c-09 T3 post-checkpoint** (autonomous after user confirms ALL PASS):
  - Update STATE.md to Phase 22c COMPLETE + resume anchor pointing at next phase
  - Commit: `docs(22c): Phase 22c complete — STATE.md resume anchor updated`

- **22c-09 T4** (autonomous):
  - Create `.planning/phases/22c-oauth-google/22c-09-SUMMARY.md`
  - Flip ROADMAP.md 22c-09 checkbox `[x]`
  - Final metadata commit: `docs(22c-09): complete cross-user isolation + manual smoke plan`

## Phase 22c close-out (autonomous after Wave 5 complete)

- code_review_gate (gsd:code-review skill on 22c source changes)
- regression_gate (prior phases' tests)
- schema_drift_gate (alembic 005 + 006 were pushed live; should pass)
- verify_phase_goal (gsd-verifier spawns)
- update_roadmap (phase.complete CLI)
- auto_copy_learnings (if workflow.global_learnings=true; default false)
- update_project_md
- offer_next routing
</remaining_work>

<decisions_made>

- **Commit Dockerfile fix within Phase 22c** (as 22c-09) — it is a plan gap we uncovered and blocks the whole smoke gate; better to surface and fix than paper over.
- **Do NOT expand Phase 22c to address the /playground + navbar "Alex Chen" + /#playground + Telegram conditional fields issues** — these are pre-existing mock-data leftovers surfaced during user-driven smoke. They are out of 22c scope (AMD-02: identity only). Log as 22c.1 gap-closure candidates.
- **Checkpoint type = human-action, not human-verify** — auto_advance=true does NOT auto-approve. Must wait for real user report of scenarios 1-4.
- **SPIKE-B ran in Mode B (7-table) not Mode A (8-table)** — expected per plan design, since Wave 0 runs before migration 005 exists. Layer 2 (22c-06 Task 1) + Layer 3 (22c-09 Task 1 STEP 0) cover the 8-table case.
</decisions_made>

<blockers>
- **Docker daemon hung** (primary): `docker version` returns nothing, all docker ops time out. User is restarting machine.
- **Cannot exercise OAuth flow until Docker is back**: the api_server image needs a rebuild (to pick up commit `4f7d8b0`) before `curl http://localhost:8000/v1/auth/google` can 302-redirect properly.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable pause state
2. `memory/MEMORY.md` (auto-loaded anyway) — project memory index
3. `memory/project_phase_22c_handoff.md` — full Phase 22c pre-execute brief
4. `.planning/phases/22c-oauth-google/22c-09-PLAN.md` — the plan's task breakdown and resume rules
5. `test/22c-manual-smoke.md` — the 6-scenario checklist the human must run
6. Git log `git log --oneline 394fd7f..HEAD` — the complete Phase 22c commit chain (12 commits incl. the Dockerfile fix)

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN: Marking Wave 5 complete without real human smoke PASS** → Structural: orchestrator MUST present `test/22c-manual-smoke.md` scenarios to the user and wait for explicit PASS before writing SUMMARY.md or updating STATE.md. auto_advance=true does NOT apply to human-action checkpoints.
- **ANTI-PATTERN: Assuming api_server source is bind-mounted into the container** → Structural: after ANY api_server source change, rebuild: `docker compose --env-file deploy/.env.prod -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml build api_server && ... up -d api_server`.
- **ANTI-PATTERN: Expanding 22c scope to the /playground Alex Chen leftover** → Out of scope. File to 22c.1 queue.

## Infrastructure State

- **Docker daemon**: HUNG. Requires machine restart.
- **api_server container**: last-running image does NOT contain the Dockerfile fix (commit 4f7d8b0). Will need rebuild + restart post-reboot.
- **postgres container**: state on disk in `pgdata` volume; last confirmed state = alembic HEAD `006_purge_anonymous` + all 8 data tables COUNT=0. Should survive the reboot cleanly.
- **frontend dev server (pnpm dev)**: stopped (pkill ran).
- **Git**: clean working tree (apart from untracked `recon/` and `api_server/uv.lock` that were already untracked). No WIP edits pending. 12 Phase 22c commits on `main`: `dc43879 → 4f7d8b0`.
- **deploy/.env.prod**: AP_OAUTH_STATE_SECRET in place (generated via `openssl rand -hex 32` at session start); 6 OAuth provider env vars present (the 2 client secrets are chat-leaked and should be rotated before any prod deploy).

<context>
This session executed all of Phase 22c end-to-end in one go. The executor agents auto-advanced cleanly from Wave 0 through Wave 4 — 8 plans, 32 commits (including metadata + test + feature commits per plan). Wave 5 Task 1 and 2 (automated + checklist file) committed fine.

When the user said "run it" for the smoke, I tried to exercise the live Docker stack and discovered:
1. The api_server container was running an image built 6 hours earlier — BEFORE Phase 22c OAuth routes existed (routes/auth.py missing from the image entirely).
2. Rebuild-then-restart revealed a plan gap: the Dockerfile's hardcoded pip install list didn't include authlib or itsdangerous (pyproject.toml was updated by Wave 0 but Dockerfile was never touched). Container crashed at boot.
3. Fixed Dockerfile inline (commit 4f7d8b0), started a second rebuild.
4. During the second rebuild / restart cycle the Docker daemon became fully unresponsive.
5. User then invoked /gsd-pause-work so we can resume after machine restart.

Scenarios 5 and 6 already passed against `pnpm dev` (before the daemon hung). The 4 human-action scenarios remain.

Don't forget the 3 out-of-scope UX findings the user spotted (they're in HANDOFF.json under `out_of_scope_findings`). They are candidates for a future 22c.1 phase — do NOT rush them into 22c.
</context>

<next_action>
Start with, in order:

1. Confirm Docker is responsive after reboot: `docker version --format '{{.Server.Version}}'` (should print a version, not hang).
2. Rebuild + restart api_server so the Dockerfile fix (commit 4f7d8b0) lands:
   ```
   docker compose --env-file deploy/.env.prod -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml build api_server
   docker compose --env-file deploy/.env.prod -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml up -d api_server
   ```
3. Wait for healthz: `curl -s http://localhost:8000/healthz` should return `{"ok":true}`.
4. Verify OAuth routes wired: `curl -sI http://localhost:8000/v1/auth/google` should return `HTTP/1.1 302 Found` with a `location:` header pointing at `https://accounts.google.com/...` — if it still returns 404, the rebuild didn't pick up or there's a second plan gap.
5. Start frontend dev server: `cd frontend && pnpm dev` (wait for `localhost:3000` HTTP 200).
6. Re-run scenarios 5 + 6 via curl — spot-check the stack after reboot.
7. Hand scenarios 1-4 to the user with `test/22c-manual-smoke.md` — wait for `ALL PASS` (or per-scenario FAIL with notes).
8. On PASS: spawn continuation executor for 22c-09 Task 3 (STATE.md close-out) + Task 4 (SUMMARY.md + metadata commit).
9. Close Phase 22c: code review → regression → schema drift → verifier → roadmap update → offer_next.

Do NOT touch the 3 out-of-scope UX findings. Surface them at phase close-out as candidates for 22c.1.
</next_action>
