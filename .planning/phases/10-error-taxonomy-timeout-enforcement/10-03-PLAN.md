---
phase: 10-error-taxonomy-timeout-enforcement
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - tools/ap.recipe.schema.json
autonomous: true
requirements: [D-01, D-02]
tags: [json-schema, taxonomy, tightening]

must_haves:
  truths:
    - "`verified_cells[]` items REQUIRE both `category` and `detail` (in addition to existing `model` + `verdict`)"
    - "`known_incompatible_cells[]` items REQUIRE both `category` and `detail`"
    - "`verdict` enum on `verified_cells[]` stays `[PASS, FAIL]` (STOCHASTIC is NOT a valid `verdict` value — it lives in `category` now)"
    - "All 5 migrated recipes from Plan 02 still pass lint under the tightened schema"
  artifacts:
    - path: "tools/ap.recipe.schema.json"
      provides: "Tightened schema with category+detail REQUIRED on both cell types"
  key_links:
    - from: "tools/ap.recipe.schema.json smoke.verified_cells.items.required"
      to: "new 4-element required array"
      via: '["model", "verdict", "category", "detail"]'
      pattern: '"required":\s*\["model",\s*"verdict",\s*"category",\s*"detail"\]'
---

<objective>
Tighten `tools/ap.recipe.schema.json` to REQUIRE `category` and `detail` on every `verified_cells[]` and `known_incompatible_cells[]` entry. This is step C of the 5-step sequencing in PATTERNS.md. Safe to run now because Plan 02 migrated all 5 committed recipes — they already carry the new fields.

Purpose: Enforce the D-01/D-02 verdict shape as a schema invariant. Without this tightening, a future recipe author could forget `category` and the lint gate would let it through. Tightening now closes the contract.

Output: One modified JSON file. No Python or recipe changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-CONTEXT.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-RESEARCH.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-PATTERNS.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-01-SUMMARY.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-02-SUMMARY.md
@tools/ap.recipe.schema.json

<interfaces>
<!-- Current state after Plan 01 (schema loosened) + Plan 02 (recipes migrated). -->
<!-- The `required` arrays are still `["model", "verdict"]` — that's what we're tightening. -->
<!-- See PATTERNS.md §"Enum-addition pattern" for the exact target. -->

Target shape for `verified_cells.items.required`:
```json
"required": ["model", "verdict", "category", "detail"]
```

Target shape for `known_incompatible_cells.items.required`:
```json
"required": ["model", "verdict", "category", "detail"]
```

NOTE on `known_incompatible_cells.items.verdict`: currently `"type": "string"` with NO enum constraint (schema line 337, verified via Read). PATTERNS.md §"recipes/hermes.yaml" calls this out explicitly: "the existing schema at lines 330-346 defines `known_incompatible_cells.items.verdict` as plain `\"type\": \"string\"` (not enum-constrained) ... Phase 10 does not tighten that." So we do NOT add an enum constraint here. Plan 02's migration already changed `verdict: STOCHASTIC` to `verdict: FAIL` so no cell in the repo violates a `[PASS, FAIL]` enum even if one existed — but we leave `verdict` on known_incompatible as plain string to keep historical slack for recon notes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten required arrays on both verified_cells and known_incompatible_cells items to include category+detail</name>
  <read_first>
    - tools/ap.recipe.schema.json (the file being edited — full read; the `verified_cells` block is around lines 298-324+ after Plan 01's additions, `known_incompatible_cells` is around lines 326-347+)
    - .planning/phases/10-error-taxonomy-timeout-enforcement/10-CONTEXT.md §D-02 (verdict shape)
    - .planning/phases/10-error-taxonomy-timeout-enforcement/10-PATTERNS.md §"Enum-addition pattern" (shows exact required-array target)
    - .planning/phases/10-error-taxonomy-timeout-enforcement/10-02-SUMMARY.md (confirms 5 recipes carry the new fields)
  </read_first>
  <files>tools/ap.recipe.schema.json</files>
  <action>
    Make TWO surgical edits to the schema.

    **EDIT 1: `smoke.verified_cells.items.required`**

    Find the `required` array INSIDE `verified_cells.items` (NOT the top-level `required` at the schema root, NOT the one inside `build`). It currently reads:
    ```json
    "required": ["model", "verdict"],
    ```

    Replace with:
    ```json
    "required": ["model", "verdict", "category", "detail"],
    ```

    Preserve indentation and trailing comma style exactly.

    **EDIT 2: `smoke.known_incompatible_cells.items.required`**

    Find the `required` array INSIDE `known_incompatible_cells.items`. It currently reads:
    ```json
    "required": ["model", "verdict"],
    ```

    Replace with:
    ```json
    "required": ["model", "verdict", "category", "detail"],
    ```

    **DO NOT** modify any other `required` array:
    - Top-level schema `required` (apiVersion, name, etc.) — LEAVE
    - `source.required` — LEAVE (source object has no required fields)
    - `build.required` — LEAVE (already `["mode"]`)
    - `runtime.required` — LEAVE
    - `runtime.process_env.required` — LEAVE
    - `runtime.volumes.items.required` — LEAVE
    - `invoke.required` — LEAVE
    - `invoke.spec.required` — LEAVE
    - `smoke.required` — LEAVE (already `["prompt", "pass_if", "verified_cells"]`)
    - `metadata.required` — LEAVE

    **DO NOT** add an `enum` to `known_incompatible_cells.items.verdict`. It stays `"type": "string"` with no enum — PATTERNS.md §"recipes/hermes.yaml" explicitly documents this scope boundary.

    **DO NOT** touch the `verified_cells.items.verdict` enum — it stays `["PASS", "FAIL"]`. STOCHASTIC belongs in `category`, not `verdict`.

    **After both edits: re-lint all 5 migrated recipes.** They must still pass because Plan 02 added `category` + `detail` to every cell.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && python3 -c "import json; s=json.load(open('tools/ap.recipe.schema.json')); vc_req=s['properties']['smoke']['properties']['verified_cells']['items']['required']; assert vc_req == ['model','verdict','category','detail'], f'verified_cells required wrong: {vc_req}'; kic_req=s['properties']['smoke']['properties']['known_incompatible_cells']['items']['required']; assert kic_req == ['model','verdict','category','detail'], f'known_incompatible_cells required wrong: {kic_req}'; assert s['properties']['smoke']['properties']['verified_cells']['items']['properties']['verdict']['enum'] == ['PASS','FAIL'], 'verified_cells verdict enum changed'; assert 'enum' not in s['properties']['smoke']['properties']['known_incompatible_cells']['items']['properties']['verdict'], 'known_incompatible_cells verdict should NOT have enum'; print('tightening OK')"</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/tools && pytest tests/test_lint.py tests/test_recipe_regression.py tests/test_roundtrip.py -x -q</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/tools && pytest -x -q</automated>
  </verify>
  <done>
    - `verified_cells.items.required` is `["model", "verdict", "category", "detail"]`
    - `known_incompatible_cells.items.required` is `["model", "verdict", "category", "detail"]`
    - No other `required` array modified
    - `verified_cells.items.verdict` enum unchanged (`["PASS", "FAIL"]`)
    - `known_incompatible_cells.items.verdict` stays plain string (no enum)
    - All 5 committed recipes still lint green
    - Full pytest suite green
    - Schema JSON parses
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipe YAML → jsonschema validator | New recipes must now carry category+detail to pass lint — this is the contract's teeth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | Recipe authors skipping category/detail | mitigate | Schema `required` tightening catches missing fields at lint time; `--lint` is a mandatory pre-step per Phase 09. |
</threat_model>

<verification>
- Schema `required` arrays tightened on both cell types
- All 5 committed recipes continue to pass lint
- Full `pytest -x -q` green
- Creating a test recipe without `category` produces a lint error (not manually verified here; Plan 05's `test_categories.py` will add a regression fixture — optional, not a blocker for this plan)
</verification>

<success_criteria>
1. `tools/ap.recipe.schema.json` tightens both items.required arrays
2. All 5 committed recipes still lint-pass
3. Full pytest suite green
4. No other schema fields modified
</success_criteria>

<output>
After completion, create `.planning/phases/10-error-taxonomy-timeout-enforcement/10-03-SUMMARY.md` recording the tightening and confirming backwards-compat with Plan 02's migrated recipes.
</output>
