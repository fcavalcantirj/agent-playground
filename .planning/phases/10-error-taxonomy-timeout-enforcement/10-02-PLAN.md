---
phase: 10-error-taxonomy-timeout-enforcement
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - recipes/hermes.yaml
  - recipes/openclaw.yaml
  - recipes/picoclaw.yaml
  - recipes/nullclaw.yaml
  - recipes/nanobot.yaml
  - scripts/migrate_recipes_phase10.py
autonomous: true
requirements: [D-04]
tags: [yaml, ruamel, migration, recipes]

must_haves:
  truths:
    - "All 5 committed recipes have `category: PASS` on every `verified_cells[]` entry (since every cell today has `verdict: PASS`)"
    - "All 5 committed recipes have `detail: \"\"` on every `verified_cells[]` entry"
    - "All `known_incompatible_cells[]` entries with current `verdict: FAIL` now have `category: ASSERT_FAIL` + a `detail` derived from the first sentence of `notes`"
    - "The hermes × google/gemini-2.5-flash `known_incompatible_cells` entry (currently `verdict: STOCHASTIC`) is remapped to `verdict: FAIL` + `category: ASSERT_FAIL` + `detail: \"flapping verdict — see notes\"` per D-04's temporary STOCHASTIC mapping"
    - "All 5 recipes still pass lint, round-trip, and recipe-regression tests after migration"
    - "YAML comments and ordering in all 5 recipes are preserved (ruamel round-trip)"
  artifacts:
    - path: "recipes/hermes.yaml"
      provides: "Migrated verified_cells[] and known_incompatible_cells[] with category+detail"
      contains: "category: PASS"
    - path: "recipes/openclaw.yaml"
      provides: "Migrated verified_cells[] and known_incompatible_cells[] with category+detail"
      contains: "category: PASS"
    - path: "recipes/picoclaw.yaml"
      provides: "Migrated verified_cells[] with category+detail"
      contains: "category: PASS"
    - path: "recipes/nullclaw.yaml"
      provides: "Migrated verified_cells[] with category+detail"
      contains: "category: PASS"
    - path: "recipes/nanobot.yaml"
      provides: "Migrated verified_cells[] with category+detail"
      contains: "category: PASS"
    - path: "scripts/migrate_recipes_phase10.py"
      provides: "One-shot ruamel.yaml round-trip migration script (committed then removable)"
      contains: "def migrate"
  key_links:
    - from: "scripts/migrate_recipes_phase10.py ruamel YAML configuration"
      to: "tools/run_recipe.py::_yaml configuration (lines 27-39)"
      via: "byte-identical instance setup"
      pattern: "YAML\\(typ=\"rt\"\\)"
    - from: "recipes/hermes.yaml known_incompatible_cells[0]"
      to: "D-04 STOCHASTIC→ASSERT_FAIL mapping"
      via: "verdict: FAIL + category: ASSERT_FAIL + detail: flapping..."
      pattern: "flapping verdict"
---

<objective>
Migrate all 5 committed recipes (hermes, openclaw, picoclaw, nullclaw, nanobot) to carry the Phase 10 fields on every cell, using a one-shot ruamel.yaml round-trip script. This is step B of the 5-step sequencing in PATTERNS.md. After this plan lands, `pytest tools/tests/test_lint.py tests/test_recipe_regression.py tests/test_roundtrip.py` stays green against the (still-optional) schema from Plan 01.

Purpose: D-04 backwards-compat — every existing cell gets `category` + `detail` fields so that Plan 03 can safely tighten the schema to `required`. The temporary STOCHASTIC→ASSERT_FAIL mapping for the single hermes × gemini-2.5-flash cell is applied per D-04 (Phase 15 restores true STOCHASTIC semantics).

Output: 5 modified YAML recipes + 1 new migration script. Zero runner code changes. Zero schema changes (Plan 01 already loosened it; Plan 03 will tighten later).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-CONTEXT.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-RESEARCH.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-PATTERNS.md
@.planning/phases/10-error-taxonomy-timeout-enforcement/10-01-SUMMARY.md
@tools/run_recipe.py
@recipes/hermes.yaml
@recipes/openclaw.yaml
@recipes/picoclaw.yaml
@recipes/nullclaw.yaml
@recipes/nanobot.yaml
@tools/tests/test_roundtrip.py

<interfaces>
<!-- ruamel YAML configuration — MUST be byte-identical to run_recipe.py (PATTERNS.md §Shared Pattern A). -->
<!-- Lifted verbatim from tools/run_recipe.py lines 27-39. -->

```python
from ruamel.yaml import YAML

_yaml = YAML(typ="rt")
_yaml.preserve_quotes = True
_yaml.width = 4096
_yaml.indent(mapping=2, sequence=4, offset=2)


def _represent_none(dumper, _data):
    return dumper.represent_scalar("tag:yaml.org,2002:null", "null")


_yaml.representer.add_representer(type(None), _represent_none)
```

Existing cell shape in hermes.yaml (lines 131-139 — REFERENCE):
```yaml
verified_cells:
  - model: anthropic/claude-haiku-4-5
    verdict: PASS
    wall_time_s: 12.86
    notes: Adopts persona cleanly. Response duplicated in stdout (Rich + plain).
```

Existing STOCHASTIC cell in hermes.yaml (lines 140-154 — REFERENCE):
```yaml
known_incompatible_cells:
  - model: google/gemini-2.5-flash
    verdict: STOCHASTIC
    notes: |
      Initial recon (2026-04-15) observed a deterministic FAIL:
      ...
```

Grep-verified known_incompatible_cells locations:
- recipes/hermes.yaml:140 (has 1 entry: google/gemini-2.5-flash, verdict=STOCHASTIC)
- recipes/openclaw.yaml:144 (has 1 entry, verdict=FAIL — standard mapping)
- recipes/picoclaw.yaml: absent
- recipes/nullclaw.yaml: absent
- recipes/nanobot.yaml: absent
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create one-shot migration script scripts/migrate_recipes_phase10.py that adds category+detail to every cell across the 5 committed recipes</name>
  <read_first>
    - .planning/phases/10-error-taxonomy-timeout-enforcement/10-CONTEXT.md §D-04 (exact migration rules)
    - .planning/phases/10-error-taxonomy-timeout-enforcement/10-RESEARCH.md §"Example 4: One-shot migration script sketch"
    - .planning/phases/10-error-taxonomy-timeout-enforcement/10-PATTERNS.md §"scripts/migrate_recipes_phase10.py (CREATE — optional one-shot migration)"
    - tools/run_recipe.py lines 27-39 (ruamel config — MUST be copied byte-identically)
    - tools/run_recipe.py lines 470-490 (`writeback_cell` — the existing ruamel round-trip reference pattern)
    - recipes/hermes.yaml (full — the only recipe with a STOCHASTIC cell; migration must handle it specially)
    - recipes/openclaw.yaml lines 144-154 (the standard known_incompatible_cells shape with verdict: FAIL)
    - tools/tests/test_roundtrip.py (understand what the round-trip regression asserts — so we don't break it)
  </read_first>
  <files>scripts/migrate_recipes_phase10.py</files>
  <action>
    Create a new file `scripts/migrate_recipes_phase10.py` (parent directory `scripts/` already exists — contains `smoke-e2e.sh`).

    The script MUST:

    1. Use the ruamel YAML configuration copied byte-identically from `tools/run_recipe.py` lines 27-39 — the block shown in `<interfaces>` above. Do NOT substitute `safe_load` or `yaml` stdlib — Pattern A in PATTERNS.md is non-negotiable for round-trip preservation.

    2. Be executable as `python3 scripts/migrate_recipes_phase10.py` from the repo root.

    3. For each file matching `recipes/*.yaml` (sorted), apply this migration exactly:

       **For `smoke.verified_cells[]`:**
       - For each cell dict, if `category` key is missing: set `category` to `"PASS"` when the existing `verdict` is `"PASS"`; otherwise set `category` to `"ASSERT_FAIL"`. (All 5 committed recipes' verified_cells are currently PASS — the OR branch is defensive.)
       - For each cell dict, if `detail` key is missing: set `detail` to `""` (empty string).
       - Use `cell.setdefault("category", ...)` and `cell.setdefault("detail", ...)` so re-runs are idempotent.

       **For `smoke.known_incompatible_cells[]`:**
       - For each cell:
         - If current `verdict == "STOCHASTIC"` (the single hermes × gemini case per D-04):
           - Rewrite `cell["verdict"] = "FAIL"` (overwrite, not setdefault — STOCHASTIC is being retired from `verdict` field per D-04 notes in CONTEXT.md and PATTERNS.md §"recipes/hermes.yaml")
           - `cell.setdefault("category", "ASSERT_FAIL")`
           - `cell.setdefault("detail", "flapping verdict — see notes")`
         - Else (typical `verdict: FAIL` shape, openclaw case):
           - `cell.setdefault("category", "ASSERT_FAIL")`
           - Derive a `detail` from the first sentence of `notes` if absent: take `cell.get("notes", "")`, split on `.` once, take the first chunk, strip whitespace, truncate to 120 chars; if `notes` is empty, use empty string.
           - `cell.setdefault("detail", derived_detail)`

    4. Handle cells where `smoke` key is missing, or `verified_cells` is None, or `known_incompatible_cells` is absent — use `data.get("smoke", {})` and `smoke.get("verified_cells", []) or []` / `smoke.get("known_incompatible_cells", []) or []` guards (matches the defensive style of `tools/run_recipe.py::writeback_cell` lines 480-481).

    5. Write back each recipe with `_yaml.dump(data, f)` using a text-mode `open(recipe_path, "w")`. Same pattern as `tools/run_recipe.py::writeback_cell` lines 489-490.

    6. Print one line per file: `f"migrating {p.name}"` at start.

    7. Include a file-top docstring that reads exactly:
       ```
       """ONE-SHOT migration: add {category, detail} to every cell across the 5
       committed recipes per D-04. Idempotent via setdefault. Commit the result,
       then delete this script.

       Usage: python3 scripts/migrate_recipes_phase10.py
       (run from repo root)
       """
       ```

    8. The `if __name__ == "__main__":` block iterates `sorted(Path("recipes").glob("*.yaml"))` and calls `migrate(p)` for each.

    Reference the full RESEARCH.md §Example 4 code listing — implement it as specified there, adapted with PATTERNS.md's guidance that the ruamel config MUST be copied verbatim from `run_recipe.py`.

    DO NOT delete the script in this task — Task 2 commits it alongside the migrated recipes. A later phase or the orchestrator may choose to remove it after reviewers verify.

    **Why a script, not hand-edits?** RESEARCH.md Open Q3 + CONTEXT.md §specifics: hand-editing 5 YAML files (~10 cells total) risks the STOCHASTIC nuance on hermes and quote/indent drift that would break `test_roundtrip.py`. The script enforces exactly one mechanical transformation.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && python3 -c "import ast; ast.parse(open('scripts/migrate_recipes_phase10.py').read()); print('syntax OK')"</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && head -15 scripts/migrate_recipes_phase10.py | grep -q "ONE-SHOT migration" && echo "docstring OK"</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && grep -c 'YAML(typ="rt")' scripts/migrate_recipes_phase10.py</automated>
  </verify>
  <done>
    - `scripts/migrate_recipes_phase10.py` exists with correct shebang, docstring, ruamel config copied from run_recipe.py, `migrate(path)` function, and `if __name__ == "__main__"` entry point
    - Script parses as valid Python 3.10+
    - Script handles all three branches: verified_cells, STOCHASTIC known_incompatible, normal known_incompatible
    - `setdefault` used for idempotence (re-running produces zero diffs if already migrated)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the migration script against all 5 recipes and verify the full test suite stays green</name>
  <read_first>
    - scripts/migrate_recipes_phase10.py (the script just created in Task 1)
    - recipes/hermes.yaml lines 131-154 (pre-migration baseline — the two sections being edited)
    - recipes/openclaw.yaml lines 123-154 (pre-migration baseline)
    - .planning/phases/10-error-taxonomy-timeout-enforcement/10-CONTEXT.md §D-04 (exact expected post-state per recipe)
    - .planning/phases/10-error-taxonomy-timeout-enforcement/10-PATTERNS.md §"recipes/hermes.yaml" (shows before/after for verified_cells AND the STOCHASTIC case)
    - tools/tests/test_roundtrip.py (the round-trip regression — must stay green after migration)
    - tools/tests/test_recipe_regression.py (validates all 5 recipes lint — must stay green)
  </read_first>
  <files>
    recipes/hermes.yaml,
    recipes/openclaw.yaml,
    recipes/picoclaw.yaml,
    recipes/nullclaw.yaml,
    recipes/nanobot.yaml
  </files>
  <action>
    Execute the migration and verify behavior.

    1. **Run migration (from repo root):**
       ```bash
       cd /Users/fcavalcanti/dev/agent-playground && python3 scripts/migrate_recipes_phase10.py
       ```
       Expect 5 lines of output: "migrating hermes.yaml", "migrating nanobot.yaml", "migrating nullclaw.yaml", "migrating openclaw.yaml", "migrating picoclaw.yaml" (sorted alphabetically).

    2. **Inspect the hermes diff manually — this is the only recipe with a STOCHASTIC cell.**
       Expected post-state for `smoke.known_incompatible_cells[0]` (google/gemini-2.5-flash):
       - `verdict: FAIL` (was `STOCHASTIC`)
       - `category: ASSERT_FAIL`
       - `detail: "flapping verdict — see notes"` (quoted because it contains whitespace and an em-dash)
       - `notes: |` block UNCHANGED (the full 10-line explanation is preserved verbatim)

       Expected post-state for `smoke.verified_cells[0]` (anthropic/claude-haiku-4-5):
       - `verdict: PASS` (unchanged)
       - `category: PASS` (new)
       - `detail: ""` (new, empty)
       - `wall_time_s: 12.86` (unchanged)
       - `notes: ...` (unchanged)

       Insertion order matters for readability (D-02 convention, PATTERNS.md §"recipes/hermes.yaml"): `category` and `detail` should appear **after `verdict`** and **before `wall_time_s`**. ruamel's `setdefault` on a CommentedMap appends new keys to the end — this means after the script runs, `category` will appear AFTER `notes`, not between `verdict` and `wall_time_s`. That is ACCEPTABLE: lint passes regardless of key order, round-trip preserves insertion order going forward, and readability is secondary to correctness in a one-shot migration. Do NOT manually reorder keys.

    3. **Run the full test suite:**
       ```bash
       cd /Users/fcavalcanti/dev/agent-playground/tools && pytest -x -q
       ```
       Expect green. The three critical suites are:
       - `test_lint.py` — all 5 recipes lint with the Plan 01 loosened schema
       - `test_recipe_regression.py` — regression gate against the 5 recipes
       - `test_roundtrip.py` — ruamel round-trip is idempotent

       If `test_roundtrip.py` fails with a YAML-formatting drift, STOP. The most likely cause is a missed byte-identical copy of the ruamel config in the migration script. Re-read `tools/run_recipe.py` lines 27-39 and the migration script, diff character-by-character.

    4. **Verify every migrated cell has the expected shape:**
       ```bash
       cd /Users/fcavalcanti/dev/agent-playground && python3 -c "
       import sys, pathlib
       sys.path.insert(0, 'tools')
       from run_recipe import load_recipe
       for p in sorted(pathlib.Path('recipes').glob('*.yaml')):
           r = load_recipe(p)
           smoke = r.get('smoke', {})
           for cell in smoke.get('verified_cells', []) or []:
               assert 'category' in cell, f'{p.name}: verified cell missing category: {cell}'
               assert 'detail' in cell, f'{p.name}: verified cell missing detail: {cell}'
               assert cell['category'] == 'PASS', f'{p.name}: verified cell category != PASS: {cell}'
           for cell in smoke.get('known_incompatible_cells', []) or []:
               assert 'category' in cell, f'{p.name}: known_incompat cell missing category: {cell}'
               assert 'detail' in cell, f'{p.name}: known_incompat cell missing detail: {cell}'
               if cell.get('model') == 'google/gemini-2.5-flash':
                   assert cell['verdict'] == 'FAIL', f'{p.name}: gemini-2.5-flash verdict not remapped: {cell}'
                   assert cell['category'] == 'ASSERT_FAIL', f'{p.name}: gemini-2.5-flash category wrong: {cell}'
                   assert 'flapping' in cell['detail'], f'{p.name}: gemini-2.5-flash detail wrong: {cell}'
       print('all 5 recipes migrated correctly')
       "
       ```

    5. **Do NOT re-run the migration script a second time** unless you want to verify idempotence. (Idempotence IS verified by the `setdefault` usage in the script; a second run produces a no-op diff.)

    6. **Do NOT delete** `scripts/migrate_recipes_phase10.py` — it's committed with the migrated recipes for audit trail. CONTEXT.md §specifics and RESEARCH.md §Assumption A4 both allow keeping or deleting it; we keep it this phase.
  </action>
  <verify>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && grep -c "category: PASS" recipes/hermes.yaml recipes/openclaw.yaml recipes/picoclaw.yaml recipes/nullclaw.yaml recipes/nanobot.yaml | awk -F: '$2 == 0 { print "MISSING category in " $1; exit 1 } { total+=$2 } END { print "total category:PASS occurrences=" total; if (total < 5) { print "expected at least 5"; exit 1 } }'</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && grep -l "category: ASSERT_FAIL" recipes/hermes.yaml recipes/openclaw.yaml && echo "ASSERT_FAIL present in expected recipes"</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground && grep -A2 "google/gemini-2.5-flash" recipes/hermes.yaml | grep -q "verdict: FAIL" && echo "STOCHASTIC remapping applied"</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/tools && pytest tests/test_lint.py tests/test_recipe_regression.py tests/test_roundtrip.py -x -q</automated>
    <automated>cd /Users/fcavalcanti/dev/agent-playground/tools && pytest -x -q</automated>
  </verify>
  <done>
    - All 5 recipes contain `category: PASS` on at least one `verified_cells[]` entry
    - hermes.yaml + openclaw.yaml contain `category: ASSERT_FAIL` on their `known_incompatible_cells[]` entries
    - hermes.yaml google/gemini-2.5-flash cell has `verdict: FAIL` (was STOCHASTIC) + `category: ASSERT_FAIL` + `detail: "flapping verdict — see notes"`
    - Full `pytest -x -q` suite green (48 existing tests still pass with migrated recipes)
    - `scripts/migrate_recipes_phase10.py` is NOT deleted in this commit
    - YAML comments in all 5 recipes preserved byte-identical to pre-migration state (spot-check hermes.yaml's `notes: |` block + any header comments)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration script → recipe YAML on disk | The script mutates committed recipes. User runs it locally; review happens via `git diff` before commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | scripts/migrate_recipes_phase10.py mutating committed YAML | mitigate | Idempotent via `setdefault`; second run is a no-op. `test_roundtrip.py` catches any byte-level drift from ruamel config mismatch. Reviewer verifies via `git diff`. |
| T-10-04 | Data loss | Mis-typed STOCHASTIC remap could corrupt hermes recipe | mitigate | Explicit branch on `verdict == "STOCHASTIC"` in the script; validated by post-migration grep + pytest. Original `notes: |` block is untouched. |
</threat_model>

<verification>
- `pytest -x -q` from `tools/` returns 0 (all 48+ tests pass)
- `grep -l "category: PASS" recipes/*.yaml | wc -l` returns 5
- `grep "verdict: STOCHASTIC" recipes/*.yaml` returns nothing (STOCHASTIC removed from `verdict` field)
- Schema from Plan 01 accepts all 5 migrated recipes (lint green)
- Running migration a second time produces zero `git diff` changes (idempotence)
</verification>

<success_criteria>
1. All 5 recipes have `category` + `detail` fields on every cell (verified + known_incompatible)
2. hermes STOCHASTIC cell is remapped to `verdict: FAIL` + `category: ASSERT_FAIL` per D-04
3. openclaw `known_incompatible_cells[0]` has `category: ASSERT_FAIL` + a `detail` derived from its `notes` first sentence
4. Full test suite green
5. Migration script is committed and reusable (idempotent)
6. YAML comments and structure preserved (ruamel round-trip did its job)
</success_criteria>

<output>
After completion, create `.planning/phases/10-error-taxonomy-timeout-enforcement/10-02-SUMMARY.md` recording:
- The exact diff count per recipe (e.g., "hermes: 2 verified + 1 known_incompat migrated")
- The STOCHASTIC remapping applied
- Confirmation that `scripts/migrate_recipes_phase10.py` was retained (not deleted) in this phase
- (Per I2) Note the observed key-order: ruamel's `setdefault` on a CommentedMap appends new keys at the end, so `category` and `detail` land AFTER `notes` in each cell rather than between `verdict` and `wall_time_s`. This is acceptable; key ordering is cosmetic and does not affect lint or round-trip.

Note: SUMMARY.md is a GSD-framework-mandated tracking artifact (per `@templates/summary.md` and tracked in STATE.md). It is exempt from CLAUDE.md's "no unsolicited markdown" rule because the GSD workflow explicitly requires it.
</output>
</output>
