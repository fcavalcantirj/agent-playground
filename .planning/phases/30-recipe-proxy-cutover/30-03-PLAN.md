---
phase: 30
plan: 03
type: execute
wave: 2
depends_on: ["00"]
files_modified:
  - recipes/nullclaw.yaml
  - api_server/tests/recipes/test_nanobot_via_proxy.py
autonomous: true
worktree: false
requirements:
  - "PHASE-30-D-06: nullclaw flips second within the openrouter group (mechanical heredoc; identical pattern to nanobot)"
  - "PHASE-30-D-08: regression-guard test extended per-flip (nullclaw added to flipped set)"
  - "nullclaw e2e via proxy → usage_logs row with status='success', cost_usd > 0 (inline cost path active per D-09)"
addresses:
  - "D-06 (nullclaw second; mechanical heredoc)"
  - "D-08 (regression-guard update — flipped count → 3)"
  - "D-09 (inline cost path validated by openrouter smoke; depends on Plan 30-00)"
  - "AMD-09 substitution pattern (sh-default form ${AP_PROXY_BASE_URL:-...} — heredoc EOF must be UNQUOTED for sh expansion)"
threats:
  - "T-30-03-01: heredoc EOF accidentally quoted ('EOF' instead of EOF) — sh expansion fails silently and base_url stays literal upstream URL; mitigated by post-edit grep for the substitution string"
must_haves:
  truths:
    - "recipes/nullclaw.yaml has runtime.via_proxy: true (single-line addition under the runtime block)"
    - "recipes/nullclaw.yaml line ~468 substitution applied: literal \"https://openrouter.ai/api/v1\" replaced by \"${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}\""
    - "Heredoc EOF marker remains UNQUOTED (line 465 'cat > /nullclaw-data/config.json <<EOF' — NOT 'cat ... <<\\'EOF\\''); sh expansion fires at container start"
    - "regression guard updated: _OTHER_RECIPES drops 'nullclaw'; flipped_count expected_count = 3"
    - "nullclaw e2e through make e2e-inapp-docker writes a usage_logs row with status='success', input_tokens > 0, output_tokens > 0, cost_usd > 0"
    - "Per Plan 30-00, the openrouter smoke uses the inline_cost_usd path: cost_usd is sourced from OpenRouter's response.usage.cost (not cost_weights)"
  artifacts:
    - path: "recipes/nullclaw.yaml"
      provides: "nullclaw flipped to the egress proxy (via_proxy: true + sh-default base_url substitution)"
      contains: "via_proxy: true"
    - path: "api_server/tests/recipes/test_nanobot_via_proxy.py"
      provides: "regression guard updated — nullclaw removed from _OTHER_RECIPES; flipped count=3"
  key_links:
    - from: "recipes/nullclaw.yaml line 468 base_url heredoc field"
      to: "AP_PROXY_BASE_URL env var injected by tools/run_recipe.py::_build_via_proxy_overrides"
      via: "sh-default substitution at container start time (heredoc EOF unquoted, alpine /bin/sh evaluates ${VAR:-default})"
      pattern: '\\$\\{AP_PROXY_BASE_URL:-https://openrouter\\.ai/api/v1\\}'
---

<objective>
Plan 30-03 flips `recipes/nullclaw.yaml` to `runtime.via_proxy: true` AND replaces the literal upstream base_url at line 468 with the sh-default substitution `${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}`. This is the smallest mechanical edit in Phase 30 — a single heredoc field swap mirroring exactly what nanobot ships at line 463.

Purpose: D-06 sequencing — first openrouter mechanical flip after the anthropic gate (Plan 30-02) opens. nullclaw is the smallest image (8 MB Zig binary), fastest cold-start, and its config heredoc has only ONE base_url occurrence (vs picoclaw's two). The pattern is identical to nanobot — heredoc-eval at container start substitutes `AP_PROXY_BASE_URL` when via_proxy=true; falls back to upstream URL when via_proxy=false (legacy path preserved).

Output: 1 recipe edit (one `via_proxy: true` add + one heredoc substitution) + regression-guard update + a real-money <$0.01 e2e smoke that writes a usage_logs row with status=success + non-zero cost_usd from the inline path (D-09 active).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-recipe-proxy-cutover/30-CONTEXT.md
@.planning/phases/30-recipe-proxy-cutover/30-RESEARCH.md
@.planning/phases/30-recipe-proxy-cutover/30-PATTERNS.md
@.planning/phases/30-recipe-proxy-cutover/30-00-PLAN.md
@recipes/nullclaw.yaml
@recipes/nanobot.yaml
@api_server/tests/recipes/test_nanobot_via_proxy.py

<interfaces>
<!-- Existing recipes/nullclaw.yaml line 460-474 (excerpt; the persistent_argv_override JSON heredoc) -->
```yaml
# BEFORE — recipes/nullclaw.yaml around line 460-472
      entrypoint: sh
      argv:
        - "-c"
        - |
          set -e
          cat > /nullclaw-data/config.json <<EOF
          {
            "agents":{"defaults":{"model":{"primary":"openrouter/$MODEL"}}},
            "models":{"providers":{"openrouter":{"api_key":"${OPENROUTER_API_KEY}","base_url":"https://openrouter.ai/api/v1"}}},
            "gateway":{"port":3000,"host":"0.0.0.0","allow_public_bind":true,"require_pairing":false,"paired_tokens":["${INAPP_AUTH_TOKEN}"]},
            "a2a":{"enabled":true,"name":"${INAPP_AGENT_NAME}","description":"Phase 22c.3 inapp","url":"${INAPP_AGENT_URL}","version":"0.3.0"}
          }
          EOF
          chown -R nobody:nobody /nullclaw-data
          exec nullclaw gateway --host 0.0.0.0 --port 3000
```

```yaml
# AFTER (this plan) — line 468 substitution applied
      entrypoint: sh
      argv:
        - "-c"
        - |
          set -e
          cat > /nullclaw-data/config.json <<EOF
          {
            "agents":{"defaults":{"model":{"primary":"openrouter/$MODEL"}}},
            "models":{"providers":{"openrouter":{"api_key":"${OPENROUTER_API_KEY}","base_url":"${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}"}}},
            "gateway":{"port":3000,"host":"0.0.0.0","allow_public_bind":true,"require_pairing":false,"paired_tokens":["${INAPP_AUTH_TOKEN}"]},
            "a2a":{"enabled":true,"name":"${INAPP_AGENT_NAME}","description":"Phase 22c.3 inapp","url":"${INAPP_AGENT_URL}","version":"0.3.0"}
          }
          EOF
          chown -R nobody:nobody /nullclaw-data
          exec nullclaw gateway --host 0.0.0.0 --port 3000
```

<!-- nanobot reference — line 463 has the same substitution form -->
```yaml
# recipes/nanobot.yaml:463 (verbatim — copy this pattern):
"api_base": "${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}"
```

<!-- runtime block — needs via_proxy: true added -->
```yaml
# Find recipes/nullclaw.yaml runtime block (search for "runtime:" near top of file)
# AFTER:
runtime:
  provider: openrouter
  via_proxy: true                # Phase 30 D-06 — nullclaw flip (Plan 30-03)
  process_env:
    api_key: OPENROUTER_API_KEY
    ...
```

<!-- Regression guard evolution — assumes Plan 30-02 has already removed openclaw -->
```python
# Before (post-30-02 state):
_OTHER_RECIPES = ["hermes", "zeroclaw", "nullclaw", "picoclaw"]
# After (this plan):
_OTHER_RECIPES = ["hermes", "zeroclaw", "picoclaw"]

# Count: 2 → 3
assert flipped_count == 3, ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip recipes/nullclaw.yaml + update regression guard (RED → GREEN)</name>
  <files>
    recipes/nullclaw.yaml,
    api_server/tests/recipes/test_nanobot_via_proxy.py
  </files>
  <read_first>
    recipes/nullclaw.yaml (full — confirm runtime block location and line 468 base_url heredoc),
    recipes/nanobot.yaml (lines 252 + 463 — reference for the sh-default substitution pattern; line 50 — reference for via_proxy: true placement),
    api_server/tests/recipes/test_nanobot_via_proxy.py (current state post-30-02 — _OTHER_RECIPES has 4 entries, count=2),
    .planning/phases/30-recipe-proxy-cutover/30-RESEARCH.md (lines 364-372 — mechanical heredoc substitution pattern),
    .planning/phases/30-recipe-proxy-cutover/30-PATTERNS.md (lines 332-353 — nullclaw edit pattern; lines 462-510 regression-guard evolution)
  </read_first>
  <behavior>
    - RED 1: `test_nullclaw_runtime_via_proxy_is_true` (NEW test) — load nullclaw.yaml, assert `recipe['runtime']['via_proxy'] is True`. RED before YAML edit.
    - RED 2: `test_nullclaw_base_url_uses_proxy_substitution` (NEW test) — assert exactly 1 occurrence of the literal `${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}` substring in nullclaw.yaml AND zero occurrences of the literal upstream URL `"base_url":"https://openrouter.ai/api/v1"` (defense-in-depth). RED before YAML edit.
    - RED 3: count assertion changes from 2 → 3; `_OTHER_RECIPES` shrinks. RED before YAML edit.
    - GREEN: after YAML edits AND test updates, all assertions PASS.
  </behavior>
  <action>
    Step A — Edit `api_server/tests/recipes/test_nanobot_via_proxy.py`:

    A.1 — Update `_OTHER_RECIPES` (drop `"nullclaw"`):
    ```python
    _OTHER_RECIPES = ["hermes", "zeroclaw", "picoclaw"]
    ```

    A.2 — Update count assertion:
    ```python
    assert flipped_count == 3, (
        f"expected exactly 3 recipes with via_proxy: true (nanobot + openclaw + nullclaw), got {flipped_count}"
    )
    ```

    A.3 — Add positive-assertion + substitution-invariant tests:
    ```python
    def test_nullclaw_runtime_via_proxy_is_true() -> None:
        """Phase 30 Plan 30-03 cutover invariant — nullclaw flipped to the proxy."""
        recipe = _load("nullclaw")
        assert recipe["runtime"]["via_proxy"] is True

    def test_nullclaw_base_url_uses_proxy_substitution() -> None:
        """AMD-09 — nullclaw config heredoc substitutes AP_PROXY_BASE_URL via sh-default form
        so via_proxy=true uses the runner-injected proxy URL and via_proxy=false falls back
        to the upstream URL (legacy path preserved)."""
        text = (RECIPES_DIR / "nullclaw.yaml").read_text()
        pattern = '"base_url":"${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}"'
        occurrences = text.count(pattern)
        assert occurrences == 1, (
            f"expected exactly 1 sh-default base_url substitution in nullclaw.yaml, got {occurrences}"
        )
        # Defense-in-depth — the original literal MUST be gone from this field.
        assert '"base_url":"https://openrouter.ai/api/v1"' not in text, (
            "the literal upstream URL leaked back into nullclaw.yaml — should be sh-defaulted via AP_PROXY_BASE_URL"
        )
    ```

    Step B — Edit `recipes/nullclaw.yaml`. TWO surgical edits:

    B.1 — Add `via_proxy: true` to the `runtime:` block. Locate the `runtime:` line (search for `^runtime:` near the top of the file). Insert ONE new line after `provider: openrouter` and before `process_env:`:
    ```yaml
      via_proxy: true                # Phase 30 D-06 — nullclaw flip (Plan 30-03)
    ```

    B.2 — Substitute the base_url at line 468. The exact substitution string (5+ chars context on each side):

    BEFORE:
    ```
    "models":{"providers":{"openrouter":{"api_key":"${OPENROUTER_API_KEY}","base_url":"https://openrouter.ai/api/v1"}}},
    ```

    AFTER:
    ```
    "models":{"providers":{"openrouter":{"api_key":"${OPENROUTER_API_KEY}","base_url":"${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}"}}},
    ```

    The substitution is byte-precise: replace exactly `"base_url":"https://openrouter.ai/api/v1"` with `"base_url":"${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}"`. Surrounding JSON-comma + braces are UNCHANGED.

    Step C — Verify the heredoc EOF marker on line ~465/466 is UNQUOTED. Search for `cat > /nullclaw-data/config.json <<` and confirm the closing tag is bare `EOF` not `'EOF'` or `"EOF"`. If quoted, sh expansion is disabled and the substitution is dead code — flag as a hotfix before proceeding (this is Pitfall mitigation; the file currently appears to use bare `EOF` per line 465 which is correct).

    Step D — Run pytest to confirm GREEN: `cd api_server && uv run pytest tests/recipes/test_nanobot_via_proxy.py -v -x`.
  </action>
  <verify>
    <automated>cd api_server && uv run pytest tests/recipes/test_nanobot_via_proxy.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    `recipes/nullclaw.yaml` contains the literal `via_proxy: true` (grep-checkable),
    `recipes/nullclaw.yaml` contains EXACTLY one occurrence of the literal `${AP_PROXY_BASE_URL:-https://openrouter.ai/api/v1}` (grep -F count == 1),
    `recipes/nullclaw.yaml` does NOT contain the literal `"base_url":"https://openrouter.ai/api/v1"` (the original literal — grep -F count == 0),
    `recipes/nullclaw.yaml` heredoc opening uses bare `<<EOF` (not `<<'EOF'` — sh expansion enabled),
    `api_server/tests/recipes/test_nanobot_via_proxy.py` `_OTHER_RECIPES` does NOT contain `"nullclaw"`,
    `api_server/tests/recipes/test_nanobot_via_proxy.py` contains `test_nullclaw_runtime_via_proxy_is_true` AND `test_nullclaw_base_url_uses_proxy_substitution`,
    `api_server/tests/recipes/test_nanobot_via_proxy.py` contains `flipped_count == 3`,
    Combined shell: `grep -lE "via_proxy: true" recipes/*.yaml | wc -l` outputs `3`,
    `cd api_server && uv run pytest tests/recipes/test_nanobot_via_proxy.py -v -x` exits 0
  </acceptance_criteria>
  <done>
    nullclaw.yaml flipped (1 line + 1 substitution); regression guard at 3/6 flipped; pytest GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: nullclaw e2e smoke through proxy — real-money <$0.01 cutover gate</name>
  <files>
    (no source edits — live e2e validation gate)
  </files>
  <read_first>
    api_server/Makefile (e2e-inapp-docker target — nullclaw is in the hardcoded matrix list at line 47),
    api_server/tests/e2e/test_inapp_5x5_matrix.py (RECIPE_MATRIX entry: ("nullclaw", "a2a_jsonrpc", 3000, "/a2a") line 64; nullclaw uses contract='a2a_jsonrpc' so the proxy path is exercised through that contract, not openai_compat),
    .planning/phases/30-recipe-proxy-cutover/30-00-PLAN.md (Plan 30-00 must be SHIPPED for the inline cost path to fire — confirm by grep on usage_recorder.py for inline_cost_usd)
  </read_first>
  <action>
    Step A — GATE CHECK: confirm Plan 30-00 has shipped. The proxy must read OpenRouter inline cost. Quick grep:
    ```bash
    grep -q "inline_cost_usd" /Users/fcavalcanti/dev/agent-playground/api_server/src/api_server/services/usage_recorder.py || (echo "ABORT — Plan 30-00 not shipped" && exit 1)
    ```

    Step B — Pre-flight container hygiene:
    ```bash
    docker ps --filter "name=ap-recipe-nullclaw" -q | xargs -r docker stop
    docker ps -a --filter "name=ap-recipe-nullclaw" -q | xargs -r docker rm
    ```

    Step C — Build the nullclaw image if missing:
    ```bash
    docker image inspect ap-recipe-nullclaw:latest >/dev/null 2>&1 || \
      (cd /Users/fcavalcanti/dev/agent-playground && uv run python tools/run_recipe.py recipes/nullclaw.yaml --build-only)
    ```

    Step D — Run the dockerized harness with `-k nullclaw`:
    ```bash
    cd api_server && set -a && source ../.env && set +a && \
      docker image inspect ap-test-runner:latest >/dev/null 2>&1 || \
        (cd .. && docker build -f tools/Dockerfile.test-runner -t ap-test-runner:latest .); \
      REPO_ROOT="$(cd .. && pwd)" && \
      docker run --rm \
        --add-host=host.docker.internal:host-gateway \
        -e OPENROUTER_API_KEY -e ANTHROPIC_API_KEY \
        -e TC_HOST=host.docker.internal \
        -e AP_E2E_NETWORK_PRESET=bridge -e AP_E2E_DOCKERIZED_HARNESS=1 \
        -v /var/run/docker.sock:/var/run/docker.sock \
        -v "$REPO_ROOT":/workspace -w /workspace/api_server \
        ap-test-runner:latest \
        pytest tests/e2e/test_inapp_5x5_matrix.py -v --tb=short -m api_integration -k nullclaw
    ```

    Step E — Post-run DB verification:
    ```bash
    docker exec deploy-postgres-1 psql -U postgres -d agent_playground_api -c \
      "SELECT u.status, u.input_tokens, u.output_tokens, u.cost_usd, u.provider, u.upstream_request_id, u.created_at, ac.upstream_provider, ac.provider_key_enc IS NOT NULL AS has_key \
       FROM usage_logs u JOIN agent_containers ac ON ac.agent_instance_id = u.agent_instance_id \
       WHERE ac.recipe_name='nullclaw' \
       ORDER BY u.created_at DESC LIMIT 1;"
    ```

    Manual assertions (paste output into SUMMARY):
    - status='success'
    - input_tokens > 0 AND output_tokens > 0
    - cost_usd > 0 (sourced from inline_cost_usd path per Plan 30-00; OpenRouter's response.usage.cost field)
    - provider='openrouter'
    - upstream_request_id starts with 'gen-' (OpenRouter id shape)
    - upstream_provider='openrouter' AND has_key=true (D-12)

    Step F — Append cost to budget log: `30-03-nullclaw-e2e: $<cost_usd> at <UTC timestamp>` to `.planning/phases/30-recipe-proxy-cutover/cost-budget.txt`. Cumulative budget < $0.10.

    Step G — On FAIL: check the failed nullclaw container's resolved config. Inside the container at `/nullclaw-data/config.json`, the `models.providers.openrouter.base_url` field should be `http://api_server:8000/v1/llm/forward` (resolved from AP_PROXY_BASE_URL by sh expansion at heredoc-write time). If the value is the literal upstream URL, sh expansion didn't fire — most likely the heredoc EOF was quoted. Diagnose with: `docker logs <failed_container_id>` then `docker exec <id> cat /nullclaw-data/config.json` (only works if container is still running).
  </action>
  <verify>
    <automated>cd api_server && set -a && source ../.env && set +a && docker run --rm --add-host=host.docker.internal:host-gateway -e OPENROUTER_API_KEY -e ANTHROPIC_API_KEY -e TC_HOST=host.docker.internal -e AP_E2E_NETWORK_PRESET=bridge -e AP_E2E_DOCKERIZED_HARNESS=1 -v /var/run/docker.sock:/var/run/docker.sock -v "$(cd .. && pwd)":/workspace -w /workspace/api_server ap-test-runner:latest pytest tests/e2e/test_inapp_5x5_matrix.py -v --tb=short -m api_integration -k nullclaw</automated>
  </verify>
  <acceptance_criteria>
    pytest exits 0 with the nullclaw cell PASS,
    `api_server/tests/e2e/e2e-report.json` shows nullclaw in `recipes` array with no failure entry,
    Postgres SELECT returns 1 row with status='success', input_tokens > 0, output_tokens > 0, cost_usd > 0, provider='openrouter', upstream_request_id starting with 'gen-',
    Postgres SELECT shows agent_containers row with upstream_provider='openrouter' AND provider_key_enc IS NOT NULL,
    Cost-budget log entry appended with the nullclaw run's cost_usd value,
    Plan 30-00 gate confirmed (grep on inline_cost_usd in usage_recorder.py succeeds before run)
  </acceptance_criteria>
  <done>
    nullclaw e2e PASS through proxy via inline cost path; usage_logs row matches D-09 + D-12 invariants; 3/6 recipes flipped (nanobot + openclaw + nullclaw).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recipe YAML → container at start | sh heredoc evaluates `${AP_PROXY_BASE_URL:-...}` at container start; injected env (or default) becomes part of /nullclaw-data/config.json |
| nullclaw container → AP proxy | bot reads providers.openrouter.base_url from config.json; sends `Authorization: Bearer ap-proxy-<token>` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Tampering | heredoc EOF accidentally quoted ('EOF') | mitigate | Acceptance criteria asserts the heredoc opening is bare `<<EOF`. Quoted EOF disables sh expansion → base_url stays literal upstream URL → e2e smoke fails fast (cost_usd path doesn't even reach proxy). Catch is automatic. |
| T-30-03-02 | Tampering | substitution applied in wrong field | mitigate | Acceptance criteria asserts EXACTLY 1 occurrence of the substitution AND 0 occurrences of the literal upstream URL — both grep-checkable. Misplaced edits surface immediately. |
| T-30-03-03 | Information Disclosure | OPENROUTER_API_KEY leaked in /nullclaw-data/config.json (file written inside container) | accept | The placeholder `ap-proxy-<token>` replaces the real key when via_proxy=true (Phase 29 AMD-12; `tools/run_recipe.py::_build_via_proxy_overrides` strips OPENROUTER_API_KEY from container env). config.json receives only the placeholder. Same pattern Phase 29 verified for nanobot. |
</threat_model>

<verification>
- 1 recipe edit (1 line + 1 substitution).
- Regression guard at 3/6 flipped.
- e2e smoke writes a usage_logs row with D-09 inline-cost path active.
- Cost ceiling: 1 e2e call ≈ $0.001-$0.01.
- Plan 30-00 (proxy enhancement) gate prerequisite asserted.
</verification>

<success_criteria>
- recipes/nullclaw.yaml flipped (1 via_proxy line + 1 base_url substitution).
- regression guard test extended (3 changes: _OTHER_RECIPES, count, 2 new positive tests).
- e2e PASS via -k nullclaw.
- usage_logs row matches D-09 (inline cost) + D-12 (BYOK custody).
- Cumulative cost still < $0.10.
</success_criteria>

<output>
After completion, create `.planning/phases/30-recipe-proxy-cutover/30-03-SUMMARY.md` documenting the YAML edit, the regression-guard update, the e2e smoke output, and the cost_usd value.
</output>
