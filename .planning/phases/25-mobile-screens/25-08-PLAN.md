---
phase: 25-mobile-screens
plan: 08
type: execute
wave: 5
depends_on: [07]
worktree_bypass: true
files_modified:
  - mobile/integration_test/screens_e2e_test.dart
  - mobile/integration_test/screens_e2e_helpers.dart
  - mobile/Makefile
  - mobile/README.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md
  - spikes/flutter-screens-roundtrip.md
autonomous: false
requirements: [UI-04]
tags: [flutter, integration-test, exit-gate, spike, amd-01, amd-02, ui-04, worktree-bypass]

must_haves:
  truths:
    - "mobile/integration_test/screens_e2e_test.dart drives Login → Dashboard → Wizard → Deploy → Chat → kill+relaunch → history-visible against the live local api_server per D-65"
    - "make screens-e2e Makefile target wired with same env-var preflight + curl /healthz preflight as make spike (Phase 24)"
    - "AMD-01 has landed: REQUIREMENTS.md UI-02 rewritten per CONTEXT.md ## Spec Amendments"
    - "AMD-02 has landed: 23-CONTEXT.md D-28 has the appended amendment paragraph"
    - "spikes/flutter-screens-roundtrip.md exists with frontmatter `verdict: PASS` AND captures iOS Simulator + (where available) Android Emulator results"
    - "Live `make screens-e2e` was run successfully against a local api_server + Postgres + Redis + container + OpenRouter (no mocks per Golden Rule #1)"
    - "mobile/README.md has a screens-e2e section mirroring the make spike section from Phase 24 D-22"
  artifacts:
    - path: "mobile/integration_test/screens_e2e_test.dart"
      provides: "Wave 5 exit-gate driver: testWidgets Login → Dashboard → Wizard → Chat → kill+relaunch round-trip"
      contains: "testWidgets"
    - path: "mobile/Makefile"
      provides: "screens-e2e target with BASE_URL/SESSION_ID/OPENROUTER_KEY preflight + curl healthcheck"
      contains: "screens-e2e:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UI-02 rewritten per AMD-01 — Telegram is REAL, not stub"
      contains: "Amended Phase 25 per AMD-01"
    - path: ".planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md"
      provides: "D-28 amendment paragraph per AMD-02 — multi-channel deploy is first-class"
      contains: "AMD (Phase 25 D-56)"
    - path: "spikes/flutter-screens-roundtrip.md"
      provides: "Phase 25 exit-gate artifact with verdict: PASS"
      contains: "verdict: PASS"
  key_links:
    - from: "mobile/integration_test/screens_e2e_test.dart"
      to: "AuthServiceTestSeam (D-66 Wave 1)"
      via: "ProviderScope override + --dart-define SESSION_ID injection"
      pattern: "AuthServiceTestSeam|authServiceProvider.overrideWith"
    - from: "mobile/Makefile screens-e2e target"
      to: "make spike (Phase 24)"
      via: "same preflight + --dart-define triple"
      pattern: "BASE_URL.*SESSION_ID.*OPENROUTER_KEY"
    - from: "spikes/flutter-screens-roundtrip.md"
      to: "spikes/flutter-api-roundtrip.md (Phase 24 sibling)"
      via: "frontmatter shape mirror"
      pattern: "verdict:|target:|date:"
---

<objective>
Wave 5 — exit gate. The phase verifier treats this plan's deliverables as
the load-bearing closure for UI-04 + the AMD-01 / AMD-02 spec amendments.

This plan is the only Wave 5 plan. It is intentionally `autonomous: false`
because Task 5 is a `checkpoint:human-verify` for the live spike run.
Tasks 1, 2, 3, 4 are autonomous and can ship via the executor.

**Worktree bypass: this plan commits directly to `main`.** The live spike
runs against the local api_server container which bind-mounts main, not
the worktree (per `feedback_worktree_breaks_for_live_infra.md`). Wave 5
plans are flagged with `worktree_bypass: true` so the orchestrator
commits without the worktree dance.

Tasks:
1. Write `mobile/integration_test/screens_e2e_test.dart` (~250 LOC)
   extending Phase 24's spike harness shape. Reuses `spike_helpers.dart`.
2. Add `make screens-e2e` Makefile target + update mobile/README.md.
3. Land AMD-01: REQUIREMENTS.md UI-02 rewrite per CONTEXT.md ## Spec Amendments.
4. Land AMD-02: 23-CONTEXT.md D-28 amendment paragraph appended.
5. Checkpoint (human-verify): run `make screens-e2e` live; capture
   `spikes/flutter-screens-roundtrip.md` with `verdict: PASS`.

Output: 7 files modified. ~500 LOC net add (screens_e2e_test dominates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-mobile-screens/25-CONTEXT.md
@.planning/phases/25-mobile-screens/25-RESEARCH.md
@.planning/phases/25-mobile-screens/25-PATTERNS.md
@.planning/phases/25-mobile-screens/25-VALIDATION.md
@.planning/phases/25-mobile-screens/25-07-SUMMARY.md
@.planning/phases/24-flutter-foundation/24-09-SUMMARY.md
@.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md
@spikes/flutter-api-roundtrip.md
@mobile/integration_test/spike_api_roundtrip_test.dart
@mobile/integration_test/spike_helpers.dart
@mobile/Makefile
@mobile/README.md
@mobile/lib/main.dart
@mobile/lib/app.dart
@mobile/lib/core/auth/auth_service.dart
@mobile/lib/core/auth/auth_service_test_seam.dart
@mobile/lib/core/auth/providers.dart

<interfaces>
<!-- Existing harness pieces reused by Wave 5. -->

From mobile/integration_test/spike_api_roundtrip_test.dart (Phase 24 — the canonical analog):
```dart
const _baseUrl = String.fromEnvironment('BASE_URL');
const _sessionId = String.fromEnvironment('SESSION_ID');
const _byokKey = String.fromEnvironment('OPENROUTER_KEY');

void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();
  testWidgets('9-step round-trip — D-46 (Phase 24 exit gate)', (tester) async {
    expect(_baseUrl, isNotEmpty);
    expect(_sessionId, isNotEmpty);
    expect(_byokKey, isNotEmpty);
    // ... 9 steps ...
  }, timeout: const Timeout(Duration(minutes: 15)));
}
```

From mobile/integration_test/spike_helpers.dart (72 LOC — reusable):
```dart
T expectOk<T>(Result<T> r, {required int step});
Future<SseEvent> waitForOutbound(List<SseEvent> received, {int afterIndex = -1});
String shortRandHex();
String? extractAssistantContent(String data);
```

From .planning/REQUIREMENTS.md UI-02 (current — to be rewritten by AMD-01):
"UI-02: New Agent screen lets the user pick a clone from `GET /v1/recipes`
... Telegram-integration toggle is rendered but disabled in MVP."

The replacement text is in 25-CONTEXT.md ## Spec Amendments AMD-01 (verbatim).

From 23-CONTEXT.md D-28 (existing — needs AMD-02 paragraph appended):
"D-28: Mobile Phase 25 deploys with `{channel: 'inapp', channel_inputs: {}}`."

The amendment paragraph is in 25-CONTEXT.md ## Spec Amendments AMD-02 (verbatim).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write screens_e2e_test.dart driver + screens_e2e_helpers.dart</name>
  <files>
    mobile/integration_test/screens_e2e_test.dart,
    mobile/integration_test/screens_e2e_helpers.dart
  </files>
  <read_first>
    @.planning/phases/25-mobile-screens/25-CONTEXT.md (D-65 exit-gate test contract; D-66 AuthService test seam)
    @.planning/phases/25-mobile-screens/25-RESEARCH.md (## Common Pitfalls #7 WidgetTester.handleAppLifecycleStateChanged for kill+relaunch; ## Code Examples §Wave 5 spike harness extension)
    @.planning/phases/25-mobile-screens/25-PATTERNS.md (### Wave 5 — Tests + spike + artifact)
    @mobile/integration_test/spike_api_roundtrip_test.dart (CANONICAL analog — same harness shape)
    @mobile/integration_test/spike_helpers.dart (reused helpers)
    @mobile/lib/core/auth/auth_service_test_seam.dart (D-66 — Wave 5 injects via ProviderScope override)
    @mobile/lib/core/auth/providers.dart (authServiceProvider — Wave 5 overrides)
    @mobile/lib/app.dart (SolvrLabsApp signature — accepts initialRoute per Wave 1)
  </read_first>
  <action>
    Write the integration_test driver per RESEARCH ## Code Examples §Wave 5
    spike harness extension + PATTERNS Wave 5 + D-65 contract.

    The 11-step flow:
    1. Boot — pump SolvrLabsApp(initialRoute: '/login') with ProviderScope
       override `authServiceProvider → AuthServiceTestSeam(storage, sessionId: _sessionId)`.
    2. Login — find 'Continue with Google' button; tap; pumpAndSettle. Test
       seam writes session_id; routes to /dashboard.
    3. Dashboard appears — find 'No agents yet' (empty state) OR find an
       AgentRow.
    4. Tap FAB → wizard step 1.
    5. Wizard step 1 — find first RecipeCard; tap; pumpAndSettle. Tap 'Next'.
    6. Wizard step 2 — tap 'Pick a model' → push picker. In picker, tap
       first ModelRow; pumpAndSettle pops back. Find BYOK TextField;
       enterText with `_byokKey`. Tap 'Next'.
    7. Wizard step 3 — enterText agent name `screens-e2e-${shortRandHex()}`.
       Telegram OFF. Tap 'Deploy'. pumpAndSettle up to ~60s for
       smoke + start.
    8. Chat — enterText 'hi' in input; tap Send. Wait up to ~2min for the
       assistant bubble.
    9. Kill+relaunch simulation per Pitfall #7:
       - tester.binding.handleAppLifecycleStateChanged(AppLifecycleState.paused)
       - .detached
       - tester.pumpWidget(const SizedBox.shrink())
       - pumpAndSettle
       - Re-pumpWidget the SAME ProviderScope override stack with
         SolvrLabsApp(initialRoute: '/dashboard').
    10. Find AgentRow with the agentName from step 7; tap.
    11. Assert: 'hi' bubble visible AND assistant reply bubble visible.

    File skeleton:
    ```dart
    // Phase 25 Wave 5 D-65 — exit-gate integration test.
    // Mirrors mobile/integration_test/spike_api_roundtrip_test.dart shape.

    import 'package:agent_playground/app.dart';
    import 'package:agent_playground/core/auth/auth_service_test_seam.dart';
    import 'package:agent_playground/core/auth/providers.dart';
    import 'package:agent_playground/core/storage/secure_storage.dart';
    import 'package:agent_playground/features/chat/bubble_widget.dart';
    import 'package:agent_playground/features/dashboard/agent_row.dart';
    import 'package:flutter/material.dart';
    import 'package:flutter_riverpod/flutter_riverpod.dart';
    import 'package:flutter_test/flutter_test.dart';
    import 'package:integration_test/integration_test.dart';
    import 'screens_e2e_helpers.dart';
    import 'spike_helpers.dart';

    const _baseUrl = String.fromEnvironment('BASE_URL');
    const _sessionId = String.fromEnvironment('SESSION_ID');
    const _byokKey = String.fromEnvironment('OPENROUTER_KEY');

    void main() {
      IntegrationTestWidgetsFlutterBinding.ensureInitialized();

      testWidgets('Phase 25 screens e2e — D-65 exit gate', (tester) async {
        expect(_baseUrl, isNotEmpty);
        expect(_sessionId, isNotEmpty);
        expect(_byokKey, isNotEmpty);
        final agentName = 'screens-e2e-${shortRandHex()}';
        // === Step 1..11 ===
      }, timeout: const Timeout(Duration(minutes: 15)));
    }
    ```

    `screens_e2e_helpers.dart` adds widget-specific helpers:
    - `Future<void> tapAndSettle(WidgetTester t, Finder f)`
    - `Future<void> waitForChatBubble(WidgetTester t, String content, {Duration timeout = const Duration(minutes: 2)})`
    - `Future<void> seedSecureStorage(SecureStorage s, String sessionId)`

    The test will produce a step-numbered `// === Step N ===` narrative
    matching Phase 24's spike pattern. Each step uses `expectOk` from
    `spike_helpers.dart` where Result<T> values surface.
  </action>
  <verify>
    <automated>cd mobile &amp;&amp; fvm flutter analyze integration_test/screens_e2e_test.dart integration_test/screens_e2e_helpers.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test -f mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "IntegrationTestWidgetsFlutterBinding.ensureInitialized" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "String.fromEnvironment('BASE_URL')" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "String.fromEnvironment('SESSION_ID')" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "String.fromEnvironment('OPENROUTER_KEY')" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "AuthServiceTestSeam" mobile/integration_test/screens_e2e_test.dart` (D-66)
    - `grep -q "authServiceProvider.overrideWith" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "handleAppLifecycleStateChanged" mobile/integration_test/screens_e2e_test.dart` (Pitfall #7)
    - `grep -q "AppLifecycleState.paused" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "AppLifecycleState.detached" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "AppLifecycleState.resumed" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "shortRandHex" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "Continue with Google\\|Continue with GitHub" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "Deploy" mobile/integration_test/screens_e2e_test.dart`
    - `grep -q "minutes: 15" mobile/integration_test/screens_e2e_test.dart` (Phase 24 timeout pattern)
    - `test -f mobile/integration_test/screens_e2e_helpers.dart`
    - `cd mobile && fvm flutter analyze` exit 0
  </acceptance_criteria>
  <done>screens_e2e_test.dart compiles cleanly + reuses Phase 24 helpers + injects AuthServiceTestSeam via Riverpod override + simulates kill+relaunch via handleAppLifecycleStateChanged per Pitfall #7; ready to be invoked by `make screens-e2e` in Task 5.</done>
</task>

<task type="auto">
  <name>Task 2: Add `make screens-e2e` Makefile target + update mobile/README.md</name>
  <files>
    mobile/Makefile,
    mobile/README.md
  </files>
  <read_first>
    @.planning/phases/25-mobile-screens/25-CONTEXT.md (D-65)
    @.planning/phases/25-mobile-screens/25-PATTERNS.md (#### `mobile/Makefile` — EXTEND)
    @mobile/Makefile (existing — `make spike` is the canonical analog)
    @mobile/README.md (existing — Phase 24 D-22 README pattern)
  </read_first>
  <action>
    **2.1 — `mobile/Makefile`**

    Add a new target `screens-e2e` that mirrors `make spike` line-for-line
    with only the test path differing:

    ```make
    screens-e2e:  ## Phase 25 D-65 — Login → Dashboard → Wizard → Chat → kill+relaunch round-trip integration_test.
    	@test -n "$$BASE_URL" || (echo "ERROR: BASE_URL not set..." && exit 1)
    	@test -n "$$SESSION_ID" || (echo "ERROR: SESSION_ID not set..." && exit 1)
    	@test -n "$$OPENROUTER_KEY" || (echo "ERROR: OPENROUTER_KEY not set..." && exit 1)
    	@curl -fsS --max-time 5 "$$BASE_URL/healthz" >/dev/null || (echo "ERROR: api_server not healthy..." && exit 1)
    	fvm flutter test integration_test/screens_e2e_test.dart \
    	  --dart-define BASE_URL=$$BASE_URL \
    	  --dart-define SESSION_ID=$$SESSION_ID \
    	  --dart-define OPENROUTER_KEY=$$OPENROUTER_KEY
    ```

    Update .PHONY: `.PHONY: doctor get test ios android spike screens-e2e clean`.

    **2.2 — `mobile/README.md`**

    Append a `### make screens-e2e (Phase 25 D-65)` section mirroring the
    Phase 24 D-22 README pattern. Document BASE_URL/SESSION_ID/OPENROUTER_KEY
    env vars + boot-simulator-first instructions.
  </action>
  <verify>
    <automated>grep -q "^screens-e2e:" mobile/Makefile &amp;&amp; grep -q "screens_e2e_test.dart" mobile/Makefile &amp;&amp; grep -q "screens-e2e" mobile/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^screens-e2e:" mobile/Makefile`
    - `grep -q "screens_e2e_test.dart" mobile/Makefile`
    - `grep -q "BASE_URL" mobile/Makefile` (under screens-e2e target context)
    - `grep -q "SESSION_ID" mobile/Makefile`
    - `grep -q "OPENROUTER_KEY" mobile/Makefile`
    - `grep -q "screens-e2e" mobile/README.md` (new section heading)
    - `grep -q ".PHONY:.*screens-e2e" mobile/Makefile` (target listed in PHONY)
  </acceptance_criteria>
  <done>make screens-e2e target shipped mirroring make spike preflight + dart-define triple; README documents the new target; .PHONY updated.</done>
</task>

<task type="auto">
  <name>Task 3: Land AMD-01 — rewrite REQUIREMENTS.md UI-02</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    @.planning/phases/25-mobile-screens/25-CONTEXT.md (## Spec Amendments AMD-01 — verbatim replacement text)
    @.planning/REQUIREMENTS.md (current UI-02 wording — line 192-193)
  </read_first>
  <action>
    Rewrite the UI-02 entry in `.planning/REQUIREMENTS.md` to match the
    AMD-01 verbatim text from 25-CONTEXT.md ## Spec Amendments. The current
    line reads:

    > **UI-02**: New Agent screen lets the user pick a clone from `GET
    > /v1/recipes` (rendered as cards: hermes, openclaw, nullclaw, picoclaw,
    > nanobot — clones the existing API exposes), pick a model from `GET
    > /v1/models`, and enter a name. Tapping Deploy POSTs to
    > `/v1/agents/:id/start` and on success navigates to Chat for the new
    > agent. Telegram-integration toggle is rendered but disabled in MVP.

    Replace it (between the matching marker lines `**UI-02**` start and the
    end of the entry) with the AMD-01 verbatim text:

    > **UI-02**: New Agent screen lets the user pick a clone from `GET
    > /v1/recipes` (rendered as cards), pick a model from `GET /v1/models`,
    > and enter a name. Tapping Deploy first POSTs to `/v1/runs` (smoke
    > gate per Phase 23 D-22), then POSTs to `/v1/agents/:id/start` with
    > `channel:'inapp'`, then (if the recipe declares `channels_supported`
    > includes `'telegram'` AND the user has toggled Telegram ON) POSTs
    > a SECOND `/v1/agents/:id/start` with `channel:'telegram'` and
    > `channel_inputs` populated from fields rendered dynamically from
    > `recipe.channels.telegram.required_user_input + optional_user_input`.
    > On full success navigates to Chat for the new agent. On telegram-only
    > failure, navigates to Chat with a sticky failed-banner per Phase 25
    > D-50. *(Amended Phase 25 per AMD-01 — original "rendered but
    > disabled" wording predated Phase 22c.3 inapp shipping; multi-channel
    > deploy is now first-class.)*

    The verifier matches the new wording verbatim — DO NOT modify whitespace
    or punctuation in the verbatim block. Use the Edit tool with `old_string`
    matching the existing UI-02 paragraph and `new_string` the AMD-01
    replacement.

    Optional bonus: also update the "Last updated" date line at the very
    bottom of REQUIREMENTS.md (currently `2026-05-01 — milestone v0.3...`)
    to add a Phase 25 marker like `2026-05-03 — Phase 25 AMD-01 (UI-02
    multi-channel deploy first-class)`.
  </action>
  <verify>
    <automated>grep -q "Amended Phase 25 per AMD-01" .planning/REQUIREMENTS.md &amp;&amp; grep -q "channel:'telegram'" .planning/REQUIREMENTS.md &amp;&amp; ! grep -q "rendered but disabled in MVP" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Amended Phase 25 per AMD-01" .planning/REQUIREMENTS.md`
    - `grep -q "channel:'inapp'" .planning/REQUIREMENTS.md` (new wording)
    - `grep -q "channel:'telegram'" .planning/REQUIREMENTS.md` (new wording)
    - `grep -q "channels.telegram.required_user_input" .planning/REQUIREMENTS.md`
    - `grep -q "Phase 25 D-50" .planning/REQUIREMENTS.md` (cross-reference to telegram-failed banner)
    - `! grep -q "rendered but disabled in MVP" .planning/REQUIREMENTS.md` (old wording removed)
    - `! grep -q "rendered but disabled" .planning/REQUIREMENTS.md` (also catches partial match)
  </acceptance_criteria>
  <done>REQUIREMENTS.md UI-02 rewritten verbatim per CONTEXT AMD-01; old "disabled in MVP" wording removed; verifier will match the new wording.</done>
</task>

<task type="auto">
  <name>Task 4: Land AMD-02 — append amendment paragraph to 23-CONTEXT.md D-28</name>
  <files>
    .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md
  </files>
  <read_first>
    @.planning/phases/25-mobile-screens/25-CONTEXT.md (## Spec Amendments AMD-02 — verbatim amendment paragraph)
    @.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md (current D-28 location)
  </read_first>
  <action>
    Append the AMD-02 amendment paragraph (verbatim from 25-CONTEXT.md ##
    Spec Amendments) immediately after D-28's existing text in
    `.planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md`.

    First, grep for `D-28` in 23-CONTEXT.md to find the existing text. Use
    the Edit tool to insert (NOT replace — D-28's original wording stays;
    only the amendment paragraph is added) after the closing of D-28's
    existing paragraph and before the next D-29 (or whatever follows D-28):

    ```markdown
    > **AMD (Phase 25 D-56):** This constraint relaxes to "the inapp
    > container is always deployed; an additional Telegram container is
    > optional and gated by the recipe's `channels_supported` metadata
    > + a user toggle". The /v1/agents/:id/start endpoint contract is
    > unchanged (still single-channel-per-call); mobile orchestrates two
    > sequential calls when Telegram is toggled. No backend code changes;
    > UI-02 amended in same commit chain (AMD-01).
    ```

    The verifier matches the verbatim paragraph — preserve quoting, the
    `AMD (Phase 25 D-56):` marker, and the punctuation exactly.
  </action>
  <verify>
    <automated>grep -q "AMD (Phase 25 D-56)" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md &amp;&amp; grep -q "channels_supported.*metadata" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "AMD (Phase 25 D-56)" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md`
    - `grep -q "inapp container is always deployed" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md`
    - `grep -q "additional Telegram container is" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md`
    - `grep -q "single-channel-per-call" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md`
    - `grep -q "AMD-01" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md` (cross-link to REQUIREMENTS amendment)
    - D-28's original text is preserved (the amendment is appended, not replacing): `grep -q "channel: 'inapp', channel_inputs: {}" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md`
  </acceptance_criteria>
  <done>23-CONTEXT.md D-28 has the AMD-02 amendment paragraph appended verbatim per CONTEXT.md; original D-28 text preserved; verifier will match.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Live screens-e2e spike run + spikes/flutter-screens-roundtrip.md artifact</name>
  <files>
    spikes/flutter-screens-roundtrip.md
  </files>
  <read_first>
    @.planning/phases/25-mobile-screens/25-CONTEXT.md (D-65 — spike artifact contract)
    @spikes/flutter-api-roundtrip.md (Phase 24 sibling artifact — frontmatter shape)
    @mobile/integration_test/screens_e2e_test.dart (just shipped in Task 1)
    @mobile/Makefile (just updated in Task 2 — make screens-e2e target)
  </read_first>
  <what-built>
    Tasks 1-4 produced the test driver, the Makefile target, and the spec
    amendments. The remaining piece is RUNNING the test against live infra
    and capturing the result — the load-bearing exit-gate signal.

    The spike runs `make screens-e2e BASE_URL=... SESSION_ID=... OPENROUTER_KEY=...`
    against an iOS Simulator (and Android Emulator if available). The test
    drives the Login → Dashboard → Wizard → Deploy → Chat → kill+relaunch
    flow in ~3-5 minutes (matches Phase 24's spike runtime + extra steps).

    On PASS, the user (or this checkpoint executor) writes
    `spikes/flutter-screens-roundtrip.md` with the verdict, target matrix
    (iOS Sim / Android Emu), reproducibility tuple (git_sha, flutter_sdk_version,
    base_url, recipe, model), step narrative, and any deviations.

    On FAIL, the user investigates per Golden Rule #4 (root-cause-first —
    NOT fix-to-pass). Common failure modes per RESEARCH ## Risks:
    - api_server not running (preflight in Makefile catches this with curl)
    - SESSION_ID expired (Phase 22c-oauth-google session TTL)
    - OPENROUTER_KEY rate limited or revoked
    - Smoke step times out (>30s on cold image pull) — bump waitForOutbound timeout
    - SSE envelope dedup_key mismatch (Wave 0 Spike B was supposed to lock this)
  </what-built>
  <how-to-verify>
    1. Confirm api_server is running and healthy:
       `curl -fsS http://localhost:8000/healthz`
    2. Boot the iOS Simulator:
       `xcrun simctl boot <iOS-sim-id>` (Phase 24 used iPhone 16e
       3D61926B-A0F6-443E-AF7E-A2E674E74DDF)
    3. Capture a fresh ap_session cookie from the web playground
       (browser DevTools → Application → Cookies → ap_session value).
    4. Capture OPENROUTER_KEY from local `.env`:
       `grep OPENROUTER_KEY ../.env | cut -d= -f2`
    5. Run:
       ```
       cd mobile && make screens-e2e \
         BASE_URL=http://localhost:8000 \
         SESSION_ID=<paste from step 3> \
         OPENROUTER_KEY=<paste from step 4>
       ```
    6. Confirm test passes (exit 0). Capture the test runtime + any
       interesting log lines.
    7. Optionally repeat on Android Emulator if available
       (`flutter emulators --launch <emu>`, BASE_URL=http://10.0.2.2:8000).
    8. Write `spikes/flutter-screens-roundtrip.md` mirroring
       `spikes/flutter-api-roundtrip.md` frontmatter shape exactly:

       ```markdown
       ---
       date: 2026-05-03
       git_sha: <fill at write time>
       flutter_sdk_version: 3.41.0
       phase: 25-mobile-screens
       spike: screens-roundtrip
       base_url: http://localhost:8000
       recipe: <name>
       model: <provider/model>
       target: iOS Simulator (iPhone 16e iOS 26.4.x)
       verdict: PASS
       ---

       # Phase 25 D-65 — Screens E2E Round-Trip Spike

       Closes UI-04 + Phase 25 exit gate.

       ## Matrix
       | Target | Outcome | Test Runtime | Notes |
       |--------|---------|--------------|-------|
       | iOS Simulator (iPhone 16e iOS 26.4.x) | PASS | 3m 12s | Kill+relaunch via lifecycle pump; Keychain persistence not verified per Pitfall #5 — physical device test required for production claim |
       | Android Emulator | PASS / SKIPPED | ... | ... |

       ## Step narrative
       1. Login → Dashboard appeared in 2.1s
       2. ... 11 step lines ...

       ## Deviations
       (none, or "Step 7 took 47s — within timeout")

       ## Reproducibility
       - git_sha: <full sha>
       - BASE_URL: http://localhost:8000
       - api_server: started via `cd ../api_server && make dev`
       - openrouter model: <id>
       ```
    9. If `verdict: PASS`: commit the artifact + the AMDs + the test +
       Makefile + README updates as one chain on `main` (worktree_bypass
       per `feedback_worktree_breaks_for_live_infra.md`).
    10. If `verdict: FAIL`: investigate per Golden Rule #4. Do NOT
        commit a failed artifact unless the failure is documented as a
        known-blocker that the user explicitly accepts.
  </how-to-verify>
  <action>
    User runs `make screens-e2e` per <how-to-verify>. After test passes
    (exit 0), user writes the artifact at `spikes/flutter-screens-roundtrip.md`
    using the frontmatter shape above. The file is committed alongside
    Tasks 1-4 commits via the orchestrator (worktree-bypassed onto main).

    Provided artifact skeleton (planner provides; user fills outcome rows):
    See <how-to-verify> step 8 above.
  </action>
  <verify>
    <automated>test -f spikes/flutter-screens-roundtrip.md &amp;&amp; grep -E "^verdict: PASS" spikes/flutter-screens-roundtrip.md &amp;&amp; grep -q "phase: 25-mobile-screens" spikes/flutter-screens-roundtrip.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f spikes/flutter-screens-roundtrip.md`
    - `grep -E "^verdict: PASS" spikes/flutter-screens-roundtrip.md` (the load-bearing exit signal)
    - `grep -q "^phase: 25-mobile-screens" spikes/flutter-screens-roundtrip.md`
    - `grep -q "^spike: screens-roundtrip" spikes/flutter-screens-roundtrip.md`
    - `grep -q "git_sha:" spikes/flutter-screens-roundtrip.md`
    - `grep -q "flutter_sdk_version:" spikes/flutter-screens-roundtrip.md`
    - `grep -q "target:" spikes/flutter-screens-roundtrip.md`
    - File contains a Matrix section with at least an iOS Simulator row reporting PASS
    - File contains a Step narrative section with at least 5 step lines
    - User has explicitly approved the verdict via resume-signal
  </acceptance_criteria>
  <done>spikes/flutter-screens-roundtrip.md committed with verdict: PASS; live make screens-e2e ran successfully; UI-04 closed; Phase 25 exit gate signaled green for the verifier.</done>
  <resume-signal>Type "approved: PASS — iOS sim 3m 12s" or "approved: PASS-with-notes (...)" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries (carry-forward; no new boundaries in Wave 5)

| Boundary | Description |
|----------|-------------|
| Spike test → live api_server + Postgres + Redis + container + OpenRouter | Real infra per Golden Rule #1; same boundary as Phase 24 spike |
| AuthServiceTestSeam → secureStorage with --dart-define SESSION_ID | Test seam injects pre-collected session_id; native OAuth sheet bypassed (D-66) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08-01 | Information Disclosure | spikes/flutter-screens-roundtrip.md captures session_id / openrouter_key in screenshots or step narrative | mitigate | Artifact instructions explicitly redact `Cookie: ap_session=<REDACTED>` and `OPENROUTER_KEY=<REDACTED>` from any captured stdout; only the FACT of values is recorded |
| T-25-08-02 | Information Disclosure | Test runtime stdout contains BYOK key fragments via dio logging | mitigate (carry-forward) | Phase 24 RedactingLogInterceptor already redacts Authorization to last 8 chars; same interceptor active in spike harness |
| T-25-08-03 | Tampering | Worktree bypass commits directly to main without normal review gate | accept | This is the documented exception per `feedback_worktree_breaks_for_live_infra.md`; the pre-flight curl healthcheck + the test PASS gate substitute for the worktree review |
</threat_model>

<verification>
- All 5 tasks complete: test driver compiles + Makefile target runs + AMD-01 + AMD-02 land + spike artifact committed with verdict: PASS
- `flutter analyze` exit 0
- AMD-01: `grep -q "Amended Phase 25 per AMD-01" .planning/REQUIREMENTS.md` AND `! grep -q "rendered but disabled in MVP" .planning/REQUIREMENTS.md`
- AMD-02: `grep -q "AMD (Phase 25 D-56)" .planning/phases/23-backend-mobile-api-chat-proxy-persistence-auth-shim/23-CONTEXT.md`
- Spike artifact: `grep -E "^verdict: PASS" spikes/flutter-screens-roundtrip.md`
- Phase 25 exit gate signaled green: AMD-01 + AMD-02 + spike PASS = phase verifier accepts
- Worktree bypass honored: commits land directly on main per worktree_bypass: true frontmatter
</verification>

<success_criteria>
- [ ] mobile/integration_test/screens_e2e_test.dart compiles + reuses Phase 24 helpers + uses AuthServiceTestSeam Riverpod override + simulates kill+relaunch via handleAppLifecycleStateChanged
- [ ] mobile/integration_test/screens_e2e_helpers.dart provides widget-level spike helpers
- [ ] mobile/Makefile screens-e2e target with same env-var preflight + curl healthcheck as make spike
- [ ] mobile/README.md documents make screens-e2e per Phase 24 D-22 README pattern
- [ ] AMD-01 landed: REQUIREMENTS.md UI-02 rewritten verbatim per CONTEXT.md
- [ ] AMD-02 landed: 23-CONTEXT.md D-28 has appended amendment paragraph verbatim
- [ ] spikes/flutter-screens-roundtrip.md exists with verdict: PASS + iOS Simulator (or Android Emulator) PASS row
- [ ] Live make screens-e2e ran successfully against local api_server + Postgres + Redis + container + OpenRouter (Golden Rule #1 honored)
- [ ] Worktree bypass honored: commits land on main per `feedback_worktree_breaks_for_live_infra.md`
- [ ] Phase 25 verifier exit signal green: AMD-01 + AMD-02 + spike PASS all checked
</success_criteria>

<output>
After completion, create `.planning/phases/25-mobile-screens/25-08-SUMMARY.md` with:
- Wave 5 tasks completed (5 atomic commits)
- Spike result + runtime + target matrix
- Confirmation that AMD-01 + AMD-02 verbatim wording matches CONTEXT.md
- Phase 25 exit gate confirmation: UI-04 closed; verifier-ready
- Hand-off note for the phase verifier: "Phase 25 ships; advance ROADMAP.md Phase 25 to COMPLETE; advance REQUIREMENTS.md UI-01..UI-04 to Pending → Done in the Traceability table"
</output>
