id: dangler
name: Dangler
config_flavor_of: nonexistent-parent
runtime:
  family: python
launch:
  cmd: ["pkg", "--help"]
chat_io:
  mode: exec_per_message
  exec_per_message:
    cmd_template: ["pkg", "chat"]
isolation:
  tier: strict
