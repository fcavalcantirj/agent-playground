id: grandchild
name: Grandchild Flavor
config_flavor_of: child
runtime:
  family: python
launch:
  cmd: ["pkg", "--alt"]
chat_io:
  mode: exec_per_message
  exec_per_message:
    cmd_template: ["pkg", "chat"]
isolation:
  tier: strict
